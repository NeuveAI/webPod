# Independent native fragment-output review

Disposition: APPROVE for the frozen five-file source/output-policy milestone; no confirmed Critical/Major source defect remains. This is not full native activation approval. Exact hashes independently match `evidence/gpu-worker/native-output/manifest.json` (policy helper36c6d21d, assembly51d94bad, sticker materialsbb0db68e, packetee6fb7c0, worker518ac617). Temporary probe is evidence instrumentation, not shipped source.

The owned material wrapper preserves the original setupOutput/cache-key receivers, applies explicit authored tone-mapping eligibility and sRGB encoding before fog/premultiplied alpha, and retains exposure as the renderer uniform. A stable render-updated target gate avoids shader-cache specialization on the first compilation target. Installation is idempotent and weakly tracked; unsupported replacement/MRT outputs reject rather than silently bypass the policy. Final-scene chassis/LCD, print fronts/backs and packet materials are installed at creation; equipped, packet, warmup, carry and initial scene roots are checked before compile/ACK. PMREM room material creation remains outside this policy. Native explicit unsigned-byte/plain-unorm output and nominal working-space color setting suppress the second global conversion; the actual scene has no nonzero color background/clear added by this patch. GL construction is unchanged.

Independent checks passed:10 retained contract cases;12 existing material/ownership tests with101 assertions; device, composite and web typechecks; scoped five-source and contract-proof lint. No application edits, build, browser or GPU execution performed by reviewer.

The first proposed GPU probe covers one transparent basic plane over clear, shared material output→linear target→output, and compilation of physical/standard/basic/custom-fragment variants. A physical-material compile alone does not establish its output arithmetic, and an isolated alpha plane does not establish ordered glass-over-LCD blending. Author was asked to include the already scoped stacked mapped/unmapped layers, second alpha layer and production MSAA behavior before final disposition. Actual matched glass-on front screenshots in both colorways remain required. Do not infer equality from opaque white alone or treat source checks as live shader/output proof.


## Actual GPU evidence reviewed

The lead executed the expanded probe in current Chrome; the reviewer independently inspected the raw result at `/tmp/webpod-native-output-gpu-proof.json` and retained `evidence/gpu-worker/native-output/gpu-result.json`, plus `gpu-probe-source.js.txt`. All five application hashes still match the frozen manifest; the temporary application probe has been removed.

All12 mapped/unmapped basic-material cases (alpha0/.2/1, straight and premultiplied alpha) exactly match the GL reference, maximum channel error0. All4 ordered cases (unmapped LCD, mapped alpha layer, then mapped basic or physical glass, straight/premultiplied) also match exactly, error0. Antialiasing was enabled in both renderers. Shared-material output→linear-target→output gives RGB137→64→137 with alpha255, verifying the runtime pass gate rather than a first-compiled-target specialization. Physical, standard, basic and custom fragment-node programs compile successfully. Output formats are explicit bgra8unorm canvas and rgba8unorm linear target.

This satisfies the bounded output arithmetic/composition and pass-switch evidence requirement. It samples an interior pixel with MSAA enabled, not silhouette-edge equivalence. The physical test does not supply the authored studio PMREM and cannot certify full glass reflections; actual device glass-on matching in both colorways remains the lead's mandatory visual gate. No live GPU work was executed by the reviewer, and no timing/whole-device parity claim is made. The three concurrent GL rejection-diagnostic files are excluded from this verdict.
