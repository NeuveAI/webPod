# Sticker compute transactions implementation

CPU-011, in progress. This author previously reviewed other lanes; a different engineer must review these changes. Read full dispatch E, audit C, global/Jotai patterns and modern web long-task guidance. Checked existing worker/transfer APIs and installed Three types. No browser, trace or commit actions.

Initial pure checkpoint: existing synchronous damage and drop-fit APIs now drain the same generator math; generators introduce only checkpoints and preserve iteration/tie order. New typed requests snapshot exact artwork/form/placement, alpha and camera matrices. One computation generator supports worker execution and cooperative failure execution. Surface geometry uses the same form-backed wrap sampler and full96 topology. Worker output transfers private buffers; the broker does not detach mounted input arrays.

Initial broker: one session worker,32 entries/FIFO jobs,30s unused cache retention and32MiB idle-result cap; last-owner cancellation retires active native/cooperative work. Execution deadline begins at dispatch. Worker instance and job identity guard late results. Failure recovery drains identical computation with existing4ms cooperative helper. This checkpoint is not final lifecycle acceptance: active/input byte accounting, reuse/private renderer handoff and retained proofs are still being completed.

Required live caller changes remain staged: EquippedSticker must adopt geometry+damage+placement atomically; StickerPrint must consume asynchronously prepared fields without synchronous damage generation in material/layout paths; Scene resolveDrop and public callback contracts must await exact final fit before persistence, retaining current carried visual. Parent owns the narrow shared-file handoff after CPU010 freeze. No helper-only final deliverable is claimed. Native image raster acquisition remains main; pure field computation belongs off main. Contour/bounds and source-target resource ownership integration remain outstanding.

## Live integration checkpoint

CPU014 independently approved collision changes, now baseline ebebb32d0b7f0b30b7e41b81778293d6b7d44405. Parent released narrow Scene/production plus sticker-contract/collection callsites. Scene captures final pointer XY, UV anchor, form and camera/world matrices into resolveStickerDropTransaction. Projection-handle retirement aborts pending fit; collection awaits it inside its existing run generation, subscribes the computation epoch for abort and checks current ownership before persistence. The current carried preview remains unchanged while fitting. Shared Scene/production files were then released to CPU009 for host extraction; preserve the adapter calls and adopted-placement query contract.

EquippedSticker now requests exact geometry asynchronously with the current form. Custom external wrap implementations use the same cooperative generator because functions cannot cross a worker boundary. StickerPrint requests exact base/surface damage off main and holds its last matched geometry+damage print while replacing it. Lightweight geometry wrappers borrow immutable arrays and own their GPU wrapper lifetime, avoiding a dense clone on main. Visible placement metadata and a commit revision follow the adopted geometry so pick-cache results cannot combine new placement state with old vertices. Native alpha image rasterization remains main; no damage field math remains in the material construction/layout path. Await independent lifecycle/appearance review before treating these new integration paths as accepted.

Pinned-reference proof now archives the committed prior device sources and compares the old implementations with native worker/cooperative output for surface, damage and drop fit. Initial cases pass. Existing surface/alpha/drop-fit/contour/bounds plus production-provider suites pass37 tests/1,162,777 assertions; affected web types and scoped shared integration lint pass. Broader cases, resource lifecycle/final-save proof and dense contour/bounds work remain unfinished.

App source temporarily frozen at supervisor request for a bounded existing-Chrome GL assembly screenshot. This author does not operate that browser or claim live readiness from local tests.

## Private producer checkpoint (implementation paused for independent host review)

The source freeze for the supervisor screenshot is released. Shared Scene/production files remain handed to the host. The transaction worker now retains canonical output and clones a private renderer result through the supplied MessagePort; `acquirePrivateStickerTransaction(key,input,port,signal)` returns an explicit release lease. `preparedStickerResources(geometry)` supplies descriptors for the displayed print's existing surface/damage jobs. Displayed wrappers retain the corresponding cache leases. Main buffers are never transferred. Canonical loss rejects native acquisition rather than silently recomputing; GL can retain its existing main resources.

Admission includes input clones, conservative active work reservation, canonical/main outputs and outstanding private copies under 96 MiB, with 32 jobs and 30-second idle cleanup. Pending private cancellation retires the producer before releasing its reservation and recovers any other active computation cooperatively. This checkpoint passes scoped lint; independent lifecycle proof of this extension remains outstanding, along with full CPU011 acceptance, contour/bounds work and hook supersession audit. It is not a reviewed host-ready guarantee. CPU011 authoring is temporarily paused for the separate CPU017 static assembly review; no claim of self-review is made.

## Resumed verification

CPU017 independent host source review finished separately. CPU011 resumed, then paused application edits again for the supervisor's first native-frame check. Nine retained actual-worker/MessagePort/cooperative lifecycle checks now pass in sticker-transactions/lifecycle.ts, including shared job identity, private exact transfer, mounted-buffer isolation, private byte/owner accounting, idempotent release, pending cancellation retirement, canonical loss without recomputation, final cleanup and exact worker-unavailable fallback. Private copies reserve two payloads until producer acknowledgment, then one until caller release; private owners are capped at32. Pinned ebebb32d0b7f0b30b7e41b81778293d6b7d44405 surface/damage/fit parity reruns pass. Scoped source/proof lint passes.

During the resumed source audit, both visible print sides were aligned to the same adopted prepared geometry (the backing previously still read the incoming geometry while damage was pending). The ready callback now follows damage/wear material assignment in the same layout commit. These fixes remain for independent CPU011 review, not self-approval.

Bounds preparation ownership expansion approved by lead: add only a matching immutable bounds index to carry computation/worker result and preparation adoption, after coordination with host (no competing carry edits). Collision/contact/deformation algorithms remain unchanged. A first retained936-case pinned bounds comparison is exact, with4,023,812 former vertex evaluations versus210,193 indexed vertex evaluations; this excludes box projection work and is not a mobile speedup claim. The output still uses original-vertex extrema, and uncertain clipping/denominators descend to the original test. Integration and mutable-external fallback cache audit remain pending.

## Coherent implementation freeze for independent review

CPU011 application source is now frozen for another engineer's review. Static host and motion work remain separately owned. The live equipped/print hooks and collection release caller use this pipeline; these are not unused workers.

Normal owned geometry, damage fields, alpha boundary tracing and full triangle interpolation now run in the shared transaction producer. Contours carry exact Float64 local endpoint/visibility samples and grip anchors; main performs only current rigid projection and exact visibility queries. Canonical contour dependency IDs avoid re-cloning equipped geometry and damage fields in normal worker jobs and reuse the field's bounded four-wear boundary cache. Carry uses its existing computation worker and gains only a matching bounds-index output/adoption. Main bounds queries use worker-prepared AABBs to reject interior subtrees, while output extrema always come from original vertices. Unprepared or updated external geometry retains the exact defensive scan; it does not use the immutable-pose cache.

Publication is atomic across displayed front/back geometry, damage, visible wear and prepared contours. Both sides retain the prior matched frame while replacement computes. A per-surface runtime has one active and one replaceable latest input, accepts compatible progress and rejects old gesture epochs. Shared Scene now passes computationEpoch through its host-owned caller edits. Skipped React publications are retained until the next commit/unmount and released, so concurrent batching cannot strand unrendered geometry or cache leases.

Ordinary gesture cancellation races the request owner's AbortSignal immediately but lets the single already-running native job finish under its existing deadline, retaining its charged reservation and discarding an unowned result. It does not destroy all canonical native-renderer resources on every gesture. Exceptional worker failure/timeout and pending private-copy cancellation still terminate before recovery/reservation release. Cooperative cancellation advances the queue only after the old generator exits. Worker handlers are bound separately from job closures and cleared on retirement to avoid retaining an evicted last job.

Byte accounting distinguishes queued input retention, the sole active work reservation, completed canonical/main buffers and pending/private renderer copies; total admission is96MiB with32 entries/private owners,30s idle expiry and32MiB idle-cost cap. Admission evicts unused cached inputs/results before rejecting capacity. Work reservations are conservative scratch estimates, not measured JavaScript heap or GPU driver memory. Complete GL/worker recovery preserves exact computation; native private acquisition after canonical loss explicitly fails rather than silently recomputing an already cached recipe.

The live final-save helper in apps/web/src/sticker-drop-transaction.ts is called by sticker-collection. It awaits the captured final-sample fit, checks current generation/AbortSignal before persistence, rechecks after synchronous feedback, and prevents an obsolete completed save from authorizing animation. This extraction adds no alternate persistence path. The parent approved sticker-interaction.ts's one-line readiness idempotence change: an already-ready artwork leaves the existing failure array identity unchanged.

Private host API: preparedStickerResources(displayedGeometry) returns borrowed descriptors {key,input} for surface/damage. acquirePrivateStickerTransaction(key,input,port,signal) transfers {version:1,id:resourceId,result} from the existing canonical producer and returns {resourceId,release}. Surface result includes ShellGeometryTransfer and matching bounds; damage includes stickerId, exact field and flipped gpu Uint8Array. Damage texture contract remains RedFormat, NearestFilter for both filters, no mipmaps, flipY=false, field.width×height. Caller supplies existing artwork/native texture and material/appearance recipe. Contour is query metadata, not a second graphics recipe. Host disposes its received wrappers/data before releasing the private lease.

### Exact owned source manifest

Modified: packages/device/src/StickerSurface.tsx; sticker-alpha.ts; sticker-drop-fit.ts; sticker-wear.ts; sticker-contract.ts; sticker-contour.ts; sticker-projected-bounds.ts; sticker-carry-computation.ts; sticker-carry-worker.ts; sticker-carry-preparation.ts. apps/web/src/sticker-collection.tsx; sticker-interaction.ts.

New: packages/device/src/sticker-transaction-data.ts; sticker-transaction-computation.ts; sticker-transaction-worker.ts; sticker-transaction-broker.ts; sticker-transaction-client.ts; sticker-prepared-surface.ts; sticker-prepared-damage.ts; sticker-latest-preparation.ts; sticker-bounds-index.ts; sticker-contour-computation.ts; sticker-contour-preparation-data.ts. apps/web/src/sticker-drop-transaction.ts.

Earlier handed-off deltas (host now owns full files): StickerPackScene.tsx async resolveDrop/final camera/world snapshot, handle AbortSignal lifetime, adopted placement/revision query metadata; production-device-view.tsx async guarded resolveDrop adapter. Preserve later host/motion edits in both. Their computationEpoch and local carry orientation subscription additions are authored by the host, not this worker lane.

### Verification and remaining acceptance

Pinned ebebb32d0b7f0b30b7e41b81778293d6b7d44405 worker/cooperative/original surface, damage and fit parity passes. Bounds936 cases match exact prior output;4,023,812 prior versus210,193 indexed original-vertex evaluations exclude all AABB corner projection work and imply no phone timing claim. Contours1320 cases match complete prior paths, grip anchors and occluded spans, including cutouts, cropped UVs, wear and wrapped rotation. Six existing carry reference poses still match original full9409-vertex geometry/contacts exactly.

Retained actual worker/private-port lifecycle, latest/epoch publication and actual final-save-helper proofs are under evidence/gpu-worker/sticker-transactions. Existing device/provider suites pass50 tests/2,024,776 assertions; existing interaction lifecycle/editor/runtime suites pass30 tests/258 assertions. Combined80 tests/2,025,034 assertions. Final affected type/lint results are reported to the lead after the running commands finish.

No browser, GPU appearance, latency, or end-to-end native sticker gate is claimed by this author. Native raster image acquisition remains a bounded browser operation on main; projection/visibility remains synchronous against current query state. Exact fallback uses cooperative generator checkpoints, but native typed-array allocation and final conversion cannot be subdivided by JavaScript. The independent source review, full host sticker-resource adoption, actual release/edit/wear/peel/cancel visuals and6×CPU traces remain required acceptance. Current source completeness does not mark the full architecture goal done.

Final frozen-source checks complete: device and web `bunx --bun tsc --noEmit` pass; scoped source/proof ESLint and `git diff --check` pass. Retained lifecycle now has11 checks, including ordinary native cancellation preserving canonical resources before a subsequent private delivery; latest runtime has6 checks, and the used final-save helper has5 cases. The rerun six-pose carry result is copied into the current sticker-transactions evidence folder; the historical performance result file is preserved. These are author verification results awaiting another engineer's review.

### Independent review correction: contour revision acceptance

CPU019 reproduced prepared contour reuse after a different requested wear and position mutation. Corrected the narrow lookup to require the same normalized byte wear threshold and position/UV/index attribute identity, backing array, count, item size, normalization, version and interleaved data/offset/stride. Mismatches retain the exact defensive projection; immutable matched results retain prepared projection. The original reviewer reproduction now reports both mismatches false; scoped two-file lint passes. Independent reviewer owns the retained reproduction and final verdict. No other application files changed for this correction.

The same correction exports preparedStickerContourWear(displayedGeometry): the raw adopted wear paired with its exact prepared contour and both print materials, with geometry-revision validation. Host updated the live Scene query to consume that value rather than a newer placement wear while old artwork remains displayed. CPU019 independently verified the selector/caller, both invalidation cases now false,1320 contour parity,8 affected tests, device types and focused lint, and reported source APPROVE. Full native integration and the lead's process ledger remain open.
