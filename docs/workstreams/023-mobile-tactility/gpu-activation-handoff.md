# Page-only preferred-worker activation handoff

Read-only preparation after native output commit37936fe. No application or browser changes. This updates the activation decision using the final checklist, output review/proof and bounded WebMCP report; it does not declare the remaining browser gates passed.

## Smallest source boundary

Own only `apps/web/src/device-page.tsx`, at the existing ProductionDeviceView prop (currently line323): change `rendererBackend={rendererBackend}` to `rendererBackend={rendererBackend ?? 'worker'}`. Keep the canonical TanStack search selection and `/webpod` validateSearch unchanged. Explicit `renderBackend=webgl` still wins; explicit worker remains identical. Do not change CompositeDevice's reusable default or add a parallel capability detector, setting, route or persistent preference. The development-only spike also uses DevicePage and therefore inherits this page preference; other CompositeDevice callers retain their current GL default.

The existing adapter chooses worker only while its per-device failed atom is empty. Worker initialization/transport/compile failures enter the existing GL replacement path. The Panel owner, Jotai/music state and public input/media callbacks remain above that renderer replacement. There is no new fallback implementation in this one-line change. The actual host rejects unsupported associated native paint canvases. Full-tier capability admission still controls whether a 3D canvas mounts; `data-renderer-effective=worker` alone does not prove a worker canvas exists. Verify canvas `wpRenderBackend`/warm metadata and rendered content, not only the request/effective labels.

## Essential gates before default activation

1. **Actual final output appearance:** independent source approval and16 exact GL/GPU pixel cases at37936fe are complete. Parent has a current glass-on Black/dark capture and is acquiring Silver/light. Independent comparison must accept those against healthy references, then inspect the packet/liner and earned/locked/placed/carried materials because the output policy applies to all of them. PMREM/full-scene parity is not established by the small arithmetic probe. Do not rerun the resolved glass-intensity or constant-quad investigations.
2. **Current GL fallback interaction:** the transient GL get/release error was concretely a print-damage-contour byte-capacity rejection, separate from the initial mesh-readiness race. Current author/lead correction and live validation must close that actual transition and initial warmup, including one physical peel/drop/return and no retained test placement. A healthy original PR5 reference or manual renderer override is not proof of automatic failure recovery.
3. **Final integrated native interaction:** earlier native production pickup/drop/edit/return, resizing and human controls already have evidence. Run one bounded final-build smoke across packet/liner→pickup/drop/cancel→edit/return, physical wheel/select/menu and a control during orientation release, plus one resize confirming sharp LCD and aligned hits. Source output changes need this integrated check, not a repeat CPU architecture investigation.
4. **Remaining live lifecycle coverage:** actual registered native page/device/list, flick front/back, open/get/release and center/menu passed in gpu-webmcp-smoke.md; actual ArrowRight/Home passed. Keyboard Enter/rear rejection is still untested. The temporary-tab hide attempt recorded no visibilitychange and therefore did not validate hidden/resumed behavior. Genuine hide/resume/reduced-motion/teardown and induced loss/unsupported runtime coverage must either be exercised through existing supported controls or explicitly remain in the lead-owned manual disposition. No fake loss/debug API and no silent waiver.
5. **Exact final build/process:** after approved one-line activation, final types/lint/build and exact-range independent review, remove all diagnostic helpers/instrumentation and ensure no active probes/test inventory. A clean `/webpod` without query must actually choose ready worker on the supported current Chrome. Explicit webgl must still render functional GL; induced failure must follow existing replacement if actually tested. Parent owns final Neuve/manual/full-range disposition and commit.

## Responsiveness evidence already sufficient to avoid redundant investigations

The measured rotation hover and carry copy corrections have matched production evidence. The reviewed priming capture recorded a52ms opening task and56ms first pickup, with a1.5ms sheet-usable-to-pickup gap; it is not stall-free and does not prove context-ready timing. Possible host-check overlap remains unknown. Retain these qualifications rather than inventing a clean isolated speedup. No need to repeat every baseline or CPU attribution for the output arithmetic correction; any final quick performance check must use an exact clean artifact and bounded observers with cleanup. Main RAF is not GPU FPS, and main-coordinated DOM entrance is not an autonomous worker entrance.

The page-only patch may be prepared once those concrete blocking validations are closed, independently reviewed as a small selection change, and then verified without query. This handoff is not authorization to claim full completion from the source milestone alone.
