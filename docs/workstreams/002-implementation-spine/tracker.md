# 002 — Initiative tracker

The operational queue for this workstream. There is no Kanban board and no `neuve` shell in this repo (owner ruling, 2026-08-28) — **this file is the queue.** The lead updates it at every state change; teammates report against their slice id.

**Status vocabulary:** `unblocked` · `dispatched` · `in-progress` · `in-review` · `changes-requested` · `approved` · `owner-validation` · `done` · `blocked`

---

## Slices

| id | Slice | Lane | Status | Teammate | Reviewer | Blocked by | Evidence |
|---|---|---|---|---|---|---|---|
| **S1** | Apple Music capability docs spike (§14.3 rows 10, 11, 18, 20, 21, 30) | L-B | **DONE** ✓ committed `cc09d52` (D-047) | sub-agent (Opus) | sub-agent (Opus) | — | `evidence/apple-capability-spike.md` |
| **S2** | Apple empirical probe — developer token, **read-only** | L-B | **APPROVED** · final review 0 Critical / 0 Major / 0 Minor | Kepler | Laplace | — | `evidence/apple-empirical-probe.md` |
| **W0** | bun monorepo scaffold + tokens + repo hygiene | L-A | **DONE** ✓ accepted (D-044) | sub-agent (Opus) | sub-agent (Opus) | — | `evidence/w0-*` (8 files) |
| **W5a** | Static gate harness (`bun run gates`) | L-A | **APPROVED** · final `dc8d734` | Hooke | Boole | W0 | `evidence/w5a-*` |
| **W1** | Provider contract + fixture + apple/spotify stubs | L-B | **APPROVED** · final review 0 Critical / 0 Major / 0 Minor | Popper | Rawls | W0, W5a gate | `evidence/w1-*` |
| **W2** | Jotai store, detent reducer, screen state machine | L-C | **APPROVED** · code + evidence correction `125e0dc` | Kierkegaard | Avicenna | W0, W5a gate | `evidence/w2-*` |
| **W3** | Panel DOM — shell, list primitives, S03 / S08 / S13 | L-D | **APPROVED** · final review 0 Critical / 0 Major / 0 Minor | Kuhn | Galileo | W0, W2 contract, W5a gate | `evidence/w3-*` |
| **W4** | Device layer — materials, geometry, screen mesh boundary | L-E | `changes-requested` · correction active after tessellated-shell commits `a61e99c`, `43668b3` | Franklin | James | W0 | `evidence/w4-*` |
| **W6.0** | Capability probe — committed `8208ccc` | L-E | **APPROVED** · final review 0 Critical / 0 Major / 0 Minor | Raman | Leibniz | W0 | `evidence/w6-capability-probe.txt` |
| **W6** | Composite seam, T1 html-in-canvas | L-E | **APPROVED** · final review 0 Critical / 0 Major / 0 Minor | Raman | Leibniz | W3, W4 UV fix, W6.3a | `evidence/w6-*` |
| **W6.3a** | Same-origin artwork proxy | L-B | **APPROVED** · final review 0 Critical / 0 Major / 0 Minor | Curie | Pasteur | W1 artwork contract | `evidence/server-artwork-*` |
| **W5b** | Browser checks — Playwright, axe, greyscale, colourway pairs | L-A | **APPROVED** · final review 0 Critical / 0 Major / 0 Minor (`a637d26`, `2421444`) | Turing | Locke | W3 correction | `evidence/w5b-*`, `reviews/w5b-review.md` |
| **W7** | Production click-wheel pointer/touch + scroll runtime | L-C / L-E | **APPROVED** · device + composite + panel final reviews each 0 Critical / 0 Major / 0 Minor | interaction teammates | separate reviewers | W2, W4/W6 device seam | `reviews/click-wheel-{device,composite,panel}-review.md` |

> Full provenance for every ruling below — rationale, consequences, reversal cost, and which 001 instructions were knowingly overridden — is in **`decision-log.md`**. The `Log` table at the foot of this file is a timeline; `decision-log.md` is the record.

## Open risks

| id | Risk | Opened | Owner | State |
|---|---|---|---|---|
| **RISK-01** | T3 CSS-3D overlay is the eventual shipping default and is **not built**. Until it lands webPod runs only in a flagged Canary. Not self-correcting: `HTMLTexture` degrading to a plain `Texture` gives a blank screen mesh, not T3. **Release gate: no user-facing release until T2–T4 land and §15 is signed off flag-off.** Re-read at the start of every subsequent workstream. | 2026-08-28 | lead | open, accepted |
| **RISK-02** | The `html-in-canvas` WebGL entry point is mid-rename (`texElementImage2D` ↔ `texElementSubImage2D`). Mitigated by routing through three.js `HTMLTexture` rather than the raw API; a direct call in our source is a blocking finding. | 2026-08-28 | W6 | open, mitigated |

## Owner-only gates outstanding

| id | Gate | Waits on | Status |
|---|---|---|---|
| ~~H-11~~ | ~~Team ID + MusicKit capability~~ | — | **closed 2026-08-28** |
| **H-10** | **Interactive MusicKit sign-in for a Music User Token.** Now the **highest-value unrun experiment** — D-036 reverses D-025's recommendation and D-018 already authorises the scope. | owner | **waiting on owner** |
| **H-1** | History rewrite + force-push (`.claude/`, `CLAUDE.md`→`AGENTS.md`, trailer removal) | lead prepared `evidence/w0-history-rewrite-plan.md`; **owner executes** — agents never force-push | `prepared` · not run |
| **H-5** | **U14** thumb-occlusion check, on a phone, in hand | W3 wheel interactive | `not ready` |
| **H-6** | Aesthetic sign-off: does the device read as the object; both-colourway polarity; panel legibility at 272×204 | W3 + W4 previews | `not ready` |

## Teammate roster

Peer sessions addressed by name via `SendMessage`. Recorded here so the lead can re-attach after a context compaction.

| Slice | Session name | Role | Dismissed? |
|---|---|---|---|
| W4 | Tesla (`01a04894-9d4d-75d2-800b-61a0147abe64`) | stale inherited implementer, replaced | yes |
| W1 | Copernicus (`01a04894-9dd2-72b0-824d-cd228b63d4d9`) | stale inherited reviewer, replaced | yes |
| W2 | Herschel (`01a04894-9e53-72f2-8cf7-87aba52b613b`) | stale inherited reviewer, replaced | yes |
| W3 | Kuhn (`01a04a38-22f3-7582-a557-292889481d82`) | design-engineering teammate | no |
| W3 review | Galileo (`01a04a4a-43f6-73c1-ab18-28d552851118`) | antagonistic design reviewer; resume for re-review | temporarily |
| W6 | Raman (`01a04a4a-a9cf-7103-880d-0133a75e2868`) | implementing teammate | no |
| W1 final review | Rawls (`01a04a4e-7b71-7c50-9832-afc6caca94e3`) | antagonistic reviewer | no |
| W2 final review | Avicenna (`01a04a4e-7be7-7f90-b9f6-23c941934df3`) | antagonistic reviewer | no |
| W4 replacement | Franklin (`01a04a4f-f8fe-7941-919d-fe1664c17b68`) | design-engineering teammate | no |
| W5a | Hooke (`01a04a53-9e79-78e2-b039-381199fb2bd2`) | implementing teammate | no |
| W1 fixes | Popper (`01a04a56-7b94-7c22-9d4e-27656485ab08`) | implementing teammate | no |
| W2 fixes | Kierkegaard (`01a04a56-7c25-77e2-8a03-9ac287c00b82`) | implementing teammate | no |
| W5a review | Boole (`01a04a5e-c12a-7c81-8aa8-f368550714bd`) | antagonistic reviewer | no |
| W6 review | Leibniz (`01a04a61-57af-70a2-b0dd-7b261d02daf5`) | antagonistic reviewer | no |
| W4 review | James (`01a04a66-8e43-71d3-8d0c-75e13fa200cb`) | antagonistic design/runtime reviewer | no |
| W6.3a | Curie (`01a04a6a-393b-7f42-bafe-573271efa1be`) | server implementation teammate | no |
| Hygiene | Hegel (`01a04a71-cb06-7cc2-9bea-4778957bf682`) | bounded S2 naming fix | no |
| W6.3a review | Russell (`01a04a7b-7ab2-7d01-b774-9e19a01cbf11`) | antagonistic security reviewer | no |

### Teammate sourcing — read before dispatching anything

**The 16 `coding-x-machina-local-*` peers on `ListAgents` are NOT available.** They are a live team running ARTEXI-WORK-137 in `/Users/vinicius/code/artexi-quartus`, inside `.claude/worktrees/` checkouts from which `/Users/vinicius/code/webPod` does not resolve. `ListAgents` reports them `idle`, which means *between turns*, **not unassigned**. Two were probed on 2026-08-28 and both correctly refused.

Rules learned, apply them before every dispatch:
1. `idle` on the roster is availability-of-the-moment, never evidence of being unassigned. Probe `pwd` + repo presence + current assignment **before** sending a packet.
2. A teammate must be running **in this repo's working tree**. A peer on the same machine in a foreign worktree cannot take a slice.
3. Owner protocol observed in this environment: **Opus implements, Fable reviews.** Do not hand an implementation slice to a session running a review lane, and never have a reviewer author repo law.
4. Never pull a session off a live audit. A standing reviewer is not spare capacity.

## Resume state — session limit hit 2026-08-28 ~11:16 · **W0, W2 and S1 RESUMED; reviewer held**

`ListAgents`/`SendMessage` can resume each from its own transcript. **Resume, do not re-dispatch** — re-dispatching loses their in-flight reasoning and risks duplicate edits over uncommitted work.

| Agent | Was doing | Uncommitted work in tree | Resume with |
|---|---|---|---|
| **W0** | Final verification sweep before committing its 5-Major patch | `globals.css`, `geometry.ts`/`geometry.test.ts`, `scripts/typecheck.ts`, `tsconfig.base.json`, `eslint.config.js`, `AGENTS.md`, `packages/ui/package.json`, `apps/web/components.json` | "continue where you left off; verify then commit" — plus D-020's corrected three-edit form |
| **S1 (implementer)** | Writing revision 3 after S2 falsified its headline | `evidence/apple-capability-spike.md`, `decisions/s1.md` | D-028/D-029 rulings already relayed; just resume |
| **S1 reviewer** | Re-aiming its re-review after the row-21 retraction | none (writes `reviews/s1-review.md`) | **deliberately NOT resumed** — reviewing a file mid-rewrite wastes the pass. Resume once S1 reports revision 3. |
| **W2** | Slice W2.4, the announcement debounce | none — 3 commits landed clean | "finish W2.4 and report" |

**Do not commit anything tree-wide while these resume** — the working tree holds four lanes' in-progress edits. See D-034.

## ~~Flag — `design.pen` modified~~ · CLOSED 2026-08-28

Owner confirmed it was their own unsaved canvas edits, saved from the pen.dev app. Committed as `4a4e3f4`, design.pen only. **No lane touched it** — the repo law (pencil MCP only, never `Read`/`Grep`/edit) held.

Committed with `git commit -- design.pen` rather than staging, because the index held S1's revision and W2's in-progress files at the time. See D-034: the pathspec-commit form is the safe way to commit one file while other lanes are live.

## Log

| Date | Event |
|---|---|
| 2026-08-29 | **W7 APPROVED across all three owned seams.** Device input surface, composite gesture runtime, and panel focus/native-action behavior each reached independent final review with 0 Critical / 0 Major / 0 Minor. The three durable review artifacts are `click-wheel-device-review.md`, `click-wheel-composite-review.md`, and `click-wheel-panel-review.md`. W4 remains active and is not cleared by W7. |
| 2026-08-29 | **D-068 binding ruling: panel rows remain non-touch targets.** design-system §11.2/U6 and authentic iPod behaviour override pm-spec §4.5 for the MVP. The direct visible-row selection commit `7d170dd` is reversed: no delayed row driver, row handlers, or undersized touch targets. Tapping panel content focuses the click-wheel application without activation; keyboard remains intact; W7's production click-wheel surface is the single-pointer alternative. |
| 2026-08-28 | W0 dispatched to `logical-giraffe` and **withdrawn**; S1 probe to `cosmic-dream` answered and **withdrawn**. Both are committed ARTEXI-WORK-137 reviewers in artexi-quartus worktrees with no webPod tree. Lead error: routed off `ListAgents` idle status without probing assignment. Rules recorded above. Workstream needs fresh sessions started in this repo before any dispatch — the peer sessions report as "a Claude session on another machine, over Remote Control", so shared-filesystem access to `/Users/vinicius/code/webPod` is **unconfirmed**. No further dispatch until a peer reports back with its `pwd` and repo state. |
| 2026-08-28 | **W0 complete**: 10 packages, 3 commits, no trailers; typecheck 10/10, lint clean, 15 tests pass, dev+build serve; planted violations verified red. Lead committed the pending `.gitignore` key fix plus this bundle (`c3cae15`). Rulings issued: D-020 (`@theme static`) and D-021 (§7.3 desktop geometry stale — §12.0 authoritative). W0 review dispatched; W2 and W6.0 dispatched; W1, W4, W5a now unblocked. |
| 2026-08-28 | **ENV DEFECT, affects every agent on this machine:** 16 references across 15 `~/.claude/skills/*` files point at `~/code/agent-context`, which does not exist — the clones are `agentic-context`. Verified. `/global-patterns`, `/bun-http`, `/effect-services`, `/jotai-state`, `/tanstack-*`, `/shadcn-ui` and others are affected. `/shadcn` additionally hard-runs `!npx shadcn@latest info --json` at load, which fails on a machine without npx and violates this repo's bun-only law. Surfaced to owner; **not fixed by an agent — user config.** |
| 2026-08-28 | **W2 implemented D-063** in five commits, one per behaviour, plus two test commits. All five gated per D-050 with **nine plants red** (37 failures) — each constant reverted to the pm-spec figure it supersedes *and* nudged the way a value drifts, with gates naming the rejected numbers (`not.toBe(3)`, `not.toBe(240)`, `not.toBe(60)`) so a reader sees each assertion settles a conflict. The 40-item precondition needed a **shape change, not a constant**: `ScreenMetrics { totalRows }` threaded through the pure reducer, with **omitting it disabling fast-scroll** — the safe direction. Hysteresis charged on reversals only, gated to prove it never reaches the keyboard, and it forced a real fix (`direction` now comes from the drain). D-060's frame-rate invariance re-measured exact 15fps–240Hz. **Then it found a flaky timer test on its own and chased it instead of re-running: the assertion had been wrong since written and passed only on idle machines → D-066.** 181 package / 641 repo tests, all 19 commits standalone-green. |
| 2026-08-28 | **W1 fixed all 6 Majors and 12 Minors** in 13 dependency-split commits plus docs. Tests **230 → 388**, repo-wide **638/0**, eslint clean repo-wide, all 14 commits standalone-green. All four previously-uncaught plants now red, including the `status = 0` route (now a `tsc` error via a closed parameter object with `Parameters<…>['length']` and `HasExactKeys<…>` assertions). **Two findings that outrank the fixes: (a)** *"the verification step failed the same way the code did"* — a plant reported green because a `perl` substitution silently failed to match a typographic apostrophe, so the mutation never applied and unmodified code passing **looked like a working gate** → **D-064**; **(b)** the other green plant was real and found a hole in the gate just written — deleting a row's evidence label let it silently inherit its neighbour's. Also **D-065**: D-045 made executable as a two-directional gate. Lead corrections applied: D-053(a) (two proofs load-bearing, not three; now seven) and `tracker.md:88` per D-061 part 2. |
| 2026-08-28 | **W2 round-2 fixes complete, four commits, one per defect — no interlock claimed** (D-059 applied against itself: *"I over-claimed on the single commit last round"*). R1: counter **deleted** — a count was never the question — guard throws on the first call, `createDeviceStore` moved out of the package's export map entirely, and its test runs in a **fresh subprocess** so nothing earlier can make it pass (D-058). R2: `PressInput.timestampMs` **deleted from the type**. R3: writable primitives moved to a non-exported `internal.ts` so the blessed-but-broken write no longer typechecks. R4: coast now **exact, not within tolerance** — 16 detents / 74 rows / 240° from 15fps to 240Hz and under a jittering variable-rate interval. 156 package / **596 repo tests**, repo-wide lint clean, all 11 commits standalone-green. **W2 then read design-system §9.4 and found three conflicts with pm-spec §4.4 — and changed nothing, escalating instead.** → **D-063**, ruled after I read both sections myself. |
| 2026-08-28 | **W1 review: REQUEST_CHANGES, 6 Major / 12 Minor.** Research fidelity best in the workstream — matrix matches the spike row for row, **20 of 26 plants red**. What fails is enforcement: **all four uncaught plants are guards, not values.** **Two Majors trace to the lead** → **D-061**: D-052 was recorded `settled` and never relayed, so the fixture still carries the pre-ruling comment verbatim; and I propagated "enforced structurally" into the tracker without checking it. **D-062**: `Function.length` stops at the first defaulted parameter, so the arity guard is satisfied by `(resource, name, status = 0)` — the exact mistake D-029 forbids, planted and green. Others: `LocalKeyed<V>` has **zero usages** and the launch catalog keys by bare string; `Session.canPlay`/`authorized` inert so signed-out and free-tier states are **unreachable in the only provider W3 can drive**; `saveToggle` writes a set nothing reads; the stubs throw synchronously from `Promise`-declared members while the fixture rejects, so `.catch()` works on one and crashes on the other. |
| 2026-08-28 | **W2 re-review: REQUEST_CHANGES, 4 NEW Majors.** All four original Majors verified genuinely fixed — the 30-detent flick gives 1 announcement with `performance.now`, `Date.now` AND `Math.random()*1e13` timestamps; both previously-green plants now red at exactly the claimed counts; `pageRows` gone from the type. But: **D-051's guard is off by one** — the first production `createDeviceStore()` succeeds, and its test was green only because 25 earlier calls in the same file had tripped the counter (**D-058**: right assertion, wrong reason); **two clocks rebuilt in `BumpEvent.at`**, the same defect one field over; **the density write the TSDoc blesses produces a window the highlight is not in** — the exact "row you can never scroll to" the fix was written to prevent; **the coast is 4× refresh-rate dependent** (30fps → 492°, 120fps → 123°), all six tests at 1/60 so ungated → **D-060** rules it time-based. Reviewer also disproved the single-commit interlock claim by symbol graph → **D-059**. |
| 2026-08-28 | **Device↔composite boundary agreed (D-056)** — `ScreenMeshHandle`, approved as proposed and the best D-054 application yet: no `mesh` field (removes the stale-`matrixWorld` slot), the change signal carries the value (removes "call this after the device moves" as documentation), and `viewport.corners` is projected device-side (removes W6's chance to get the y-flip or DPR wrong silently). Notification via `onBeforeRender`, so no `useFrame` exists in `packages/device` and an idle device costs zero. Two conditions: no raw `Object3D` for now, and **both lanes must pin the corner convention with a test at the seam**. W4 also fixed the repo-wide lint red. **D-057**: §4.2 and §4.4 are not simultaneously satisfiable by one room — ≈11× on the black poly — resolved with per-surface `envMapIntensity`, because 001 describes §4.2 as *light* and §4.4 as *reflections*. |
| 2026-08-28 | **W2 fixed all 9 findings**, each with a test that goes red when reverted. Fixed **structurally, not documentarily**: the clock mismatch is now unconstructible (`AnnouncerDriverOptions.now` removed — "a second injection point is how the halves came apart"); `pageRows` **deleted** from `DetentInput` so a caller cannot pass a wrong page size; `{7,7,7}` now fails 10 tests and `350→50` fails 4; density wired rather than deleted, with U11's 1.3 type-scale forcing; the coast **implemented rather than disclosed**, citing D-049 — deferring would have left "every coasted detent still clicks" as an unowned invariant. D-051 singleton with `createDeviceStore` throwing outside tests plus a static gate. 136 package / 413 repo tests. **D-054 promoted**: W1 and W2 independently converged on making the wrong thing *unrepresentable* rather than forbidden. **D-055**: Jotai `atom(fn)` footgun, added to W3's packet. |
| 2026-08-28 | **W1 complete** — 5 commits, 22 files, typecheck 11/11, **413 tests pass repo-wide** (230 its own), each commit standalone-verified via `git archive` rather than a checkout because three lanes are live. Used `git commit --only` throughout and audited `--name-only` on all five. All research rulings applied: 25-key union, no `ratingStars`, no `offline`, rows 10/11/18/7 held at `LIKELY` with the Swift-MusicKit trap written out. **Include hazard:** originally claimed enforced structurally; **that claim was false and the lead propagated it unverified** (D-062, D-061b). **Now true, and verified by the W1 reviewer rather than reported by the author:** `readSongRelationship(read: RelationshipRead)` takes one closed parameter object, and both attack routes are compile errors — a second parameter breaks a `Parameters<…>['length'] extends 1` assertion, a `status` field breaks a `HasExactKeys<…>` assertion. The reviewer's exact plant was re-run and goes red at `tsc`. Found two of its own naming-hygiene violations by running the grep rather than reading the rule. Rulings issued: **D-052** (`setShuffle`/`setRepeat` added — §14.2 omits what three other sections require), **D-053** (branded `LocalKey` and `"fixture"` in `ProviderId`, both accepted). |
| 2026-08-28 | **W2 review: REQUEST_CHANGES, 4 Major / 5 Minor.** Every gate claim reproduced; keyboard-no-acceleration and the silence seam survived 200 keydowns at 1ms, alternating directions, a keydown injected into an arc at ×7, and 10 path×source combinations. 10 plants, all different in mechanism from the engineer's; **8 red, 2 green — and the 2 green are Major 2.** Majors: (1) **U13 inverts to 30/30 in a real browser** — `Date.now()` vs `event.timeStamp`'s performance.now origin, a clock-domain mismatch the fixture hid; (2) D-042's densities and U13's 350ms **have no gate** — planted the exact flat 7 I ruled against, 93 pass; (3) `densityAtom` is inert, TSDoc contradicts code; (4) §4.4's inertial coast absent and, unlike three others, undisclosed. Three laws promoted: **D-049** (enforce invariants where you own them), **D-050** (a test computing both sides from the symbol is not a gate), **D-051** (one device store per document — binding on W3, added to its packet). |
| 2026-08-28 | **S1 DONE, committed `cc09d52`.** Both Minors applied; row 30 now carries the structural-vs-testimonial principle rather than "per the lead's ruling", and `libraryRemove`'s comment carries both legs. Index handed back byte-identical. Two laws promoted from its self-analysis: **D-048** (a flag is not an answer — its own log had recorded the labelling inconsistency and discharged it by deferring; the resolution took one paragraph once asked) and **D-047** (the asymmetry: never wrong about what the documents said, wrong four times about what that meant — three GETs beat two agents reading carefully). |
| 2026-08-28 | **S1 APPROVED (D-046)** after four revisions. **D-041 closed properly:** the reviewer built its own instrument rather than re-running the one under review — imported only `mintDeveloperToken`, issued 11 GETs itself. `/lyrics` → `40012`, nonsense → `40008`, and **all three negative controls → `40008`, so the oracle discriminates**. `/station` re-confirmed D-028 from a third source. Sharpest replication: **`credits` → `40403`, undocumented AND ungated** — the doc gap is not just hidden entitled endpoints, ordinary relationships are missing too, which is what actually kills the enumeration inference. Two Minors, both D-038 question-2 instances, one **against the lead**: row 30's label rested on "per the lead's ruling" rather than a principle. **D-045** adopts the reviewer's distinction — structural evidence (a mechanism) may carry `VERIFIED`; testimonial evidence (staff statements) may not — and promotes it to standing law. |
| 2026-08-28 | **W0 ACCEPTED (D-044), first slice fully done.** Both held Minors closed in `4c47531`: five `useState` selectors with three red plants including the untried `const { useState } = R` shorthand, plus a false-positive guard; and the rewrite plan's §4 rebuilt so it finds commits by message not hash, checks every commit from the rename onward, and states `--amend` is now the wrong recovery tool. ⚑ **It recommends the owner skip pass 2** — the rename is cosmetic, the `.claude/` removal and trailer strip are load-bearing. W1 and W4 dispatched. |
| 2026-08-28 | **W2 complete** — 4 slices + 2 follow-ups, 93 package tests / 140 repo-wide, 11/11 typecheck, each commit standalone-verified by extraction into a clean dir. Store-outside-React proven with 18 tests and an assertion that React is not even resolvable from the package; 40 keydowns at 5ms all `multiplier === 1`; 30 detents → exactly one announcement. Pre-planted its own violations (1/4/9 failures) anticipating review. **Incident: `55b34dd` swept six S1 files** — found in its own audit, and it proves **D-034 was too narrow**: a plain `git commit` after a scoped `git add` writes the whole index. Corrected to `git commit --only -- <paths>`; incident left as-is, nothing lost, no credential touched. Rulings: **D-042** (Shift+Arrow pages by density 8/6/4, resolving a two-against-one conflict inside 001) and **D-043** (pin `jotai@2.20.3` exactly — version skew fails silently, added to W3's packet). |
| 2026-08-28 | **S1 revision 4** — both Majors fixed; type construct synced to D-026 and verified 25 keys exactly; `displacedCount` relabelled `UNVERIFIED · docs` with both readings and their opposite consequences, and an explicit instruction not to cite it as grounds for relaxing the `> 3` gate. **D-040**: the author of D-038 was found still holding one unswept instance of its own pattern — and the tell was inconsistency, two neighbouring findings hedged and the third asserted, with no principle dividing them. **D-041**: the lyrics oracle has exactly one source; folded a second independent run into the final review pass. |
| 2026-08-28 | **W0 re-review: APPROVE.** Reviewer re-planted every gate with different plants plus five untried ones; all red. §12.1 diff is exactly 3 lines, 146/146 vars in build, both actor ramps present, all five parity plants red, zero type escapes. Blessed the `packages/ui/src/lib/utils.ts` cross-package exception as load-bearing and logged. **Lead holding 2 Minors as blocking-for-acceptance (D-039):** two remaining `useState` escapes (`R['useState']`, destructured alias) because `AGENTS.md` states the law absolutely, and the rewrite plan's §4 recovery block which is still tip-pinned and would hand the owner a wrong tree on a path they walk alone under D-003. |
| 2026-08-28 | **W0 patch complete, 4 commits** (`206742e`, `66db5b0`, `4cfd539`, `5ec0be6`). All 5 Majors closed with re-verified plants: D-020 applied as three edits — **146 declared, 0 missing**, both actor ramps present, with a test pinning all three blocks; both blurs `toBe()`-locked (plant 4.06 → exit 1); `scripts/` now its own project, **11 in the sweep** (plant → exit 1, was exit 0); shadcn wiring resolves and `components.json` no longer points at the transcription; CSS↔TS parity test both directions. `useState` namespace-alias escape closed. Gates at HEAD: typecheck 11/11, 140 tests, lint clean, `2305f4b` still an ancestor. Rulings issued on two flags it raised — **D-037**. |
| 2026-08-28 | **S1 revision 3 complete, re-review dispatched.** All four rulings applied; document now LEADS with the falsified inference rule. Reversed revision 2's narrowing of `pod-start-station` — the full `seedType` enum survives on Apple. **D-035** records its self-analysis: revision 1's row-18 sub-finding was a counterexample to its own inference rule, found by itself, forty lines from four findings depending on that rule — correctly labelled, then unspent. **D-036 reverses my endorsement of D-025**: the write probe for rows 10/11 is now the highest-value open experiment, blocked only on the owner's interactive sign-in (H-10). |
| 2026-08-28 | `design.pen` flag closed — owner's own unsaved canvas edits, committed as `4a4e3f4`. Index was dirty with two other lanes' staged work at the time; used `git commit -- <path>` to bypass it. D-034 confirmed in practice, not just in theory. |
| 2026-08-28 | **Resumed W0, W2 and S1** from their own transcripts (not re-dispatched, so their in-flight reasoning survives). S1's reviewer deliberately held until revision 3 lands. Packets corrected ahead of the work: **W6 respecified against `getElementTransform`** with a visible RESPECIFIED block over the wrong section, and **W4's route renamed to `[_]spike.device.tsx`** before it starts, so D-032 costs nothing there. |
| 2026-08-28 | **W6.0 complete, committed `8208ccc`. The main path's first real answer.** Chrome 151 **stable** with `--enable-blink-features=CanvasDrawElement` reaches **T1** — Canary not required, both tiers reproducible on this machine. **RISK-02 CLOSED** (D-031): ships `texElementImage2D` arity 3, matching three.js's Chrome 150+ branch, so `HTMLTexture` works unmodified; the IDL name `texElementSubImage2D` exists in no shipping browser. **BLOCKING finding (D-030): `updateElementGeometry` does not exist** — the W6 packet called it mandatory and it is unimplemented; Chrome ships `getElementTransform` instead, which is what three.js's `InteractionManager` already uses. W6.2 respecified; the thesis holds but not for the packet's stated reason. Also D-032 (dev routes need `[_]` escaping — `_` is a pathless layout, affects W4 identically), D-033 (a probe that crashes only when the feature is present), D-034 (explicit `git add` pathspecs are necessary but not sufficient — `--amend` re-reads the whole index). |
| 2026-08-28 | **Session limit hit ~11:16; W0, S1, S1's reviewer and W2 all killed mid-work.** Index verified clean, no credentials tracked, `cert/` still ignored. Resume state recorded above. W2 landed 3 of 4 commits before dying. |
| 2026-08-28 | **S2 probes run — three rows closed at `live` provenance, and S1's headline finding falsified.** Row 20 SUPPORTED (`VERIFIED · live`, D-028) — station id is `ra.` + the song's own catalog id, the only `supports()` value that moved. Row 21: the lyrics endpoint **exists but is gated** (`40012` vs the `40008` nonsense control) — **001 was right, S1's correction retracted, the `⚠` on §14.3 row 21 withdrawn**. S2.4 falsified S1's inference rule: three relationship names live outside the documented set, so rows 10/11/18/7 downgrade to `LIKELY · docs` (D-029). Adapter hazard recorded for W1: Apple silently ignores invalid `include`/`views`/`extend` and returns `200`. Both S1 and its reviewer interrupted mid-flight and re-aimed. Lint green tree-wide; no credential incident, 22 GETs, read-only enforced in code throughout. |
| 2026-08-28 | **S1 revision 2 delivered, re-review dispatched.** All 4 Majors and 6 Minors addressed; every count re-verified by the author (nine DELETEs not ten, 302 not 301, five topic entries / three endpoints). Row 30 enumeration now grep-derived with line numbers: 25 lines case-sensitive / 30 insensitive / 45 occurrences, seven per-screen Offline rows. Author accepted the S2 methodological hit without hedging and added a warning block to row 20 so nobody runs the non-discriminating fixture. **Surfaced a real 001 inconsistency** — the `Capability` union contains `ratingStars` while the interface has no method for it and §14.3 row 22 makes stars local-only — which corrected D-023's stated justification and produced **D-026** (drop it from the union) and **D-027** (`displacedCount` escalation is dead code on Apple, an agent-safety estimate the WebMCP workstream needs). |
| 2026-08-28 | **H-11 closed.** Owner supplied `APPLE_TEAM_ID` via `.env.local` and confirmed MusicKit capability on the key. Verified without printing the value — present, correct format, ignored, untracked. S2 unblocked and instructed to run its own corrected row-20 design (3 artists x 2 songs + artist-station control), not S1's non-discriminating fixture, and to label on both D-022 axes. Read-only enforcement stays; D-018 remains uninvoked. |
| 2026-08-28 | **W0 review: REQUEST_CHANGES, 5 Major / 7 Minor.** §12.1 transcription confirmed byte-identical (reviewer extracted and diffed independently), every §12.0 geometry number correct, commits trailer-free and standalone-typechecking, history intact, all four AGENTS.md laws correct, no key material anywhere. Blockers are **enforcement holes, not wrong numbers**: (1) D-020 unapplied — **and my ruling was insufficient**, corrected in place, `@theme static inline` needed on two further blocks or 36 vars including both actor ramps stay shaken out; (2) `HALO.blur` not locked — planted 4.06 passed 15/15; (3) `scripts/` outside every tsconfig, so the gate runner itself is ungated — planted type error gave "10/10 clean"; (4) shadcn wiring dead on arrival and `components.json` points its writer at the byte-identical token file; (5) CSS and TS hold unreconciled copies of the same numbers with no binding test. |
| 2026-08-28 | **S2 returned blocked: no `APPLE_TEAM_ID` anywhere, and not derivable from the key** (H-11). Both scripts written, tsc+eslint clean, read-only enforced in code via `assertReadOnly()`. No credential incident — key never printed, imported non-extractable, DER zeroed in `finally`, token withheld by design, verified mechanically absent from all four written files. **S2 found a real defect in S1's row-20 experimental design** (D-024): three songs by three different artists cannot discriminate the two hypotheses, so the probe would have confidently upgraded row 20 on evidence that does not support it. Rebuilt as 3 artists x 2 songs plus the artist-station control. **S2 declined write probes and argued against them; lead endorses** (D-025). |
| 2026-08-28 | **S1 review: REQUEST_CHANGES, 4 Major / 6 Minor.** Research itself validated — every quote re-fetched from primary sources, row 21's correction of 001 §14.3 confirmed four ways, enumerations genuinely exhaustive, no inflated label, guardrails clean. Majors: (1) label vocabulary conflict disarmed the §15.3 #14 tripwire -> **D-022**, two-axis labelling; (2) `ratingStars: false` contradicts §14.3 row 22, never examined -> **D-023**, stars are not a provider capability; (3) row 30 blast radius under-enumerated — **error propagated into my own D-019, corrected in place with a visible CORRECTION block**; (4) three §7.2 tools omitted from consequences. Findings relayed verbatim to the implementer. |
| 2026-08-28 | Owner rulings: **write probes authorised** on a throwaway playlist only (D-018), and **offline cut repo-wide** (D-019, ⚠ deviates from 001 §14.2/§5.1/J6b — closes H-7). Obstacle surfaced: write probes need a Music User Token from an interactive browser sign-in, which no script can obtain — H-10 opened, S2 amended to deliver the auth page rather than flail at it. |
| 2026-08-28 | **S1 returned: row 10 NOT SUPPORTED (`VERIFIED-docs`)** — Apple's playlist API is append-only, the bad-news branch. Row 18 makes S17 read-only on **both** providers. Row 30 settles the Service-Worker premise. Row 21 ⚠ contradicts 001 §14.3's own premise. Antagonistic review dispatched (separate instance). Findings recorded as D-015; **do not build on them until review returns APPROVE**. |
| 2026-08-28 | **Owner supplied MusicKit credentials** (`cert/`). D-006 partially superseded by D-016. Key arrived untracked but **not gitignored** — closed immediately (`cert/`, `*.p8`, `*.pem`, `*.key`, `*.p12`), verified with `git check-ignore`. D-017 opened as credential-handling law: server-side only, never in git/logs/prompts. S2 dispatched: **read-only probes with a developer token; library writes explicitly unauthorised** because they would mutate the owner's real Apple Music library. |
| 2026-08-28 | Owner amendment: fallback tiers may require **different textures, shaders or even a different renderer**, accepted as end-of-line work. Seam widened — a strategy now declares `requires: {renderer, materialVariant, shaderVariants, textureSet}`, and W4's materials/env-map/shaders become injected inputs defaulting to §12.3 rather than inlined constants. Explicit anti-overbuild rule attached to both packets: injection points only, exactly one variant proven end to end, **no variant registry or second renderer in 002**. |
| 2026-08-28 | **Owner ruling: T1 `html-in-canvas` is the main path, built first; T2/T3/T4 deferred, seam designed now.** W4 promoted from throwaway spike to the device layer proper (it must expose a screen mesh). New lane W6 owns `packages/composite` — tier detection + `PanelPixelSource` strategy. W0 amended mid-flight to scaffold a 10th package. U10's a11y re-review pulled into scope: axe must run on the composited page. RISK-01 and RISK-02 opened. |
| 2026-08-28 | Owner ruling: fall back to sub-agents for implementation, since no session exists in this working tree. W0 and S1 dispatched in parallel (zero file overlap). Reviewers still to be spawned as separate instances before either slice is accepted. |
| 2026-08-28 | Workstream opened. 001 handover ingested. Scoping bundle written. No Kanban (owner ruling). Force-push barred for all agents (owner ruling, now repo law). H-2 answered: Apple Music docs-only. H-3, H-4 defaulted by lead. |
