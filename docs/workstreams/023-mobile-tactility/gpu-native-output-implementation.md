# Native per-material output implementation

CPU009 bounded output correction follows gpu-native-output-proposal.md and independent pinned-source review. Baseline source copies and hashes are in `/tmp/webpod-native-output-original` and evidence/gpu-worker/native-output/baseline.json. No GL source behavior, physical formula, geometry, texture or DOM effect changes are authorized.

Target/cache decision under review: a compile-time renderer.isOutputTarget branch is insufficient because installed RenderObject.getDynamicCacheKey does not explicitly discriminate that context. Use one stable shader graph with a typed render-update boolean uniform selecting encoded final RGB versus unchanged linear RGB. NodeManager.getNodeFrameForRender supplies the actual renderer, and updateForRender updates nodes before submission. Distinct renderId values allow a shared material to switch output→linear target→output without retaining a first-compile decision. The proof must exercise that actual reuse path. Preserve original customProgramCacheKey receiver plus policy/toneMapped identity and needsUpdate. Exposure remains renderer's live uniform. PMREM-room factories are explicitly excluded, independently of the pass uniform.

Pending: implementation, exact source/math/ownership proof, types/lint/existing suites, lead-owned bounded actual GPU compilation/readback and independent implementation review. No source or full-quality approval is asserted by this planning record.

## Coherent source checkpoint

Reviewer confirmed the runtime pass gate contract before implementation: NodeManager supplies the current renderer, Renderer updates nodes/bindings before draw, NodeFrame deduplicates by renderId, and UniformNode preserves a returned false value. Implemented the stable gate, explicit RGB mapping/encoding before original setupOutput, policy/cache identity, and pre-compile detached-root coverage assertions. Native canvas is explicit UnsignedByteType (installed backend bgra8unorm), NoToneMapping and nominal LinearSRGBColorSpace; exposure remains1/live renderer reference. No PMREM factory was wrapped. GL material construction executes no installer.

Current application checkpoint is frozen for the lead's unrelated GL validation. Device, composite and web typechecks pass. Existing materials.test.ts and material-map-ownership.test.ts pass12tests/101assertions. Five-source scoped lint passes. Retained contracts.ts runs10 assertions for idempotence, receiver/cache identity, toneMapped identity, detached-root coverage and unsupported/mutated outputs. These are source/lifecycle evidence, not GPU arithmetic proof. The browser probe is prepared but not executed; exact alpha/output and shared main→linearTarget→main results remain mandatory before source acceptance.

The lead approved a temporary `packages/device/src/diagnostics/native-output-probe.js` solely for canonical Vite dependency resolution. Its complete source is retained as gpu-probe-source.js.txt. It is not imported by the app and must be removed after capture, before clean builds/commits. The probe uses two owned4×4 OffscreenCanvas renderers with production antialias:true, a1pixel read per case, owned read buffers destroyed in finally, existing backend initialize/compile bounds and a45second abort deadline. It compares12 mapped/unmapped alpha/premult cases and4 ordered unmapped-LCD→mapped-alpha→mapped-basic/physical-glass cases against actual GL bytes. Shared material output→linear target→output verifies the runtime pass uniform across cached reuse. The physical stack intentionally has no PMREM and does not replace the full real-scene optical comparison. Actual results remain pending.

## Actual GPU proof and final source freeze

Lead executed the bounded probe through the existing current Chrome page21; its returned value is retained in gpu-result.json and summarized in gpu-summary.json. All12 mapped/unmapped alpha/premult cases and4 ordered MSAA cases matched actual GL bytes exactly (max error0). Shared output→linear target→output produced RGB137→64→137, demonstrating the actual pass gate across reuse. Basic/standard/physical and scalar fragment-node variants compiled. These are small deterministic output cases, not GPU timing or full PMREM/appearance certification.

Successful function return occurred after its finally: deadline cleared, read buffer destroyed, target/geometry/materials disposed, GL renderer disposed and native backend owner disposed. Temporary diagnostics/native-output-probe.js has been removed. To reproduce under a new approved browser handoff, temporarily copy retained gpu-probe-source.js.txt to that exact diagnostic path, run its exported function through the existing Vite page, and remove it in the handoff finally; gpu-probe.js is only the evidence-side loader for that temporary module. No diagnostic helper remains in app source or may enter a clean build.

Final five-file hashes remain those in manifest.json; no application source changed after the coherent types/lint checkpoint. Independent implementation review and full existing-route glass-on Silver/light, Black/dark, packet/liner/sticker parity remain required. Source acceptance must not be mistaken for final native preference activation.
