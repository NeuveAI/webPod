# Production packet opening and first-peel validation

Prepared for reviewed GLwarmup8520800 and carryprimingbd4b9d5, without running browser/server/build or editing appsource. Lead owns the current production browser. Capture helper `evidence/gpu-worker/priming-validation/probe.ts` exports install/stop/marker expressions only; importing it does not contact a browser. No tracing. Observers have25s automaticstop, count caps, support flags, pending-record drain and final listener/RAF removal. The supervisor MUST evaluate stopExpression in an outerfinally and save its returned object, even after a failed gesture; it deletes the temporary global. An automatic timeout stops work but the small result remains available until thatfinally collects/deletes it.

## Minimum provenance and sequence

1. Build only clean reviewed source with all temporaryLCD instrumentation restored. Retain exactcommit/tree, dirty-source check, clientasset ID/build log and dependency-lock hash. Prefer the established gitarchive build. If using clean working-directory dist instead, label that provenance accurately; do not call it an archive. No builds or HMR during capture. Record actualcurrentChrome target,440×956 CSSviewport,DPR3,6×CPU, sourcebackendworker orwebgl, T1/readiness/no failure and zero testplacements. Preserve existing userplacements if not zero; use a before/after ID list to distinguish the testitem.
2. Native **cold opening**: clean reload, wait for frontready/entrysettled and sourceartworkready. No prior pickup on this renderer. Start observer while front or rear **packet still closed**; markbefore-open. Capture the trusted transition to rear, packet opening and liner/sheet reveal using current visible semantic targets. Keep priming started byopening inside this window. Report down→open, open→usable and longtask/LoAF/RAF windows separately from any preceding rotation; actualstate samples/markers define boundaries. Do not wait for an unexposed internalcontext-ready signal before pickup.
3. **Immediate first peel**: as soon as the earned sheet slot is actually visible/enabled, mark sheet-usable and before-first-peel, then dispatch the same NightShift drag to rear. Keep one continuous observer across opening→peel so work is not lost between two installs. Split analysis at actual input/down timestamps; report the measured gap between firstobservableusable and actualpeeldown. If a tool/poll delay leaves a long gap, label it delayed-first-peel, not immediate. A second run with the inspector held ready to dispatch may be necessary; do not manufacture a latency guarantee from a manualmarker.
4. Verify trusted down lands on the expectedslot, complete samepointer down→moves→up with no pointercancel, and actualsavedplacement+nativebackend/no failure afterdrop. Stop/drain/remove observer immediately after boundedsettling. Retain before/afterstate plus inputlog; DOMstage alone is not proof of saved persistence. Return only the testplacement using existing userflow, verifybeforestate restored; no storagewrites.
5. GLwarmup source8520800 needs a separate cleanGL route readiness check: T1, actualprogram/artworkready, packet+liner+earnedslotvisible, no artworkfailure, workingfirstpeel/drop and testcleanup. Use sameboundedobserver if performance is being compared. Do not treat a passednative capture as GLwarmup evidence.

## Trusted coordinates and interruption

ExistingCDP rawcoordinates have occasionally been scaled. Resolve the current earnedslot boundingrect and target rearpoint immediately before input. Capture actualtrustedPointerEvent clientX/Y,target,slot and pointerID with this helper. Do not assume the previous0.75 factor survived navigation. Preflight a harmless existingcontrol/rotation on a separate calibration navigation if necessary, then reload for the coldcapture. If actualdown does not match intendedslot/coordinates, immediately sendtouchCancel in the gesturefinally, mark the run invalid and retain it separately; it must not enter timing comparisons. Never continue a wrong-target drag hoping it reaches the rightplace. Afternavigation verifythe currenttarget/viewport again. Touchdriver should always clear activecontact in its ownfinally, and the observer must independentlystop in the supervisorfinally. A missingup or wrongtarget invalidates a claimedgesture result regardless of niceRAFstats.

## Readout and decision

Compare opening and firstpeel separately against the alreadyretaineda822 cold/repeat examples, qualifying changedobserveroverhead and singletoncaptures. This helper additionally records small DOMstates perRAF and trustedinput; for causalbefore/after comparison rerun both sources with this samehelper ifneeded. MainRAF is notGPUFPS. Report eventinputdelay/processing/roundedpresentationremainder, actualgesturelength, task/LoAF timing relative to openingandpickup, observertruncation/support and actualbackend/state. No wholewindowp95 mayhide an openingstall. The previouslyobserved20.9MBcontextcopy must not merelymove into a longopeningtask. Priming is acceptable only with boundedopeningwork/currentanimation and input responsiveness preserved, pluscold-interruption/cancellation correctness alreadyindependentlyreviewed. If openingdegrades or immediatepeelstilldelays, retain the failure and isolate renderpriority/coldreadiness rather than claimingvictory from longtaskabsence.

Current lead runtimehandoff: cleanworkingdistbd4b9d5,14types/buildpassed, clientindex-v0F8sn3j.js, sameoriginlocalhost3000 launcher/tmp/webpod-same-origin-production.ts, session45736. This is suppliedprovenance, not a buildperformedbythisanalyst. Leadmustrestore normaldev aftervalidation. No additionalapp edits/fullbuilds while thisdist is reserved.

## Executed bounded native capture

Captured one continuous production opening→liner→firstpeel on currentChrome21,440×956,DPR3,6×, clientindex-v0F8sn3j.js, sourcebd4b9d5. Authoritative retainedbuildprovenance is production-build.json supplied by lead. WindowUTC2026-09-11T19:28:27.3735–19:28:31.2681. No browsertrace, build, server or appsource changes by this operator. Browser was explicitly returned to lead after cleanup; no repeatcapture was performed.

Preflight rawinput used a now-stale0.75 assumption: down307.5/337.5 actually reachedOL atthosecoordinates; immediatelycancelled, no drag or packetwork. This was outside measurement, not included as a validgesture, and could unlockinteractionaudio. Scale1 then delivered trustedCANVAS410/450→28/450/fullup and revealed the closedpacket. Thus this is the firstpickup on the freshnativeowner, not the firsttrustedinteraction afterload. No deliberatecontext-ready wait was introduced.

The retained run contains3trusteddowns,72moves,3ups,0pointercancel and3expectedlostcaptures. Opening/liner down hit theirSPAN children at currentcomputedlipcenters. Firstpeel down hitactualBUTTONslotPW-A01 at131.6797/433.2723 and ended220/150. The slotwasenabled atsheet-reveal1; actualpeeldown occurred1.5ms after the **sheet-usable marker**,20.5ms afterlinerup timestamp (5.2ms afterlineruphandler). This is not a context-ready timestamp. Slotreadiness was checked through existingDOMonly; no internalprimingstate was awaited.

| Window | MainRAF max | Recordedtasks / LoAF | Down event / handler | Up event / handler |
|---|---:|---|---:|---:|
| Packet opening until linerdown (1368.8ms) |66.6ms|1task52ms /1LoAF74.6ms|72 /26.5ms|24 /7.3ms|
| Liner opening until peeldown (857.2ms) |18.7ms|0 /0|24 /4.4ms|24 /4.9ms|
| Firstpeel and boundedsettle (1633.2ms) |18.6ms|0 /0|56 /6.4ms|24 /10.8ms|

The52ms openingtask starts116.1ms afteropeningdown; its associatedLoAF starts85ms afterdown and includes19.9ms MessagePort.onmessage with noforcedlayout, but the remainingcost is notattributed. This must not be called stallfreeopening or assigned to the20.9MBcontextcopy without namedtiming. Firstpeel's56ms event has7.7ms inputdelay and approximately41.9ms roundedpresentationremainder. It is better than the priora822 cold184ms observation but singletoncaptures and differentobserverinstrumentation prevent an isolatedpriming-speedup claim. No wholewindowstat hides the openingstall.

Actualplacement1 and uservisible“Night Shift stuck to your iPod” were verified. Cleanup used existingEditNightShift→Returntopack UI, then verifiedplaced0,worker,no reportedfailure, temporaryprobe/calibrationglobalsundefined. AllRAF/observers/listeners were stopped and drained in the capturefinally; allsupportflags true and no collectioncaps reached. `opening-first-peel.input.json`, `opening-first-peel.summary.json`, `capture-runner.ts.txt` and `cleanup.json` retain the exact evidence. The runner adds a boundedinputlog reference to the temporarycollector solely to verify eachactualdown; it is deleted by the samefinally. No applicationdebugglobal was added.

**Validity limitation:** lead reported concurrent scopedtypes/lint may have overlapped measurement despite the requestedhold. Exactcapturetimestamps above are retained; until externalchecktimestamps exclude overlap, hostcontention is possible and these timings are observational, not a clean performanceacceptance gate. It would be unjustified to attribute the52ms event solely to the app or to dismiss it as tooling. Only a lead-authorized quietrepeat could resolve that confound. The capture remains valid for trustedgesture/state/cleanup correctness.

GL8520800 validation in this handoff is limited to lead's actualstage-present/no-artworkerror observation beforethecleanbuild. This operator did not exerciseaGLdrop and doesnotclaimthefullGLinteractionmatrixpassed. Lead restoreddev after thisbrowserhandoff; no furtherbrowseraction is planned by thisanalyst.
