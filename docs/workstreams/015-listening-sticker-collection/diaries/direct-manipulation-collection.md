# Direct manipulation implementation diary

2026-09-06. The designer brief and full direct-manipulation scope were read before source edits; independent review refinements are binding. Existing collection/material briefs remain authority for immutable prints and matte packet laminate. Modern Web Guidance was executed first via bunx; Interface Craft, all guardrail resources, Neuve Motion and Jotai/global guidance were reused/read as listed in the new design brief. Canonical Jotai atom read/write semantics were checked in resources/jotai/docs/core/atom.mdx. Installed Three0.185.1 Vector3 project/unproject and WebGLRenderer compileAsync/initTexture were inspected because pinned source governs this renderer.

## In-progress contract

Single app/scene owner. Shared interaction now retains an optional saved rear origin and own-sheet return target; native existing place/remove endpoints retain persistence authority. Rear pickup uses actual rendered geometry UV plus image alpha, excluding transparent corners before shell input. Rear peel geometry preserves the saved size/rotation and stationary adhesive tail. Static equipped materials stay mounted but hidden during carry, avoiding texture teardown and duplicate print.

Active five-slot preparation is mounted independently from visible packet geometry. It uploads decoded maps with initTexture and compiles actual earned/locked/placed/back materials against the current studio scene before declaring ready. Same-session counter/import refresh does not block this readiness. New liner pointer lane follows actual displacement; the giant click CTA is removed. Keyboard/direct-rear input and return behavior are being integrated, not yet final-verified.

## Profile-derived choices

Profiler baseline shows first liner146ms long frame/~130ms render callback, while three warm cycles avoid interaction long tasks. Texture loading/upload and a new program were observed after first open. This supports first-use preparation; it does not establish tessellation as the dominant cause. No paper geometry simplification is made. Close phase now hands off on the first visibly clamped zero rather than waiting through invisible spring undershoot. Acceptance is packet movement on the next scheduled frame after visible liner zero, not an arbitrary additional timeout. Preparation latency must be reported separately from admitted-ready gestures in the after profile.

## First actual-render iteration

App/package types passed at the first checkpoint, and thirteen readiness/model/status/return tests pass102 assertions. First real native production capture caught preparation remounting across pack-hidden/visible tree shapes, which caused readiness to flap; preparation now remains a stable sibling. It also caught the DOM ResizeObserver missing delayed-ready host mounting, leaving fallback viewport hit positions; the observer now reacts to readiness. A subsequent real liner drag exposed the packet lip's z-index stealing the new notch grab; corrected notch hit layering. These are rejected intermediate states, not final evidence. Native reruns remain in progress and expanded rear-pickup/return/rollback tests are still required.

Evidence directory: evidence/direct-manipulation/browser. No commits, no live credentials, no owner server manipulation. All test runners own their temporary native SQLite and browser processes.

## Native integration and corrections

The expanded native runner now exercises signed-out/owned-but-five-PNGs-delayed denial, first artwork503 with actionable retry, real packet/liner drag (CDP touch on375), direct placed pickup/move, own-seat return and reload, failed native removal preserving the authoritative placement, real concurrent-revision409 with a nondefault .18 width/23-degree transform, and Escape while a native placement write is pending. Actual partial and failed-empty Apple imports produce compact desktop/375 status captures. No browser request interception or product-only test switch was added.

The first own-seat screenshot exposed detached rear vinyl occluded by foreground paper despite successful persistence. The free carrier now moves toward the packet's front plane while retaining its projected grab point/apparent size; attached material stays on the exact rear geometry. Returning interpolates that world shape into the sheet's own shape before uncurling. Own-seat targeting wins over the rear hidden beneath it. A bottom approach keeps the mobile return workspace exposed for the rest of that gesture. A later mobile test caught semantic coordinates rendered before a reduced-motion model reframe; the renderer now publishes a projection revision after updating the actual model frame. Captures wait for real painted vinyl pixels over the seat, rather than treating the prior GPU frame as current.

`isStickerCarried` is shared by static rear and sheet seats: canonical server publication cannot show a second static copy while carry/press/return still owns the print. Alpha preparation retains only one byte per source pixel and occurs before the first hit; transparent corners do not claim shell input. Failure status is scoped to active/pending preparation or existing placements.

The material preparation helper was independently implemented/reviewed by the profile engineer: pinned Three185 public compile plus validated captured-program readiness replaces its uncancellable compileAsync poll. Exact material callbacks/cache keys are preserved. Program-owning clones remain alive after readiness until the scene's AbortSignal is cancelled; effect supersession, unmount and context loss abort the ownership. Retry clears failure only once current preparation succeeds. Eight helper tests/128 assertions cover lifetime/failure paths. No geometry reduction or shader speedup claim is made; after-profile remains an independent gate.

Checkpoint seven: native183 assertions passed31.2s, app/device/state typechecks and scoped lint passed after a narrow string-union typing correction. Focused source regressions:27 tests/78,146 assertions passed373ms (high count includes geometric vertex checks). The final native rerun adds return progression captures and exact final source fingerprint. Browser processes, native server and temporary SQLite are owned/cleaned by each run. Final performance and independent visual/correctness approval remain pending; these checkpoints are not completion claims.

## Frozen implementation handoff

Final native183 assertions passed31.30s, with actual return progression0/1/2/rest captures visually inspected. Source fingerprint `2c117a567e1fe08ef6426c5af726f17543f1a6a951255ff01965360a645d357c` /380 files; client SHA `f7bed7387902ad89f2f508063e433b0a5cc30f0d5c60e65f25e063d73355d773`; server SHA `1bfbb2b9a1351aef5123d2836158ae198a1e411cb337eb4d8968b972350c8ddb`. Exact evidence metadata in browser/browser-verification.json and owned logs preserved beside captures. All owned processes exited. The profile engineer receives the browser slot first, then independent reviewer. No source changes after this checkpoint without reopening fingerprint/validation.

## Independent request-changes and corrected candidate

The2c117a checkpoint above was rejected before any after-profile: direct placement set landing1 while its target was already flat, so the subsequent peel spring did not visibly press. The target now uses the same real rear-bound cylinder at current peel; landing retains its contact tail, and peel0 progressively flattens the raised section before static handoff. Normal free carry retains0.4curl continuously through placement, making the top contour/contact change visible at the actual100px print size. Reduced motion stays flat/direct. Reviewer is inspecting press0/contact-mid/rest before final after-profile. No screenshot-only pose or tessellation increase was used.

Review also caught readiness incorrectly gating the physical rear animation clock and a host mount omitted from ResizeObserver dependencies. Rear visibility now governs existing vinyl independently; packet readiness governs only workspace admission. Native375 reload with one locked own-sheet PNG held proves saved vinyl is visibly rendered, draggable, cancellable and persistently movable while packet geometry/DOM remain hidden. On final asset readiness, it returns to a coherent tease. A bounded pixel assertion uses the actual projected rear region after legitimate movement instead of an old fixed crop.

Corrected candidate: native188 assertions passed35.74s;27 focused tests/78,150assertions passed372ms; app/device/state types and scoped lint passed. Source58fa8839f150a8fe61175ca416d58ee442fd421b0801b9bd05a4bbca7387c261/380; client1e6328fa47fe42190ab4f6581256d196cf932dbc9d5448af634bfbfb599ad33d; serverdd5d5f31dfbc8a236f20458e5b24ef88bc4b5651bc9d90ca40df5369c50f4217. All owned processes exited. Current logs replace previous candidate logs; historical prose retains rejected checkpoints honestly.
