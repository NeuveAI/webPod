# Final contour integration runtime pass

Run only after C1/C2 freeze and independent reviews. Lead owns the existing Chrome target, localhost server and temporary probes. The baseline clean production is e940356; preserve its profiles/manifests rather than overwrite them. Main RAF timing is not GPU frame rate.

1. Record the reviewed source commit and clean production asset hashes. Build once, restart the owned localhost production server with the absolute root environment path, and reload only the existing /webpod target. Retain the user's saved Night Shift placement exactly; capture only relevant numeric state, not music catalog or account data. Keep phone emulation and CPU6×.
2. Cold editor: front→physical rear drag→select placed sticker. Observe time from query demand to publication, including first worker/copy setup. Validate control/contour appearance and no error. Use scoped temporary instrumentation only if necessary, with source hashes before/after restoration; never ship a diagnostic API.
3. Warm transition: physical dismissal drag395,490→80,490, then hidden repeat40,490→340,490. Use the existing bounded RAF/LoAF observer and separately a short CPU profile. Every recording has a stop guard and finally cleanup. Verify no full contour sweep remains on the main-thread chain, inspect remaining long tasks, and compare compatible result age to the measured synchronous baseline without asserting zero-age results.
4. Active human edit and registered tool preview: current input authority must remain accurate while poses change; final stationary result drains. Cancel a temporary edit rather than persisting changes. Verify tool-preview and human fade are inert and reopening shows the right print. Native and explicit GL use the same query lifecycle, with truthful pose stamps.
5. Repeat query lifecycle after hide/resume, resize and reduced-motion entry; preserve the original phone emulation and restore media emulation to No emulation. Check real visible→hidden→visible, not minimized-window inference. A forced-focus connector must not invalidate the test. Genuine GPU-loss remains unavailable safely in this shared browser; controlled recovery is reported separately.
6. Final playback/navigation during physical handling, packet pickup/cancel and exact saved placement restoration. Restore the ordinary development server, stop all owned diagnostics/profilers/observers, and leave the user's original trace and browser tabs intact.

The post-query profile determines the panel measurement follow-up. Source-level duplicate box access is already attributed, but the implementation is a separate reviewed slice. Whole-scope acceptance requires final source checks, current runtime evidence and updated PR6, not merely passing query unit tests.
