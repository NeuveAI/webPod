# W1 · gates

Run 2026-08-28 on `main` at b17ca75, from /Users/vinicius/code/webPod.
Four other lanes are live in this working tree; where a command reports on a package
W1 does not own, that is stated rather than tidied away.

## $ bunx tsc --noEmit -p packages/providers/tsconfig.json
```
exit=0  (no output above means clean)
```

## $ bunx --bun eslint packages/providers
```
exit=0  (no output above means clean)
```

## $ bun test packages/providers
```
bun test v1.4.0 (34cbb9a40)

 230 pass
 0 fail
 1272 expect() calls
Ran 230 tests across 8 files. [61.00ms]
```
Per-test roster in `w1-tests.txt` — the reporter condenses to a summary off a TTY.

## $ bun run typecheck
```
   ok  packages/composite

── tsc --noEmit -p packages/device/tsconfig.json
   ok  packages/device

── tsc --noEmit -p packages/panel/tsconfig.json
   ok  packages/panel

── tsc --noEmit -p packages/providers/tsconfig.json
   ok  packages/providers

── tsc --noEmit -p packages/server-core/tsconfig.json
   ok  packages/server-core

── tsc --noEmit -p packages/state/tsconfig.json
   ok  packages/state

── tsc --noEmit -p packages/tokens/tsconfig.json
   ok  packages/tokens

── tsc --noEmit -p packages/tools/tsconfig.json
   ok  packages/tools

── tsc --noEmit -p packages/ui/tsconfig.json
   ok  packages/ui

── tsc --noEmit -p scripts/tsconfig.json
   ok  scripts

11/11 projects clean
```

## $ bun run lint
```

/Users/vinicius/code/webPod/apps/web/src/routes/[_]spike.device.tsx
  164:3  error  Error: Cannot access refs during render

React refs are values that are not needed for rendering. Refs should only be accessed outside of render, such as in event handlers or effects. Accessing a ref value (the `current` property) during render can cause your component not to update as expected (https://react.dev/reference/react/useRef).

/Users/vinicius/code/webPod/apps/web/src/routes/[_]spike.device.tsx:164:3
  162 |   const camera = useThree((state) => state.camera)
  163 |   const paramsRef = useRef(params)
> 164 |   paramsRef.current = params
      |   ^^^^^^^^^^^^^^^^^ Cannot update ref during render
  165 |
  166 |   const sample = useCallback((): Array<ProbeResult> => {
  167 |     const active = paramsRef.current  react-hooks/refs

✖ 1 problem (1 error, 0 warnings)
```

⚑ **The one repo-wide lint error is not W1's and W1 must not fix it.**
`apps/web/src/routes/[_]spike.device.tsx` belongs to the device lane, which is mid-slice in this
working tree — the file is uncommitted at the time of this run. `bunx --bun eslint packages/providers`
is clean, and that is the scope W1 owns. Reported here rather than filtered out, because a lint run
trimmed to the lane that produced it is not evidence that the repo lints.

## $ bun test
```
bun test v1.4.0 (34cbb9a40)

 402 pass
 0 fail
 1843 expect() calls
Ran 402 tests across 15 files. [1043.00ms]
```

## $ bun run gates
```
$ bun run scripts/gates.ts
bun run gates: not implemented
error: script "gates" exited with code 1
```
exit=1

⚑ **Expected, and not a W1 failure.** `scripts/gates.ts` is still W0's deliberate placeholder: it
exits non-zero on purpose so that an unimplemented gate set can never report "no findings" for
checks that never ran. The gate harness is slice W5a and is unstarted. The greps this slice is
accountable for are run by hand in `w1-greps.txt`, with their exit codes.
