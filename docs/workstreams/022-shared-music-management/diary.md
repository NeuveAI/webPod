# Implementation diary

- Read Ready scope, decisions, architecture and provider contract packet before implementation. Applied global-patterns/Jotai/React guidance. Canonical local resource directory absent; read installed Jotai vanilla Store declarations. Modern guidance search executed through bunx; returned only low-relevance guides, no framework migration undertaken.
- Added @webpod/music-management with Jotai vanilla state, stable managed facade, accepted selection and queue context, common command and queue-read generation fences, lifecycle and account boundaries. Plain ordered writes preserve rejection recovery; play/pause supersede pending confirmation immediately. Normal progress remains independent from metadata events and requires no mounted React subscriber.
- Moved progressive library/source contract from app into neutral library. App retains auth, HMR/sticker setup and provider composition; publishes managed facades. Panel retains DOM, granular counter projections, navigation and scrub preview, delegates intent/queue truth/seek-resume. Navigation and production clickwheel paths resolve to same manager. Removed duplicate write queues.
- Spotify retains SDK relinking/UID occurrence and Connect mechanics. Removed its submitted-context total/append policy; native queueTotal is explicitly unknown. Added native play-request generation fencing across activation/connect and old HTTP failures. Common layer supplies selected-list totals and invalidates completeness after mutation/shuffle.
- Existing fixture expectation that confirmed duplicate selection hides its counter changed to require retained 3 of 3. Existing delayed double-toggle expectation changed to immediate pause admission plus mandatory pause after old play settles; proves cancellation without delaying pause behind a native start.
- Extracted existing Apple SDK fixture into test-fixtures.ts for reuse by native suite and real-adapter manager test. Spotify broad native contract fixture runs both directly and through manager; unknown native total versus known managed submitted total asserted explicitly.
- Review fixes: bounded pending SDK errors settle failure before item matching; complete native queue evidence recovers after mutation; selection confirmation refreshes queue even when optimistic identity already matches; logout preserves session notifications while playback is fenced. Apple station seed lookup captures native cancellation generation and uses existing transactional play for queue changes; native paused acknowledgements cannot overwrite newer resume. Accepted PlayTarget is copied before async dispatch so progressive source mutation cannot change the submitted list/count.
- Additional queue-read fix: read validity follows account epoch and queue-read/selection sequence, not unrelated pause generation. A pause during a same-context read no longer strands loading. Added the explicit delayed-read regression.
- Second review fixes: every supported queue mutation uses the same context invalidation helper; complete native evidence recovers after remove/reorder as well as append/insert. Accepted new play immediately clears old queue items to an honest loading state, preventing stale queue selection during handoff. Added actual Apple window+append test (absolute175/native101 remains unknown), and exact Spotify repeat0/1/2 SDK enum assertions in native/managed fixture runs.

## Spotify Play/Pause acknowledgement regression

User reported a JSON syntax error through physical Play/Pause after PR#2 publication. Source audit found no opaque identity parsing in common management or Panel. Lead sanitized live stack localized the failure to Spotify api.request JSON.parse, then safely observed PUT/v1/me/player/play HTTP200, no Content-Type,27-character non-JSON response; no payload or headers containing credentials were printed.

Added an explicit command acknowledgement mode to the existing Spotify request helper, preserving its authentication, retry and HTTP error handling. Play/resume and sibling void transport writes (shuffle/repeat/queue append) now consume success status without decoding opaque acknowledgements. Data reads, token decoding and playlist creation retain strict JSON semantics. No identity, common state, server credential or account behavior changes.

Deterministic physical toggle→common manager→actual Spotify adapter test failed before fix for synthetic200 opaque acknowledgement while documented204 passed; after fix both start/pause/resume paths pass. Command success alone still leaves target loading until native confirmation. API tests preserve malformed JSON read/create failures, token validation,403/429/500 failures and one-time401 retry.

User explicitly requested the same Play/Pause verification for Apple. Added three actual production toggle→common manager→createAppleProvider tests using existing MusicKit fixture: normal void native start/pause/resume with confirmation and retained selected counter; rapid second toggle while native start is pending; failed native resume followed by successful void resume. All17 production-control tests pass88 assertions. No Apple/common production source change was needed for these cases.

## Login readiness follow-up

The welcome handler treated resolved authorization promises as successful login. The runtime catches initial library errors and can retain an authorized SDK session, so checking session alone entered the player despite an error phase. The runtime now returns an explicit operation-fenced readiness result; the shared predicate requires both authorized runtime phase and session. Welcome consumes that exact snapshot, and rechecks it after the exit animation. BrowserExperience applies the same readiness gate before mounting the player on direct/restored routes. Spotify callback already returns the landing route, so server OAuth remains unchanged.

The existing runtime logic is enclosed in an injectable controller, allowing the production welcome-entry coordinator to be exercised with the real Apple adapter. Required first-page failures remain blocking; empty successful libraries remain ready and subsequent pagination still runs in the background. Spotify error retry restores its own runtime; the alternate provider sign-in link remains available. Optional sticker bootstrap remains independent.

New regressions cover caught 403/500 with an authorized SDK session, rejected and cancelled authorization, retry success, both-provider restore failure and empty-library success, optional sticker rejection, provider change during authorization, and concurrent/stale animated entry. Expanded focused regression: 508 tests / 2244 assertions across 20 files, no failures (`/tmp/login-regression.log`). Final all-workspace typecheck/build results pending. No commit, push or deployment by implementer.
