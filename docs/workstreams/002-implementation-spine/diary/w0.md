# Diary — W0 · bun monorepo scaffold, tokens, repo hygiene

**Lane:** L-A · **Status:** slices W0.1, W0.2 (non-destructive half) and W0.3 complete;
history rewrite prepared and handed to the owner.

---

## What exists now

A bun-workspace monorepo, ten typechecked packages, three commits, no trailers.

```
package.json            workspaces: apps/*, packages/*   (no pnpm, no turbo)
tsconfig.base.json      strict base; every package extends it
eslint.config.js        flat config; bans useState repo-wide
.gitignore
scripts/tsconfig.json   scripts/ is a project too, so the gate covers its own runner
scripts/typecheck.ts    the per-project gate loop
scripts/gates.ts        placeholder, exits 1 until W5a
AGENTS.md               the law; CLAUDE.md is an untracked symlink to it
apps/web/               TanStack Start, Tailwind v4, one route
packages/tokens/        §12.1 verbatim + typed geometry, FX and motion
packages/{ui,state,providers,panel,device,composite,tools,server-core}/
                        package.json + tsconfig.json + src/index.ts stub, nothing more
```

`packages/composite` was added mid-slice on the lead's amendment — it owns the seam
between the DOM panel and the WebGL screen mesh. Stub only, same treatment as the others.
That is what makes the count ten rather than nine.

---

## Gate results

Every one of these was run, and its real output is in `evidence/`. Nothing is reported
green from memory.

| Gate | Result | Evidence |
|---|---|---|
| `bun install` from a wiped `node_modules` and no lockfile | clean, 214 root packages, all ten workspaces linked | `w0-install.txt` |
| `bun run typecheck` | **11/11 projects clean**, exit 0 | `w0-typecheck.txt` |
| `bun run lint` | clean, exit 0 | `w0-dev-and-test.txt` |
| `bun test` (tokens) | 47 pass, 0 fail | `w0-tokens-test.txt` |
| `bun dev` serves a route | HTTP 200, SSR markup, `data-mode="dark"` on `<html>` | `w0-dev-and-test.txt` |
| `bun run build` | client + SSR bundles, no nitro needed | — |
| tokens: `globals.css` vs §12.1 | diverges by **exactly 3 lines**, all D-020 | `w0-d020-applied.txt` |
| tokens: every declared var reaches the build | 146 declared, **0 missing** | `w0-d020-applied.txt` |
| shadcn aliases resolve | all four `Bun.resolveSync` calls OK | `w0-shadcn-resolution.txt` |
| tokens: geometry + parity tests | 47 pass; wheel/body 0.697 to 3dp; CSS bound to TS | `w0-tokens-test.txt` |
| hygiene: `git ls-files \| grep -i claude` | no output — `AGENTS.md` only | `w0-hygiene.txt` |
| hygiene: `CLAUDE.md` untracked symlink → `AGENTS.md` | confirmed, and gitignored | `w0-hygiene.txt` |
| each commit typechecks on its own | clean at all three | `w0-gates-go-red.txt` |
| **a planted violation makes the gate go red** | lint, typecheck and test all exit 1 | `w0-gates-go-red.txt` |

That last row is the reviewer's own sharpest question. My first pass answered it too
narrowly — the plants I chose all landed inside the gates I had built, and the review found
three that did not: `HALO.blur`, a type error in the gate runner itself, and a `useState`
reached through a namespace alias. The plant set now covers all of them (A, B, C, D, E, F,
H, I in `w0-gates-go-red.txt`), every one goes red, and every planted file was removed with
the tree verified back to its committed state afterwards.

Per-commit typechecks were done by extracting each commit with `git archive | tar -x`
into a scratch directory rather than a worktree, because git worktrees are forbidden by
the packet.

---

## Review outcome

The first review came back **REQUEST_CHANGES — 5 Major, 7 Minor**. Every finding was real
and all are now fixed. The numbers were never wrong: the §12.1 transcription was
byte-identical, every §12.0 constant correct, commits trailer-free, history intact. **The
blockers were all holes in enforcement** — gates that did not catch what they claimed to.

| Finding | Fix | Proof it is closed |
|---|---|---|
| **M1** D-020 not applied, and the ruling was itself insufficient | `@theme static` + `@theme static inline` on all three blocks | 146 declared vars, **0 missing** from the build |
| **M2** `HALO.blur` not locked — 4.25 → 4.06 gave 15 pass, 0 fail | both blurs pinned with `toBe()` | plant 4.06 → 2 fail; plant 4.13 → 4 fail |
| **M3** gate runner outside the gate | `scripts/` is a tsconfig project in the sweep | plant in `typecheck.ts` → `FAIL scripts`, 10/11, exit 1 |
| **M4** shadcn wiring dead on arrival | dependency + widened exports + `css` moved + `utils.ts` | all four `Bun.resolveSync` calls resolve |
| **M5** CSS and TS unbound | `tokens-parity.test.ts`, 28 bound properties + completeness both ways | plant CSS-only divergence → caught |
| **m** `useState` escaped a namespace alias | `MemberExpression[property.name='useState']` | plant `R.useState()` → 1 error, exit 1 |
| **m** 6 further minors | TSDoc, D-017 into `AGENTS.md`, `__root` comment, rewrite plan, lint invariant, stale diary | — |

The gate now reports **11 projects** (10 packages + `scripts`) and **47 tests**.

M1 is the one worth remembering. I raised the tree-shaking rather than silently fixing it,
which was right; the lead's ruling was then wrong in a way neither of us caught, and it took
the reviewer *applying* it to find that 36 more variables — both actor ramps and every
shadcn semantic — were still being dropped. Three passes to land one three-line change,
on the token that carries attribution channel 6.

## What is still open

1. **`exactOptionalPropertyTypes` is off.** Worth a ruling now rather than after four lanes
   have written code against the looser setting.
2. **`allowImportingTsExtensions` is now on** in the shared base — needed to bring
   `scripts/spikes/` under the gate without touching another lane's file. Logged in
   `decisions/w0.md §12`.
3. **§7.3's desktop geometry column is stale** and deliberately unexported (D-021). Needs
   re-derivation in 001 when the desktop shell is built.
4. **`/global-patterns` and `/bun-http` point at a non-existent `~/code/agent-context/`**,
   and `/shadcn` shells out to `npx`, which this repo forbids. Broken for every lane, not
   just this one, and recorded only here.

## Handover notes for the lanes that come next

- **The typecheck loop needs no maintenance.** `scripts/typecheck.ts` discovers any
  directory under `apps/` or `packages/` that has a `tsconfig.json`. Add a package and it
  is covered; there is no list to update.
- **Add dependencies to your own package's `package.json`.** Bun 1.4's isolated linker
  gives each workspace its own `node_modules`, so nothing is hoisted and nothing collides.
  The skeletons deliberately ship with zero dependencies.
- **`bun run gates` exits 1 by design** until W5a implements it. Do not "fix" it by making
  it exit 0.
- **The route tree is generated and committed** at `apps/web/src/routeTree.gen.ts`. The
  Start plugin rewrites it on `dev` and `build`; commit the result rather than gitignoring
  it, or a cold `bun run typecheck` fails.
- **`useState` is a lint error**, including the `React.useState` and import forms.
- **Consume geometry from `@webpod/tokens` as TS**, not from CSS custom properties — see
  the ruling above.
