# Preflight, not final interaction proof

Current candidates/preflight-cold-fixed.json combines actual front-residual-1-native matrices and exact source-matched C03 buffers with the later corrected grab helper. Its build ID identifies captured geometry/matrices, not the later inverse source. A fresh build/capture must precede approved interaction proofs. Source rear/front fixture omits some assembly hardware; final witnesses require correlation to actual drawArrays (the previous recorder only saw indexed drawElements). Glass/wheel/select indexed buffers already correlate.

Historical rejected-all-points-warm-start.json called the inverse during partial peel, unlike production admission after64px; it is superseded and cannot prove real cold admission. An earlier offline setup also copied Object3D.matrix with automatic updates disabled but omitted forced world synchronization. That invalid fixture left matrixWorld identity. All current preflight uses updateMatrixWorld(true).

With the correct matrices and admission, cold-front-failure.json records a real old-helper defect: a68px first target sends the first Newton trial beyond the rounded rear domain and returns null despite a valid target. Current helper uses bounded residual-decreasing legal trials and one physical XY plane seed, followed by the same actual shown-UV solve. preflight-cold-fixed has every admitted call accepted for four straightforward paths (15/17/8/14 calls), preserving partial samples as explicitly unadmitted and center:null. These are offline proof prerequisites; native attached contact, invalid-point refusal, event flow and latency remain unproven.
