# Current Chrome capability observations

2026-09-11, existing Chrome instance through chrome-devtools-mcp --autoConnect (local stdio bridge), page 113 corresponding to existing Computer tab 804261003 at http://localhost:3000/webpod. No new browser was launched. User's iPhone emulation remains 440 x 956, DPR 3. Current route HTTP 200 and native screenshot shows full device and Music screen; earlier context-loss cause remains unresolved.

Read-only runtime capability inspection returned navigator.gpu=true; captureElementImage, requestPaint, ElementImage, OffscreenCanvas, OffscreenCanvasRenderingContext2D.drawElementImage and GPUQueue.copyElementImageToTexture all exposed. GPUQueue.drawElementImageToTexture is undefined. Exposure alone does not prove transferable capture, worker upload, visual parity or hardware adapter availability.

Existing canvas reports three.js r185, 1320 x 2664 backing size, layoutsubtree present, one 320 x 240 DOM child, wpCompositeSourceState=painted, wpRasterDensity=3, raster=960 x 720, wpRenderWarm=ready. These are baseline observations, not after-change performance claims. No trace has been started during this architecture pass.

## Native snapshot transfer probe

Bounded five-second probe captures the existing panel during paint and transfers only the ElementImage to a temporary worker; no live canvas transfer or context replacement. Worker and snapshot resources are closed and listeners/timeouts removed in cleanup. First draw attempt used an options overload absent from installed Chrome 152.0.7977.83 and failed overload resolution. Correct five-argument retry reached drawElementImage but failed InvalidStateError: source captured from a different canvas. Both returned unchanged presentation canvas dimensions and unchanged panel transform. This disproves the detached staging-canvas route in the current browser; it does not disprove direct texture upload from a snapshot associated with the transferred presentation canvas. The latter remains to be established before implementation selection.

The subsequent associated-canvas contract probe PASSED through the current Chrome MCP. A temporary diagnostic canvas was transferred before context acquisition; worker WebGPU adapter/device initialized, main captured its solid native DOM child on paint and transferred ElementImage, worker copied via copyElementImageToTexture to rgba8unorm. Source and texture were both 64 x 32. A validation-only GPU row readback returned exact center pixel [100,150,200,255], matching the solid child; validation scope returned no error. Cleanup removed the temporary canvas and worker/device/texture/buffer resources. The live player canvas was untouched. This establishes the native associated-canvas upload contract only, not full LCD/shader/hit parity or speedup. Implementation must preserve that association and transfer before renderer context creation.

## Preparation and material verification

After preparation adoption, a foreground current-Chrome check showed the complete device and Music screen. Temporary cache inspection of the exact loaded module reported ready data, one owner, no active/queued request, no error and 23,148,696 bytes. Entry age is not preparation duration. The temporary diagnostics were then removed. Retained prepared-device-front.png shows readiness, not exhaustive parity or frame timing. Earlier background-only state and delayed inspection have no established root cause; navigation also interrupted initial probes.

With source edits paused and the same tab foreground, staged native material initialization passed with core features available. All 15 authored material variants compiled and submitted renders; console inspection showed only the existing Three.Clock deprecation warning. Actual WGSL readback for eight full-grid paper cases (18,818 vertices each) matched the CPU reference within the stated 0.00005 position/0.0005 normal limits; worst observed errors were about 1.192e-7 position and 6.728e-6 normal. Results are retained under evidence/gpu-worker/materials. These prove native execution and that numerical slice, not full player visual parity, production speedup or completed worker renderer integration. No new performance trace was started during these probes.

## Combined preparation/material source validation

Preparation committed as 88ecc44 (Prepare complete device geometry and textures in a bounded worker). Combined `bun run typecheck` passed all 14 projects; `bun run build` passed. Device/composite suite initially reported 413 pass and two source-text assertion failures: the known unchanged StudioEnvironment route assertion, and physical-continuity following the staged aperture API. The latter was corrected and its existing 17 assertions passed. Scoped lint across device/composite source and all new GPU-worker proof scripts passed after explicit globals corrected a diagnostic. Full repository lint also reports historical evidence-script errors; it is not recorded as clean.

The same Chrome MCP page113/CUA804261003 remains localhost:3000/webpod, visible,440x956 DPR3. Screenshot after preparation commit shows the full device and Music screen. CUA rotation drag timed out in Input.dispatchMouseEvent and opened a native context menu; Escape cancelled the attempted interaction. This is not a successful rotation or performance check. No trace was started. Full CPU6x interaction/performance acceptance remains pending integration.

Neuve doctor reports no configured workflow manifests/review packs. Local scan and focused source/triage reported missing machine correlations across changed files despite explicit source-linked Kanban proof. MCP latest lookup remained stale after refresh; explicit compact artifact `.neuve-artifact/triage-1789115650-295724000-95460.json` returned a validated review model. This is advisory review context, not correctness evidence or automatic approval. Final integrated review and source dispositions remain required.

Remote-debugging tooling note: a read-only direct connection through the default Chrome profile's DevToolsActivePort found no matching localhost player target and performed no page action. That helper was removed. Continue using the established autoConnect MCP bridge targeting page113/CUA804261003; the default profile endpoint is not evidence of the user's active instance. No recording was started by this check.

Supervisor independently reran the current renderer-host channel and prepared-lease experiments after usage reset: ten channel/capture cases and seven actual Bun worker/MessagePort lease cases passed. One canonical+main+render assembly accounts for69,446,088 bytes; mounted buffers remain intact and last-owner cleanup releases the worker. These are offline ownership checks, not browser rendering or latency proof. Full host integration is still in progress.

## Shared GL assembly readiness

With all three app writers paused, reconnected to the same user localhost tab: CUA804261003, new autoConnect MCP page241 after connection refresh. DOM reports complete/visible,440x956,DPR3. The shared GL assembly renders the front device and Music list; console query returned no errors or warnings. Retained screenshot `evidence/gpu-worker/browser/shared-gl-assembly-front.png` was inspected. The screenshot output is vertically cropped despite the DOM viewport report, so it supports scene readiness only, not full framing or pixel parity. No trace was started. Writers resumed afterward. Full native worker scene, sticker interactions, motion, recovery and comparative6x timing remain pending.

## Live native screen dimensions

On existing Chrome page241, an extra one-shot paint listener captured the actual panel ElementImage and closed it in finally; listener/timer were removed. No context transfer or trace. Result: ElementImage640x480; panel offset/style320x240; canvas1320x2664 backing/CSS440x888; window440x956,DPR3. Canvas metadata declares wpRasterDensity3 and960x720 target, with projected screen325.020x243.765 CSS. Native snapshot dimensions therefore differ from destination raster metadata. Host must use the observed native copy/scale contract and preserve intended destination quality, not equate snapshot dimensions with target allocation. Raw result retained in evidence/gpu-worker/browser/native-screen-dimensions.txt.

Interpretation limit: existing GL uses direct texElementImage2D native-element upload. The960x720 dataset is intended raster metadata, not a queried GL allocation. A640x480 capture does not by itself prove the GL source or GPU target is lower quality. Explicit destination copy scaling and actual glyph/color/alpha appearance need verification; no quality equivalence is inferred from these dimensions.

## Worker-route first integration attempt

Current existing Chrome page241 was navigated to /webpod?renderBackend=worker with the same440×956DPR3 emulation. Initial provider connection completed; the final page showed the intact Music GL front view and a1320×2664 layoutsubtree canvas with painted native screen. No worker readiness/backend attributes survived. The only collected warning was the teaser Three.Clock deprecation. This does not prove a worker frame: source authors confirmed propagation and are retaining the native failure reason on the stable composite wrapper to distinguish fallback. No performance trace started.

Shared assembly source/interface checkpoint074b9c1 was independently approved and pushed to draftPR6. Neuve focused triage artifact .neuve-artifact/triage-1789123389-330202000-7183.json still reports missing machine-correlated source/check metadata; source artifact .neuve-artifact/sources-1789123386-149197000-7464.json reports unavailable source graph. These are not correctness approval, and final review/acceptance remains open.

## Native scaled transfer confirmation

The current Chrome remote connection was recovered by accepting its queued remote-debugging dialogs. Native DevTools confirmed iPhone16ProMax440×956 and6×CPU, with Record off. MCP now identifies the same localhost route as page109. The associated canvas one-shot experiment captured640×320 and copied into960×480 in its associated worker GPU context. Six stripe-center RGBA samples matched exactly and GPU validation returned null. The10-second bounded experiment completed and closed the image, destroyed GPU resources, terminated the worker, revoked its URL and removed its temporary canvas. Retained associated-scale-probe.js and associated-scale-result.json demonstrate scaling extent/color/alpha, not glyph quality or full-scene visual parity. No trace started.

## First worker activation root cause

After forwarding diagnostics through CompositeInputBoundary to its real DOM wrapper, current Chrome109 reports requested worker/effective webgl. Successive bounded cause diagnostics identify the retained recipe as unavailable because a pending renderer lease was cancelled and its producer retired. This is pre-frame fallback, not shader or native pixel-transfer failure. Host author is investigating the initiating mount/effect cancellation and exact resource recovery. Device/composite typechecks passed at the diagnostic checkpoint; no source or full visual acceptance is implied.

## First actual worker frame

The pending-lease failure was resolved for initial React effect replay by deferring acquisition until the still-live mount; genuine cancellation recovery remains unfinished. The next upload error, No context found for ElementImage, came from Three185 lazy context configuration. Host now calls the public renderer.getContext() before announcing capture readiness.

Current Chrome109 then reports requested/effective worker, wpRenderBackend worker-webgpu and wpRenderWarm ready. The retained worker-first-frame.png shows the complete chassis and upright native LCD text. It also exposes a real visual defect: LCD content occupies only the upper-left portion of the intended active screen with unused black space right/bottom. This is NOT visual parity or activation acceptance. The author is fixing projection/capture sizing; full sticker/input/motion/fallback gates remain open. No trace started.

The LCD extent defect was the missing existing272×204 authored Panel to320×240 logical frame fit. Host extracted the exact shared fit used by GL and native. A subsequent current-Chrome worker-webgpu ready screenshot, worker-fitted-frame.png, shows upright content filling the active display. This supports front-view extent only; it does not prove shader/color parity across views, sticker/input completeness or performance. One screenshot call reported an inactive-page attachment; selecting the same page109 restored capture without creating another tab/browser.

## Native input first check

With the native adapter wired and host source frozen, current Chrome6× Reset view returned the device to its front. A trusted CUA click on the visible Select center (native window310,346; viewport approximately210..410×78..512) unlocked audio lifecycle and focused the device but did not enter the selected Songs view. A Menu-position tap likewise produced no observed navigation. The author is adding bounded admission diagnostics to distinguish query routing from coordinates; this is a failed preliminary behavior check, not input acceptance. No trace was started.

The native input diagnostic confirmed the Select pointer atCSS218.96875,589.28125 with front admission true. The nearest hits were visible device-select at3532.9447812795793 and click-wheel-select-input at3532.945773380183, with no dispatched channel. The adapter was raycasting all visual meshes rather than Fiber event targets. Author is restoring the same registered-event target semantics while keeping full geometry for other exact queries; no geometry/depth offset workaround. Retained native-select-hit.txt.

After a full reload of the corrected event-target adapter, the same trusted Select tap dispatched select and entered Songs. Menu returned to Music; clockwise quarter-wheel drag changed selected Songs to Radio (aria-selected checked). Right-edge drag rotated the device visibly. Worker backend remained ready. Native DevTools live INP reached272ms during these preliminary6×CPU gestures, so this is behavior progress, not responsiveness acceptance or a comparative profile. No trace started; further complete-scene profiling is required. A later rear-view screenshot is retained as native-input-rear-view.png without claiming pose timing or sticker parity.

## Native paint provenance after the screen transport review

Current Chrome page109, existing worker override route, iPhone16ProMax440×956 and Performance CPU6× confirmed through the current native window. A temporary passive canvas paint listener and root-style observer were bounded to55seconds and explicitly removed after about45seconds; no performance trace was started. Retained probe and output: browser/native-paint-observer.js and native-paint-observation.txt.

Stationary Music produced no observed paint. One trusted native edge drag visibly rotated the device and reported one paint whose changedElements contained the exact wp-composite-panel-host root, with one root style mutation. Native Select then changed Music to Songs and reported the same root-only changedElements identity, with zero root style mutations. Root-only filtering would therefore suppress a real content update. This is a short provenance observation, not a per-frame capture-rate measurement or stationary marquee proof. Live DevTools INP read112ms after the drag and232ms after Select; these are preliminary local observations, not a matched before/after performance pass.

## Full packet checkpoint: native admission failure

All three application authors explicitly froze source. Lead reloaded the existing current-Chrome page109 at /webpod?renderBackend=worker; native AX confirmed iPhone16ProMax440×956 and CPU6× with Record off. No trace started. Music appeared; a trusted edge drag showed the rear chassis. No packet actions were exposed. Inspection then confirmed rendererRequested=worker, rendererEffective=webgl and rendererFailure=Sticker computation byte capacity exceeded. Only the existing Clock warning was logged. The packet/material checkpoint failed; screenshots must not be presented as native packet acceptance.

The recovered complete GL chassis/player retained Music and usable Settings; source state was painted and audio lifecycle running. Backing size1320×2664 and screen raster960×720 remained. This is one observed fallback path, not full state/media/failure-matrix acceptance. Settings was closed after inspection. Retained browser/native-packet-capacity-failure.txt and native-packet-fallback.png. Warmup/capacity cause requires source diagnosis; no attribution to a specific batch or fix is inferred from the error string alone. Application freeze released for correction, then this same checkpoint must be repeated.

## Full-scene retry after exact resource admission

All application authors froze source;39 changed application file hashes matched before/after the checkpoint. A fresh current-Chrome page109 reload used the existing iPhone16ProMax440×956, CPU6× setup. Requested/effective worker, worker-webgpu ready, backing1320×2664, upright fitted Music LCD confirmed. Trusted native outer-edge drag reached the rear chassis. No trace was started; Record remained off.

Only Reset view and Settings remained visible actions: the packet and liner controls were absent. Host traced the concrete integration cause to StickerCollection reading frozen renderedState.orientation while the new motion authority changes. This is a failed full-packet interaction checkpoint, despite successful native renderer activation. Source freeze released for the narrow live collection subscription correction and independently found private pending-owner admission fix. Retained browser/native-full-scene-observation.txt and native-full-scene-rear.png. No packet/carry, quality-matrix or performance acceptance is claimed.

## Native packet and first-drop checkpoint

After the live collection subscription correction, a fresh current-Chrome native reload and trusted rear-edge drag exposed Pull sticker pack into view. Opening it and the liner rendered the exact packet cover plus five earned/locked prints while effective backend remained worker. The DevTools click tool reported interaction timeout as each target changed its label/state, but subsequent snapshots and screenshots confirmed the actions completed; they were not blindly retried. Retained browser/native-packet-liner.png.

A trusted native drag from the earned Night Shift print onto the rear created its placement, then replacement packet preparation exceeded the managed buffer limit at print3, completed2,26 entries,18 private owners,1 pending copy and83,146,840 accounted bytes. The renderer recovered to WebGL and displayed the placement plus artwork retry status. Retained browser/native-peel-failure.txt and native-peel-fallback.png. This fails full native carry/drop acceptance despite initial native packet rendering. Source freeze released for exact current/candidate resource reuse and independently reproduced duplicate equipped preparation. No trace started.

The single test-created Night Shift placement was selected through existing UI and returned to its pack using Return to pack. A subsequent screenshot confirmed the rear no longer displays it. No other placement or inventory was modified. Final persistence verification remains part of the next clean reload.
