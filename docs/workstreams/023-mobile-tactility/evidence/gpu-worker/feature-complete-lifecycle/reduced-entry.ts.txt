async function c(method:string,params:Record<string,unknown>){const r=await fetch('http://127.0.0.1:9348',{method:'POST',body:JSON.stringify({method,params})}).then(r=>r.json());if(r.error)throw Error(r.error);return r.result}
await c('Page.reload',{ignoreCache:true});const end=Date.now()+15000;let last;
while(Date.now()<end){try{const r=await c('Runtime.evaluate',{expression:"JSON.stringify({url:location.pathname,reduced:matchMedia('(prefers-reduced-motion: reduce)').matches,reveal:document.querySelector('[data-device-reveal]')?.getAttribute('data-device-reveal'),renderer:document.querySelector('[data-renderer-effective]')?.dataset.rendererEffective,alerts:[...document.querySelectorAll('[role=alert]')].map(e=>e.textContent?.slice(0,100))})",returnByValue:true});if(r.result?.value){last=JSON.parse(r.result.value);if(last.reveal==='complete'&&last.renderer==='worker')break}}catch{}await Bun.sleep(150)}
await Bun.write('/tmp/webpod-lifecycle-reduced-entry.json',JSON.stringify(last,null,2));console.log(JSON.stringify(last));if(last?.reveal!=='complete')throw Error('Entry not ready within bounded wait');
