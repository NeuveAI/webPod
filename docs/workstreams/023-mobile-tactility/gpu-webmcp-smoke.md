# Bounded GL WebMCP smoke — 2026-09-11

The supervisor handed over existing Chrome page 21 at localhost:3000/webpod, default GL, mobile 440×956 at DPR 3 and 6× CPU emulation. No application, server or build changes, no trace, and no performance observers were introduced in this pass. Browser ownership has been returned to the supervisor.

The registered document.modelContext API was exercised with its actual returned tool objects and JSON argument strings. Flicking to the back, opening the packet and acquiring PW-A01 from the collection succeeded. Acquisition returned a held draft and did not persist a placement. Releasing it returned held:null and pendingSaves:0, but pageState.status became error. The actual accessibility tree showed “A sticker image could not load.” with “Retry artwork” and disabled sticker slots. This is a failure after get/release; it must not be conflated with the earlier initial warmup failure.

One attempt to click the existing Retry artwork control through MCP timed out because the element did not become interactive. Successful dispatch was not verified, so recovery cannot be attributed to Retry. After that timeout the alert was absent and the actual page-state tool returned ready. Filtered browser console errors/warnings contained only two Three.Clock deprecation warnings, with no underlying application failure reason. The generic banner alone does not identify the failing preparation owner.

Opening the packet again through the registered tool produced an expanded liner and enabled Night Shift button in the accessibility tree. The final sticker-list response confirms PW-A01 earned/available, placement:null, held:null, pendingSaves:0, rearReady:true, collectionStatus:ready, loadedItems:5/5, stage:open, packOpen:true and sheetOpen:true. No test placement remains. Retained compact responses are in evidence/gpu-worker/webmcp-smoke/gl-responses.jsonl; the initial successful flick/acquisition occurred before this response log was installed and are observations rather than retained raw response artifacts.

The supervisor requested an immediate handoff at this point. Full GL pointer peel/drop/return was **not tested** in this pass. Native navigation, native WebMCP menu/center/list checks, keyboard, hide/resume and the remaining interaction matrix were **not tested**. The transient GL error remains an investigation target despite the later ready state; this note grants no full GL or native acceptance.

## Subsequent bounded native pass

After a separate explicit browser handoff, navigated the same page21 to `/webpod?renderBackend=worker`. Actual canvas metadata reported `wpRenderBackend:worker-webgpu` and `wpRenderWarm:ready`. Registered page/device/list tools responded. Back flick settled yaw180, packet open began its authored pulling stage, and PW-A01 acquisition returned a held collection draft. The actual accessibility tree contained the sticker controls. Release returned held:null and pageState:ready with no pending saves; the GL release error was not reproduced in this native sequence. These observations verify semantic/DOM state, not a screenshot comparison of carried artwork.

Front flick settled yaw0. Registered center and menu each returned accepted:true; center produced a loading page state and menu restored ready. No playback command was issued and no audible-output claim is made. Actual CDP keyboard down/up for ArrowRight changed native device-state yaw0→5; Home down/up restored yaw0. No keyboard Enter/rear-rejection case was exercised.

A temporary foreground about:blank tab was opened and then page21 was brought forward again. A bounded visibilitychange listener recorded no events, so actual hidden/resumed lifecycle was **not demonstrated**. The temporary tab was closed and the listener and both temporary globals were removed. No private tab content was inspected. No reduced-motion setting was changed. No trace or performance observer was installed.

Final registered state: front yaw0, not animating, not held, placed:[], held:null, pendingSaves:0. Worker canvas remained ready. Browser ownership was returned immediately. Combined retained tool responses are in `evidence/gpu-worker/webmcp-smoke/combined-responses.jsonl`; unrelated library/catalogue fields, artwork URLs and sticker meaning metadata are omitted. Native physical pointer drop, keyboard Enter/rear rejection, genuine hide/resume, teardown, reduced-motion and visual fidelity remain outside this bounded pass. Full GL UI peel/drop remains pending.
