# Finite GPU responsiveness closeout

**Completed at d9434f5.** The checklist below is the original finite acceptance contract. See [final results](gpu-feature-complete-final-results.md) for passed gates, corrected hidden-tab finding, cleanup and coverage qualifications.

This checklist follows the latest checkpoint in [the feature-complete plan](gpu-feature-complete-plan.md), superseding older unresolved rows in [the earlier validation matrix](gpu-final-validation-checklist.md). The active blocker is the real large wrapped GL preview capacity failure. Finish its independently reviewed correction, then run the bounded pass below on the exact clean production build. No further optimization of every remaining sampled function is required for completion.

## Established evidence — do not rediscover

- Preferred-worker selection is active. The earlier clean final build verified no-query worker entry, physical navigation/play-pause, packet/liner/drop/edit/return and a resize round trip. Exact node/GL output proofs and corrected actual front/rear material captures remain the visual constraints. No new whole-optics matrix is required for a resource-lifetime correction.
- [Contour runtime](gpu-visible-contour-runtime.md): exact native tool get/rotate/release and human corner-drag/cancel preserved the existing saved Night Shift placement. Warm sampled contour ages were 17–36 ms; qualified clean dismissal/hidden-repeat runs had no observed long tasks/LoAF. Cold first-query ages were separately reported, not disguised as warm results.
- [Post-contour attribution](gpu-post-contour-attribution.md) removed the former main contour/visibility/segment chain. [Panel runtime](gpu-panel-measurement-runtime.md) and [post-panel attribution](gpu-post-panel-attribution.md) show the repeated width-getter hotspot absent, with actual integer/fractional box invalidation and exact restoration tested. Those scoped long-task findings are resolved; unrelated residual matrix, messaging and rectangle samples do not automatically open new work.
- [Lifecycle baseline](gpu-feature-complete-lifecycle-plan.md), latest e940356 checkpoint: actual hidden→visible events, no stuck hold/animation, exact saved-placement preservation, late reduced-motion controls/orientation, and reduced-motion entry passed. These predate final query integration; only the short regression below remains, not another capability investigation.
- Genuine native GPU loss has **not** been safely induced in the shared Chrome session. Controlled lifecycle proofs and earlier real preparation-failure fallback evidence remain valid, but do not prove genuine GPU loss. Keep that coverage qualification explicit at closeout. Do not crash the shared GPU process, invent a loss API, or turn this limitation into a new permission/process gate.

## One final live pass after the GL correction

Lead owns the existing Chrome and localhost server. Before starting, record exact build/revision, effective backend, readiness, saved placement records/count, held draft and pending-save count. Preserve the user's existing Night Shift/PW-A01 placement, including its actual large wrapped width and full saved values. Never reset inventory or return that user placement as test cleanup. Use actual registered WebMCP metadata and existing physical controls; no invented fixtures or debug route.

| Gate | Minimal action | Required observation |
|---|---|---|
| Native immediate pickup and cancellation | On the ready default worker route, pull the packet and liner into use and start an available earned sticker pickup immediately, then cancel through the existing interaction path. Also make one normal settled pickup if the immediate attempt never admitted a draft. | Admitted visible carry and exact origin restoration, no alert/fallback/capacity failure, no stuck capture/draft or pending save. Preserve all existing placements. Reuse earlier drop/return coverage; do not persist a new test sticker solely for cleanup. |
| Large wrapped human edit — native and explicit GL | For **each backend**, open the actual existing large wrapped PW-A01 editor, make one reversible corner rotation/scale preview, then cancel. In GL reproduce the captured large-width rotation case, not a centered width .25 substitute. | Draft geometry/texture/contour/grips update coherently, no capacity/artwork alert, current input follows the admitted pose, and cancellation leaves the full saved placement byte-equivalent. GL remains ready/effective GL. Native remains ready/effective worker. |
| Large wrapped tool edit — native and explicit GL | For each backend, use the registered placed `get` command, one existing reversible preview command, then registered `release`; read state afterward. | Tool preview is visible with correct inert presentation semantics, no rejected preparation/stale save, `held:null`, `pendingSaves:0`, and exact saved placement retained. The previous native pass is a baseline; the GL correction needs actual current-source coverage. |
| Controls/playback during handling | On native, use physical wheel/Select/MENU and Play/Pause while performing one orientation release or packet interaction. Include one press/release during motion. | Navigation and player state continue to update; physical control returns to rest; no stale held state or lost input after handling. Restore the original pause/navigation state. Claim audible output only if actually observed; state/time evidence alone is sufficient for this bounded functional check. |
| Resize + hide/resume regression | One existing phone→alternate size→phone round trip with the editor/packet involved. Then one actual hidden→visible transition during a reversible interaction using the previously established same-window method. | LCD/panel and hit/contour alignment recover; backend stays healthy; actual visibility events occur; no held capture, obsolete contour or continuing stale animation; exact saved placement remains. Remove the bounded listener and close only the temporary task-owned tab. A blur/minimize with no hidden event is not a pass. |
| Reduced-motion regression | Preserve prior media settings, toggle reduce, confirm `matchMedia`, perform one control press/release plus orientation or packet/edit cancellation, then restore the exact setting. | Authored reduced-motion behavior, settled controls, no stale projection restoring a released press, no obsolete draft/save. The earlier reduced-motion entrance already passed; repeat entry only if the GL correction changes entry/readiness behavior or the final claim specifically needs a fresh entry check. |

Combine cases where possible. Ordinary live failures remain actionable blockers and receive one source-grounded correction/review/recheck. Successful finite coverage is not a mandate for a new broad timing matrix. The existing qualified contour and panel captures satisfy their targeted performance gates unless the correction changes those execution paths or introduces a new observed recurring stall.

## Build, cleanup and delivery gate

1. **Exact source:** independently approve the GL correction and any direct caller delta; no unresolved Critical/Major finding. Record final commit/manifests. Existing source reviews stand for unchanged slices.
2. **Clean validation:** final required affected types, scoped source/proof lint and relevant resource/parity/lifecycle checks pass; clean production build from that source. Confirm temporary error markers/diagnostics are absent from both source **and the served bundle**, not merely restored on disk. Lead records the final build provenance.
3. **State and resources:** final registered state reports no held draft/capture, no animation unexpectedly left running, no pending save, no artwork/error alert and exact original placements. Restore temporary viewport/media changes and the intended navigation/playback state. No recording, profiler, trace, observer, temporary listener/tab or diagnostic global remains active.
4. **Environment:** restore the normal devserver at the user's original origin after the temporary production pass. Preserve other tabs, the user's stopped original trace and unrelated repository changes. Do not deploy or merge as an implicit consequence of local acceptance.
5. **PR and closeout:** commit/push the coherent reviewed changes to the existing PR, refresh its body and workstream evidence with actual final results and the genuine-GPU-loss qualification. State explicitly whether PR review, merge or deployment remains pending. Current AGENTS makes this workstream the tracker: no Neuve or Kanban gate applies.

Feature completion means the GL blocker is resolved, the finite live rows pass with original state restored, clean exact-source validation succeeds, and the reviewed work/evidence is delivered in the PR. It does not mean every main-thread cost is zero, every possible browser failure was induced, or universal GPU frame rate has been proven.
