# Edge wrap handover

Status: implementation and validation active; no completion claim. Owner-requested editing performance and drag-outline corrections take priority. Source fixes and final fixture corrections passed review; fresh mouse/touch native validation and independent final evidence review pass. Physical peel contact/transition remains unresolved.

Current direction: procedural perimeter bend cage. Straight-edge static wrapping and one maximum-C03 corner pose have independent visual approval. Native cloth and earlier radial/geodesic/relaxation experiments below are retained as history, not active approaches. The engineer is correcting full-rear identity and quadrant continuity before a prepared 19-pose/25-frame spatial diagnostic. The app owner has independently reviewed native wrapped-interaction tests, but actual authoritative grab/occlusion/peel integration and evidence remain pending. No relaxed placement-only commit or task closure is authorized.

Latest state supersedes that checkpoint: the front ribbon regression and composited DOM ray-coordinate defect are corrected. The desktop side case passes pickup, partial peel/cancel, shell flick, relocation, exactly one save and reload. The desktop front case passes pickup/cancel and shell flick, then crashes during relocation. The geometry engineer is correcting the reproduced unsupported front mapping; touch cases, full-range mapping, interaction performance and final approval remain pending. App grab/contour/phase and device lifecycle fixes have bounded independent source/test approval.

Owner explicitly wants placement anywhere on the backplate and real wrapping of overhanging artwork around the physical edges. Screenshot in sticker-edge-wrap-scope.md shows the obsolete inset-refusal notice. Prior contour feature is committed (ed4e81c,a9355f4,2f1001a); its whole-art safe-inset behavior is now superseded. Concurrent WebMCP work is owner-authorized and outside this task's staging scope.

## Established source facts

At the task baseline, shared sticker validation required the entire rotated artwork inside normalized [.08,.92]×[.06,.94]. Editor, drop, runtime, HTTP and repository shared that restriction. The uncommitted domain/editor replacement is independently verified below and must ship together with accepted wrapping geometry. Existing coordinates map xlocal=(.5-x)*330 and ylocal=(.5-y)*552; maintain their meaning for old placements. Replacement validation checks the center against the true rounded rear domain through pure shared math, without server imports of Three/device runtime. Device geometry supplies real continuous overflow mapping.

## Completion audit

| Requirement | Evidence gate | State |
| --- | --- | --- |
| Anywhere on true backplate | Center-domain math + allfour edges/corners native placements, no old insetrefusal | Pending |
| Physical overflow wrapping | Continuous surface vertices/normals, actual side/oblique captures, no clipping/floating | Pending |
| Rounded corners and max-size continuation | Irregular and nearrectangular maxsize rotated stickers on corners, actual adjoining surfaces | Pending |
| Editing/hit/contour follows mesh | Real pointer hit + projected contour/grips, hidden-surface occlusion, stable grab | Pending |
| Peel/return preserves physical behavior | Wrapped peel avoids shell penetration, adhesive/UV/wear preserved, same choreography | Pending |
| Persistence and compatibility | Edge save/reload/re-stick, same legacy coordinates, shared server/client validation, guarded revisions | Pending |
| Validation and closeout | Deterministic/native tests, types/lint, independent review, scoped commits and no owned dirtyfiles | Pending |

Independent reviewer raised contract risks before code: planar rear peel can penetrate wrapped sides/front; contour indexing must match new topology; side-hit pickup must preserve grab offset; far-side controls must not draw or catch hits through the shell. These must be resolved in sticker-edge-wrap-contract.md and tested, not deferred behind relaxed bounds.

## Implementation checkpoints

Shared domain/editor/server changes independently passed44 tests /6,996 assertions. Legacy coordinates and ownership/revision guards remain; actual geometry acceptance is separate. Native driver is prepared for full-edge/corner mouse/touch persistence and max-aspect PW-C03 earned through actual observation endpoints, not direct ledger writes.

First global radial atlas was rejected:13.1% sampled triangles lost more than half area on the front. Tangential physical-arclength correction initially inverted a flat-front cell; independent diagnosis ruled out angular unwrap, and bisection solved the numerical residual. Area distribution improved (p5 .728, median .999), but extreme directional stretch (.174×2.367) remains unapproved. Lead/reviewer requested actual worst-case glyph renders, including occupied-alpha metric assessment, before more abstract refinement. Real Device front/glass/wheel resources are wired; wrapped peel/grab/occlusion remain unfinished. No static diagnostic is final native acceptance.

## Actual render rejects tangential-only atlas

Static-1 captured18 actual rear/side/front views before its final aggregate fingerprint failed under concurrent source changes. It is retained as diagnostic, not a passing suite. Independent visual review rejected maxPW-C03: visible side taper and front lettering shear confirm coupled strain, not merely camera foreshortening. No clipping, smaller size or edge exclusion is an acceptable substitute.

Lead/reviewer approved an isolated17×17 coupled surface-coordinate strain solver experiment with fixed center/orientation, edge/diagonal lengths and angles, orientation barriers and backtracking. First gates are the same rejected corner and ordinary Pulse edge, original-alpha weighted principal stretches, full-mesh orientation/intersection/clearance, and actual glyph fidelity. Stated stretch targets are falsifiable experiment goals, not a feasibility assertion. Physical bounded wrinkle slack is a separate option if exact contact cannot meet strain; no cuts. Runtime cache/solve costs must remain bounded and responsive before integration.

A test-only immutable-build provenance helper is approved for explicit external concurrency: stable complete source during build, full source/artifact manifests, exact post-build external allowlist only, and recorded hash deltas. No dependency/lock/config/test/fixture or sticker geometry/domain/controller exception; compiled artifacts must remain unchanged. This certifies the built artifact, never the later external checkout integration.

## Numerical architecture reset

Coarse17-control chart optimization and bounded slack probes were rejected: refining to33/65/129 exposed near-zero local stretches/interior folds, while pointwise fine optimization exceeded useful time without fidelity. No such experiment is accepted product behavior. Lead/reviewer selected a bounded physical3D sheet candidate with identical solved/rendered vertices, UV strain, bending, actual-shell collision and localized adhesion. Analytic cylinder convergence is the first falsifiable gate before retrying the corner; neither the method name nor positive normal lengths prove success.

External WebMCP owner directly confirmed its shared hunks and passing separate snapshot. Its registry seam in sticker-collection.tsx and generation/cancellation guards in sticker-interaction.ts must remain separately owned; packages/state/stickers.test.ts obsolete inset expectation was corrected in this task. Broad-test historical generated outputs were restored by their owner only; active wrap source/evidence was preserved.

## Cylinder checkpoint

Independent reviewer reproduced the actual 3D distance/contact prototype and metric oracle (3 tests, 693 assertions). With 500 bounded iterations, refinement from 17 to 33 to 65 vertices per side reduced edge strain ranges from .9938–1.0073 to .9988–1.0015, angular error from .0112 to .00128 rad, outward gap from .705 to .045 units and edge penetration from .000912 to .000016. Cold CPU was 69/396/807 ms; this is experimental convergence evidence, not interactive performance evidence.

The next authorized dependency is the actual exterior collider, with explicit exposed face partitions and bounded assembly bridges. Hidden inner faces cannot attract the sheet. The current probe only pins its center and checks edges; explicit orientation, bending/compliance, full triangle clearance, self-intersection, corner fidelity and warm interaction performance remain unapproved. No product completion or source commit follows from the cylinder checkpoint.

The initial collision helper reports four tests / 17 assertions before independent review. Actual front geometry exposes `crownCap` partitions for excluding its hidden cap. Hardware apertures require source-specific span bounds: the earlier 12-unit bound described front-meridian gaps, while the engineer identified hold/dock openings of 16/15.5 units. Lead authorized explicit, independently verified aperture endpoints and individual bounds under the existing thin-film bridge policy; this does not authorize unidentified gaps or a global fictitious side wall. Ordinary Pulse edge is the next concrete sheet target before maximum C03 corner.

## First actual 3D sheet probes

The 16×16-cell, 200-iteration distance/contact prototype produced ordinary Pulse principal stretches .94717–1.07374 and zero static triangle intersections, taking 2,075 ms. Its collider borrowed the source rear and a nominal unobstructed front; it is not complete assembly proof. The same capped maximum C03 corner failed: .09794–1.35935 stretches and four static intersections, taking 2,293 ms. Neither result is accepted product geometry or interactive runtime evidence.

The next step is independent diagnosis of the saved original/solved meshes and constraint conflicts before adding the already specified area, bending and full nonpenetration constraints. More iterations alone are not an accepted response. Zero static intersections also requires exterior-state/containment evidence; it does not prove the whole sheet remained outside the device.

The next constraint prototype also failed and is not integrated: coarse .083–1.913 stretch with 11 intersections; locally refined .377–3.306 with five intersections in 10.53 seconds. Area correction improved tracked cells, then point/triangle/sweep-gap phases collapsed them again. The engineer also found a concrete gap-cap defect: it used signed normal distance rather than true Euclidean distance, leaving 3–3.75-unit gaps. Lead stopped repeated full expensive solves pending independent diagnosis and a small causal convergence experiment from a valid exterior/rest-metric seed. Correcting the cap alone is not sufficient evidence. Current runtime costs remain far outside direct interaction requirements.

## Causal strip checkpoint, awaiting independent review

The engineer confirmed an unintended constraint: both quad diagonals were fixed although only one belongs to the rendered triangulation. That resists nonplanar bending. Removing the virtual diagonal and using an exterior isometric seed with staged adhesion produced a positive bounded analytic strip result: 65 vertices, eight iterations per load, 20/40/50 loads reduced Euclidean max gap from 4.072 to 2.164 to 1.233; final full stretches .97869–1.02844 and analytic edge/interior penetration residual 2.75e-5. Total 8.57 ms and mean load .170 ms apply only to this small analytic fixture, not the actual shell BVH or product. Bending was deliberately absent to isolate the constraint conflict. Saved strip-causal artifacts await independent review before actual shell/corner advancement.

App lifecycle inspection also found that an expensive solver cannot remain in geometry `useMemo` or repeated peel layout effects. The integration diary specifies bounded prepared resources, latest-only cancellable targets, and atomic matching render/hit/contour versions. A worker does not make stale multi-second gestures acceptable. These runtime requirements remain pending implementation and measurement.

## Actual shell timing diagnosis

The causal strip was independently reproduced and approved only as a bounded checkpoint. Increasing its load count added total work; this was not a demonstration of time-step independence.

The first straight-edge run rebuilt actual production rear/front/aperture/glass/wheel resources. Its current collider includes 216,630 triangles (including a duplicated support patch), about 16.46 MB of typed data plus JS structures; geometry construction took 247 ms and BVH construction 317 ms. Full Pulse retained .951–1.069 stretch and zero static intersections but a 36.75-unit gap, so it failed. The copied strip schedule moved at most .4 unit per load despite roughly 49 units of initial gap. More decisively, 1,087,318 support queries consumed 7,942 of 8,057 ms total.

Next authorized bounded correction: dimension-scaled staged adhesion and bounded per-vertex facet/neighborhood contact reuse with exact BVH refresh when invalidated. Cached-versus-exact trajectory/crossing checks, provenance and teardown are required. No stale-contact approximation or unbounded cache is authorized, and no full corner or browser integration follows from these failed timings.

## Whole-sheet solver stopped

Exact warm queries independently passed six tests / 622 assertions and matched benchmark contact points, distances and normals, but improved representative timing only about 1.7×. The next actual-edge load run still failed: .8823–1.0922 strain, no static intersections, 29.53-unit gap, 50 rejected attempts and 4.687 seconds total. Support searches consumed 4.344 seconds across 869,673 queries. This does not justify further iteration or query tuning of the same whole-sheet approach.

Lead stopped full-sheet PBD optimization and requested an independently assessed product-specific architecture: direct developable mapping along the actual straight-edge cross-section, with local corner handling only where double curvature requires it. The user requires physically plausible wrapping, not a general cloth solver. The previous device-centered radial chart remains rejected; any replacement still must prove full backplate access, continuity, original print fidelity, actual shell clearance, corner behavior and responsive interaction on the same fixtures. No source integration or completion claim is authorized by this change in direction.

## First accepted bounded visual diagnostic

The direct Cartesian strip produced ordinary .20-width Pulse stretches .99121–1.00032 under refinement. A maximum .35 sticker instead reached the glass corner, where varying per-row path lengths caused a 3.04 stretch spike; this remains failed and size bounds are unchanged.

The ordinary actual-route `early/strip-1` capture completed with 374 assertions in 3.88 seconds, stable build/test manifests and zero source/artifact deltas. Digest: b7547fdfb7bdb644cb8aab34d15076419e7479a77310c0d99f1a5bbaba74487d (422 inputs). Lead inspected rear, side and front images: the side print and front title remain readable without the rejected diagonal banner distortion. Pulse is hidden from the rear at this exact outer-perimeter center. Independent visual review is pending; this establishes one static .20 pose only. Owned build/browser processes exited.

Next proposal for independent assessment: retain a shared material phase onto the front and cross actual insert steps using bounded endpoint-supported local ramps, rather than injecting differing step lengths into every row. No fictitious flat cover, reduced maximum size or corner exclusion is authorized. Corners, max-size transitions, full interaction/peel and runtime remain unfinished.

## Maximum straight-edge visual diagnostic

The shared-phase front transition now uses a six-unit ramp at actual glass endpoints. The post-join 96-grid mesh has zero intersections across 18,432 triangles. Its adaptive exact-support-distance Lipschitz check covers every triangle within a two-unit gap: 22,172 queries, depth at most six, zero violations or unresolved cells; sampled maximum 1.92625055. This distance certificate does not establish continuous collision detection or exterior connectivity by itself. Provenance and ramp-switching continuity remain independent review concerns.

The actual .35 Pulse straight-edge capture `early/strip-max-1` completed with 374 assertions in 3.98 seconds and zero source/artifact drift. Digest a3a82ee8e603a9514042d408dfc3539987f7a0814d72d0ffa47033a56d27b670 (424 inputs). Lead inspected side/front images: printed title, keys and controls remain upright and readable across the rim/glass transition without the prior diagonal warp. Independent visual/certificate review is in progress. Processes exited and the source freeze was released.

This is still one static straight-edge pose. Uniform 96-grid sampling is diagnostic topology, not approved interaction performance. The next geometry dependency is the actual local rounded-corner patch and its joins to the direct strips; full-corner, editing/hit/contour, peel and native performance gates remain pending.

Independent reviewer accepted the maximum strip's bounded static fidelity and reproduced the continuous gap certificate exactly. That closes the earlier vertex-only gap concern for this specific mesh/support set; it does not approve all poses or motion.

The reviewer approved the next bounded corner experiment from the contract: center-local Fermi geodesics on the real corner sector, beginning with the same C03 .35 angle-zero pose. Transverse traces must be perpendicular to the transported unit centerline tangent; the corner metric cannot be extended over unrelated front crown/insert surfaces. Saved center/orientation stay fixed. Step-halving and material refinement, both singular values, cut-locus/crossing checks, actual shell clearance and print fidelity determine advancement. The construction is not assumed isometric, and no whole-sheet relaxation or new parameter search is authorized.

## Corner crossing and app lifecycle checkpoint

The first projected-step corner experiment completed in 45/75 ms at step 1/.5 but left 83 of 512 triangles unresolved. Apparent collapse was located on the unbridged rear seam, with additional traces reaching the actual top hold aperture. This is a failed crossing implementation, not evidence of intrinsic corner feasibility or infeasibility. The next bounded correction is adjacent-facet edge unfolding and explicit source-backed bridges for the seam/hold opening reached by this fixture. Traces must stop at unsupported boundaries instead of consuming material distance while sliding.

Independent app lifecycle work now captures an authoritative grab capability and allows off-rear carrying only through verified visible-hit admission; the geometry adapter is still pending. Eight focused tests / 52 assertions initially passed, but independent review found three cancellation gaps: capture release when collection data disappears, Escape during pending pickup, and release continuing after grab invalidation. The app engineer is fixing them with regressions before approval. External WebMCP hunks remain preserved.

## Supported corner focusing and independent material advisory

Adjacent-edge unfolding eliminated false rim sliding (151 ms adjacency construction, 6.6 ms for 289 sampled points), but the Fermi candidate still focused on fully supported rear geometry. Under refinement its minimum stretch fell from .0408 to .00194; 12 of 479 wholly flat-rear triangles reversed near (138,266,-28). Bridge completion cannot resolve this intrinsic chart failure. The candidate is stopped, with reproducible source and meshes retained.

A fresh independent geometry advisor is assessing one local physical crease/pleat construction. Initial insight: normal displacement of the collapsed chart cannot restore its material metric, and a pleat cannot terminate arbitrarily inside an otherwise unstrained sheet. Excess material needs a route to a free sheet boundary (or an explicitly strained local core), with adjoining regions allowed compatible material motion. No new source experiment is authorized until the bounded construction is independently assessed. The earlier two-unit slack threshold was an experimental assumption, not an owner-specified number; any proposed revision must have explicit physical rationale, remain localized and pass visual review rather than create a floating sheet.

The app cancellation corrections independently passed 11 tests / 67 assertions and source review. This approves the bounded app base only. Actual wrapped native pickup/cancellation and the geometry adapter remain outstanding; two helper-level tests do not replace native event-path validation.

## Rigid fan rejected before contact optimization

The declared fan failed its early compatibility gate. Its fixed root places the shared apex at z84.609245, while the actual assembly maximum is z30.433964: an unavoidable gap of at least 54.175281 units. The declared collar requires endpoint stretch at least 1.985 against the candidate 1.153 upper bound. Coherent phase variables do not authorize relocating the specified join onto another surface. The advisor accepted rejection of this family; no contact optimization, moving outlet or hidden subdivision followed. Exact fan compatibility/envelope probes are retained under early evidence.

The independent advisor is now making a bounded capability assessment of an established WebAssembly soft-body engine before any dependency or implementation change. Failed custom JS solvers do not by themselves rule out a mature collision/constraint runtime. No engine has been selected, installed or claimed suitable; required shell contact, material behavior, selfcontact and interactive cost must be evidenced from official sources before proposing a prototype.

## Bounded Ammo probe authorization

The advisor identified the exact Ammo commit referenced by installed Three 0.185.1 as a viable substrate for one isolated test; Jolt's documented soft-body collision limitations make it unsuitable for this particular selfcontact experiment. Ammo provides cluster-based selfcollision, not guaranteed full triangle contact, and requires an application adhesion adapter. No package installation or product integration is authorized by this finding.

Reviewer approved isolated asset/API/cleanup preflight and the single C03 probe in `diaries/sticker-wasm-advisory.md`, with two pre-run corrections. Pressing duration must be derived from the validated seed-to-target maximum distance, with a free-node reachability check using the same force/ramp; the original .5-second stage could not cover the required travel under its acceleration cap. All three pinned nodes and incident triangles must be exterior, and their unavoidable support gap must be recorded without independently moving neighbors and changing the material rest frame. Keep the fixed half-step comparison, two-second measured CPU cutoff with partial progress reporting, independent complete triangle oracle, source-pinned assets and explicit native-object cleanup. Broader UI/worker/runtime adoption remains unapproved.

## Ammo artifact limits and recovered partial geometry

The first memory diagnosis was corrected: all construction fits within 64 MiB; the original process failed during native step 184 after 183 completed steps (1.525 physical seconds). Native work was 640.37 ms and force work 34.47 ms. Source inspection identifies sparse-SDF cache growth as a plausible cause, not a measured cell count. The default cell clamp can itself permit more payload than the heap. A separate soft-concave triangle-hull cache also retains memory.

The approved fixed 128 MiB compatibility attempt failed at WASM instantiation. The binary import requires min=max 1024 pages; the JavaScript INITIAL_MEMORY option did not establish larger-binary-memory support. That prior capability assumption is explicitly corrected. The pinned binding exposes no safe sparse-SDF cache reset/configuration API. No binary patch, toolchain installation, dependency change or rebuilt engine followed.

An unchanged 64 MiB run capped at 150 steps recovered the missing partial mesh before OOM. It completed 1.25 physical seconds in 519.28 ms native plus 24.51 ms force work. Full material stretch is .9311–1.0617 with no degenerate cells, but the mesh fails contact: 134 shell-intersecting triangles, including 119 with a node behind an exposed facet; 41 such nodes were found, worst 4.412 units behind the rear. A separate all-vertices-outside triangle confirms a chord/interior collision case too. Independent review confirmed six nonadjacent self-intersection pairs with strict interior witnesses. This is early pressing, not an adhesion/convergence verdict, but native collision flags do not establish acceptable geometry.

Logs, exact meshes, memory limits, cleanup ledgers and probes are retained under `early/ammo/`. Current next decision is a bounded source-backed assessment of collision quality and safe runtime cache controls before deciding whether a minimal pinned source rebuild is worthwhile. No memory-only escalation or longer-settling run is authorized as a fix for the verified intersections.

## Collision diagnostics and product-contract check

The per-face native cluster experiment avoided the earlier memory failure but still produced shell intersections and exhausted its CPU budget before pressing completed. Its approximately 17 ms native step is not an acceptable direct-interaction result. Static contact already uses hardness 1; changing the dynamic-body hardness would have no effect.

The subsequent front-cap-only collision simplification is stopped. It reduced the complete collider by only 6.83%, and 16 fixed collar overlay samples undercut the original shell beyond the declared certificate tolerance. No native replay or speed improvement is claimed. Exact failed artifacts are retained under `early/cap-lod/`.

Next bounded work is diagnostic, with no full simulation authorized: the geometry engineer inventories authored collider faces that are actually exposed versus hidden; the advisor classifies existing penetration witnesses against native contact construction. The independent reviewer also checks experimental assumptions against the owner's product requirement. A cloth engine, simulation-derived adhesion and the numerical two-unit gap were not owner requirements. Any simpler approach must still deliver continuous real-surface wrapping, readable artwork, full placement access and responsive interaction; visible clipping, detached floating sheets and placement restrictions remain unacceptable. These checks do not approve a substitute or close the task.

The diagnostic inventory is complete in `early/ammo/inventory/`: 30,475 front underside faces are internal, but outer rear/core/front bevels are exterior and cannot be dropped just because their slopes face backward. Other aperture layers need coverage proof. The older native fixture omits authored wheel-gap floors/hardware, so its assembly claims are qualified. No collider exclusion or new replay followed.

Saved-state contact classification found 42 of 47 penetrating nodes beyond the native contact envelope, with none of the 184 incident faces anchor-excluded. A single frame does not establish tunneling. No further native optimization is planned.

Independent review accepted moving to one procedural corner prototype under the observable product criteria recorded in the scope's superseding section. The geometry engineer owns the concrete construction and immediate native visual diagnostic. The former advisor is recovering app integration ownership from the existing integration diary, starting with a read-only API/dependency plan; device geometry ownership remains unchanged. All existing foreign WebMCP hunks remain protected.

## First procedural corner render

The local perimeter bend cage passed initial finite/zero-area/gross-fold checks at 9,409 vertices and 18,432 triangles; the cold 32.65 ms diagnostic construction is not an interactive performance claim. The actual maximum C03 angle-zero route capture is saved in `early/cage-1/max-PW-C03-0-{rear,side,front}.png`. All 374 assertions passed in 3.77 seconds; source and artifact deltas were zero. Fingerprint: `5ba0e818c7f4a423e7ae27e1aac516886ddfd36dbca6f22d615c4555559b16ec` (427 inputs). Owned processes exited.

Lead inspected all three images: artwork visibly continues over the corner onto the front. Independent comparison with the original C03 artwork is pending; its narrow rear/side tail must be assessed against the original lightning silhouette rather than assumed to be distortion. A single static pose does not establish movement continuity, full range, hit/contour agreement, wrapped peel or responsiveness. The app owner is preparing the remaining actual-route interaction checks; geometry ownership and the agreed authoritative grab interface are unchanged.

The additional `early/cage-1-close` captures reuse the same compiled build at 2560×1800 through a recorded test-only viewport preload. All 374 assertions passed, all source/artifact deltas remained zero, and processes exited. Independent review accepted this bounded current-pose visual: side ticket text/barcode and the authored lightning tail remain recognizable; the front ENCORE region rolls around the rim without the earlier banner distortion. No conspicuous floating or clipping is visible in the captured views.

Next approved geometry test is a continuous nearby-center and 0/45/90 rotation sweep, crossing flat/straight/corner selectors in both directions, particularly nominal |x|139 and |y|250. The current diagnostic selector is not a shipping solution. Public placement updates may isolate geometry continuity but do not prove direct grabbing. Object/UV jumps, front-row collapse and axis-switch artifacts remain blockers; actual grab, wrapped peel and persistence validation still follow.

The initial boundary probe caught a real failure at X156.09/Y250.01 (`Collapsed corner cage`) and a 1,292 ms legacy fallback just below Y250. The first unified correction removes that old atlas from placement rendering, chooses the common direction from footprint-reachable perimeter normals, and inverts the same chart for the exact saved center/orientation. The formerly failing center is finite without degenerate/opposed triangles at 0/45/90. Boundary maximum vertex movement .4564 for .02 center motion still needs step-halving and visual sweep; the 15 ms diagnostic construction is not final interaction acceptance.

The app owner's native extension source review found three defects before execution: witness evidence was not bound to the actual build/pose/UV/depth, off-rear cancellation expected an attribute on an appropriately unmounted overlay, and relocation could pass without one meaningful write. Corrections are in progress. Partial-peel contact remains a separate actual visual acceptance gate, not inferred from animation state.

Those driver corrections passed independent review and three focused tests / 217 assertions. Actual witness generation and native execution remain pending.

The unified procedural mapping now inverts the unbent perimeter chart per material vertex and keeps a root-anchored unwrapped transverse phase at every radius. Flat C03 error is 7.63e-6 (Float32 scale); axis crossings move .020001/.020004 for .02 center travel. Dense 84-pose diagnostics have no nonfinite, zero-area or opposed-triangle failures. Maximum movement per .01 center step is .05259 near Y250, .05705 near X139, and .01009/.01000 at the body axes. Types and scoped lint pass; source is frozen for the prepared actual-route 19-pose/25-frame run.

Independent source inspection found no immediate blocker to that visual run. Still pending: extremes where root-frame correction enlarges the actual footprint beyond direction-selection samples, and measured interactive cost of the current dense sampler. Neither the pose diagnostics nor static captures establish final grabbing, contour occlusion or wrapped peel correctness.

The actual sampled run completed in `early/cage-sweep-1`: 19 saved poses, 25 frames and video; four tests / 663 assertions in 22.09 seconds. All build/test/build-to-end/artifact deltas were zero and cleanup passed. Browser source fingerprint `09e4355ca319faae0fe97d30be8876986b6d05372b94810fd2b5ecd270d150ed` (427 inputs). The captures use public revisioned PUT plus reload and do not prove uninterrupted pointer motion. Independent visual review is underway; lead inspected representative 45-degree side, 90-degree corner rear and straight-edge views without spotting the earlier banner failure.

Live integration revealed an existing topology mismatch: wrapped source uses 96×96 subdivisions while the old peel subtraction indexes 24×24 meshes, reading beyond their buffers. The engineer is unifying peel with the exact displayed source and replacing the global camera-depth lift with displacement of detached rows only. App adapter forwarding, actual visible/occluded witnesses and direct native mouse/touch behavior remain separate pending steps. Existing animation timing is preserved.

Independent review inspected all 25 sweep PNGs and gave a bounded visual PASS for those sampled C03 poses: recognizable ticket/guitar/lightning proportions, shoulder rollover and no visible banner collapse, detached tent or axis-pair jump. The set adds no front views and remains spatial sampling; unseen front contact, full-range geometry and uninterrupted interaction are not approved by it.

The prepared-source peel correction now keeps 9,409 vertices finite at progress 0/.1/.4/.9/1 and preserves all attached rows bit-exact, without mutating the source. Its measured 2–3.8 ms diagnostic cost avoids full remapping on each spring tick. Native held side/front clearance is still required. The app owner is wiring the exported grab adapter and honoring open contour spans plus explicit anchor visibility; hidden spans must not be closed across the shell or expose invisible grip targets.

App active-grip lifetime correction independently passed 15 tests / 100 assertions: a grip that becomes occluded during its admitted gesture keeps ownership until completion/cancel, without exposing hidden idle input targets. Device review separately requested visibility subdivision along reduced contour edges, removal of a discontinuous full-peel transport ternary, and BVH cache identity robust to common-parent transform roundoff and in-place geometry versions. Those device fixes are in progress.

Reviewer accepted a continuous transport scalar owned by the existing app animation owner: transport stays zero while geometry has attached rows, then advances smoothly after detachment; free-carry curl must not cause reattachment. Return/cancel reverses transport before reattaching, and reduced motion may resolve directly. Preserve the existing total choreography/timing and use no competing device clock.

The concurrent WebMCP owner explicitly confirmed their work is complete and they have no active/planned writes to `packages/state/src/contract.ts` or `apps/web/src/sticker-interaction.ts`. Narrow required transport edits may proceed with separate hunk staging. Their animation/interaction-generation guards between publications and before completion/requeue remain protected; their changes are still uncommitted. Relevant regressions include app interaction lifecycle, sticker WebMCP and state feedback tests. No registry or workstream016 edits are needed.

App physical phases are implemented as `sourcePeelFront` and `detachTransport`, retaining the existing 64px lift and animation owner. With normalized progress q, the first .8 completes source detachment and the final .2 advances transport; free-carry visual curl remains independent. The app owner reports 33 tests / 308 assertions across phase, model, lifecycle, return, WebMCP and state feedback, plus app types/lint. Independent phase review is pending.

Device contour, transform-cache and transport-helper regressions passed independent bounded tests. A remaining cache defect involving replacement buffer objects at unchanged version numbers is being corrected. Mesh normal calculation now reuses sampled adjacency instead of four additional chart inversions per vertex. Because the film lift uses those normals too, final lifted positions change slightly; fresh native visuals and UV witnesses are required, with no claim of bit-identical positions. The original topology/UV remain. Actual draw-matrix capture for final witnesses is test-only and must identify the matching sticker/shell draw, viewport and frozen source/build.

Subsequent independent review approved the app physical timeline (33 tests / 308 assertions), device replacement-buffer identities and exact displayed-grid UV solving. The old radial atlas/right-strip dead APIs are removed from production source, with exact historical source retained in `early/obsolete-source`. A sampler binding lifecycle issue was corrected through explicit child sampler inputs, symmetric layout binding and unique cleanup tokens; independent lifecycle tests passed (3 tests / 103 assertions).

Fresh `final-matrix-1` capture completed for 1280/375 side/front views: 599 assertions in 9.31 seconds, all source/artifact deltas zero, owned processes closed. Source fingerprint `890a4fcfb4f2461a34c1bf7c46da34f04ef787e4df35e992bdf8def12dcd7000` (437 inputs), full source manifest `968e1b1d2ac452b2719c16a759ad94040b5c39b252d0188790955929f920cfba`, artifact manifest `9139f541bf2632fa7b803a9bf93e703e6a21d756de120c9770874fb0d17b94e8`. Two C03 draw candidates each contain 55,296 indices and matching geometry hashes across views. Exact comparison against the equipped source is required before witness attribution; matching texture alone is insufficient. Protected source remains frozen for same-build final interaction tests; only evidence generation is active.

## Current blocker: front deformation regression

Independent review and lead inspection rejected `final-matrix-1/matrix-1280-front.png` and the 375px front capture: the ENCORE/ticket is severely compressed into a long diagonal ribbon across the screen. The earlier cage-1 front was compact and readable at the same declared seed. The rear/side-only sweep did not cover this view. Passing numeric assertions and provenance do not override the visible failure.

Full interaction/witness acceptance is stopped and the source freeze is released for the geometry correction. Preserve completed app lifecycle/phase/driver fixes. Compare exact seed and source changes to identify the unified flat-inverse/root-compensation regression; the small normal-lift optimization is not assumed causal. The corrected mapper must retain both exact flat-back behavior and readable front rollover with continuous placement. Future geometric changes require rear, side and front visual checks before advancement. No new engine or global optimizer is authorized by this failure.

The material-coordinate cause was confirmed: an upper C03 sample gained roughly 57 extra depth units from the nonlinear inverse residual, while root-frame amplification was small. The correction preserves the exact flat inverse and fades its nonlinear residual over the actual bend using an implicit final-depth phase. A direct explicit fade had folded rows and was rejected. The corrected current sample passes 20 mesh and 84 boundary diagnostics without nonfinite/zero/opposed triangles; a solver iteration-cap residual diagnostic remains to be recorded.

Fresh `front-residual-1-native` six-view captures passed 603 assertions in 10.57 seconds with zero source/artifact drift and closed processes. Source `6c86ffc6f2f5006d25de9c4c47ad32660c8756e74f02da9dc89ce6a6c6fadebe` (437 inputs), source manifest `fc110d05cbd969a7d1ebaa9c8e0151756d767c878396d21691adaf7db0fa6d02`, artifact `0aff24d6700f847477c74164b04f9fbacf0dc1c605c704a32b75eb52035c5f1f`. Reviewer independently inspected all six originals and accepted the bounded current-pose visual: compact front ticket/title rollover restored at both sizes, no long ribbon, coherent rear/side contact. The specific front regression is closed; full-range and interactive contact/performance are not. Exact equipped-buffer matching and witness generation are authorized on this frozen build, followed by native interactions.

## Cold grab inverse correction

Actual equipped position/UV/index buffers matched the source. An offline matrix reconstruction initially failed to update Three's world matrix; that fixture error was fixed. The subsequent statement that all four paths solved was too broad: it covered release endpoints and incorrectly warmed the inverse before the production 64px admission threshold. Intermediate null previews were retained, and that claim is superseded.

With the actual first call beyond 64px, a real cold front inverse defect emerged: from center (156.09,266.616), the first 68px move proposes (139.8566,284.293), leaves the legal rear domain, and returns null despite an existing valid target. The source freeze is released for this product correction; native execution remains pending. Reviewer approved bounded residual-decreasing Newton backtracking through legal centers, with one frozen grab-depth-plane seed only if needed and always followed by the same exact displayed-UV refinement. No placement-domain or endpoint-tolerance relaxation is allowed. Tests must start at actual cold admission, cover desktop/mobile front/side, valid reentry, invalid target rejection and invalidation. Fresh source/build provenance follows the fix.

The cold inverse fix independently passed 3 tests / 146 assertions; the helper fixtures use captured cameras with a synthetic front support and are not native assembly proof. New actual `cold-inverse-matrix-1` six-view captures passed 603 assertions with zero provenance drift, source `005b39cb6ce7f174eb695699d7e2dfbddb704a727bfe827bd4fbeefc2163213f` (438 inputs). Recorder now captures both indexed and nonindexed draws plus actual raster/cull state.

Reviewer independently accepted `early/wrapped-witness-2` for bounded same-build native testing: rehashed actual selected ink-draw position/UV/index buffers in all four cases, exact model/sampler/grab source correlations, same-pass shell model error at most 4.77e-7. Actual admitted-call counts are 15/17/8/14, with zero nulls and no prewarming at or below 64px. Important limitation: the hidden witnesses are also backfacing, so they cannot isolate BVH rejection from front-face culling. The native four-case interaction run is starting in `early/cold-inverse-interaction-1`, with the recorder disabled. Full-scene interaction, partial-peel contact, latency and full-range geometry remain pending.

That native run passed the first desktop-side positive pickup, partial peel, cancel, no-write and reload assertions, then failed the hidden-point rotation expectation (629 assertions, 3 pass / 1 fail). Cleanup completed and all provenance deltas remained zero. Reviewer and app owner independently classified the failure as an invalid witness expectation: actual first shell hit inset 18.356619 exceeds the existing 18-unit orientation band. Desktop-front hidden witness was also ineligible; mobile ones were eligible. No product routing change is justified.

Evidence-only replacements require actual first-shell orientation eligibility with comfortable margin (new insets 7.51–9.95). Pickup/release coordinates stay unchanged. For stronger partial-peel visuals, use side 32px (frontier .625, visible attached reference row .698) and front 20px (frontier .391, reference row .427). The exact front view has no visible painted row beyond .427 because the remaining rear is occluded; demanding a visible attached reference there after 32–40px would be physically invalid. Side views provide the clearer contact comparison. Regenerate exact cold admission paths for these partial endpoints; keep source/test build frozen and review replacement proofs before same-build rerun.

The completed replacement bundle `wrapped-witness-4` passed independent review (manifest SHA `d862a6803c3e039faa8c66ac2620e05ace327f064335e2e766d28f342c9ddd60`): unchanged pickup/release targets, 32px side / 20px front, admitted-call counts 18/18/10/15 all accepted without prewarming, valid first-shell orientation points. The same-build `cold-inverse-interaction-2` still failed the first hidden rotation check after successful positive pickup/partial/cancel/no-write/reload (629 assertions, 11.46 seconds). This second failure was not explained away by another witness replacement.

## Current blocker: composited DOM event coordinates

Bounded actual browser instrumentation in `early/hidden-route-1` established the concrete mismatch. The pointerdown target was a STRONG titlebar descendant inside the composited panel/canvas event source. Client coordinates were (567.07,147.83), but element offsets were (151.08,8.69), with canvas rect (0,0,1280,900). No sticker stop-propagation or capture intercepted the event. A painted-frame barrier did not resolve it.

Pinned Fiber 9.7 default event computation in DeviceCanvas uses offsetX/Y, producing ray NDC (-.764,.981) rather than (-.114,.671). The device engineer owns a narrow canvas-relative client-coordinate event compute correction, with translated/descendant/scroll/scale regression coverage and unchanged event routing/admission. Independent confirmation is in progress. Source freeze is released for this necessary product fix; the exact failing witness and gesture remain the regression, with fresh build provenance afterward. Both failed native and diagnostic processes closed with zero provenance deltas; diagnostic preload SHA `c12abf4cdefd90fbc0c004f3ec154f6b6bd187f4e766a29622d40c96db36b520` is recorded separately.

## Current blocker: valid front placement crashes the mapper

The canvas-relative ray correction independently passed three tests / 28 assertions and fresh six-view capture in `early/canvas-ray-matrix-1` (603 assertions, all provenance deltas zero). The recorder-disabled `early/canvas-ray-interaction-1` confirms both desktop shell-flick checks now pass. Desktop side additionally completes relocation to (.3499636947,.1499867297), exactly one PUT, preservation of other sticker properties and reload. The run stops at the desktop front relocation; touch cases have not executed.

Bounded unchanged-path diagnostics in `early/front-carry-route-1` and `early/front-carry-error-1` establish a route crash while already placing, rather than an admission/capture/readiness failure. At pointer (607.0069846,157.5806812), the mapped center is (.314332653716106,.0205851851589971), C03 width .35 and angle 0. The stack reports `Bend has no actual front support` during preview geometry creation. Cleanup and provenance checks passed; no trajectory workaround was introduced.

The exact full-mesh diagnostic in `early/front-support-failure-1` finds ten vertices crossing the opposite shell edge. The conservative half-diagonal footprint selects a diagonal bend direction; an upper-rim point then travels across the unsupported left envelope. The geometry engineer owns a correction based on the actual rotated footprint/common phase, with an independent reviewer checking the exact failing pose and adjacent allowed placements. Artificial front support, swallowing the exception, restricting legal centers or changing the witness trajectory are not fixes. Source freeze is released only for this correction; fresh geometry visuals and native validation follow review.

The first correction uses the rotated rectangle's continuous tangential support span, explicitly an approximation rather than exact boundary intersections. Initial evidence reports 1,620 coarse eight-grid poses without throws/orientation flags, and all 61 previous admitted centers with full 96-grid finite meshes. The implicit solver now handles constant-weight roots analytically and solves only within the actual bend; reported maximum residual is below 1e-9. These results do not establish the newly mapped pointer paths. Recomputing unchanged front trajectories exposes null inverse samples and shifted release centers, while side paths still solve. Engineer/reviewer are distinguishing a real inverse miss from changed displayed-UV/reachability evidence before further correction or fixture regeneration. Build approval remains pending.

Tiny-step continuation classifies the remaining front nulls as direction-transition folds: the desktop branch reverses near (685.22,147.739), normalized center (.17049,.03506), at diagonal saturation; the mobile branch reverses near (125.065,240.161), center (.74621,.00459), at left-corner activation. Distant projected roots are not valid substitutes. Reviewer approved a bounded direction-only smoothing experiment over material scale, retaining actual shell support and checking transversality, zero-direction behavior, both folds and the original full-mesh crash. No fixture trajectory changes or multi-branch teleports are authorized.

A passive test-only frame observer is implemented in the existing native fixture/driver and awaiting independent review. It records rAF intervals and long-task overlap during actual gestures, including first/tail gaps and explicit insufficient/overflow outcomes. These are presentation proxies, not photon latency or function-level causal attribution. Reported helper tests are five / 35 assertions and lightweight fixture regressions three / 217; no native performance claim follows until the corrected source runs with the GL matrix recorder disabled.

## Superseding carry/adhesion contract

Matched current-source continuation did not prove a domain exit: valid-sided Jacobians and legal Newton proposals remain at the stopping points. The earlier boundary conclusion is withdrawn. Instead of adding further inverse branches, the team inspected the interaction coupling: held valid previews already animate landing to .97, effectively reseating the sheet during drag. This created the self-imposed exact newly seated UV-under-pointer constraint.

The independent reviewer recommends, and lead authorizes, preserving free carry under the pointer until valid release. Device grab becomes one frozen affine mapping from the actual hit triangle's UV-oriented material tangent frame, with stable edge-on behavior, authoritative visibility and pose invalidation. App held landing remains zero; existing release spring presses onto the true wrapped target. Invalid target clears preview, cannot save stale state, and supports valid reentry. The scope's superseding interaction decision records this change explicitly. Actual full-range geometry/support, visual continuity, input responsiveness and persistence remain required; no owner requirement is waived. Both engineers own disjoint changes and independent review precedes the next build.

The simplified app state contract independently passes seven lifecycle tests / 133 assertions: held targets remain landing zero, invalid/reentry updates are atomic, release resamples the current target and invalid release cannot write. Device affine mapping and geometry independently pass eight tests / 569 assertions plus a rotated/translated triangle delta check. The frozen screen/material map bounds weak-axis gain to 20 times the strong-axis gain rather than imposing a grazing-angle pickup cutoff. No new-pose nonlinear inverse, iterative branch state or previous-root fallback remains in target mapping.

The release-to-settled frame observer also independently passes six tests / 45 assertions. Fresh browser evidence is authorized after resolving actual drop-location visibility during free carry. It must use new actual-hit witnesses and affine target expectations, validate full target meshes, inspect all rear/side/front views and complete mouse/touch interaction/persistence/frame gates. Old nonlinear expected centers are obsolete, not retained as product correctness criteria.

Combined source/test freeze approved: the target cue independently passed five tests / 66 assertions (exact pose matching, borrowed geometry ownership, readiness and visibility); final driver schema/negative-reentry changes independently passed three tests / 359 assertions with native execution excluded. The driver now separately checks invalid release/no PUT and valid-invalid-valid reentry with exactly one final write. Fresh six-view capture is starting in `early/affine-target-matrix-1`; app lane owns the only heavy process. New actual affine witness proof must cover all intermediate target meshes and invalid/reentry classification. No final feature approval follows from source gates.

Fresh six-view `affine-target-matrix-1` passed four tests / 745 assertions; independent visual review accepts the bounded seed in all six views. Source fingerprint 52c6892dd17181e17226c6d074d50415f3fb3e0fdde7be2764fa8685e9d959a4, artifact 07d71b1b4f97c07dd43e866e63e6a0c6a5d757c3b35dcc1aa6721c6ec4500b53; all deltas zero. Witness6 independently passed raw-buffer/hit/affine/attached-row correlation (manifest c63792c4bd3dfe93b5909d5f74047353d52c201e89e6bd2ffde9da471771a112).

The actual `affine-target-interaction-1` stops at the first desktop-side relocation: carry remains peeling with no target, after successful partial peel/cancel/no-write and shell flick. No route crash occurs. Four-test run has three pass / one fail, 774 assertions, 13.44 seconds; processes closed and provenance passed. The incomplete return performance sample is correctly insufficient, not a pass.

Engineer and reviewer independently identify a concrete native hit-to-grab seam: intersectStickerPrint returns a temporary parentless probe with copied matrixWorld but identity local matrix. captureStickerSurfaceGrab refreshes that probe and overwrites the copied transform with identity before material-frame extraction. The offline witness used a parented mesh and therefore missed this difference. A narrow transform-lifecycle fix and regression through the actual intersect-to-capture path are authorized; no trajectory change or another diagnostic browser is necessary before deterministic reproduction. Source freeze is released only for that fix, followed by fresh provenance and native verification.

The narrow hit-to-grab fix independently passed ten tests / 578 assertions. Fresh `affine-hit-matrix-1` passed four / 745 with zero drift; all six PNGs are byte-identical to the approved prior capture. Source b0cd4b600c7148ac0a5cf5ed8d7313f3022ef2c5a6e1051831c06e4d45cb060d, artifact 5050154db139ec4effda646e5008559d54a290d5ca3a7b494ad3a903e6de62eb. Witness7 (81f6f983f6cfcfd81793663167eb879ac7744c8eb95e6bc792d9525eac118a19) independently exercises actual intersect-to-capture parity; all gesture/target/full-mesh hashes match witness6.

`affine-hit-interaction-1` now completes the desktop side case through exactly one save and reload. Front reaches valid placing but stops on an assumed visible-outline count. Exact geometry visibility diagnosis shows both front targets are fully shell-occluded; their absent cue is correct. The driver must use verified visibility expectations, never draw through the device.

Actual held-side inspection exposes two outstanding visual issues: free carry stretches the upper print into a broad banner, and the shown target outline appears displaced from the final sticker. Functional side success is qualified, not final acceptance. Device lane is diagnosing the detached-source flattening and exact contour-to-render screen correspondence. Source freeze is released for necessary corrections after review.

The frame observer also had a test-only timestamp mismatch: a supplied rAF timestamp can precede a pointer event within the same frame. Callback execution now uses performance.now for event-boundary scheduling intervals while retaining supplied frame timestamps separately. Independent seven tests / 51 assertions pass, with thresholds and insufficient outcomes unchanged. This is a measurement correction, not an improvement claim about product performance.

Target offset diagnosis is confirmed: the parentless target mesh receives a copied local matrix without marking its world matrix dirty. Pinned Three 185 does not refresh it merely because updateWorldMatrix is called with default force=false. The exact projected center was about x800.5 instead of x583.5. The prior front-null visibility classification is therefore withdrawn. The fix independently passes two tests / 32 assertions through the actual slot-to-projection entry point and successive transforms. The general proof-bound cue schema independently passes three tests / 560 assertions; concrete counts will be regenerated from corrected projection.

Held carry diagnosis: each material column retains a different wrapped bottom-row frame even after the peel frontier reaches one, so transport moves an already sheared fan. Reviewer and lead authorize a single canonical original-UV cylindrical sheet restored during the existing detachTransport phase, after all rows detach. Transport zero preserves the current source-peel endpoint; transport one preserves the original width/aspect. Both shapes share the captured material point during interpolation, preventing pickup drift. Reverse uses the same mapping and existing clock. A transient, source-bound local anchor survives release/return and clears with lifecycle teardown; it is never persisted as placement data. Midphase shape/normal continuity, endpoint readability and actual held/return visuals remain required before another native acceptance run.

Grabbed-point peel now responds to actual input, with the untouched half-plane exact; source-bound rawPull ownership independently passes 17 tests / 206 assertions, including same-source synchronous takeover. The candidate phase diagnostic `grabbed-peel-phase-1` completed four tests / 942 assertions with zero drift and closed processes (source3ff2616c7037279bdb2975140b3a0fe6edc6fbb67deb37fb196be3ec0f31ad8d; artifact413a37ca018946847bded9de03eba3ea885d49c6b58c95a3eba5f0cd3d4d0bd8). Side16/32 no longer sends unrelated upper artwork offscreen. This is partial improvement, not acceptance.

Exact classification in `early/free-carry-1/phase-classification.json` confirms two remaining product defects: a side32 painted vertex crosses the actual front shell and ends below its surface; at57.6, opaque triangle areas fall to about .00532(front)/.00826(side) versus canonical1.4306. This is real geometric compression, not merely an edge-on camera projection.

The64px phase only reaches legacy visualcurl1; it does not represent relaxed free carry. A corrected same-build evidence preload (`45b3346b6538c96cae89e337b4d0bd21476a8cab97d86da7615047fa0af757b7`) captured80/100/164 in `grabbed-peel-free-phase-2`, actual curl.904/.784/.4. The original preload invocation failed to activate and is not extra-phase evidence. Corrected run passed four / 942 with zero drift, closed processes, explicit front rightward diagnostic turn and cancel/no-save. Independent review accepts readable original artwork at164 in both orientations; backing at64 is temporary curl, not a permanent basis error. Side outline now aligns with its actual destination.

Next bounded correction reuses the existing actual-shell BVH: sweep only moving source-to-candidate vertices, stop/slide at exterior contact with the existing film offset and fixed caps, leave attached material exact. Record unresolved contacts, triangle crossings and runtime cost; node checks alone are insufficient. A single restrained canonical-curl experiment may address the detachment compression while preserving the accepted endpoint and existing timing. No new global physics system, collider cache or blind tuning loop is authorized.

Both post-capture experiments failed before renderer integration: per-vertex sweep/slide costs83–109ms on side phases, leaves many capped contacts and produces near-zero painted areas; canonical curl capped at.4 alone leaves .006/.003 mid-transition areas. Neither is wired into rendered production behavior. Do not resume more contact iterations or curl tuning.

The bounded replacement feasibility test uses one material-parameterized crease curve and shared arc, with an explicit picked-point endpoint. Current full-span crease has a nonempty outward normal cone, but fixing the picked point to the original camera-depth plane makes its chord point inward. That fixed depth is not a product constraint. Reviewer and lead approve analytic pointer-ray depth freedom plus bounded crease advance: intersect front-of-shell depth, chosen conservative outward inequalities and chord/material-length interval, then choose continuous depth near the prior capture. An empty interval rejects a particular crease, not all outward peel constructions. Actual resulting full triangles/metric must still be tested; no global optimizer or invented mesh follows from feasibility alone.

## Latest owner steering: remove drag outlines and fix editing stalls

The owner explicitly rejects silhouette/edit-like outlines during dragging and reports rotation/wear lag, with rAF259ms and pointerdown160ms warnings in their screenshot. The target-cue decision is withdrawn. App/device lanes are removing the drag outline and unused targetContour machinery while preserving the actual editing HUD. Independent runtime diagnosis will profile the current rotation/wear paths and compare source-derived causes against native evidence. Existing WebMCP work stays intact; its task was checked and is idle, with its last coordination message confirming uncommitted changes and generation guards to preserve.

Peel geometry work remains incomplete and is paused for these requested corrections. The latest strip prototype isolates its failure to artificial localized end collars; the core and crease collar have no contacts. A proposed crease returning to physical sheet boundaries would remove those artificial outlets, but is not yet tested or integrated. No new completion claim or commit follows from that proposal.

Editing correction source gates passed. Carry target outline/API/helper removed; appearance HUD is absent during peeling/placing/settling and restored only for editing. Derived current-or-exit state and complete query memo reduce metadata contour calls2→0 and distinct wear/rotation preview calls2→1 while preserving readiness and source changes. Independent app/fixture checks passed.

Measured source counterfactual identifies ~2,754–3,106 rays per outline query and allocation-heavy precision fallback work. Matrix hoisting alone has no meaningful warm win. Scratch/scalar intersection plus correct finite ray-entry pruning lowers exact-output contour cost from about28–32ms to7–8ms. Complete query-cache invalidation includes content transforms, geometry/attribute replacement, camera/viewport and occlusion; identical queries perform zero rays. Cold cached triangle bounds/centroids preserve the ordered BVH hash but only reduce construction about298.2→219.7ms. These are isolated Bun measurements, not browser responsiveness claims.

Combined source freeze independently approved: device14tests/1,473 assertions, types/lint clean; app no-outline/dedup and actual editing fixture approved. Fresh `editor-performance-native-1` will exercise1280mouse/375touch first selection,24-step rotation/wear, exact one-save/reload and move-cancel without HUD. Input and release/setup timings are both gated; cold collider latency remains a specific concern beyond coarse pass thresholds. Source remains frozen and app lane owns the sole heavy process.

First `editor-performance-native-1` result: desktop rotation/wear exact save/reload and32px move-cancel without HUD pass. Touch stops because the fixture's pre-up DOM angle69.9711 differs from saved76.75635. The delivered-event diagnostic proves this is a stale fixture read: at identical finalmove/up coordinates173.875/394.0625, pointerup capture already shows76.75635496526479 before the product handler, exactly matching PUT. No product commit fix is justified. The fixture will wait for an explicit post-last-move paint boundary before reading its expected visible angle, while timing observation remains active.

Performance evidence remains qualified: desktop rotation max18.6/p95≈18.5ms; initial wear has a real raw-frame116.6ms/execution122.2ms gap with no longtask attribution. A single same-build CPU/scheduler diagnostic does not reproduce it (wearmax25.3/p9518.6), but is instrumented and cannot establish its cause. First selection callbacks35.1/18.8ms occur after Scene mount readiness prepares the collider; they do not erase the isolated~220ms startup cost. The fast wear release13.4ms/one frame was incorrectly classified insufficient. A separate completed-short semantic window classification will preserve boundary gaps/validity/overflow checks without artificial waiting. Final unprofiled native validation follows those fixture corrections.

Fresh uninstrumented `editor-performance-native-2` passed four tests / 741 assertions with zero source/artifact drift and all owned processes closed. Native mouse1280 and touch375 both preserve exact rotation/wear through one save each and reload;32px move/cancel has no editor outline or HUD and no placement save. Rotation rAF P95≈16.7ms and wear≈16.8ms; execution-window maxima19/18.9ms rotation and24.5/25.7ms wear. The previous116.6ms frame gap did not recur but remains unattributed. Short semantic wear releases20/20.5ms correctly carry no cadence claim. First selection callbacks36.4/17.7ms do not include earlier collider preparation. Root full typecheck passes12/12 projects. Independent final evidence review accepted these two owner corrections; no whole edge-wrap completion claim.

Source identity61ba54082a9a11ad7cbe9858a6d016bed82a13aa870a0240a0b364f93d9e55ee; artifactd9fe4fe1693743f440b5a8b64a54f386aaf7d576951e78e3c8880f0605f14373. Evidence-only curved-crease feasibility can resume independently while product source stays frozen for review. Concurrent WebMCP changes remain unstaged and preserved.

Independent final native2 review ACCEPTED the latest performance/no-drag-outline corrections on the tested mouse/touch surfaces. Reviewer checked568 source entries and76 artifact entries at start/end, all four settled originals and chronological/moving video frames. Exact saves/reloads and absence of silhouette/grips/rail during movement are verified. Residual cold collider cost and the unexplained earlier wear outlier remain recorded; ordinary rear editing acceptance does not approve unresolved wrapped peel or full-range geometry.

The resumed evidence-only curved-outlet probe is closed without product integration. `early/free-carry-1/curved-crease-handover.md` records side32 pickup error0.000076px, exact untouched vertices and zero core contacts, but21 modified join/outlet contacts at occupied wear-surviving centroids and occupied stretch up to12.91. Full38 contact count includes17 unchanged-region contacts and must not all be attributed to this candidate. Isolated build6.76ms;57.6/native expansion was not run. Physical peel remains unresolved; no further candidate is silently accepted.

App-only correction committed as577b6fa — Keep sticker editing responsive and hide controls during moves. Independently reviewed selective four-file patch against isolated HEAD2f1001a,8 tests/67 assertions, app-source types and four-file lint pass; staged whitespace check clean. Commit includes carry HUD suppression, shown-state/query memoization and placement identity selection with tests. It excludes unresolved device/wrap work and foreign WebMCP hunks. The measured combined native performance gain also depends on uncommitted device optimizations; do not attribute that entire gain to577b6fa alone. Remaining worktree is deliberately not declared clean or whole-feature complete.

## Owner-requested commit and push checkpoint

The owner explicitly requested committing and pushing all outstanding work. This supersedes the earlier task-local restriction on checkpointing incomplete wrap changes. Source/tests and documentation/evidence are committed separately, including the concurrent WebMCP work as requested. This records the current implementation; it does not change the unresolved physical peel/contact findings or assert whole-feature acceptance. Latest combined checkout passed all12 project typechecks, native editing/no-outline validation and independent bounded review; git whitespace checks pass.
