# Worker renderer host implementation

CPU009 is active. No native route activation or full-device visual/motion acceptance is claimed. The existing GL route remains the current renderer until complete shared assembly, native host, query projection, fallback and browser gates are integrated.

## Source/interface checkpoint

`device-render-protocol.ts` independently approved under CPU015. Exact final commands carry their pose and pointer sample; programs retain originating command/motion epoch through checkpoints. Control release stores original start/last timestamps, preserving regressing-clock/stall behavior. Reveal settlement and timeline completion remain distinct. The protocol is frozen for CPU016 consumption; runtime scheduler validation remains outstanding.

`render-channels.ts` bounds projection to one in-flight notification plus one latest scalar sample. Native paint has one capture credit tied to renderer, layout, raster, visibility and paint revisions. Resize/hide/dispose reject and close stale captures; stale acknowledgments cannot release a current credit. Received paint can be consumed immediately without an extra RAF. `native-screen-transport.ts` captures only in the associated canvas paint event and transfers ownership only after successful send.

`native-element-image.ts` uses official `@webgpu/types` 0.1.72 plus narrow Chrome152 experimental IDL declarations. It checks concrete WebGPU backend/native handles, allocates one Three StorageTexture per raster generation, copies ElementImage into that allocation, and closes every consumed snapshot. Raw rgba8unorm and authored LCD EOTF remain paired. Standard GPU objects use canonical types. The package/lock delta is only this dev dependency and its package entry; no unrelated upgrades.

The existing immutable preparation authority now supports non-React renderer leases. Its one worker retains canonical data while cache owners live, without polling. Main query wrappers and renderer-private transfer copies are distinct owned buffers. Copies occur in the producer worker; the mounted main buffer is never transferred. A failed canonical worker does not secretly recompute a cached recipe for native retry: native admission fails and complete GL recovery may reuse main's exact data. Pending cancellation terminates the producer before releasing the copy reservation. All consumers share cache/queue/accounting; there is no second preparation authority.

Unused cache accounting remains 64 MiB and includes canonical residency. Renderer admission allows at most four leases and 128 MiB total accounted canonical/main/private/pending bytes; a pending copy reserves two payloads until producer acknowledgment. Live owners preserve their already-admitted data. Default recipe is 23,148,696 bytes; one canonical + main + renderer copy is 69,446,088 bytes. Worker identity, request ID and lease ID guard results. Final cache eviction releases canonical data and terminates the idle worker.

The static extraction is now wired into the existing GL Device: `device-assembly-recipe.ts`, `device-assembly-materials.ts` and `DeviceAssembly.tsx` supply exact geometry/rest frames/order and backend-aware materials. Its query/material mutation notifications remain in Device. Main font raster factories and existing sticker component are preserved. No worker scene activation yet. Five existing source-location assertions now follow the extraction; all 35 affected tests pass (164,556 assertions), retained as `renderer-host/assembly-tests.txt`. Device types and scoped lint pass. Supervisor released the source freeze after actual Chrome showed front Music, without errors/warnings. `shared-gl-assembly-front.png` is readiness evidence, not full framing/pixel parity: canvas CSS440×888 differed from browser inner440×956.

CPU011 handed off StickerPackScene and production-device-view after its async drop integration froze. Preserve current `resolveStickerDropTransaction`, AbortSignal combination, prepared placement/surface revision adoption and awaiting final-save contract during extraction. CPU011 still owns contracts/collection/pure/hooks. control-physics remains reserved to CPU016.

CPU016's source refinement retains control restMatrix for byte-exact zero-depth restoration and adds original restPosition/restQuaternion/restScale tuples for active motion, avoiding matrix decomposition perturbation. This coordinated protocol delta after CPU015 needs follow-up independent review.

## Retained checks so far

- `bun docs/workstreams/023-mobile-tactility/evidence/gpu-worker/renderer-host/channels.ts`: ten deterministic lifecycle checks passed; 10,000 offers remain one in flight/one latest. No timing/browser claim.
- `bun docs/workstreams/023-mobile-tactility/evidence/gpu-worker/renderer-host/prepared-leases.ts`: seven checks passed using actual Bun module worker and MessagePorts, including mounted-buffer transfer isolation, byte accounting, pending abort, no cached-recipe recomputation and final cleanup. React commit hook is deterministic shim.
- Existing `evidence/gpu-worker/preparation/lifecycle.ts`: all 12 existing lifecycle checks pass against the extended broker.
- Device/composite typecheck and scoped implementation/evidence ESLint passed at this checkpoint. Combined build/full affected tests follow coherent integration, not every intermediate write.

Supervisor owns actual Chrome validation and traces. Prior associated-canvas and CPU008 shader proofs are source/capability evidence, not full host appearance or input alignment acceptance.

## First runnable host checkpoint

The existing `/webpod?renderBackend=worker` internal override now connects WorkerDeviceCanvas, createDeviceRenderHost and device-render-worker. Canonical TanStack search validates the override. Default is GL; no native preference is enabled. The first checkpoint assembles the shared chassis/light/material/LCD scene, transfers the associated presentation canvas before context acquisition, obtains private prepared resources over MessagePort, transfers native font rasters, and waits for material compilation plus real paint before readiness. Exact sticker pack/equipped/carry/query integration is still outstanding; this checkpoint is not a completed product backend and cannot satisfy activation gates.

The stable portal now outlives renderer generations. Native failure selects a fresh GL canvas and keeps the same Panel tree. Host resource/snapshot/projection lifecycle and final input alignment remain under implementation and require retained failure tests before acceptance. Source is temporarily frozen for supervisor's first actual worker frame check; web types and scoped host lint passed.

Supervisor measured a native capture at640×480 while current GL canvas metadata advertises960×720, with logical panel320×240 andDPR3. Metadata is raster intent, not a queried GL texture allocation. Native upload therefore uses explicit admitted destination dimensions and does not equate snapshot.width to target.width. `/tmp/webpod-associated-scale-probe.js` is a bounded associated640→960 contract probe with alternating stripes and native text, handed to supervisor; actual Chrome passed all six stripe-center RGBA samples at960×480 after capture640×320, with validationnull and cleanup complete (f9ede55). Its one-row stripe sample can verify API extent/color/alpha, not full glyph quality. Actual GL versus native LCD visual parity remains required.


## Actual native first-frame diagnosis

Supervisor's existing Chrome6× mobile route now produced the first worker WebGPU frame (f069442): requested/effective worker, wpRenderWarm ready, only existing Clock warning. This remains chassis/LCD readiness, not full scene/input acceptance. Three issues were corrected against actual failures:

- Wrapper diagnostics had been dropped by the custom CompositeInputBoundary component; its explicit props now forward requested/effective/failure to the actual div.
- A pending private lease was cancelled during the initial host mount lifetime, retiring the canonical worker under the reviewed cancellation rule. Deferring acquisition one microtask with a disposed check prevents synchronous effect replay from enqueueing a doomed copy. The next actual run passed the lease gate. Genuine later abort still retires the producer; copied canonical reseeding recovery remains outstanding and must retain one producer/budget accounting.
- Native copy initially failed “No context found for ElementImage”. Installed Three185 WebGPUBackend lazily configures the associated canvas in its context getter. Calling public renderer.getContext() after init/layout and before initialized/paint admission fixed the actual failure, without a dummy render.

The first frame displayed upright LCD text at85% extent. Source comparison identified the omitted existing GL fit: authored Panel272×204 must scale to logical host320×240. Both paths now call shared fitPanelContentToFrame; actual follow-up front-only extent screenshot passed (7c606a6): LCD fills the active screen and glyphs remain upright. This does not prove rear/stickers/full material visual parity. Existing HTML-in-canvas tests5/5 and scoped host/device lint pass; composite/device types pass. Worker motion admission no longer incorrectly requires main acknowledgment of an earlier command: resource/layout validation remains host-owned, monotonic command/epoch validation is delegated to the driver. CPU016 owns independent rapid-command proof.


## Native query/input checkpoint

The ready host now borrows its private-lease main query buffers into a bounded flattened chassis query view. Wheel and Select physics retain separate control frames; Select's child-local zero/depth is not confused with its translated rest parent. Main immediate control/pose intent updates these query matrices and sends the same pose to the render worker. Input uses CPU016's exact shared controller, the extracted unchanged native-compatible orientation hit helper, and actual canvas pointer capture; media/audio/haptic callbacks remain in CompositeInputBoundary.

The retained query-matrices.ts/json experiment compares40 poses against the authored hierarchical render graph (37,120 scalar world-matrix comparisons), including model translation37, wheel tilt and Select travel. Maximum floating-point difference2.842170943040401e-14; Select child-local matrix is exact. It caught a Three185 dirty-flag omission on explicit model matrices, corrected before live input checking. This is offline mathematical parity, not browser input latency. Existing orientation/input/control25 tests pass928 assertions; device/composite types and scoped lint pass. Native rear sticker query/render/admission remains outstanding and blocks complete native acceptance.

## Full sticker integration checkpoint

The native equipped and packet paths now execute through the actual host/worker. Equipped resources call the same `prepareSurface`/`preparePrint` functions as GL and receive private copies from the existing sticker producer. Packet geometry uses the accepted paper producer and shared packet recipe, including sleeve folds, full-resolution node paper curl and the three authored print appearances. Scalar packet pose/curl messages use one notification credit; main flattened print queries adopt the matching submitted worker pose. Paper return bounds are sampled lazily only when a bounds query requests them, not on every packet transform.

The unchanged GL projection implementation was extracted into `sticker-projection.ts`; both backends now use its exact pick/grab/project/fit/drop/quad/contour/bounds methods. Existing hit/grab/drop/projection tests:16 pass,173 assertions. Existing carry/contour/insert/metric tests:17 pass,864236 assertions. The CPU020 Scene freeze is archived separately at `/tmp/webpod-pack-scene-frozen.tsx`; the host extraction preserves that accepted source independently.

The initial native private-recipe cancellation recovery is now implemented by the separate accepted CPU022 same-authority reseed change (61858fb). It does not recompute mounted recipes or detach main buffers. Native wheel/select/quarter-arc/edge rotation preliminary browser interactions passed; the supervisor's observed live INP272ms at6× remains unresolved and is not a performance acceptance result.

Native raster updates now send one atomic layout/raster generation. An old capture cannot resize the texture. The first matching new capture synchronously invokes installed Three185 StorageTexture.setSize/initTexture and native copy; old pixels remain intact before that capture. The texture/sampler identity stays stable. Installed `Textures.js` disposal clears referencing backend bind groups and sampler state, so the next render recreates bindings. The retained raster/resize.ts/json experiment verifies equal-size suppression, old allocation retention, matching resize/copy and failure/disposal image ownership using actual wrapper/StorageTexture with controlled GPU allocation/copy callbacks. Actual current-Chrome resized GPU output is still a required gate.

Native readiness now uses the separate CPU025 warmup owner wired into the real worker's serialized scene compilation queue. Requested artwork variants must compile before main receives readiness; an empty retirement barrier does not claim artwork readiness. No warmup root is presented. The source author is still completing lifecycle evidence.

Carried-print private delivery is in progress under CPU026; main/worker carry presentation, exact source retention through first carry adoption and cancel/handoff checks remain outstanding. No preferred native activation, full-scene acceptance or timing claim is made. Default stays GL. Latest composite/device typechecks and scoped authored-file lint pass at this checkpoint; final combined checks and actual-route visual/recovery/6× traces follow complete integration.

## Complete-scene source checkpoint (awaiting browser)

The first cold packet browser attempt fell back with `Sticker computation byte capacity exceeded`; the preserved GL player is evidence of that one failure path, not native packet success. The same producer now delivers renderer damage as its exact GPU texels and width/height, omitting CPU-only alpha/onset/distance/contour arrays from the private render copy. Main query preparation still uses full fields. Detached warmup alone skips contours and releases its main query leases after exact compile ACK; private render leases and compiled materials remain alive and charged. CPU025 author reports the actual ten-artwork warmup, eight-print packet, and two carry frames fit with final zero ownership; independent review is pending.

Carry is now wired into the host and render worker. It acquires the existing carry producer's completed private geometry immediately, pins the main query frame, prepares the matching print/damage through the shared authority, and only adopts on matching gesture epoch and assembly revision. Worker artwork transfers have generation-long ownership, including frames later rejected for gesture supersession. Main queries adopt only the matching accepted frame and retain private leases until replacement or renderer retirement.

`native-carry-adoption.ts` is the policy actually used by the worker: while the first carry is pending, it holds at most one complete equipped and packet replacement. The original displayed source remains alive; a valid carry attachment drains replacements without an intermediate render, and early cancellation rejects undisplayed candidates. The retained `renderer-host/carry-adoption.ts/json` checks nine ownership cases with actual Three source geometry identity. It is an offline policy proof, not actual GPU/DOM alignment evidence. Return/landing waits for the matching source owner before clearing carried presentation.

Terminal visibility/dispose messages now consume their exact command pose through the existing motion driver before pause/retirement. Hidden rendering/projection is suppressed before command processing. Native idle device loss retires into complete fallback. Main projection rejects older pose sequences and mismatched scene/resource/layout revisions; locally published input gets a monotonically increasing sequence. Failed terminal postMessage still hard-terminates the worker exactly once. Incoming queued scene resources close on generation rejection, queue overflow, or retirement.

At this source checkpoint, 101 existing composite/input/audio/hit/drop tests pass (588 assertions), device and composite types pass, and scoped moving-source lint passes. Four query/raster helpers have been independently accepted; four render assembly helpers are under separate review. Application source is briefly frozen for the supervisor's first complete packet/carry browser checkpoint. Default remains GL; no full-scene visual, failure-matrix, or performance acceptance is claimed.

## Rear admission correction after actual complete-scene attempt

The next actual cold reload stayed on worker WebGPU and rotated rear, but exposed no packet action. Source audit found a missed consumer of CPU016's stable appearance snapshot: `StickerCollection` still derived rear admission from its frozen orientation prop. It now receives the existing per-device motion authority, synchronizes the original rear-admission and gesture-cancellation logic immediately from `readIntent()`, and subscribes directly with cleanup on replacement/unmount. It does not subscribe React to each pose. Other production orientation consumers were audited: chassis, wheel queries, Canvas semantics, carry and WebMCP already use current authority/controls; stage diagnostic attributes are not product admission inputs.

Native keyboard ownership now follows the same front-interactive boundary as GL. Mouse hover uses the original shell admission and control cursor helpers, and pointerleave/disposal clear cursor state. Cancellation/rejection settlements may publish the authored control rest state; ordinary remote control progress remains suppressed from echoing as fresh intent. Temporary native input diagnostic JSON attributes were removed. The actual adapter with controlled Happy DOM events passes five keyboard/cursor/disposal checks (`renderer-host/native-input-gates.ts/json`); 45 existing orientation/sticker lifecycle tests pass360 assertions. This is source/lifecycle evidence; the supervisor must retry native packet/carry behavior.

## Packet replacement and final input corrections

The supervisor's next actual native checkpoint showed packet/liner and earned/locked prints, then persisted a NightShift rear placement. Packet replacement exceeded the unchanged sticker byte limit and selected the complete GL fallback. This was a failed native transition, not full-scene acceptance. Independent reproduction now covers warmup10, current packet8, current/candidate carry, landed equipped surface, and replacement packet8 with the actual shared artwork cache.

The current and candidate packet owners now share unchanged exact geometry, damage texels, artwork and query preparation by their existing complete resource keys. The reuse scope is one native controller/renderer generation. New ports and bitmaps are sent only for changed resources; worker resource references are retained before asynchronous candidate preparation, and materials remain frame-local. An appearance-only replacement now needs zero new geometry/damage/artwork deliveries. The independent transition proof passes within existing limits; all final ownership returns to zero. `renderer-host/pack-reuse.ts/json` separately verifies actual assembler/MessagePort identity, failed/aborted candidate isolation, retired-owner rejection and exactly one final geometry/texture/bitmap disposal.

Detached candidate compilation no longer clears displayed-scene readiness. Installed Three185 restores render globals before its asynchronous object compilation awaits; the previously compiled displayed graph can keep rendering while candidates remain detached. `held-compile.ts/json` executes the exact worker publish/warmup orchestration with a held backend boundary and proves live rendering/projection plus cancellation without candidate readiness. It is a controlled orchestration proof, not GPU timing.

Additional bounded corrections are frozen for independent review: identical equipped updates deduplicate during pending adoption; native control physics registers with the existing agent authority; keyboard and captured-pointer admission reads current main intent at event time; collection subscription attaches before initial synchronization and drains reentrant publications; carry artwork bytes are measured before bitmap transfer. Reliable settlement/checkpoint samples now use the same pose freshness guards and query adoption as projections, applying native Panel geometry before public listeners without waiting for projection credit or inventing a submitted frame.

Composite/web types and scoped source/proof lint pass at this checkpoint. Full worker/host source review, actual replacement behavior, raster/visibility/loss recovery, and matched6× performance remain open. Default remains GL.

## Native frame callback receiver correction

A later actual browser transition from emulated mobile to fullscreen desktop fell back with `Uncaught TypeError: Illegal invocation`; the retired worker stack was unavailable, and a clean desktop reload returned to native. Source inspection found bare native frame callbacks stored as dependencies: worker cancellation and main physical control request/cancel callbacks were invoked with the dependency object as their WebIDL receiver. Both native adapters now call `globalThis.requestAnimationFrame`/`globalThis.cancelAnimationFrame` explicitly. Driver/control algorithms are unchanged.

`native-frame-receiver.ts/json` executes the exact extracted adapters through the actual driver pause/resume/dispose and physical control release/dispose, using a strict receiver-checking platform boundary. The former bare callbacks reject; all corrected calls preserve the realm receiver and leave zero frames. Device types and scoped source/proof lint pass. This proves the corrected dependency seam, not attribution of the retired browser error; actual mobile/desktop/DPR/visibility validation remains required.

## Exact mobile-to-desktop packet admission

The supervisor reproduced capacity failure without opening the packet: mobile440×956/DPR3 to desktop2952×1636/DPR2. Captured packet dimensions changed from `[231.22394283886186,242.78513998080496,0.8116761084662326]` to `[130.81970435848044,137.36068957640447,0.4088115761202514]` (width,height,pixel). Warmup IDs were PW-A01 through PW-A05, placements were empty, packet neighbors PW-B01/PW-C01, and the active sheet contained the five A prints. The existing passing appearance-only replacement proof had not covered this dimension change.

Strict consumed-byte reuse shared invariant flat covers but the complete resize still failed. The final correction additionally certifies the authored no-effect branch: every valid grid sample's exact exposure expression is below the existing0.05 threshold. All actual old/new packet surfaces satisfy this condition (maximum0.0017958879470825195). Their bowed normals are not treated as numerically equal; the original damage algorithm provably leaves base onset unchanged. Other surfaces require exact consumed mask/normal/UV bytes. Original canonical key/input and private texture owner are reused; resized position-based geometry and contours remain independent. Comparison yields within the existing4ms cooperative budget, supports cancellation and scans at most64 current-owner descriptors.

`renderer-host/damage-equivalence.ts/json` uses actual16 captured geometries and the original surface-damage generator to prove exact field equality; active exposure, changed mask/sticker/artwork, invalid indexing and NaN cannot obtain the certificate. The independent exact full8→8 resize workload passes with final zero ownership. Fourteen existing surface/alpha tests pass (1,161,917 assertions), alongside type/lint checks. The separate ten-ID warmup/current-packet overlap remains an open bounded lifetime case; it is not asserted to be the cause of the reproduced five-ID resize failure.
