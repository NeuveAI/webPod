# CPU fixes after PR 4

User authorizes latest main production deployment separately from a prompt follow-up PR, with Neuve Kanban tracking and supervised implementation. The explicit Kanban request supersedes the older repo note saying no board. Board webpod-cpu-fixes, repo /Users/vinicius/code/webPod. Existing seed tickets remain untouched. Branch codex/device-cpu-profile, base0f9732a. Preserve external .gitignore edit; never read cert/; Bun only; Jotai, no useState.

Correctness: remove measured first-carry serialization and prevent redundant render-density ownership changes without changing geometry, DPR resolution policy, effects, lighting, animation, hit testing or persistent sticker data. Also preserve immutable wear resource identity across worker frames where source proof identifies unnecessary transfer/mask rebuild. No WebGPU migration or quality reduction. No force push, merge or deployment of fixes; open PR after independent reviews/checks.

Tasks: WEBPOD-CPU-001 snapshot and wear reuse owns collision/carry preparation/worker/transfer files. WEBPOD-CPU-002 unified DPR owns Canvas sizing only. WEBPOD-CPU-003 independent combined review/browser evidence owned by lead/reviewer; depends on first two. Read cpu-followup-result, trace, motion and stickers reports before coding. Use installed pinned libraries and local reference sources. No new unit tests; existing tests plus retained deterministic lifecycle/parity experiments allowed.

Each engineer records source-linked Kanban context/comments/evidence and diary cpu-fixes-<lane>.md. Validation: exact output equivalence, disposal/replacement/stale worker cleanup, existing affected tests, independent type/lint, full lead typecheck/build/scoped lint. Known unrelated StudioEnvironment legacy-route expectation remains documented. Reviewer independently reviews both lanes, requests fixes for correctness failures. Live profiling uses only user's current Chrome localhost3000 tab, iPhone16ProMax440x9566x; never substitute another browser. Start/stop in bounded operation with finally; complete both trace save dialogs, verify newest file; raw traces stay outside git. CPU samples cannot establish GPU time or production costs; distinguish DEV replay. If remote attachment fails use Computer native current-tab controls. User-authorized connection prompts may be accepted; leave no recorder, save or permission dialog pending.
