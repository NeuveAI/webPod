# Preferred-worker activation: remaining evidence

**Not ready for activation:** the front LCD/glass discrepancy is resolved by37936fe and independently verified in both finishes. Current GL post-placement contour admission remains a confirmed blocker. Independent source approvals do not close integrated browser gates. This matrix consolidates the existing GPU scope and acceptance checklist; it adds no feature or new benchmark target. Status reflects retained reports available at this audit, not an unreported live run.

| Existing requirement | Already supported | Remaining minimal action / closure evidence |
|---|---|---|
| Exact front/LCD/material/raster appearance | Front chassis/framing closely match. Matched healthy PR5 Silver/light and current native confirm an LCD/glass difference; Black/Silver settings work. [Visual review](gpu-native-visual-review.md). | Resolve diagnosed LCD/glass difference, remove temporary instrumentation byte-exactly, then capture same settled content/finish/room in both backends. Verify header, selection, blank LCD field, glass reflection and edge/rear materials. Front-only images cannot certify all optics. |
| GL fallback remains fully functional | Earlier native resource failures visibly recovered GL chassis/Music/Settings and retained a saved sticker; later GL T1 artwork failure is separately confirmed. [Browser evidence](gpu-browser-evidence.md). GL readiness source correction approved with actual empty-group repro. [GL review](gpu-gl-warmup-review.md). | On corrected GL route, reload, rotate rear, open packet/liner and manipulate/return one earned sticker. Confirm no artwork retry/error. A healthy baseline archive is optical reference, not proof corrected current fallback works. |
| Native packet/carry/edit/return | f89 native packet/liner, NightShift drop, wear, corner rotation, scaling, reset and return passed; placement restored to zero. a822 production drop also passed. [Browser evidence](gpu-browser-evidence.md), [CPU follow-up](gpu-cpu-fixes-runtime.md). | After final integrated build, one immediate open→pickup/drop and one ordinary liner→pickup, then edit/return. Exercise Escape/cancel before save and verify no obsolete placement. Preserve final visible contour/texture/geometry and zero test placements afterward. |
| Resize and current/candidate resources | Actual phone440×956/DPR3→desktop2952×1636/DPR2→phone, rear rotation and packet remained worker; exact resize/resource proofs pass. [Browser evidence](gpu-browser-evidence.md), [worker review](gpu-native-worker-review.md). | No need to repeat the entire investigation. Final-build smoke resize once, checking LCD sharpness, hit alignment and worker/no failure after the latest optics/readiness changes. |
| Human wheel/navigation/media | Native Select entered Songs/Now Playing; playback scalar/time agreed with LCD, pause worked, MENU returned and wheel moved selection. No audio recording. [Browser evidence](gpu-browser-evidence.md). | Brief final physical wheel/Select/MENU/play-pause check, including one control press during orientation release. Verify local press rests and shared public state; if claiming audible playback, observe it rather than inferring from state alone. |
| Native keyboard and WebMCP parity | Immediate admission, physical registry and same authority passed independent source/controlled proofs. [Host review](gpu-native-host-review.md), [motion review](gpu-motion-review.md). No retained full native live WebMCP check found. | Through the existing registered tools: read `webpod_page_state`/`webpod_device_state` and `webpod_list_status`; `webpod_click_wheel` Menu/center and `webpod_flick_ipod` front/back, checking physical response and returned state. Rear: `webpod_open_sticker_pack`, `webpod_get_sticker` for an available ID, then `webpod_release_sticker` (no persistence). Compare visible draft/restored origin. Also focus the existing region: arrows/Home; front Enter admitted, rear control action rejected. No synthetic substitute for missing tool registration. |
| Hide/resume, reduced motion and teardown | Actual-worker/controller lifecycle proofs cover hide, cancellation, latest intent, disposal, context/frame ownership; native source reviewed. [Priming review](gpu-carry-priming-review.md), [carry review](gpu-carry-context-review.md). No final live hide/resume/reduced-motion evidence found. | Hide tab during a release/packet interaction, return and verify no stale press/capture or stuck draft. Navigate away/back once and confirm clean ready state. Toggle existing browser reduced-motion emulation, check press/release and orientation settle without stale response, restore setting. Use existing route only. |
| Loss/unsupported backend selection | Real native failures recovered GL earlier; device-loss/readiness/port timeout and stale-epoch teardown covered by source/lifecycle proofs. No current-route induced WebGPU loss control exists. [Acceptance checklist](gpu-native-acceptance-checklist.md). | Record whether genuine loss/unsupported selection is actually exercised on final source. On a genuine failure, verify current player/inventory and GL interaction survive. `renderBackend=webgl` is a manual reference choice, **not** proof automatic loss/unsupported fallback. Do not invent a loss API/debug route. If unavailable, keep live induced-loss coverage explicitly unperformed for lead/HITL disposition; do not silently waive it. |
| Responsiveness and work placement | Valid PR5/current production entrance comparison and f89→a822 rotation/peel examples exist. a822 has no recorded long tasks in those gestures, but first pickup184ms versus repeat56ms means no overall pickup win proven. [Production](gpu-production-runtime.md), [CPU follow-up](gpu-cpu-fixes-runtime.md). Priming source accepted, not measured here. | Complete the currently scoped priming opening/immediate/normal pickup observation on the exact clean build. Preserve EventTiming delay/handler/presentation distinction; main RAF is not GPU FPS. Existing matched baseline need not be rerun for every unrelated change. Do not claim autonomous entrance during main stalls or GPU cadence from the current main-window observers. |
| Default preference and final process | Explicit worker query route works; default remains GL pending activation. Source reviews and bounded tests/types/builds cover milestones, not whole-goal completion. | Once blockers close, review the bounded preferred-backend selection change and verify supported browser selects worker without query while fallback remains correct. Run final exact-range integrated checks/build; confirm no diagnostic source/active observer, clean user state, and authoritative artifact hashes. Full Neuve final-range/manual/HITL ledger remains lead-owned. |

Execution order: finish LCD diagnosis/correction and current GL readiness check; combine a single final native interaction pass covering packet/media/keyboard/WebMCP/hide/resize; finish the already scoped priming measurements; then preferred-selection and final process gates. Existing exact selectors and ARIA actions are in [gpu-native-acceptance-checklist.md](gpu-native-acceptance-checklist.md). No application, browser/server or heavy-check actions were performed for this audit.

## Latest integrated checkpoint

Source37936fe actual native fronts pass the bounded independent visual review. CurrentChrome phone6× native liner→PW-A01pickup→rear drop→Edit→Return passes and restores zero placements. Native source output GPU16-case proof has exact GL pixels; full rear/motion proof remains separate. Broker81e2556 removes only impossible non-grid contour reservation, resolving cold packet admission; current GL physically opens/peels/drops but reports a cap error after placement settles. Placement return succeeds and restores zero. The captured postdrop ledger motivates the separately scoped exact current-print reuse; no whole-fallback acceptance yet. All temporary origin/broker diagnostics were restored byte-exactly, including the corrected broker, before commit.

The initially named output-fixed GL Silver/light screenshots actually reset to Black/dark on navigation. They are not matched finish/room evidence; no verdict may rely on those names. A subsequent GL capture was interrupted by diagnostic HMR and shows connection UI, so is also excluded. The original healthy PR5 Silver/light reference remains the matched front optical reference. No performance trace was started during these checkpoints.
