# Review: shared visible contour integration

## Verdict: APPROVE

### Correctness Check

- Source of truth: current AGENTS.md, gpu-visible-contour-dispatch.md, design Production follow-up, integration implementation diary, installed Three/Jotai sources and existing current-input projection path. The dispatch explicitly permits compatible admitted historical presentation and distinguishes truthful native/GL capture stamps. No separate repository decision register was present at the standard paths.
- Workstream tracker: this workstream's documents; repo law prohibits Neuve/board use. No ticket or shell mutation.
- Correctness target: remove editor full contour sweeps from main-thread rendering while preserving exact adopted resources, current pointer authority, selection barriers, GL/native lifecycle and existing fade/tool inert semantics.
- Dispatch scope: nine source files plus existing mounted editor test; exact reviewed hashes copied to evidence/gpu-worker/visible-contour/integration/reviewer-manifest.json. No reviewer application edits.
- Dependencies: A/B independently reviewed; C1 has its own independent reviewer. This C2 verdict does not approve C1 author's own code or replace its independent disposition.
- Runtime gates: production query age, cold-copy attribution, native/GL visual parity and interaction/lifecycle remain lead-owned pending gates. Source approval does not claim the feature complete.
- Type/lint/doc gates: independently reran device, composite and web typechecks; scoped lint of all ten manifest files and capture proof passes. Diary matches the scoped behavior and records evidence limits.
- Staging: one coherent integration slice, separate from owner/worker and temporary runtime instrumentation. No commits/build/browser operations by reviewer.

### Findings

- [RESOLVED MAJOR] Candidate capacity rejection can preserve C1's usable current result while reporting a latched error. The initial editor error branch replaced all controls with the alert. Author corrected it to keep stamped current shape/grips/input and render the recoverable alert alongside. Cold-error-only and retained alerts respect fade/tool inertness. Independently reran the extended mounted proof (35 assertions).
- [RESOLVED MINOR] An accidental never-assigned projection ref/useFrame callback in PeelingPrint was removed. Only the actual stable StickerPackContents projection owner refreshes from the existing R3F cadence.
- No remaining Critical/Major finding in the reviewed integration slice.

### Verified boundaries

The editor never invokes the legacy synchronous contour prop; the mounted test supplies a throwing prop. Capture selects the actual equipped/carried mesh, validates Phase B descriptor/geometry metadata, captures nine samples and coherent matrices/rectangle, and sends borrowed collider authority to C1. Missing metadata does not trigger a full-sweep fallback. Production visibility owners use worker-only preparation. Existing synchronous contour remains available outside this editor path.

Human select increments an explicit session even for the same sticker ID. Tool epoch uses a disjoint session space. The editor checks session and sticker identity before showing results; retained fade uses the retained session. Owner replacement clears/unsubscribes the old projection and invalidates its queued microtask publications. Document hide clears query ownership; resume recaptures. GL projection lifetime no longer depends on moving pack/face state. Native host disposal disposes its projection, so renderer-owner replacement cannot reuse the prior query worker generation.

Pointer transforms still begin through the live `beginTransform` plane and current placement, with existing plane-validity cancellation. Historical contour coordinates place presentation controls but do not become transform authority. Human fade and tool preview remain inert. Saved-placement semantic changes clear previous presentation before recapture. Ordinary compatible pose notifications do not install a cancellation cleanup. Native delayed notifications read the latest binding directly rather than carrying an old pose payload.

The bridge publishes outside renderer commits via a supersession-guarded microtask. C1 provides stable snapshot identity and geometric dedupe; no extra continuous app RAF loop was added. GL refresh uses existing useFrame and returns immediately without active demand. Main work remains nine-sample capture and bounded scalar/matrix comparisons; this is a source boundary claim, not measured browser throughput.

### Independent verification evidence

Commands run from repository root:

```sh
bun docs/workstreams/023-mobile-tactility/evidence/gpu-worker/visible-contour/integration/capture.ts
bun test apps/web/src/sticker-editor-carry.test.tsx
bun test packages/device/src/sticker-transform-projection.test.ts packages/device/src/sticker-drop-projection.test.ts
bun run --cwd packages/device typecheck
bun run --cwd packages/composite typecheck
bun run --cwd apps/web typecheck
```

Results: 13 capture/lifetime cases; mounted editor 35 assertions; seven projection/drop tests with 85 assertions; all three typechecks pass. Scoped `bunx eslint` covers every manifest source/test and capture.ts and passes. Actual source hashes match the frozen manifest. The capture proof uses real Three geometry/visibility with mocked query transport; C1's separate actual-worker tests establish transport/ownership. Mounted tests cover retained controls on error, stale previous-session suppression, same-ID reselect, positive fade, carry/tool transitions, and no synchronous contour invocation. Installed Jotai's Object.is value comparison is consistent with the stable snapshot publication contract.

### Suggestions (non-blocking)

- During lead runtime measurement, separate capture/copy/startup age from sweep and RAF publication latency. Do not infer end-to-end responsiveness from worker duration alone.

### Neuve Dogfood Feedback

Not run: current AGENTS.md and dispatch explicitly mandate workstream documents and prohibit Neuve shell/board. No human review routing was fabricated. This independent source verdict and the lead's pending real-route runtime gates are recorded separately.
