# Panel review fixes

The review findings are addressed in source: canonical package consumption; provider `play` plus playback/progress subscriptions; TanStack Virtual for lists above 100 rows; post-commit store initialization; numeric Dynamic Type search parsing through a closed state predicate; minimum 11px authored text; explicit listbox/option selection semantics; hidden battery glyph; actor-specific success styling; state-specific S03 rows/footers/borders; adaptive same-origin artwork rendering; and mutation-resistant source tests for skeleton count, typography, provider seams, and virtualization.

The route keeps both colourways synchronized against the singleton client store. `?scale=1.3` and `?scale=2` are reachable. `bun run gates` remains an explicit W5a dependency under the coordination ruling. U14 and H-6 remain owner-only.
