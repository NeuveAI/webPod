# W3 decisions

## Sources read

- `AGENTS.md`
- the full W3 packet and 002 scoping bundle named by it
- the 001 sections named by the W3 packet
- Pencil nodes `A76Ib`, `DLqSo`, `H4QpB`, `HYNXu`, `mObBW` through Pencil MCP only
- `/Users/vinicius/code/agentic-context/jotai/docs/core/store.mdx`
- `/Users/vinicius/code/agentic-context/jotai/src/react/Provider.ts`
- `/Users/vinicius/code/agentic-context/jotai/src/react/useAtomValue.ts`
- `/Users/vinicius/code/agentic-context/tanstack/router/e2e/react-router/scroll-restoration-sandbox-vite/src/routes/index.tsx`
- `/Users/vinicius/code/agentic-context/tanstack/virtual/packages/react-virtual/src/index.tsx`
- `/Users/vinicius/code/agentic-context/tanstack/virtual/packages/react-virtual/e2e/app/react-compiler/main.tsx`
- `/Users/vinicius/code/agentic-context/react.dev/src/content/reference/rules/components-and-hooks-must-be-pure.md`
- `/Users/vinicius/code/agentic-context/react.dev/src/content/reference/eslint-plugin-react-hooks/lints/globals.md`
- `/Users/vinicius/code/agentic-context/tanstack/router/packages/router-core/src/searchParams.ts`

## Decisions

1. `deviceStore` is the sole store. The route mounts two Providers over that same store so the colourways remain synchronized and tool callbacks observe the rendered state.
2. Panel internals remain exactly 272×204. The owner preview uses integer page zoom; Dynamic Type scales only the bounded panel stage and is capped at 1.25.
3. Unsupported capabilities are absent from the model, never disabled controls. Radio is therefore absent from S03 under the launch matrix.
4. S08 uses TanStack Virtual when the fixture exceeds 100 rows. The test fixture contains 120 rows, while loading remains exactly eight 26px rows. A browser measurement records 60 animation frames under 4× CPU throttling and separately bounds the mounted row count.
5. After coordination approval, the route consumes canonical `@webpod/panel`; the app manifest and lockfile preserve the device/composite lane dependencies already present.
6. HTML-in-canvas, device materials, and composite transforms are outside this package. The panel is independently focusable semantic DOM.
7. H-6 and U14 require owner observation and remain explicitly open.
8. W5a implemented the universal runner. Its command phases (types, lint, and tests) pass, including root Bun discovery after the browser-test suffix moved to `.e2e.ts`. The latest rerun is presently blocked before those phases by a concurrent, W5a-owned deletion of `scripts/gate-core.ts`; W3 neither restores nor commits that foreign file. Earlier W5a static failures were acknowledged false positives under W5a review, so truthful prescribed state names remain intact.
9. Browser evidence is locator-cropped at 544×408: exactly 2× the 272×204 raster, without page chrome. Overview captures remain only as navigation aids.
10. The adaptive artwork fixtures are deterministic visual inputs: pale and dark surfaces pair with exact 3×3 dominant-colour and 8×8 luminance samples. Provider artwork remains the normal non-fixture path; the fixture surfaces make all four adaptive branches independently inspectable.
11. The panel package contains no CSS blend or filter effect. Artwork ambience is a pre-composited radial colour field made from raster-safe gradients and custom properties; scanline/sub-pixel treatment remains the screen-mesh shader’s responsibility. This preserves the bare-DOM colourway treatment without importing composite, branching on tier, or duplicating the shader overlay. Reduced transparency removes the ambience entirely at the same package seam.
12. The production artwork input is the provider's same-origin `/artwork` URL, used by both the visible `<img>` and treatment sampler. `ImageDecoder` handles supported raster formats. For the fixture proxy's SVG, the browser-decoded image is copied through `VideoFrame` and sampled at 3×3 and 8×8 cell centres. This avoids canvas and keeps the HTMLTexture-compatible DOM path unchanged. `?art=pale|dark` remains an explicit deterministic test-vector path only.
13. The fixture provider owns no timer, so S13 acquires a reference-counted document clock. Both colourways share one 250ms interval; elapsed monotonic time is passed to `fixtureProvider.tick`, and the final cleanup cancels the interval. The unit test proves continuous advancement and final-lease cleanup.
14. D-019 supersedes stale 001 download rows. Offline means cached shell, art, and metadata only. There is no download menu, glyph, count, filter, or recovery action in panel source or browser evidence.
15. A success receipt is the result of a provider operation. S03/S08 create a playlist and then read the playlist library total; S13 changes volume. Operation promises are cached per document and operation key so React Strict Mode can cancel and remount listeners without duplicating the mutation or losing its receipt.
16. The focusable `role="application"` owns `aria-activedescendant`; each colourway gets unique option IDs from `useId`. Browser evidence proves focus stays on the application while the active descendant changes. The native button is 44×44 panel pixels before any preview scaling; negative inline margins preserve its former 24px flex footprint, so the visual composition is byte-identical while the hit area is compliant. The box is shifted two panel pixels inward while its glyph is counter-positioned, keeping all four hit-test corners inside the clipped panel without moving the visible heart. Browser evidence records untransformed `offsetWidth`/`offsetHeight`, computed minimum sizes, and four-corner `elementFromPoint` coverage independently of axe's transformed preview result.
