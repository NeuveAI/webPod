# Independent sticker transaction review

Verdict: APPROVE — bounded sticker transaction source only, after independent correction verification. Full native host and performance gates remain pending.

Reviewer did not author this implementation. Read the complete gpu-worker-scope.md including dispatch E and all ownership expansions, final gpu-sticker-transactions-implementation.md, strict-critique skill and team review protocol. Correctness is exact prepared graphics/query output, bounded ownership and cancellation, and current-generation persistence. No repository-wide decisions/platform_decisions or workstream gpu-decisions.md file exists; binding decisions are recorded in the scope. Current AGENTS.md instructs workstream-only tracking; lead maintains the previously requested existing board, so this review does not fabricate ticket status, advisory output or waivers. No Neuve classifier output was produced or treated as approval.

## Finding and verified correction

- **Resolved Major — stale prepared contour acceptance.** `packages/device/src/sticker-contour-preparation-data.ts:9–11` records wear but checks only field identity. `sticker-contour.ts:91–92` then bypasses both the requested wear and current geometry attributes. Querying a prepared wear=0 print at wear=1 returns its old silhouette; editing position values and marking needsUpdate likewise returns old local contour coordinates. The public projection function has no immutable/wear precondition, and the normal Scene contour caller supplies current placement wear while displayed prepared wear can lag. The bounds cache correctly guards attribute versions; contours need equivalent validation plus the normalized byte wear threshold, followed by the exact defensive path when invalid. Reproduction: `bun docs/workstreams/023-mobile-tactility/evidence/gpu-worker/sticker-transactions/reviewer-contour-invalidation.ts`; initial reproduction reported wearMismatch=true and positionMutationMismatch=true against the same geometry cloned into the exact unprepared path. Author agrees there is no stronger external invariant. Author added normalized byte-wear and attribute/index dependency acceptance; mismatches now use the exact defensive path. The retained reviewer proof now asserts equality and reports both mismatch flags false. It also checks raw adopted wear exists for the unchanged print and disappears after mutation. Host changed the existing Scene contour caller to `preparedStickerContourWear(print.geometry) ?? placement.wear ?? 0`, so current displayed geometry/material wear remains coherent while a newer transaction computes. Independently inspected that narrow caller and selector; the finding is resolved.

## Verified boundaries

Inspected the shared broker, worker canonical/private transfers, typed computation data, geometry/damage/latest hooks, contour preparation/projection, bounds index, carry bounds adoption, async final-fit/save helper and its collection caller. Native cancellation leaves the sole existing job charged until completion/deadline rather than destroying canonical resources; worker loss terminates before cooperative fallback. Private delivery charges pending copies and releases on failure, cancellation, acknowledgement and owner cleanup. Public main buffers are not transferred. Ordinary acquired resources have explicit idempotent release owners; hook replacement retains prior matched front/back geometry and damage until new publication. Main save remains guarded after asynchronous fitting, before persistence, after synchronous feedback and after save before animation. Existing carried appearance remains present during fitting.

Prepared surface and damage descriptors grant no transfer authority; private transfer acquires its own lease. The worker retains canonical data, main and native renderer receive distinct copies. Bounds output uses original vertices and only culls safely interior boxes; mutable geometry invalidates the prepared index by attribute/version. Damage texture setup preserves RedFormat, nearest sampling, no mipmaps, flipY=false and exact byte row order. Native image raster/alpha extraction remains a main browser operation; this review makes no new performance claim for it.

## Independent checks

- All six retained scripts rerun: parity, bounds, contour, lifecycle, latest and final-save. Pinned original geometry/damage/fit/contour references use ebebb32d0b7f0b30b7e41b81778293d6b7d44405. Bounds936 and contour1320 cases pass. Worker/private lifecycle11, latest6 and final-save5 checks pass. The independent added reproduction covers the previously missing invalidation cases. After correction, reran1320 contour parity cases, eight affected tests/17,568 assertions, the reviewer invalidation proof, device typecheck and focused source/proof lint; all pass.
- Existing command: `bun test packages/device/src/sticker-surface.test.ts packages/device/src/sticker-alpha.test.ts packages/device/src/sticker-drop-fit.test.ts packages/device/src/sticker-contour-visibility.test.ts packages/device/src/sticker-projected-bounds.test.ts packages/device/src/sticker-wear.test.ts packages/device/src/sticker-contract.test.ts apps/web/src/sticker-interaction-lifecycle.test.ts apps/web/src/sticker-editor-carry.test.tsx apps/web/src/sticker-runtime.test.ts` — **53 pass, 0 fail, 1,163,275 assertions**, `/tmp/cpu019-tests.log`.
- Device and web typechecks pass, `/tmp/cpu019-device-types.log` and `/tmp/cpu019-web-types.log`.
- Scoped ESLint over the exact manifest's source and retained proof scripts passes, `/tmp/cpu019-lint.log`.

Native host appearance, sticker resource adoption, browser release/edit/wear/peel/cancel parity, main-thread6x responsiveness and complete architecture acceptance remain mandatory separate gates. This source review does not approve unreviewed host changes in shared files or claim those runtime gates passed. No browser actions, traces, application fixes or commits by this reviewer.
