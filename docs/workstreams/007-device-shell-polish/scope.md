# Device shell polish

Status: Ready for implementation. Owner requested smoother iPod model, elimination of quarter-view seam artifacts, and settings hidden until menu activation. Owner authorized an Astra 3D engineer and direct repair or Blender replacement.

## Sources and decisions
Primary: current user request and four attached reference screenshots (visual evidence only). Repo AGENTS.md is binding. Supporting: installed Three 0.185.1 / R3F 9.7.0 source and types, current production rendering, existing tests. Existing always-on rear settings are behavior to replace. Other workstreams are not assigned and must not be read/edited. ~/code/agentic-context is absent; use installed source and official docs. No new asset/download required unless procedural repair cannot meet targets. Preserve iPod 5G proportions, black/white finishes, wheel, LCD, ports, rotation, playback and agent capabilities. Default product choice: an explicit accessible Settings activation, closed initially, with a close path. Rear should show its metal surface without a permanently visible settings UI. Engineers may choose implementation details and log them; no further permission needed for reversible changes.

## Slices and dispatch
1. Astra 3D engineer owns packages/device/**. Diagnose and repair faceted shell/bevel lighting and rear text leakage. Remove permanently baked settings composition, preserving tasteful static rear markings only if they do not leak through sides. Add a visibility prop only if coordination requires it. Use procedural model first; Blender is allowed if needed. Log geometry/material decisions. Geometry invariants and browser angle comparisons establish correctness.
2. Settings engineer owns apps/web/src/** (except unrelated existing modifications), packages/composite/** if required, and a new apps/web/tests/settings-menu.e2e.ts. Inspect existing settings capabilities and expose an explicit working menu open/close path, initially closed, accessible by keyboard and pointer, using Jotai-backed state if state is needed. Coordinate device prop requirements with 3D engineer rather than editing their files. Preserve existing dirty route/UI changes. No write overlap with slice 1.
3. Independent reviewer after both implementations: inspect geometry/runtime and settings/accessibility behavior, run typechecks and scoped checks, issue request changes for any major defect. No Neuve exists in this repo; no tickets. Task tools unavailable; this document is the durable tracker.

## Verification and done
Run bunx tsc --noEmit -p packages/device/tsconfig.json, packages/composite/tsconfig.json and apps/web/tsconfig.json; bunx --bun eslint on changed code; bun test for affected package logic; browser tests on existing production / and/or /_spike/device routes using apps/web/tests/playwright.config.ts. Capture quarter, rear-quarter, side and front screenshots for black and white where practical. Settings regression must exercise initial hidden state, activation and closing, keyboard access, and preserve rotation/playback. No special proof-only route. Do not access cert contents or log tokens. No useState, npm/npx, force pushes, design.pen file access, or unrelated changes. Do not commit/push; keep two stageable code slices plus evidence.

## Artifacts / review
Each engineer writes decisions and handover to docs/workstreams/007-device-shell-polish/{geometry,settings}-handover.md; evidence in evidence/{geometry,settings}/. Reviewer writes reviews/final.md. Lead maintains this scope and final handover.md. UI and geometry aesthetic quality is proxy-verifiable through screenshots; final aesthetic judgment belongs to owner. Behavior is deterministic. Reviewer independently checks canonical API use, disposal, seam occlusion, visibility and keyboard behavior. Ready slices have no dependency; final review depends on both.
