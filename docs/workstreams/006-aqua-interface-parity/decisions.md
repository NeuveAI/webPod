# Decisions

- The nine supplied HEIC photos are geometric authority. All were converted with sips under evidence/references and visually inspected along with both attached PNGs. The 272×204 authored raster and 21px header remain; compact capacity becomes nine rows. Lead explicitly authorized the necessary shared state change, preserving wheel, keyboard and WebMCP agreement. Medium/airy capacity remains 6/4; row heights derive from the same 183px viewport.
- Light is neutral near-white with near-black 14px labels and white-on-blue Aqua selection. Dark follows the supplied slate Now Playing direction. Header and battery use period edges and green charge fill. No extra dependencies or replacement state layer were added.
- The supplied photos favor title-only track rows, so album/playlist tracks omit redundant track number and artist columns. Provider metadata remains in the model and Now Playing. Now Playing uses real queueRead position/count, 82px artwork, peer metadata, and existing progress/scrub/volume behavior. No queue count is invented if the provider cannot supply it.
- Removed the artwork background bloom and card shadows from LCD composition. Provider art sampling remains intact for compatibility; no server/provider behavior was changed.
- Review identified a real color transfer bug: pinned Three 0.185 WebGLTextures uploads HTMLTexture through RGBA8, bypassing hardware sRGB decoding. A scoped material hook applies Three's own EOTF once to that map sample. SRGBColorSpace stays correct; other device materials are unchanged. A regression checks the real shader and pinned upload invariant so an upstream sRGB-storage change requires review.
- The requested ~/code/agentic-context directory is absent. Read ~/code/agent-context/global.md and pinned dependency source, including Three's renderer and map_fragment, as available authoritative fallback. Used modern-web-guidance through bunx, repo law taking precedence over its npx example.
- Preserve existing experimental CanvasDrawElement capability requirements; no new proof/product route. Browser captures use installed Chrome with the existing flag and an explicit deterministic MusicKit seam. No live signing keys or account credentials were accessed.

## Owner-requested revision decisions

- New owner material direction supersedes the earlier opaque molded-lip/striped-well assertions. Progress and volume now share one translucent gradient with low-opacity inset shading and faint cast. The glossy fill and indeterminate stripes remain; HTMLTexture color decoding is unchanged.
- Marquee delay is 2s, with no additional initial keyframe hold. The 12s traversal loop pauses only at its ends. A changed title remounts its moving copy, resetting its own animation; active overflow remains required and reduced motion retains stable text.
- The panel and device presentation opt out of user text selection using CSS, including WebKit. Search inputs, textareas and editable content opt back in. No blanket event cancellation was added; native input selection/editing and keyboard focus stay intact. Existing diagnostic text remains explicitly selectable.
- Scrub uses its existing shared optimistic wheel value, now without integer-percent quantization or fill-only interpolation. Marker position accounts for the same 1px inner inset as the fill. A rejected obsolete seek may no longer overwrite a newer scrub revision. Delayed success, current failure, stale failure, endpoints and Menu cancellation were exercised on the actual product route.
- Scroll allocation remains 9px, with a 7px visible neutral trough and a 6px blue thumb. No repeating stripe, metal rim or thumb shadow remains. Vertical travel still uses the complete 183px content height, with the existing minimum-thumb clamp and all densities unchanged.
