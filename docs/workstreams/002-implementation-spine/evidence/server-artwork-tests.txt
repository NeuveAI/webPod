Server artwork verification — 2026-08-29 Europe/Stockholm

COMMAND
bun test packages/server-core

RESULT
65 pass
0 fail
194 expect() calls

Coverage includes:
- deterministic fixture artwork with zero outbound fetches
- Apple and Spotify provider URL shapes
- loopback, IPv6 loopback, RFC1918, metadata, file, data, javascript,
  user-info, custom-port, and host-suffix SSRF attempts
- manual redirect rejection before a private target
- non-image response rejection
- declared and streamed oversize cancellation
- stalled upstream abort and 504 classification
- missing, duplicate, malformed, and unexpected query values
- GET-only service contract
- reject-before-fetch cross-site Fetch Metadata policy
- deterministic 128-way saturation: 8 admitted, 120 refused, then released
- duplicate in-flight coalescing and release after failure/timeout
- lying MIME headers, truncated/trailing polyglots, and dimension mismatch
- unsafe finite/non-finite byte, timeout, and concurrency configuration
- owner-first and follower-first cancellation isolation for coalesced work
- all-waiters cancellation aborts upstream once and releases admission once
- real ImageIO AVIF positive fixture and exact BMFF box traversal
- fabricated ispe, malformed extent, missing mdat, and trailing AVIF mutations
- box-complete shell with junk mdat rejected by a full AV1 decode
- real AVIF accepted at decoded 1x1, rejected at unassociated ispe 2x2
- px above 3000 rejected before transport or decode

COMMAND
bunx tsc --noEmit -p packages/server-core/tsconfig.json

RESULT
exit 0

COMMAND
bunx --bun eslint packages/server-core apps/web/src/routes/artwork.ts

RESULT
exit 0

COMMAND
bun run --cwd apps/web build

RESULT
client build: 207 modules transformed, exit 0
server build: 410 modules transformed, exit 0

CLIENT BUNDLE CHECK
No source_not_allowed, ArtworkTransport, artwork-server error copy, Effect HTTP
identifier, Sharp, or libvips identifier appears under apps/web/dist/client.
