# Edge-wrap geometry investigation

2026-09-07. Read the full edge-wrap scope before source. Inspected actual rear-shell and front-shell construction, canonical dimensions and catalogue aspect extremes. Modern Web Guidance executed via bunx for Three surface geometry/raycast UV interpolation. Reused required UI/motion/material skills and installed/canonical reference workflow. No product source changed.

The current implementation only casts +Z rays into the rear and throws outside it. Its rear peel adds flat displacements, and contour projection assumes a24-grid: expanding shared bounds alone cannot implement wrapping. Wrote sticker-edge-wrap-contract.md with actual shell measurements, unchanged center coordinates/full rounded-domain predicate, proposed radial arc-length mapping and explicit side/front/peel/occlusion dependencies. Local nearest-corner-normal mapping is unsuitable for long front overflow because it focuses and folds. Maximum catalogue half diagonal is127.937 model units, so front continuation is required.

Outstanding contract decision before implementation is contact across real hardware apertures/assembly gaps: proposed taut thin-film bridges between actual boundary endpoints, never down into ports or over an invented shell. Proposed mapping still needs measured metric/orientation and exact-shell sampler validation; no geometrical correctness is claimed from this document. App/domain engineer received the exact center predicate. No browser, build, material source edit or commit was made.

Lead accepted thin-film bridging across small real assembly gaps/ports as the physical default. This resolves the support-policy question; actual endpoint/span bounds and no invented external plane remain required in implementation. Contract sent to independent reviewer before source.


### Actual glyph rejection and bounded coupled experiment

The static C03 front screenshot shows visibly skewed lettering and a tapered continuation. The current radial/cross-arc mapping is rejected; positive triangles did not establish acceptable material distortion. Static-1 was a diagnostic build with a concurrent source fingerprint mismatch, not a final validation pass.

An isolated 17×17 joint-coordinate experiment minimizes normalized horizontal, vertical and both diagonal rest-length errors while fixing the center and constraining its local tangent orientation. A positive-area rejection alone allowed nearly collapsed cells and was rejected. An inverse-area energy barrier improved the result, but the capped 5,000-iteration strict-contact solve still fails: full principal stretches 0.218–1.394; original-alpha centroid occupied stretches 0.279–1.355, lower fifth percentile 0.495 and upper ninety-fifth percentile 1.212. It took 8.07 seconds and remained slowly converging. This is a nominal front fixture, not final actual-shell clearance or product runtime proof. It is not suitable for a pointer event. Reproduction and raw results are retained under early/coupled-contact-*.

The alpha-weighted metrics for the preceding atlas were corrected to account for fixture axes opposite production artwork UVs. Occupied minimum area is 0.429; extreme principal stretches remain 0.169–3.057. The correction does not change the rejection and cannot excuse visible glyph damage as transparent padding.

The coarse contact experiment was subsequently falsified by refinement: its interpolated 33/65/129-vertex charts have minimum principal stretches 0.0195/0.0015/0.0048. A coarse positive-triangle barrier was insufficient. A two-unit slack probe stalled after 147 iterations and also failed; it has no physical feasibility or clearance claim. A fresh 65×65 pointwise descent capped at 500 iterations took 15.85 seconds and remained far from converged (0.158–2.23 stretch range). Further brute-force chart tuning stopped. The revised contract proposes an actual 3D sheet with consistent rendered topology and an analytic cylinder refinement test, pending independent numerical review. Production bindings are unchanged and remain rejected diagnostic code.

### Actual 3D cylinder checkpoint

The revised metric oracle (`sticker-sheet-metric.ts`) evaluates deformation singular values from arbitrary flat material triangles to their actual rendered 3D triangles. Three tests / 693 assertions pass, together with device typecheck and scoped lint. The analytic cylinder regression measures the known sinc chord contraction and positive sagitta penetration explicitly. These are measurement tests, not proof of a solved product wrap.

A bounded isolated 3D distance/contact probe, initialized from a deliberately perturbed cylindrical sheet, uses the solved vertices directly. At 17/33/65 vertices per dimension after 500 iterations, both-half triangle principal ranges are respectively 0.9931–1.0162, 0.99735–1.00535 and 0.99855–1.00173. Maximum angular errors are 0.01121/0.00246/0.00128 radians, maximum outward gaps 0.7046/0.1797/0.0451 units, and remaining edge-interior penetration 0.000912/0.000086/0.000016 units. The final measured CPU times were 73/284/746 ms. Reproduction is `early/cylinder-sheet-probe.txt` with exact output beside it.

This checkpoint supports refinement convergence and avoids the previous chart interpolation failure. It remains a PBD-style cylinder probe: no XPBD compliance invariance, dihedral bending, actual shell collision acceleration, full triangle-interior collision or self-intersection acceptance is claimed. It is not product-ready and is not placed in the input path. The prototype uses separate original-material constraints and hard outward contact; the eventual actual-shell implementation must preserve that distinction.

The independent reviewer reproduced the cylinder checkpoint and approved proceeding to a bounded actual-collider experiment. They correctly narrowed the pose claim: the cylinder probe pins only its center; its tangent orientation follows the symmetric initialization rather than an explicit frame constraint. A nonsymmetric seed and explicit local frame constraint are still required. The proposed collider API deliberately separates earliest collision from nearest support, so hidden assembly faces cannot silently become adhesion targets.

### Bounded nominal-front sheet outcomes

The ordinary Pulse edge probe reached full principal stretches 0.9472–1.0737, zero static triangle intersections and maximum support gap 1.106 model units after 200 iterations (2.075 seconds). The same incomplete distance/contact probe fails at the maximum C03 corner: stretches 0.0979–1.3593, four intersections and maximum gap 15.87. These remain nominal unobstructed-front diagnostics, not the complete actual assembly. Original-alpha centroid metrics and exact solved positions are retained beside both probes.

A subsequent capped constraint experiment added material area, independent dihedral bending, sampled barycentric triangle contacts, vertex sweeps and a gap phase. It also compared a 16-cell grid to a locally refined 40-cell grid with unchanged outer spacing. Both fail. The coarse minimum stretch improves from 0.326 to 0.562 in its area phase, then falls to 0.289/0.186/0.083 across point contact, triangle correction and sweep/gap phases. The refined candidate still has five intersections and a 0.377–3.306 stretch range. Its 10.53-second duration is unsuitable for input. No iteration escalation or acceptance follows.

A concrete gap implementation defect was identified: clamping signed normal distance did not enforce the required Euclidean support distance, leaving 3.0–3.75-unit gaps. The initial atlas is also severely sheared before solving; immediate whole-sheet adhesion can oppose metric recovery. The next proposal is a correct Euclidean gap bound and a metric-preserving exterior tangent-sheet initialization with staged adhesion, pending numerical review. This is not evidence that corner contact is intrinsically impossible. Bending equations were checked against the primary PositionBasedDynamics implementation; no new dependency was installed.

### Small staged-adhesion causal test

The earlier quad network included both diagonals although the rendered topology contains only one. That can resist nonplanar bending independently of the intended dihedral term. The unsigned area restoration also is not an inversion barrier. The new isolated causal strip uses only actual mesh edges; it does not pretend that the former area/sampled-contact passes were a complete solver.

A 40×12 material strip, rotated 30 degrees relative to a radius-26 cylinder, starts as an exactly isometric flat exterior tangent patch. It uses 65 vertices, a fixed center and local tangent direction, eight constraint iterations per incremental load, compliant attraction and exact analytic edge-radius contacts. The adhesion front reaches all vertices halfway through the bounded 20/40/50 load sequence, leaving a finite settling interval. An earlier admission-only sequence left outermost vertices newly admitted on its final step; that diagnostic is retained separately and is not described as converged.

At 50 loads the full triangle stretch range is 0.97869–1.02844, maximum Euclidean gap 1.2332, and edge-interior penetration residual 0.0000275 model units (below a 0.0001 numerical tolerance). Total measured CPU time is 8.57 ms and mean incremental load 0.170 ms in this tiny analytic case. The 20/40-load gaps are 4.072/2.164, showing why immediate final-gap clamping was the wrong causal test. Exact logs, phases, material coordinates and vertices are retained in `early/strip-causal.*`.

This is bounded analytic evidence only. It omits dihedral bending to isolate metric/contact compatibility, uses a cylinder-specific full-triangle radius argument under an angular span below pi, and does not establish arbitrary-shell continuous collision, self-intersection, final product timing or C03 corner feasibility. The actual collision BVH and larger mesh cost still require measurement.

### Actual assembled geometry and exact-query cost

The next probe reconstructs the actual Device geometry functions, including crowned front apertures, cut rear, glass and wheel/select transforms. It explicitly approves only the ordinary right-edge front patch for attraction. The prototype temporarily duplicates those support triangles; the copied collider totals 216,630 triangles, 65,535 BVH nodes and 16.46 MB of typed arrays, excluding JS node/leaf overhead. Actual transformed maximum X is 165, validating the entire x=165.18 initial tangent plane as exterior. This does not approve arbitrary aperture support.

The original small-strip 0.4-unit attraction cap was inappropriate for the full Pulse initial gap near 49 units: after 50 loads its gap remained 36.75. This schedule failure is retained, not called converged. Its 1,087,318 support queries consumed 7.94 of 8.06 seconds. A minimal warm-facet hint still performs exact BVH lower-bound traversal and uses deterministic facet ties. On 6,069 representative linear crossing queries, maximum point/distance/normal error is zero; time falls from 56.86 to 32.62 ms. This benchmark is not a solver replay. Near-contact queries average 3.82 microseconds; far queries 6.89 microseconds, so expensive work is not exclusively startup distance.

A bounded corrected schedule uses four iterations per load, a step cap tied to local material edge length and two-attempt strain/intersection backtracking. It remains rejected: full stretch 0.8823–1.0922, zero static intersections, gap 29.53 and 50 rejected attempts. Total time is 4.687 seconds, mean load 93.1 ms. More iterations and an off-thread wrapper would not establish responsive manipulation. No corner or actual browser integration follows from these measurements.

The prior analytic 20/40/50 sequence represents increasing total work under fixed attraction coefficients, not fixed-load time-step refinement or XPBD compliance invariance. The next acceleration must preserve exact collision/support semantics and be measured, with bounded resources.


### Straight-edge Cartesian diagnostic after whole-sheet reset

The whole-sheet iterative candidates remain rejected. The new `sticker-strip.ts` parameterizes an actual polyline section by material arclength. Negative distance preserves the rear plane exactly; inverse rear X preserves saved center coordinates. A constant longitudinal coordinate makes rear/side segments developable. The production front has vertical and cross crown, so constant-Y front sections are extracted from actual triangles and their strain is measured rather than claimed isometric. Profiles are bounded to 256 exact Y entries per borrowed shell sampler.

The optional right-strip binding is diagnostic only and is limited to the verified longitudinal band. Other regions still use the previously rejected radial diagnostic. No change to allowed widths or stored placements was made. Ordinary Pulse at width .20, center world (165,0), rotation0, 48 cells measures principal stretch .99121–1.00032 and minimum area .99121. This is a mesh measurement, not collision or browser acceptance. Section construction took23.29ms and sampling88.89ms in this cold probe; these are not acceptable per-pointer costs and are not presented as a runtime pass.

Maximum .35 Pulse remains rejected: at96 cells it reaches the glass lower corner near world y42, where independent section lengths around the raised insert diverge. Maximum stretch3.03946 occurs across x113.8..115.8 and z29.74→27.37; minimum .70364 is around x142 at the glass edge. This transition needs separate continuous support handling, not a claimed straight extrusion. The ordinary fixture does not waive this mandatory maximum-size case.

Evidence: early/straight-strip-probe.txt, straight-strip-ordinary.json, straight-strip-max-refined.json, straight-strip-source.json. The probe imports the earlier saved actual-shell fixture. Reproduce with WIDTH=66 GRID=48 for ordinary or WIDTH=115.5 GRID=96 for maximum. Targeted helper1test100assertions, device typecheck and scoped ESLint pass. Native actual-route capture handed to collection engineer; no browser acceptance yet.


### Maximum Pulse shared-phase insert candidate

Ordinary .20 native rear/side/front images in early/strip-1 were inspected: key and title proportions remain coherent. That acceptance is limited to this static case.

The next maximum .35 candidate shares one material phase across rows and takes front Z from actual shell/insert support. An explicit rounded-glass insert ramp uses actual exterior rim and inner glass samples; it does not accumulate the glass step into each row's material travel. Initial8-unit and6.5-unit spans exceeded the2-unit gap target (2.0444/2.0050); the6-unit span stays within it. A tiny analytic-crown/triangle-cap position mismatch was found using decreasing .001/.00001 sample spacing; a .5-unit cubic join removes that measured step. The Math.max support/ramp switching curve and exact endpoint provenance still need independent scrutiny; this is not final geometry approval.

At96cells, final lifted mesh has zero18,432 full-triangle intersections against the actual copied assembly. The first gap report sampled vertices/centroids and was insufficient to certify triangle interiors. A subsequent adaptive bound uses exact nearest approved-support distance at each subtriangle centroid plus its enclosing radius: the1-Lipschitz distance upper bound certifies every subtriangle≤2. It required22,172queries, reacheddepth6, and had0violations/0unresolved. This is a gap certificate for the explicit support subset, not proof of exterior connectivity or dynamic collision. Source/probe/mesh are saved as early/shared-strip-final-96.json, shared-strip-certified-probe.txt and shared-strip-certified.json.

Current24cell geometry had207intersections and is rejected. Uniform96 was authorized only for bounded native maximum-Pulse diagnostic, with contour indexing sharing the same constant. Cold96sampling was~37ms; responsive update cost and final topology remain unapproved. No stored width, center domain, artwork UV, or allowed placement was reduced. UI owns the next exact-build native capture; protected source frozen.


### Center-local Fermi falsification

Primary references consulted for local decal/stroke coordinates and transported frames: Schmidt/Grimm/Wyvill, Interactive Decal Compositing with Discrete Exponential Maps (https://web.engr.oregonstate.edu/~grimmc/content/papers/sig2006tm/sig2006tm.pdf), and Schmidt, Stroke Parameterization (https://papervideos.s3.amazonaws.com/StrokeParamEG13.pdf). These motivate local coordinates and minimal normal rotation, but do not validate this implementation or promise low distortion over a wide positively curved region. The latter paper explicitly discusses degeneration with large curvature variation. No new library dependency was introduced.

The first projected-step probe used actual support with exact saved center(157,268,3.11227). Its loose progress test allowed material distance to be consumed while sliding along an unbridged seam. Step-halving did not cure this: apparent nearzero strain at the seam was not an intrinsic corner verdict. That numerical method is rejected.

A second temporary prototype unfolds across adjacent triangle edges, rotates the unit tangent across the shared edge, and fails at missing boundaries. It builds adjacency in~151ms and traces289points in6.6ms. No protected product source was changed. The exact corner still has unsupported front/hold crossings, yielding264/512 missing triangles at16cells. Those missing regions are not silently excluded from acceptance.

Separately, a fully supported rear neighborhood focuses: minimum principal .0408 at16cells, .00194 at32cells. At32cells,12 of479 triangles wholly on the flat rear plane reverse orientation. Their worst neighborhood is near world(138,266,-28), away from the unbridged front seam. Completing bridges cannot by itself fix this contact-coordinate failure. No broader angles or native corner render were run; this candidate is stopped for reviewer diagnosis. Evidence: early/fermi-unfold-probe.txt, fermi-unfold.json, fermi-unfold-32.json and source fixture fermi-shell-probe.txt. Adjacency welding/unique-crossing assumptions in this prototype still require independent scrutiny before attributing every failure to intrinsic curvature.


### Boundary-apex rigid fan: early compatibility rejection

The advisor fixed material patch[-18,57.75]×[-18,114.161425], outlet(57.75,57.75), rigidfan innercorner(-6,-6), and12-unit two-ringcollar. With the exact savedcenter and transported material frame, every hinge shares the fixed apex(161.5457,270.7757,84.6092). Actual transformed assembly maximumZ is30.43396, so apex gap is at least54.17528 regardless of contact search. At least54.29085units of its straight center-to-apex generator lie beyond even that globalfront bound. Hinges cannot bend this generator. This fails localization for the declared rigidfan family.

The nominal commonrearjoin also gives a necessary collar bound: endpointdistance33.69363/restdiagonal16.97056 requires maximumstretch≥1.98542, versus the declared1.153 comparison. A simple two-triangle witness has principal ranges1–1 and0.99999–3.30065; it is not an optimized collar. The collar bound was initially recorded as conditional on allowable contact phase. Advisor clarified that phases do not permit moving the declared commonrearjoin to another surface or changing the polygon, and explicitly accepted early family rejection. The apex bound independently rejects localization.

No hinge/contact optimization, radial subdivision, source change, or native run was launched. This rejects the restricted rigidfan, not physical vinyl wrapping. Durable evidence: early/fan-compatibility-probe.txt/.json and fan-envelope-probe.txt/.json. The probes use the already saved actual-shell fixture and Fermi exactcenter/frame data.


### Exact pinned Ammo preflight

Isolated cache /tmp/webpod-ammo-79190a1f holds unmodified Ammo JS/WASM/IDL/LICENSE from installed Three's exact79190a1f03845794b1bba1777f30037349967658 commit. Assets are396,123 and651,779bytes. No dependency, lockfile or product source changed. ExactURLs andSHA256 hashes are in early/ammo/preflight.json; zlib LICENSE preserved alongside.

Bun1.4.0 initializes the WASM module in6.261ms. Required soft-rigid world, statictriangleBVH, original-trimesh softbody, node/material/config access, weakbending links and cluster selfcontact flags are exposed. A4node/2face fixture with flags0x61 and declared margins takes.234ms for one zero-gravity step; this is capability proof, not C03 or selfcontact correctness. generateClusters(2,4) returns3, so later evidence must recordactualcluster count, not infer it from requestedcount.

All18 explicitly owned native objects were removed/destroyed in dependency-safe order. Borrowed config/node/material/worldinfo wrappers were not destroyed independently. Reserved WASM capacity remains64MiB before/aftercleanup; it is not a live-allocation measurement or proof of zero internalretention. The isolated process ended. No C03 solve was launched. Durable reproduction is early/ammo/preflight-probe.txt; engine binaries remain temporary cache rather than new repository dependencies.


### Ammo C03 admission prerequisites — stopped before simulation

The original1089-node tangent-plane seed is wholly exterior to the copied actual fixture: its plane projection is301.065639 and maximum transformed fixture projection300.965639, a.1gap. This halfspace bound covers every incident seed triangle, not just pins. The three declared pinned nodes have nearest-support gaps.1,.279324,.535456; none was independently projected to reduce those unavoidable orientation-anchor gaps.

75candidate targets fail the exterior first-contact match, primarily at the open rear rim. They were retained as explicit blocked targets, not omitted. No bridge was invented and no C03 simulation ran. Largest currently legal targetdistance is100.704993. The prescribed press-duration screening formula yields2.407038seconds, but the actual capped-force/ramp free-node integration at120/240Hz still ends3.483units away with significant velocity. It does not reach the.25 capture interval. Reviewer/lead were notified that target coverage and reachability both fail admission; force parameters were not tuned. Durable early/ammo/seed-preflight-probe.txt and seed-preflight.json preserve the complete seed, targets andblocked entries.


### Ammo prerequisites corrected, then fixed-target substrate limitation

The 75 initial target mismatches did not justify inventing seam bridges. For the independently exterior seed, using the closest-point separation direction instead of one incident triangle normal resolved 54. The remaining 21 were strict edge barycentric roundoff: the worst outside barycentric component was −7.35e−13 and plane intersection error was at most 1.07e−14. A narrowly bounded transverse-edge fallback preserves exact first-contact distance/provenance, rejects coplanar rays, and still rejects a real 1e−7 gap. Independent collider checks passed 7 tests and 627 assertions; all 1,089 initial targets then matched actual first contact. This does not classify future inside queries or approve every support seam.

The fixed force parameters require more pressing time than the initial formula predicted. Bounded free-node integration reaches a sustained 0.1-second interval within 0.1 distance and speed at most 1 at 2.9 seconds (120 Hz) and 2.904167 seconds (240 Hz). The common press duration is 2.908333 seconds, followed by 0.5 seconds of capture. Force coefficients were unchanged. Corrected seed, exact edge cases and free-node results are durable under early/ammo/.

The reviewer authorized a narrower first substrate experiment using fixed initial exterior targets. This does not test physical adhesion against current support after sliding. Its original uninstrumented run failed with OOM; my first attribution to construction was premature. A construction-only diagnostic showed all stages succeed in the default 64 MiB heap: 203,743 static triangles without vertex welding, three reused temporary vectors, 6,015 bending links, and 100 actual clusters (99 collidable if the one documented aggregate is retained). The phase-instrumented run completed 183 steps at 120 Hz and failed in step 184, after 1.525 seconds of simulated motion. Before the failing step, native stepping consumed 640.366 ms and force application 34.471 ms. Neither pressing nor capture completed. No partial mesh was durably captured, so no strain/contact/glyph claim is available.

An independently approved 128 MiB repeat failed at module instantiation: although the JS loader exposes INITIAL_MEMORY, the unmodified pinned binary declares both minimum and maximum memory as 1,024 pages (64 MiB). The direct import-section parser and LinkError are preserved. No redundant 240 Hz loader run, binary mutation, memory escalation or engine rebuild was attempted.

Pinned sparse-SDF source permits a cell cache whose configured ceiling can exceed 64 MiB; it uses 0.25-unit voxels and 262,144 cells of 64 scalar distances plus metadata. This is a plausible pressure source, not a measured allocation attribution. Explicit native owners were destroyed, but the ledger is not proof that unexposed internal linked SDF cells were freed. All isolated processes exited. The exact failure logs, probe versions, source hashes and memory-limit evidence remain under early/ammo/. This substrate result is incomplete and does not reject or approve the physical corner requirement.


### One authorized 150-step mesh recovery

An unchanged 64 MiB, 120 Hz run stopped at 150 steps (1.25 simulated seconds), before the known OOM. Native time was 519.280 ms, force time 24.511 ms and final/periodic copying 3.264 ms. Native step median was 3.350 ms, p95 3.874 ms and maximum 10.565 ms. Static BVH construction took 87.552 ms, with total native construction 136.013 ms. These timings are isolated substrate measurements, not product input latency.

The complete 2,048-triangle partial mesh has principal stretches 0.93109–1.06174 and no degenerate cells. Nevertheless, 134 triangles intersect the copied actual shell and 10 self-overlap pairs are detected, six without any shared vertex. The self-overlap diagnostic shrinks connected triangles by a relative 1e−6 to exclude their expected common edges; it is not a CCD proof. The maximum sampled vertex-support gap is 62.083, median 14.220, and maximum velocity 68.440. This state remains midpress, so the gap is not a final adhesion verdict. Three pins retain their seed frame within 1.1e−5 model units, consistent with native float storage.

The final mesh, original material coordinates, faces, timing timeline and audit are saved as early/ammo/c03-partial-150-*. The every-30-step exporter overwrote one file; earlier intermediate positions were not retained. This evidence gap is explicit and no swept trajectory claim is made. All 19 owners were destroyed and the isolated process exited. No additional run, physical configuration adjustment, binary or package change was made.

A subsequent read-only contact stratification does not explain away the intersections as coarse chords. For affected vertices, each nearest support facet was oriented only when exactly one far-normal approach first hit that exact point. A behind-surface sign was used only when the nearest-point barycentrics were strictly interior; boundary/ambiguous cases stayed unknown. This gives 41 nodes behind exposed facet interiors, 66 outside, 15 nearest-boundary cases and 5 ambiguous orientations. Of 134 intersecting triangles, 119 contain a classified behind-surface node, one has all three nodes classified outside (a distinct chord-cutting witness), and 14 remain unclassified. Node 377 is 4.412 units behind the actual rear facet interior. This is a local exposed-facet diagnostic, not a general volumetric/swept classifier. Exact coordinates, barycentrics and first-hit checks are saved in c03-partial-150-contact-strata.json.


### Explicit per-face cluster collision alternative

After source review, one bounded experiment changed only collision flags to 0x62 and generateClusters(0). The pinned face branch creates 2,048 collidable three-node clusters with no aggregate. Its 4,194,304-entry bool connectivity table has a source-derived 4 MiB payload before overhead. This routes rigid contact through cluster/convex handling rather than the prior sparse-SDF node path, while shared-node self pairs and anchored-cluster/static pairs remain excluded.

The isolated 120 Hz run stopped after 121 steps (1.008333 simulated seconds): native 1,979.542 ms plus force 24.308 ms. Median native step was 17.161 ms, p95 19.404 ms. It avoided the earlier OOM but reached the 2-second cap before pressing finished. Principal stretches are 0.93725–1.06191, with 155 actual-shell intersections and no detected self-overlap pairs. None of the thirteen root-incident faces intersects, so these contacts occur elsewhere. Local exposed-facet stratification finds 47 behind-surface nodes and 130 intersecting triangles containing one; two cases have all nodes locally outside and 23 remain uncertain.

The separate 240 Hz process stopped after 135 steps (0.5625 seconds), also at the cap. I inadvertently overlapped part of it with the first run's read-only audit; its wall timing is therefore confounded and is not a clean cost comparison. Its earlier state has 58 shell intersections and no self-overlap. At the common retained 0.5-second state, 120/240 Hz have 83/55 shell intersections, and node positions differ by up to 9.164 units (4.415 RMS). These results do not establish timestep convergence, completed contact or adhesion. The six prior nonshared selfpairs are absent in these earlier partial states; no later-collision inference is made.

Unique every-30-step meshes and exact final states are now durably retained, together with unchanged rest coordinates, topology, velocities, logs, source and full oracle results in early/ammo/face-cluster/. Both isolated processes exited after explicit owner cleanup. No additional physical parameter, mesh refinement, memory increase, binary or product change was made.


### Source-derived cap proxy: certificate rejected before replay

The next isolated candidate changed the source crown tessellator's grid from 4 to 12 only for a prospective exposed front cap. It retained fine triangles in 296 physical-boundary cells and inserted fine interface points. Actual source classification is 30,475 exposed cap faces, 30,475 back cap faces and 108,417 other front faces; the dense resource is not predominantly the exposed cap. The candidate retains 16,565 cap triangles, only a 1.84× cap reduction and 6.83% reduction of the complete 203,743-triangle collider. No native cost attribution follows from those counts.

A full projected-triangle overlay evaluates 237,426 polygon vertices/edge intersections. Its per-candidate covered-area discrepancy is at most 2.42e−13. Raw signed height error is −0.003178 to +0.010278. A 0.011787 lift of free vertices would bound the positive error to 0.021300, but sixteen samples on a fixed-endpoint internal collar chord remain up to 0.00008346 below the original. Their free barycentric weight is zero, so the declared lift cannot correct them. This fails the unchanged 1e−5 tolerance; the candidate was stopped rather than moving the boundary or widening the error budget.

An earlier interface-recognition error came from Float32 grid coordinates exceeding the initial 1e−5 grid-membership predicate. Recognizing a grid plane within one Float32 ULP (3.1e−5 at this scale) corrected that classification without moving coordinates or changing the height certificate. Both failures and the final rejected mesh are retained under early/cap-lod/. Global domain, hole and T-junction proof were not completed after the height gate failed.

The approved paired replay therefore did not run. Its prepared protocol uses fresh worlds with identical rest mesh and saved x/v, explicitly q=x, zero initial force, pin masses and regenerated per-face clusters. This is a common reconstructed state, not historical native continuation. First steps would measure cold contact from that identical state; subsequent warm trajectories would be labelled naturally diverging and retain positions. Replay must be serial with all certificate/audit work outside timed windows. No new full solve, product change or toolchain acquisition occurred.


### Authored collision inventory, then stop native optimization

A source-traced inventory splits the front into 30,475 front cap, 30,475 internal back cap, 35,025 outer rear bevel, 2,140 outer core wall, 35,026 outer front bevel, 17,589 wheel rear bevel, 1,048 wheel core wall and 17,589 wheel front bevel triangles. Instrumented tessellation retains the exact original positions. The outer rear bevel is exposed above the steel seam and must not be removed merely because some normals face backward. Wheel-hole layers require actual overlay coverage before any exclusion. The 392 rear return-lip triangles span both exposed seam and covered material, so they also cannot be removed wholesale.

The internal front cap is a source-grounded exclusion candidate, at 14.96% of the current 203,743-triangle fixture, but no exclusion/replay was implemented. It is already absent from attraction support and does not repair the existing actual rear/front penetration witnesses. The old fixture also omits rendered wheel-gap floors and hardware resources; its whole-assembly wording requires that qualification. Full original fixture/oracle remains unchanged. Evidence is in early/ammo/inventory/. Lead superseded further engine optimization with a product-specific procedural prototype; no new native physics run or dependency work followed.


### First procedural perimeter cage sent to actual capture

Reviewer approved a first near-bisector C03 visual prototype. The new local cage uses material longitudinal/transverse coordinates, actual rear section distances and a rounded-perimeter secondary bend that continues into adjacent straight edges. After the front rim, a twelve-unit integrated smoothstep joins into one common inward front direction for every row; actual front/insert height supplies the surface. This avoids the earlier polar convergence of front rows. It does not claim full-range acceptance or preserve the old experimental strain envelope.

At the existing actual-route pose x=.027/y=.017/width=.35/angle0, the first guard finds all 9,409 vertices finite, no zero-area or sampled-normal-reversed triangles among 18,432, minimum area .7350. Cold construction took 32.65 ms in this diagnostic. Device types pass. Source was frozen and the app integration owner was handed the single verified build/static rear-side-front capture. No further geometry changes are made before inspecting those images; interaction, sweep and general-corner completion remain pending.

### Cage first visual and boundary falsification

The actual C03 original PNG contains the long lightning tail seen in cage-1. Its presence is not itself evidence of mapper shear. Root and independent reviewer accepted the bounded angle-0 rear/side/front and close views for recognizable ticket/barcode and coherent front rollover. This does not establish general placement, motion, hit, or peel behavior.

An isolated source probe (`early/cage-boundary-1/probe.txt`, raw `result.json`) then checked original indexed geometry at rotations 0/45/90, without a build or browser. The accepted center (156.09,266.616) has finite coordinates and zero zero-area/opposed-normal triangles for all three rotations. This normal comparison is not a self-intersection or clearance proof. At center (156.09,250.01), rotation0, the current cage throws `Collapsed corner cage`. At (156.09,250.1), rotation45, its minimum triangle area is .004394. The previous atlas branch at Y249.99 takes 1292ms, and cannot remain a hidden shipping fallback. The requested .02-unit cross-boundary delta could not be computed because the second mesh throws; no fabricated continuity number is reported.

The cause is structural: near a straight-edge normal, the shared front travel direction becomes parallel to the adjacent edge's perimeter tangent. The next correction must preserve the accepted near-bisector visual while giving all allowed centers one continuous mapping. No further native capture of this known throw is needed.

### Unified candidate freeze for actual sampled sweep

Replaced quadrant reflection with a full closed perimeter and per-point analytic inverse of the unbent material plane. A second intermediate test caught a circumference-phase error (58/346/3 opposed triangles on right-edge Y200 at 0/45/90); anchoring every section radius to the same geometric root corrected it. No geometry source changes follow the explicit app capture freeze.

Final bounded evidence `early/cage-unified-1` contains exact source hashes, probes and raw rows. Twenty prepared meshes and 84 dense boundary/axis meshes have no finite/zero/opposed failures. Flat C03 centered(0,100) differs from exact original XY/rearZ only by Float32 error7.63e−6. For .01-unit center increments, maximum mesh increments are .05259 at Y250, .05705 at X139, .01009 at top axis, .01000 at right axis. These are local finite continuity samples, not a global Lipschitz or collision proof. Device tsc and targeted eslint pass. Current ~20ms warm full96-grid construction is still too expensive to infer direct-manipulation readiness; optimization follows credible visual sweep.

### Integrated pickup, visibility and wrapped contact source

Actual sampled cage-sweep-1 (19 public poses/25 frames, 663 assertions) passed source/build/artifact identity and independent visual review; it is sampled endpoint/reload geometry evidence, not direct motion. The subsequent device integration uses the same source UV grid for authoritative painted hits, shared shell occlusion, frozen grab inverse and projected visible contour spans. The visibility collider copies current visible assembly meshes (excluding stickers), including inserts/hardware present in the actual scene rather than relying on the older incomplete physics fixture. It owns only its BVH/arrays; borrowed geometry is never disposed. Local ancestor transforms avoid inverse-world cancellation roundoff; mesh/attribute/backing/count/version/visibility identities invalidate it. Tests prove global yaw reuse, actual local/visibility changes and fresh version-zero attribute/index replacements.

The contour now samples reduced alpha segments through the displayed grid before splitting hidden spans. A regression has four visible corners and a hidden edge midpoint; it produces open spans instead of a false closed silhouette. This finite subdivision is not a mathematical certificate for arbitrarily subpixel occluders. Native visible/occluded material witnesses remain required.

A topology bug was corrected: old source peel indexed a 96-grid surface with 24-grid peel coordinates. The carried print, prepared source and target now share topology. Each column peels from its actual front point/tangent/outward normal; attached rows retain exact source vertices. The app's physical source frontier and detached transport are distinct from its visual free curl, which intentionally relaxes to .4. Existing app-clock phases now drive continuous final transport rather than a device-side curl===1 switch. The source geometry is prepared per pose, not globally remapped per spring tick. Tests include every attached row, the last vertex, source immutability and epsilon-continuous nonzero-offset tail transport.

One-pass mesh position evaluation plus adjacency normals removes four extra chart inversions per vertex. Base positions remain within Float32 quantization; film lift follows the new mesh normals and is not byte-identical to the earlier analytic-normal lift (C03 maximum changes .0466/.1247/.0836 model units at 0/45/90). Thus final visual/witness provenance must be refreshed. The grab solver now evaluates the identical local indexed adjacency-lifted triangles, verified within1e−6 in its fixture, rather than hiding this difference in an initial pointer offset. Raw optimized construction+audit samples are diagnostic Bun timings, not actual browser input latency.

The rejected radial atlas and unused rightStrip object methods were removed from production sticker-wrap.ts. Exact pre-cleanup source plus hash remains in early/obsolete-source. Reference section, front envelope, insert bridge and current collision/metric test dependencies remain.59 targeted sticker tests pass; device tsc and scoped eslint pass at this checkpoint. Native visible pickup/partial peel/return, actual input performance and maximum catalogue reach still remain required.

### Final front capture rejected; nonlinear inverse residual correction

The first final-matrix-1 captures passed buffer/build identity and native assertions, but reviewer/root rejected the front image: the ticket/ENCORE ribbon extended much farther across the display than accepted cage-1. This supersedes the earlier sampled visual assessment. Exact submitted C03 positions/UV/index match current source, so this is actual geometry, not a different art or stale draw. Adjacency film lift is far too small to explain it.

At material dx0/dy90 in C03 .027/.017/.35/0, the unified planar inverse adds depth120.899 versus its root Taylor depth63.640:57.260 extra inward units; arc86.089 versus63.640. Root Gram is near identity(xx1.0195,yy.9812). Thus the planar inverse's nonlinear corner residual, carried unchanged onto the front, dominates. Original before-source and numeric evidence are preserved in early/front-residual-1.

A direct smooth fade evaluated at uncorrected Q-depth failed immediately (70 opposed C03 corner triangles,169 at rear100/200). It is retained as rejected evidence and was never built for native. The bounded correction solves D=origin+Ldepth+w(D)(Qdepth−Ldepth), then uses arc=Larc+w(D)(Qarc−Larc), where w is1 on the flat cap and reaches0 at the front rim. The nonnegative convex depth residual makes the scalar derivative1−w'R positive; safeguarded Newton is capped at12. This retains root value/derivative, exact flat geometry and the same actual surface mapper, without world-position blending or placement selectors.

Twenty meshes and84 dense boundary/axis meshes are finite with no zero/opposed triangles. Flat error7.65e−6. Largest .01 center-step mesh changes are .2174 at Y250,.1999 at X139,.01001 at axes; these are bounded samples, not a global continuity certificate. Three current wrap tests103assertions, device types and scopedlint pass. Fresh rear/side/front native visual proof is required before interaction; device source frozen for that diagnostic capture.

### Cold off-rear inverse admission

Corrected front-residual-1-native six views were independently accepted (603 assertions/all provenance deltas0). C03 source position SHA3a8c7b7aaf6841b8ca383e9796facc786dbd78996dca5cf358d5d71d03fcbb2c matches the actual indexed draw, with unchanged UV/index. Offline witness setup first omitted forced world synchronization after manually copying a matrix with matrixAutoUpdate=false; that invalid fixture was corrected, not called a product defect.

A second, real defect remained after matching production timing: projectCenter is first called after64px, so warming it during partial peel was invalid evidence. Cold68px front input gave a Newton candidate139.8566/284.293 from156.09/266.616, outside the legal rear. The old helper returnednull immediately even though the desired final center existed. The corrected helper backtracks at most4trials per Newton step, accepting only a legal center with lower exact shown-UV residual. Six steps per seed remain bounded; one alternate frozen physical XY grab-depth plane seed can recover a cold branch. The seed is never accepted without exact UV refinement to the unchanged.5px tolerance. Failed attempts never publish a new warm start.

Four actual-matrix paths now make15/17/8/14 admitted calls with no nulls and meaningful final relocation; no calls occur during partial peel. Bun-only mean inverse costs .188/.081/.077/.070ms, maximum.809ms; this is not full browser pointer latency or geometry preparation cost. Three helper tests146assertions cover cold admission with captured matrices, valid reentry after an invalid pointer, and pose invalidation. Device types/scopedlint pass. New immutable build/matrix capture is required for final interaction; the app recorder is adding nonindexed drawArrays needed to correlate source rear/front shell triangles.

### Composited descendant pointer ray correction

The first eligible hidden-print witness still failed native shell orientation admission after positive side pickup, 32px partial peel, cancel and reload passed. The unchanged-build pointer trace in early/hidden-route-1 isolated the cause: the actual event target is STRONG inside the composited screen panel, with offset151.0838/8.6893 but client567.0681/147.8312 and canvas rect0/0/1280/900. No sticker propagation stop or capture occurred. Pinned Fiber9.7.0 web events use offsetX/Y, so the resulting NDC(-.764,.981) misses the intended shell ray(-.114,.671). This is a product event-coordinate defect, not grounds to relocate the witness.

DeviceCanvas now supplies a factory spreading Fiber's existing event manager and overriding only compute. It derives NDC from client coordinates minus the actual canvas bounding rectangle, using its CSS dimensions. Descendant offsets, scroll and axis-aligned CSS scaling therefore do not change the intended ray; out-of-canvas coordinates remain unclamped. Nonfinite/zero rectangles clear the ray and use Fiber's documented-in-source null camera sentinel to suppress stale intersections. Existing connection, dispatch, pointer capture and orientation eligibility remain intact.

Three focused tests/28 assertions, device types and scoped lint pass. Tests include the exact observed descendant offsets, translated/scrolled/scaled canvas, outside coordinates and invalid-to-valid recovery. Source reference: installed Fiber dist/events-156d8d12.esm.js createPointerEvents and intersect handleRaycast. Modern-web guidance was consulted for the existing composited DOM context; no dependency or rendering architecture changed. Fresh actual matrix/build correlation and unchanged hidden/positive gesture native runs remain required.

### Full-preview crash, direction correction and detached interaction reset

Canvas-ray native validates the coordinate fix, desktop side relocation/reload and both hidden shell flicks. Front carry then enters the route error boundary at607.0069846/157.5806812: 'Bend has no actual front support'. Exact full-mesh reproduction finds ten vertices at C03 center .314332653716106/.0205851851589971 reaching x−171.4868 across the opposite edge. The grabbed UV alone had remained valid; that never certified the whole preview. Evidence early/front-support-failure-1 preserves the failing geometry.

The first support-span direction candidate fixes that overflow but introduces source-connected front inverse folds at adjacent-corner direction activation/saturation. Small-step64/128 continuation and the rejected source are retained under early/footprint-support-1. A fixed material-scale softened direction removes those sharp direction transitions; actual surface support is unchanged. The pending implicit-root issue was separately corrected using exact flat/front constant-region roots, with Newton confined to the true bend. Current1620 sampled range cases have no throw/coarse orientation flag; max equation residual<1e−9, negative Taylor noise no worse than−1.94e−8. Current source-seed max displacement versus last build is.2694 model units, requiring fresh visual/buffer proof.

Boundary-like failures of the nonlinear inverse were not declared invalid placements: valid-sided continuation still stopped on legal proposed centers, so earlier boundary classification was withdrawn. Parent/reviewer instead approved removing the unnecessary nonlinear seated-UV constraint from detached carry. The current grab uses the actual picked triangle's UV-oriented orthonormal material frame and a frozen affine screen mapping. A singular-gain cap retains grazing painted pickup without runaway target motion; there is no5% pickup cutoff. The app independently retains landing0 while held and preserves release-only landing. Existing source validity and full rounded-center validation remain.

Old native gesture paths are retained as historical/negative diagnostics, not silently changed to match a new expected center. Under the new affine frame both old side paths stay valid (ending near .090/.212 and .094/.206), while the old front directions leave the legal destination domain. New positive front evidence must be derived from an explicit legal target through the frozen frame, use actual shown triangle/matrices/build identity, and check every resulting full mesh. Exact source, scripts and outputs are under early/footprint-support-1. Product helper tests replace obsolete inverse-branch tests with known material delta, actual four-camera frames, grazing bounded gain, pose invalidation and deterministic invalid/reentry. No native readiness or complete wrapping acceptance is claimed yet.

### Affine grab and prepared landing cue checkpoint

The frozen material-frame implementation passes 9 tests / 571 assertions, device types and scoped lint, including an actual 37-degree rotated, translated triangle basis. It replaces the obsolete new-pose UV inverse. Native evidence must now reconstruct the actual picked triangle and verify every resulting full target mesh; historical nonlinear expected centers are not an oracle.

`targetContour(placement)` borrows the already prepared PeelingPrint target surface and original alpha/wear field. A one-resource slot checks every placement field against both the current preview and prepared resource; stale/unprepared/released targets return null. Projection uses the same shell-visible alpha span splitter, with content transform refreshed per query. Layout publication and committed preview notification expose readiness without another clock or geometry generation. The wrapper owns no GPU material, geometry or texture; token-specific cleanup cannot clear a later resource. The target slot and existing hidden-middle regression pass 2 tests / 725 assertions. App presentation and fresh native verification remain pending.

### Actual hit-to-grab transform seam

`affine-target-interaction-1` passed desktop side partial/cancel and hidden shell flick, but every detached destination remained invalid. The source cause was the raycast proxy: `intersectStickerPrint` copied only `matrixWorld` to an unparented probe, then affine capture called `updateWorldMatrix`, resetting it to identity. This corrupted the material basis without invalidating the captured camera/content. The exact current-camera reproduction in `early/affine-probe-transform-1` returns null for all18 desktop-side /10 mobile-side admitted samples; it preserves the rejected helper source.

The narrow fix returns the real rendered mesh as intersection.object, retaining the probe's point/face/UV. A new regression exercises actual alpha-tested `intersectStickerPrint` into capture with disabled real mesh raycast and transformed parent/child: it failed with wrong centers before the fix and passes afterward. Combined hit/grab/wrap tests10/578, device typecheck and scoped lint pass. No mapper, destination policy or gesture coordinates changed. Fresh native proof must exercise this actual hit-to-capture seam, not just an ideal parented raycast.

### Grabbed peel / free carry diagnostic checkpoint

`affine-hit-interaction-1` subsequently passed the entire desktop-side functional gesture, but actual images rejected the old wrapped peel's stretched free banner and unrelated top-row peel. The target outline also used a stale identity world matrix: the borrowed manual-matrix target now marks `matrixWorldNeedsUpdate` on every copy, with a two-transform projection regression. Corrected side outline coordinates match the final print; front target is actually hidden.

The current rendered prototype uses an exact-source transient captured UV/point/tangent and raw CSS pull from the app. A frozen nearest material rectangle edge defines an advancing half-plane; outside points remain exact, while a smooth displacement field makes the actual picked material follow the pointer immediately. Canonical detachment uses one original-UV camera-facing cylindrical sheet and proper-frame residual interpolation. This supersedes the old universal top-row attachment witness. It is not accepted shipping geometry.

`grabbed-peel-phase-1` captures actual side/front16/32/51.2/57.6/64 px. Side early response improves, but shell-colored splits are actual inward motion, and the 57.6 transition compresses painted triangles in 3D. `grabbed-peel-free-phase-2` adds80/100/164 px; the existing relaxed curl .4 endpoint is legible, so there is no permanent ink-facing basis defect. Canonical peak curl1 explains backing at64 but does not excuse transition compression.

All diagnostics below are under `early/free-carry-1`: `phase-classification.ts/.json` retain source-to-shown segment witnesses and actual area/facing. `contact-prototype.ts/.json` test a fixed three-contact sweep/slide against the existing shell BVH: side contact cost83–109 ms, collapsed painted areas around4e-7, unresolved contacts, and front picked-point errors up to33.55 px. Full triangle audit remains nonzero. This helper is NOT wired into the renderer. `curl-cap-prototype.ts/.json` independently caps canonical curl at.4 and still yields57.6 areas .006 side/.003016 front; no further curl tuning is justified.

`crease-feasibility.ts/.json` starts the next reviewer-authorized common-frame crease family. The current full-span crease has a coherent sampled outward-normal cone, but its required grabbed chord points inward against every sampled crease normal (maximum D·n: side32 −6.923, side57.6 −12.649, front32 −35.147, front57.6 −40.949). Side32 additionally requires40.253 chord from38.382 material length. Thus this particular crease/anchor choice cannot generate the proposed outward circular extrusion. No extrusion or collar was fabricated; localization/crease relocation and visible grabbed-ray depth require a new concrete bounded disposition.

### Crease feasibility continuation, then user-directed editor performance priority

Allowing depth along the captured screen ray removes the fixed-depth assumption above. `crease-local-ray-feasibility` and `crease-strip-side32` produce one connected localized side32 prototype: exact pickup within0.000076 px, core3,936 triangles with zero shell contacts and stretches .862–1.057. All159 contacts arise in artificial end collars, which absorb92.75 units of mismatch. The proposed next construction returns the crease to the real sheet edge at both ends instead of blending those end residuals. It is paused, not accepted or integrated;57.6 remains unresolved.

The user rejected carry silhouettes and reported editor rotation/wear lag. Device `targetContour`, publication/readiness slot and dead helper/tests are removed; equipped editor contours remain. Removed helper source is retained only as non-executable `.snapshot` evidence in `early/rejected-target-outline`.

`early/editor-performance-1` separates causes. Wear updates two material uniforms and does not remesh equipped geometry. C03 rotation preparation at96 segments is median2.86 ms rear /5.55 ms corner in isolated Bun. Old dense contour visibility costs27–30 ms warm, about3,000 rays/query. Transform hoisting alone does not materially improve it. Instrumented20-query traversal sees7.61 million edge-fallback entries and11.88 million edge checks. Fixed collider-owned scratch/scalar edge math plus finite-ray entry-distance pruning reduces complete exact-output queries to about7–8 ms. A one-result WeakMap cache keys actual geometry/buffers, alpha byte/field, camera/viewport, shell identity/revision AND content transform; identical queries issue zero rays and take roughly.003–.09 ms. No topology/alpha/visibility fidelity was reduced.

Build-only centroid/bounds caches preserve the exact stable partitions: independent cold processes produce identical ordered tree SHA256 `8c98ea15aeb5038a561dfe3eae57aa7234583e9c3e7804bdb2641cffa882b59c`,203,743 triangles/65,535 nodes. Build time falls298.209→219.652 ms; this remains significant and is not claimed solved. Existing handle-publication preparation is retained rather than introducing a new cold first-pointer build. Build scratch is released. Targeted collision/visibility/contour tests14/1473, device types and scoped lint pass. Actual editor-native measurement is the next gate; no browser responsiveness claim yet.
