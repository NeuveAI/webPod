# Revision diary

Read revisions.md, original scope/decisions/handover and all three new defect PNGs. Reused original photo target reading and skills; loaded Jotai and motion instructions for the direct-manipulation checks. Source inspection confirms marquee onset is currently 1.8s + 18% of 12s = 3.96s. Scrub fill transitions for 120ms while the marker/time update immediately; the marker also measures the outer track while fill measures its 1px inset. The scrollbar has a repeating white/gray stripe and a double beveled edge. Current revision source baseline is HEAD e1a6995. No production edits made before this record.

Verification will use the existing actual-product MusicKit seam and installed Chrome CanvasDrawElement path, with explicitly delayed seek acknowledgement. Focused owner screenshots are baseline evidence; measured before/after browser checks will verify timing and motion/selection geometry.

Implemented all five revisions. Focused pre-change browser checks failed exactly as predicted: the overflowing title still had transform 0 at 2.8s, and the scrub fill had a 0.12s transition. The same checks pass after the change. Native progress/volume and scrollbar crops in both themes were visually inspected; they show the shallow channel and plain narrow neutral rail.

Final checks: 422 scoped panel/state/composite tests pass; six actual-product direct-manipulation browser tests pass; two existing wheel/DynamicType browser tests pass; typecheck 11/11, lint and build pass. Build retains the pre-existing chunk-size advisory. Logs live in evidence/revisions. The new product suite records every optimistic scrub sample (fill endpoint, marker center, full-precision percent, elapsed/remaining, ARIA, DOM position and intentionally stale provider clock), then exercises delayed commit, stale failure, current failure, success and cancellation. CSS animation timeline is sampled deterministically at 2.8s in both list and Now Playing, including reduced-motion checks. Native mouse double/triple-click and drag tests cover panel text and the real outer device help label; the actual search input still selects and edits normally. Scroll bounds are checked at top/middle/bottom for 200 rows in both themes; short root list absence and existing Dynamic Type checks cover the other geometry cases.

One independent browser run overlapped with the implementer's snapshot server, so its Vite crash and the corresponding port-in-use attempt were discarded. Agents coordinated subsequent runs; the final clean logs are authoritative. Revisions remain uncommitted for the lead/user; the prior committed baseline is preserved.
