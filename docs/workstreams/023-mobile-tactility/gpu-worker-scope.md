# UI-priority GPU and worker architecture

Status: preparation and material implementations in independent review; worker host integration remains pending. This is the full active goal, not complete when an audit or isolated optimization lands.

Current checkpoint: the full native host and resource lifetime corrections are independently reviewed through f89be7f. Native resize, packet/liner, peel/drop, editing/return and player controls have passed bounded existing-route checks. Preferred-backend activation and final integrated acceptance remain pending.

## Correctness and decisions

Primary source: user authorizes supervised implementation that prioritizes UI/input on the main thread, uses GPU/WebGPU for suitable graphics/computation, and workers for remaining heavy work. Preserve device geometry, shader appearance, effects, screen fidelity, sticker behavior, audio responsiveness, WebMCP and physical resolution. Prefer WebGPU when supported and compatible; retain equivalent GPU/worker fallbacks. No quality reduction or replacement of a working screen by a placeholder. The user explicitly requested Neuve tracking, superseding the older repository statement that no board exists.

Current architecture branch: codex/gpu-worker-rendering, branched from local 1e11d02 with in-progress changes preserved. Remote codex/device-cpu-profile remains at 4cc7f4a for the bounded open draft PR #5. Its unresolved context loss remains a validation obligation. Do not merge or deploy this architecture without final evidence. Preserve the unrelated .gitignore edit. No cert access, direct design.pen access, force pushes, useState, or package tools other than bun/bunx.

## Research dispatch and dependencies

- WEBPOD-CPU-004: map GPU/worker renderer and DOM-screen transport compatibility. Read packages/device/src/DeviceCanvas.tsx, packages/composite/src/{pixel-source,html-in-canvas,coordinator}.ts and installed Three/Fiber sources. Produce gpu-renderer-audit.md with exact canonical exports, custom shader migration list, screen transfer options, supported fallback architecture, and concrete implementation ownership/dependencies. No application edits. No dependencies; ready. Proof: installed-source renderer and screen compatibility map.
- WEBPOD-CPU-005: map and remove the remaining heavy preparation/main-loop ownership barriers through a concrete implementation proposal. Read immutable-shell preparation, collision/carry preparation, device orientation/input and animation code. Produce gpu-worker-audit.md with call paths, lifecycle/timeout behavior, synchronous fallback hazards and non-overlapping implementation slices. No application edits during audit. No dependencies; ready. Proof: main-thread work and lifecycle inventory.
- Lead: establish current browser/devserver health and profile access, maintain scope, ticket state and implementation contract. Browser only existing user Chrome localhost tab, iPhone emulation and 6x CPU. Each trace bounded with guaranteed stop; acknowledge any save dialogs. Never substitute an isolated browser and call it the user's session.
- Implementation follows these audits with exact ownership and verification added here; separate reviewer validates full final range. Audits do not narrow the goal to easier changes.

## Verification and completion

Required evidence: explicit execution-placement inventory for device render loop, deformation/effects, preparation/collision and DOM integration; deterministic output parity and lifecycle/error/cancellation/backpressure checks; independent source review; bun run typecheck, repository lint/build and relevant existing device/composite tests; actual existing-route visual and interaction checks for entrance, rotation, sticker peel/carry/stick, clickwheel/navigation and playback. Compare bounded current-Chrome 6x CPU traces under the same conditions, distinguishing DEV overhead and production results. Record main-thread long tasks and frame/input results without inferring speedup from passing tests. Verify GPU preference and fallback selection, teardown/context loss, resource release and no redundant concurrent jobs. Any remaining main-thread heavy work needs specific evidence of an unavoidable browser constraint, not a convenience exemption.

Canonical API/types must come from installed dependencies and official primary references, grounded first in /Users/vinicius/code/.better-coding-agents/resources/. No invented renderer compatibility casts. Public/lifecycle adapters require invariant documentation. Decisions may autonomously choose reversible internal designs; log architecture tradeoffs and limitations. Changes to visual quality or product behavior are outside authorization.

## Artifacts and review

All paths relative to docs/workstreams/023-mobile-tactility: gpu-renderer-audit.md, gpu-worker-audit.md, gpu-decisions.md, gpu-implementation.md, gpu-review.md, evidence/gpu-worker/. Each agent maintains its own named audit/diary, records source-linked Kanban comments and evidence, and does not commit or touch another lane. Lead stages coherent implementation commits after review. Review covers concurrency/resource ownership, graphics/screen parity, responsiveness and fallback behavior. Full goal remains active until all requested execution placement and end-to-end gates are verified.

## Implementation dispatch A — immutable preparation (Ready)

WEBPOD-CPU-006 implements slice A in gpu-worker-audit.md. Owning engineer may edit packages/device/src immutable-shell/preparation/worker modules, geometry and texture factory modules, Device.tsx, DeviceHardware.tsx, and new typed preparation/broker modules. Renderer/Canvas, composite screen adapters, orientation and sticker interaction are excluded. No concurrent lane may edit those owned files. Existing immutable-shell protocol/types and installed Three BufferGeometry/BufferAttribute/DataTexture are canonical; extend typed data boundaries instead of sending Three instances.

Ownership decision corrected by independent review: do not add mesh dispose={null} or AxialSelectControl forwarding to protect prop-owned geometry. Installed Fiber does not recursively dispose geometry passed as a mesh prop, while the flag suppresses disposal of declarative material children. Remove newly introduced flags, retain explicit cache geometry ownership and prove actual Fiber material teardown plus multi-device geometry lifetime. The existing screen-aperture source-ownership assertion may follow its extracted factory/generator.

Required behavior: move deterministic remaining device geometry and dense texture generation from React render into bounded shared worker preparation, retaining exact output and immutable keyed reuse; eliminate the monolithic shell failure timer fallback in favor of genuinely yielding exact stages. No arbitrary worker timeout may start a duplicate long main-thread build. Cancellation/queue limits/idle cleanup and transferable buffer ownership must hold under StrictMode and multiple devices. If worker font raster cannot preserve exact glyph pixels, split and document the small browser-only raster stage; this does not exempt dense procedural texel generation. Report any required ownership expansion before editing it.

Proof: compare prior versus new full geometry attributes/groups and texel output with retained deterministic checks, run existing relevant device tests, bun run typecheck and scoped bunx --bun eslint on changed modules; lead runs full build and live cold-entry/handling checks. Document reproducible commands and outputs in gpu-preparation-implementation.md and evidence/gpu-worker/preparation/. Public worker/broker lifecycle APIs require TSDoc; no any/unchecked compatibility casts. No commits until independent review; keep preparation data protocol, async adoption, and verification stageable as coherent changes. No dependency on renderer migration; its current GL scene consumes the same exact prepared data. This slice is required progress, not completion of the broader goal.

## Implementation dispatch B — native WebGPU materials (Ready)

WEBPOD-CPU-008 owns the six custom shader categories in gpu-renderer-audit.md and the required typed backend/material readiness boundary. Implement exact TSL/node equivalents for physical subsurface and glass light cards, locked/seated/earned sticker appearance, wear/damage, GPU paper curl and LCD color transfer. Preserve authored light ordering, geometry, texture filtering, color spaces, alpha/depth behavior, full deformation topology and existing fallback shaders. Use installed three/webgpu and three/tsl canonical APIs. Provide backend-aware PMREM and compile/readiness selection without pretending RootState.gl is WebGPURenderer via a cast. A supported WebGPU adapter alone must not enable incomplete material variants.

Own physical-materials.ts, StickerSurface.tsx, sticker-wear.ts, sticker-paper-gpu.ts, sticker program/support readiness and new node-material modules; coordinate composite html-in-canvas/StudioEnvironment changes through lead before editing. Do not edit CPU-006 owned Device.tsx, DeviceHardware.tsx, geometry/texture factories or preparation modules. New adapters may be developed independently; final live backend selection and worker renderer integration follow complete material and associated-canvas transport parity. No dependency for material implementation; no silent production enablement of partial quality. Diary gpu-materials-implementation.md, evidence/gpu-worker/materials/. Required checks: installed node compilation, exact mathematical/reference parity, existing relevant device tests, device/composite/web typecheck and scoped eslint, followed by actual route front/edge/rear/glass/sticker/wear/paper/LCD parity before acceptance. Independent reviewer owns final approval.

Approved new modules composite/lcd-material-nodes.ts and device/render-backend-services.ts for LCD nodes and explicit backend PMREM/compile lifecycle. No existing StudioEnvironment/html-in-canvas ownership expansion yet. Async cancellation must dispose late backend resources and reject old generation readiness; source compilation is not visual parity.

Renderer-host follow-on remains mandatory: associated presentation canvas must transfer before context creation; bound native snapshots by count/bytes, perform direct ElementImage upload with canonical backend allocation and explicit cleanup, preserve main input/media authority, ordered commands and displayed-pose hit alignment. The current browser contract probe succeeded (gpu-browser-evidence.md); a detached staging canvas is forbidden by observed lineage checks. Host integration waits for stable preparation/material interfaces, not an exemption from the goal.

## Remaining queue and completion ledger

- CPU-009 worker renderer/native screen host depends on CPU-006 and CPU-008. Autonomous animation must not await a main-thread acknowledgement every frame. Resource/resize adoption and a bounded latest projection-notification channel use separate credits. Current Chrome152 has no updateElementGeometry event path; validate resumed input against pose-stamped projection state using the existing renderer as baseline, not an invented physical-presentation API.
- CPU-010 packed collision resources depends on CPU-006; retains audit B's exact provenance/side/alpha query contract and removes avoidable tree reconstruction and repeated assembly walks.
- CPU-011 sticker transactional work depends on CPU-006; retains audit C's exact geometry, final-sample fitting, damage and persistence/cancellation requirements.
- CPU-012 pose/media coordination depends on CPU-009; implements audit D without delaying media activation or flooding React with immutable scene rebuilds.

These source-linked Neuve tickets are queued, not implementation-ready until their exact file ownership and commands are added. No parent or full-goal completion is implied by their existence. Final acceptance requires all four plus preparation/material parity and the full interaction/browser validation matrix above.

Dependency clarification: CPU-008's source/interface milestone requires independent authored-formula review, real native GPU compilation and numerical checks, and lifecycle proof. The actual full-device visual/interaction matrix is the CPU-009 integration acceptance gate, and remains mandatory before native backend preference is activated. This avoids making host implementation depend on visual evidence that requires that host to exist; it does not waive or narrow any full-goal requirement. Source milestone acceptance must explicitly report the integrated appearance gate as pending.

## Implementation dispatch C — packed collision resources (Ready)

CPU-010 parent preparation source/interface milestone is complete at 88ecc44, with full integrated performance acceptance still open. Own packages/device/src/sticker-collision.ts, sticker-collision-cooperative.ts, sticker-collision-preparation.ts, sticker-collision-worker.ts, sticker-visibility.ts, shell-picking.ts, shell-picking-index.ts, shell-picking-preparation.ts, shell-picking-worker.ts and new typed packed-query modules. Adapt collision resource transport callers in sticker-carry-{preparation,worker,computation}.ts only as needed; CPU-011 must not edit those files concurrently. Existing collision/picking tests may be adapted only for the representation contract while retaining behavioral assertions. DeviceCanvas, Device/DeviceHardware, GPU material modules, composite, and production route ownership are excluded. Request explicit ownership expansion before other edits.

Implement audit slice B with exact packed geometry/BVH query representation, preserving face/material/side/UV/alpha/provenance/closest-hit semantics and previous immutable snapshot reuse. Remove dense main-thread tree-object restoration and avoid redundant structured cloning/reassembly. Preserve immediate exact admission through immutable query views; do not replace correctness with approximate ray bounds. Explicit assembly revisions must account for local control geometry/visibility/transform changes while whole-device rotation reuses local geometry. Inventory actual mutation owners before choosing publication; report required call-site expansion. Do not assume SharedArrayBuffer availability or detach displayed cache buffers. Brokers bound queue/bytes, use execution-start deadlines, cancel/retire generations, and release idle resources. Preserve equivalent worker/cooperative fallback.

Diary gpu-collision-implementation.md, evidence/gpu-worker/collision/. Retain reproducible pinned-baseline query/contact/provenance/alpha comparisons, lifecycle/failure/ownership checks and actual allocation/transfer/work-count evidence; no performance claim from tests. Run existing exact collision/visibility/picking/contact suites, device/web typecheck and scoped lint including proof scripts. Independent reviewer required before commit, lead owns current-Chrome 6x interaction traces after combined integration. Canonical installed sources and CPU006 typed data are authoritative; no any, unchecked compatibility casts, or hidden fallback retries. Public and lifecycle APIs require invariant docs. Record decisions, source-linked board comments and proof handoffs; no commits by engineer.

CPU-010 ownership expansion from concrete mutation inventory: control-physics.ts may publish Select/wheel local-transform revisions; Device.tsx may publish layout-effect assembly revision for prepared/material/finish changes; StickerPackScene may remove the redundant recursive world update immediately before visibility inspection, retaining correct root/ancestor transforms. These are narrow collision invalidation changes, not pose or renderer redesign. Explicitly registered assemblies use mutation signals and child topology events; external/unregistered assemblies retain defensive correctness. CPU-009 must coordinate before editing these three files; CPU-012 control changes wait for CPU-010 freeze. Preserve same-turn public state and correct local physics invalidation.

## Implementation dispatch D — worker renderer host (Ready with shared-file locks)

CPU-009 consumes accepted preparation88ecc44 and material67478ad contracts. Its exact ownership, assembly/native-upload/projection protocol, cleanup, fallback and verification are specified in gpu-renderer-host-plan.md, now an authoritative implementation dispatch alongside this scope. Implement initially in new worker/bridge/scene modules and free Canvas/composite/production integration files. CPU-010 retains its narrow Device.tsx/control-physics.ts/StickerPackScene.tsx and collision/carry ownership until explicitly released; do not edit those concurrently. After that freeze, lead may release the shared assembly and non-React preparation lease extraction listed in the plan. No duplicated approximate device or partial renderer may be activated.

Independent architecture refinements are required in the implementation: every final semantic command carries its exact pose/sample barrier and ordering identity, even if transient pose messages coalesce; asynchronous native capture holds a bounded credit stamped with epoch/raster/visibility/sequence until resolved, including resize/hide/dispose; the same Panel portal owner, Jotai and media/input authority survive native-to-GL canvas generation replacement rather than remounting under the existing tier guard. Projection/resource notification credit must never gate every animation frame. Main query and DOM projection adopt coherent revisions, but actual display alignment still requires current-Chrome rapid-motion/resumed-input proof.

Owning engineer records gpu-renderer-host-implementation.md and evidence/gpu-worker/renderer-host/, source-linked CPU009 progress and retained lifecycle/protocol proofs. Use existing tests, affected types/lint and full build. Lead coordinates all actual browser probes and traces with source freezes. Report readiness failures explicitly; initial capability support or shader compilation cannot activate an incomplete scene. Native preference remains gated on the full existing-route visual/interaction/fallback matrix; the task must progress through that gate rather than leaving an unused alternative renderer as its final deliverable.

## Implementation dispatch E — sticker compute transactions (Ready; staged integration)

CPU-011 uses accepted CPU006 data/resource contract. Own pure sticker-surface.ts, sticker-drop-fit.ts, sticker-contour.ts, sticker-projected-bounds.ts, sticker-wear.ts, StickerSurface.tsx and new typed sticker-transaction computation/worker/broker modules. These files are reserved from CPU009's later shared-scene extraction until freeze; coordinate the prepared equipped-sticker contract with that engineer. CPU010 retains collision/visibility/shell-picking and carry transport files; CPU009 retains Canvas/composite/production integration. Do not edit StickerPackScene, carry protocols, production callbacks or shared state/persistence call sites until lead explicitly releases narrow integration ownership. First implement exact prepared-data and transaction boundaries in the free owned files; report the concrete caller handoff required rather than stop at unused helpers.

Implement audit C: exact saved-placement geometry/damage preparation and release fitting in bounded workers, stable cache keys including artwork/placement/form revisions, explicit private transfer ownership and nonduplicated jobs. Preserve source-target wear identity, full topology/contact constraints, final pointer sample and cancellation/supersession. Retain the current carried appearance while fitting; only the current transaction may save/place. Immediate DOM/media/input stays main. Main may project a bounded prepared contour/query representation; no dense vertex walk per pointer is exempt without measured evidence. CPU/GPU numerical equivalence and alpha/material geometry semantics must hold; do not claim GPU compute by adding synchronous full-buffer readback. Existing exact workers remain a valid fallback where GPU computation is unsuitable.

Diary gpu-sticker-transactions-implementation.md, evidence/gpu-worker/sticker-transactions/. Retain pinned-baseline exact geometry/damage/fit/contour output and failure/lifecycle/final-sample ownership evidence. Run existing sticker surface/drop-fit/contour/bounds/wear suites, affected device/web types and scoped lint including proof files; lead runs combined browser verification. No new unit tests unless explicitly justified; prefer retained deterministic reference experiments plus existing tests. No commits; a different engineer must independently review this source. Author must not approve their own implementation even if they reviewed an earlier lane.

CPU-011 concrete ownership expansion: sticker-alpha.ts contains the dense prepareStickerSurfaceDamage/prepareStickerDamage fields and is included, along with a pure extracted damage helper if needed. Native artwork raster/alpha extraction may remain a bounded main browser operation; pure field generation belongs to the compute worker. Preserve all alpha/damage pixel semantics and immutable resource identity. Shared StickerPrint/Scene effect/callback integration remains a coordinated handoff rather than concurrent edits.

CPU-009 dependency ownership: official @webgpu/types may be added as a device development/type dependency because no canonical WebGPU declarations are currently installed. Owning engineer exclusively edits packages/device/package.json and bun.lock for this purpose; it must not introduce a runtime payload or unrelated dependency upgrades. Bun/bunx remain the only package tools.

CPU-010 final mutation owner: screen-mesh.ts may import and call markStickerAssemblyChanged(mesh) when setMaterial replaces the LCD material. CPU-009 pauses edits to this file until the tiny collision notification change freezes. This preserves registered query invalidation for material changes outside Device props.

Dependency correction without shrinking acceptance: CPU-015 independently accepts the stable renderer motion interface. CPU-016 implements the full gpu-pose-coordination-plan.md after that source contract and explicit shared-file handoffs. CPU-012 remains the final integration/acceptance join depending on CPU-009 and CPU-016. This removes the circular implementation wait while keeping the original full visual/motion/media gate intact. CPU-009 need not be declared fully accepted merely to begin motion work. No CPU016 implementation dispatch until exact ownership is added and CPU015 passes.

Shared integration reservation: CPU-009 confirms production-device-view.tsx and StickerPackScene.tsx remain unedited. After CPU-010 freezes its narrow StickerPackScene mutation change, CPU-011 may own those two files solely for the async resolveDrop/final-save transaction and adopted placement/resource correctness. CPU-009 waits for that short integration freeze before shared scene extraction. CPU-016's production memo/subscription changes likewise wait for explicit handoff; its free pure motion modules may proceed earlier after protocol review.

CPU-009 early shared-preparation release: immutable-shell-preparation.ts, immutable-shell-worker.ts and device-preparation-data.ts are now available for the non-React private lease/MessagePort extension; CPU010 does not write them. Extend the existing preparation authority and accounting rather than introduce a second independent producer/cache/fallback budget. Both React fallback and native host must share that resource authority. Necessary private query/render buffer copies are distinct from recomputing identical geometry; make ownership, cache bytes and transfer paths explicit. Device/StickerPackScene/control/screen locks remain unchanged until collision review and handoff.

Shared-file handoff after CPU010 independent source approval and commit ebebb32: CPU009 now owns Device.tsx/DeviceHardware.tsx/screen-mesh.ts for exact shared-scene extraction, retaining collision mutation notifications. CPU011 now owns StickerPackScene.tsx and production-device-view.tsx for async final-drop/save and adopted placement/resource integration, then releases them to host extraction after a coherent freeze. control-physics.ts is reserved for the motion implementation after protocol acceptance. No concurrent edits to these shared files.

CPU011 companion integration ownership: packages/device/src/sticker-contract.ts and apps/web/src/sticker-collection.tsx may adapt resolveDrop's async result and propagate transaction AbortSignal/current-generation checks. The collection must await exact final fitting inside its existing guarded async transaction before saving, rather than perform a synchronous fit before the guard. Preserve cancellation, final sample and visible carried state; no unrelated collection behavior changes.

## Implementation dispatch F — autonomous motion (Ready)

CPU016 consumes independently accepted protocol f3d738f and the full gpu-pose-coordination-plan.md. Own apps/web/src/device-preview-orientation.ts, device-orientation-motion.ts, device-reveal.ts, device-reveal-state.ts, narrow device-page.tsx subscription/reveal integration; packages/device/src/control-physics.ts and its binding hooks; new pure device motion runtime and typed main authority adapter modules. CPU009 owns renderer/scene/protocol/Canvas/composite and calls the shared worker motion runtime; coordinate its minimal runtime interface directly, never edit their active files. CPU011 retains production-device-view.tsx and sticker collection/contracts through its integration freeze. Production memo/subscription edits wait for explicit release. CSS ownership expansion for coordinated reveal requires a concrete proposal before edits.

Preserve every authored curve/clamp/control travel and final-sample/reentrant public-state/media-activation rule in the pose plan. Share one pure timeline implementation between worker and equivalent fallback, avoid a second perpetual motion worker, publish bounded progress independently from frame submission, and never wait for main notification credit to animate. Store/tool authority remains synchronous main Jotai; stale replies cannot overwrite newer input or settle another command. Keep physical projection/query adoption coherent with submitted visual state. Moving only yaw while canvas translation/glow remain independent is not complete reveal integration.

Diary gpu-motion-implementation.md, evidence/gpu-worker/motion/. Retain pinned-reference trajectory/clock/settlement/control-matrix, interruption/reentrancy/hidden/two-owner and provider-before-ack evidence; run existing motion/control/production/composite suites, affected types and scoped lint. No commits or browser actions by engineer. A different reviewer must approve source; lead validates complete current-route 6x motion/playback/input/visual behavior. Implementation is not complete with an unused runtime; coordinate live host and main callers through the full gate.

CPU016 ownership clarification: exact orientation equations and reveal curve may move to packages/device/src/device-orientation-motion.ts and device-reveal-motion.ts with the app re-exporting shared logic. Existing source-text ownership assertions may follow those extractions while retaining behavioral requirements. Main preview authority becomes scoped Jotai; no second product state owner. The working host adapter is per-device read/sendCommand/subscribe; its implementation still needs independent lifecycle and integrated visual proof.

CPU011 shared Scene/production async fitting handoff is coherent and released to CPU009. Preserve async resolveDrop AbortSignal, exact final camera/world/sample snapshot, adopted geometry+placement revision, and guarded persistence. CPU011 retains collection/contracts/pure preparation and remaining contour/bounds ownership until review. Its source is not yet independently accepted.

Protocol refinement after f3d738f: control-release rest state uses explicit original position/quaternion/scale tuples instead of decomposing restMatrix; this avoids numerical perturbation and must be included in independent motion/host review. CPU009 static shared assembly may adapt existing source-location assertions to the exact extracted implementation, retaining all behavioral and physical geometry requirements. No assertion removal merely to make tests pass.

Reveal presentation decision from concrete DOM ownership: retain one minimal main logical clock for the authored 32ms-clamped entrance while the existing canvas CSS translation and portal glow require DOM updates. Publish that same logical progress/pose to the worker; do not run an independent reveal clock there. This is the permitted browser/DOM coordination minimum, not a claim of autonomous entrance. The reveal tick must not rebuild React scene descriptions or geometry; worker rendering/submission and post-entry orientation/control animation remain off main. Preserve all original curves, glow/motes and pixels. Final 6x entrance timing is still required: if jank remains after heavy work moves, investigate coordinated compositor/GPU presentation further before acceptance. This decision does not exempt other heavy computation or waive responsiveness requirements.

Existing-route integration override: CPU009 may validate the complete renderer on the canonical /webpod route with renderBackend=worker or webgl; CPU009 owns route search validation and CPU016 owns device-page propagation. Default stays GL until full acceptance, after which GPU preference must be activated as scoped. This is a staged integration control, not a separate proof route or a final unused renderer. CPU011 may narrowly adopt prepared exact bounds into carry preparation output after coordinating protocol/computation file locks with CPU009.

CPU016 exact native input ownership expansion: extract the existing ClickWheelInputSurface native-event controller into a shared module consumed by both Fiber GL and the worker host main query scene. CPU016 owns click-wheel-input.tsx and the extracted controller; CPU009 supplies canvas, exact raycast/query admission, CompositeArcHandlers and the same ControlPhysicsController. Preserve pointer capture, exact arc/cardinal/Select math, final samples, cancellation, and same-turn media activation. No duplicate approximate input implementation; require existing input checks and independent review.

CPU009 observed cancellation recovery decision: current Chrome first activation loses the canonical producer after a pending renderer lease cancellation. Investigate and fix initiating host lifecycle churn while preserving retirement for genuine cancellation. A serialized reseed of the SAME preparation authority from retained exact immutable query buffers is permitted if required: copying, not recomputing geometry; account canonical/pending/private bytes, preserve mounted arrays, prohibit parallel producers and release on every failure. Requires independent review and explicit cancelled-old/new-lease lifecycle proof before acceptance. This refines the earlier reject-after-loss behavior to support required native recovery without duplicate computation.

Independent review queue: CPU018 accepts frozen CPU016 authored motion/public authority/shared native input source; CPU019 accepts frozen CPU011 exact sticker preparation/resource/persistence source. Neither depends on implementation ticket completion; each unlocks only on author freeze and separate reviewer. Cross-review is permitted because each reviewer did not author the other slice. Full CPU009/012 native appearance, recovery and6×interaction gates remain mandatory. CPU011 also owns the narrow idempotent reportStickerArtworkReady update in sticker-interaction.ts: preserve existing ids identity if already ready, avoiding duplicate shared updates without suppressing meaningful notifications.

## Implementation dispatch G — complete sticker pack assembly

CPU020 is ready after CPU011 independent source approval and CPU009 explicit Scene freeze/handoff. No dependency on completed native renderer acceptance; this implementation enables that required gate. Own StickerPackScene.tsx, sticker-sleeve.ts, sticker-paper-preparation.ts and its exact worker/data/helpers; new sticker-pack-recipe.ts, sticker-pack-resources.ts, sticker-pack-graph.ts. Host owns native worker/host/query/input and equipped/carry integration. Do not edit StickerSurface, transaction broker, motion, Composite or renderer host. Before editing Scene coordinate the already-reviewed CPU011 staging patch with lead/host; preserve async drop, adopted geometry/wear, epochs and per-carry orientation updates.

Extract exact pack hierarchy, sleeve/liner/neighbor transforms and material parameters into one data recipe shared by GL and worker. Prepare and privately lease exact sleeve, parked print24-segment and GPU-paper rest geometry through existing preparation authority, never a second independent producer/cache. Expose exact leaf slots with artId,width,bow,appearance,wear, local/world transform and geometry resource identity for host print material adoption. Use accepted paper TSL and exact stock/back/edge/sleeve material contracts; preserve original segment counts, curves, normals, shadows and all effects. Keep main reframe/projection/carry callback behavior unchanged and return those integration seams to host after freeze.

Pin the starting commit and hash/archive the handed-off uncommitted Scene before extraction so parity comparisons cannot drift with HEAD. Retain exact geometry/material/transform and private transfer lifecycle comparisons under evidence/gpu-worker/pack; use existing pack/paper/sleeve/carry tests, affected device/web types and scoped source/proof lint. No new unit tests or proof-only UI route; existing-route browser validation remains lead-owned. Diary gpu-pack-implementation.md with decision/ownership/budget/source limitations. Independent reviewer required before source commit; no commits by engineer. Full native pack/carry/equipped visuals, interactions, failure recovery and6×performance remain mandatory, not a recipe-only final deliverable.

## Implementation dispatch H — cancelled preparation lease recovery

CPU022 is independent of complete host activation and starts after CPU009 releases immutable-shell-preparation.ts, immutable-shell-worker.ts and device-preparation-data.ts. The handed-off working diff is archived at /tmp/webpod-preparation-recovery-handoff.patch; copy that baseline and hashes into retained recovery evidence before editing. Preserve acquirePreparedDeviceForRenderer(form,port,signal) and usePreparedImmutableShells. This slice implements the already-recorded same-authority reseeding decision, not a new producer or geometry algorithm.

After genuine private-copy cancellation retires the old producer, recover by serialized adoption/copy of retained exact immutable query buffers into the same preparation authority. Preserve mounted buffers and canonical/main/pending/private byte accounting. No parallel producer, duplicate geometry computation, detached live main arrays, unbounded queue or stale worker/port result. Ordinary synchronous effect replay remains handled by host microtask admission, outside this ownership. Existing worker failure fallback rules remain; all ports/resources must close on failure/disposal.

Correctness proof is exact buffer preservation and resource lifecycle under cancel→retry, late old result, concurrent requests, cancellation during reseed, worker failure and final release. Retain deterministic experiments using the actual existing producer/private port where available, existing preparation tests, affected device/composite types and scoped source/proof lint. No new unit tests or browser/proof route. Diary gpu-preparation-recovery-implementation.md, evidence/gpu-worker/preparation-recovery/. Independent reviewer required before committing. Host caller integration and complete native fallback are separate final obligations; no source-only completion of the overall renderer task.

CPU009 narrow sticker ownership expansion: export the existing prepared surface/damage functions and types from sticker-prepared-surface.ts and sticker-prepared-damage.ts, and expose a non-React subscription lease over the existing artwork cache in sticker-textures.ts. Preserve the same authority, avoid duplicate preparation/cache, and include these uncommitted ownership changes in independent host/resource review.

## Implementation dispatch I — native layout and raster density publication

CPU023 starts after CPU009 explicitly releases WorkerDeviceCanvas.tsx. Own that file and a narrow measurement helper if necessary; CPU009 retains device-render-host.ts, worker/protocol, native sticker controllers and all scene integration. Existing panel density/frame resolvers and unified physical-pixel policy are the source of truth. Preserve exactly the admitted CSS/backing/screen dimensions across resize, DPR-only changes and canvas replacement. Observe and coalesce real layout changes without duplicating observers or repeatedly reading layout on pose frames.

Agreed host interface: updateLayout(layout:RenderLayout,raster?:{readonly width:number;readonly height:number}):void. Measurement computes the existing exact resolver; host owns monotonic raster revision, capture transport update and worker texture readiness/adoption. Coordinate any necessary interface corrections directly with CPU009; never edit the active host/worker files. Retain existing stable Panel portal/input lifetime, visibility/disposal cancellation and current controls. No quality/DPR cap changes, new user setting or approximate screen dimensions.

Archive frozen input before edits. Diary gpu-native-layout-implementation.md and evidence/gpu-worker/native-layout/. Verify existing density/frame tests and deterministic real measurement-owner lifecycle for duplicate notifications, CSS resize, DPR-only change, zero/hidden dimensions, replacement and disposal. Run affected composite/device/web types and scoped source/proof lint; report unrelated in-progress integration errors precisely. No new unit tests, browser operations, commits or proof-only route. Different engineer reviews this source before commit; lead owns actual current-Chrome resize/raster/fidelity validation and full renderer completion.

CPU009 raster extension ownership: native-element-image.ts may add the narrow dynamic-size allocation helper needed by the live host. Other CPU021 modules remain frozen unless a concrete correction is handed off. Verify installed Three185 StorageTexture.setSize, disposal/initTexture and bind-group refresh semantics; stable JavaScript texture identity alone is not proof of a current GPU binding. Keep the previous LCD visible until a matching new-generation capture is available; allocation/upload failure retires through complete fallback. Retain resize/late-old/failure ownership evidence. CPU021's prior approval does not cover this new extension; independent host/raster review must.

## Implementation dispatch J — native sticker readiness

CPU025 owns new composite/native-sticker-warmup.ts and device/sticker-warmup-renderer.ts only, after CPU009 explicit release. Read-only consumers are existing prepareNativePackFrame/native-pack-resources and prepareStickerPackRenderFrame. CPU009 retains native host/protocol/worker, serialized GPU compile queue, pack/carry/query integration. Native startup must reproduce the existing artwork/program readiness contract: production withholds preparedSheet/pack until onPrepared; rendering only already-published pack creates a cold-start deadlock.

Main API createNativeStickerWarmup({send(sequence,frame:NativePackFrame,transfer),fail}) returns update(scene),ack(sequence),dispose(),rendererRetired(). Prepare the requested IDs across the three original earned/locked/placed appearances using existing artwork, paper, damage and private resource authorities; no independent cache/producer or main dense clone. The current catalog has at most10 requested IDs, but this observation is not a substitute for explicit admission budgets and queue bounds. Only exact current worker compile acknowledgement may publish onPrepared/onArtworkReady. Superseded acknowledgement cannot authorize newer artwork; old private leases stay owned until explicit worker replacement/retirement. Preserve errors, cancellation, cleanup and current user/media state.

Worker helper prepareStickerWarmup(frame,environment,signal) may reuse prepareStickerPackRenderFrame to return root/dispose. Warmup root is not presented. Compile all requested actual appearance variants with the real rendering environment/program inputs; flag any detached-root/light/material constraint rather than treating an incomplete shader compile as ready. CPU009 wires warmup-frame/ack through the existing serialized compile queue. No hidden visual/proof route, new worker, approximate visible scene or UI diagnostic flow.

Diary gpu-sticker-warmup-implementation.md and evidence/gpu-worker/sticker-warmup/. Retain actual existing-owner readiness/cancellation/supersession/ack/private-release bounds proofs and existing program/asset preparation tests; affected composite/device/web types and scoped source/proof lint. Archive dependencies/interfaces consumed if still changing. No new unit tests/browser/commits. A different reviewer must approve this source and caller integration; lead owns complete cold native startup, artwork, all appearances and full native acceptance.

## Implementation dispatch K — private carry result delivery

CPU026 owns sticker-carry-preparation.ts, sticker-carry-worker.ts, optional new sticker-carry-render-resources.ts, and sticker-carry-wear.ts only if needed for retained wear delivery. CPU009 explicitly released these files without edits and retains actual carry query controller, host/worker renderer protocol and GPU adoption. Current preparation transfers completed geometry only to main; native rendering needs a private result from that same producer without recomputation or dense main cloning.

Preserve the default GL mount/request/commit API. Coordinate the exact optional private acquisition interface with CPU009 before integration; a client-side acquired-frame lease or optional renderer port is permitted, but functions must never cross structured-clone data protocols. Stamp private results with originating owner, resource/job/computation epoch and compatible revision; preserve exact geometry, bounds and wear-source metadata. Never detach mounted main buffers. Keep the prior result pinned until acquisition; limit retained results to the current plus one active completed candidate, with explicit private byte/count and queue bounds through acknowledgement, cancellation and release. Retired source/target workers and late output cannot adopt into a newer gesture.

Host requests at most one active private candidate and one latest compatible frame; old GPU leases release only after replacement or renderer retirement. Producer failure/unavailable private path explicitly rejects native acquisition so the complete GL fallback can run; no second producer or hidden main preparation. Preserve final-sample/release ordering and existing carry cancellation/fit semantics.

Diary gpu-carry-render-resources-implementation.md, evidence/gpu-worker/carry-render-resources/. Archive source baseline before edits, prove actual-worker/main/private byte equivalence and ownership across supersession, cancellation, late ACK, worker failure and final disposal; use existing carry tests, device/composite/web types and scoped source/proof lint. No new unit tests/browser/commits, and no edits in Scene/host/renderer worker. Independent review precedes source commit; complete native peel/carry/stick and6×interaction remain lead-owned acceptance.

CPU025 catalogue admission correction: the first real ten-artwork native warmup exceeded the existing96MiB transaction budget while preparing unused contours. CPU025 exclusively owns a narrow optional contour:false input in sticker-prepared-damage.ts; default visible/query preparation and exact damage keys remain unchanged. Archive the pre-edit file including CPU009 export delta. CPU009 threads the option through native-pack-resources only. No new producer/cache or increased memory budget. Verify full catalogue plus subsequent visible-pack admission and all release paths, then independent review before acceptance.

CPU025 postcompile ownership correction: contour suppression alone fits the catalogue but leaves insufficient budget for the first visible print. Author may narrowly release the redundant canonical/main acquisition pin after successful delivery of an independent private copy in the existing transaction broker, keeping the private bytes charged and resources alive until renderer release. CPU009 splits frame query ownership so warmup alone releases unused main query/texture owners after exact current compile ACK. Retain all actual worker geometry/textures/materials and compiled program owners unchanged. Archive broker baseline; preserve visible query ownership/default behavior and cover cancellation, failure, stale response, eviction while private resources survive, catalogue-to-visible admission and final zero. Separate review includes this broker extension; no budget increase or second producer.

CPU025 complete renderer payload correction: full packet admission still exceeds budget because private damage copies include CPU-only alpha/distance/boundary arrays. CPU025 may extend sticker-transaction-broker.ts, sticker-transaction-data.ts and sticker-transaction-worker.ts with optional private purpose render-damage that copies only the SAME canonical exact GPU Uint8 texels, dimensions and sticker identity. Default full result/main query remains unchanged. Pending private reservation is exact GPU bytes twice, delivered bytes once until release; preserve cancellation/stale/retirement guards. CPU025 also owns type-only damage input narrowing in sticker-material-nodes.ts and sticker-wear-nodes.ts, with no shader math or texture-format change. CPU009 exclusively adapts pack/equipped/carry native consumers. Archive all baselines and independently review the complete delta. Required integrated budget proof retains warmup plus full eight-print packet plus current/candidate carry damage concurrently, compares exact private GPU and default full outputs, and reaches final zero. No new producer, recomputation, budget increase or quality reduction.

CPU025 exact accounting refinement after combined carry admission: count each main backing ArrayBuffer once by identity across retained inputs/results; independent canonical-worker results and private pending/delivered copies remain separately charged. For a contour-reference job, subtract an avoided field clone only when the same live producer/generation retains the exact pinned canonical field. Preserve output reservations, nonbuffer scalar allowance, and active-cancel charge until completion/retirement. Admission, eviction and queued-to-active transition use the same prospective accounting function. No changed96MiB limit or broader heap claim. Prove shared backing/views versus independent buffers, stale reference/cancellation behavior and complete warmup/packet/current-candidatecarry workload before independent review.

CPU009 collection integration correction: native rear rotation now renders, but StickerCollection consumes frozen renderedState.orientation and never admits the packet. CPU009 may narrowly pass the existing motionAuthority through ProductionDeviceView and subscribe the collection rear-admission/cancellation owner to current per-device intent, including initial state, replacement and cleanup. Preserve existing thresholds/cancellation behavior and avoid broad React pose rerenders. Audit remaining frozen orientation/pose consumers for the same gap; independently review this caller seam. This is required integration of the accepted motion architecture, not a new pose authority.

CPU009 transition corrections: preserve unchanged current/candidate print resource owners across packet generations inside the existing renderer authority, with exact data keys plus renderer-generation identity and last-reference disposal. No independent producer/cache or increased budget. Full warmup/currentpacket/candidatepacket/equipped/current-candidatecarry transition admission must pass; static one-packet fit is insufficient. Deduplicate semantically identical equipped updates received while preparing/awaiting ACK. Separately retain displayed-scene readiness during detached candidate compilation using verified installed Three concurrency semantics: candidate stays hidden until exact completion, current rendering/projection continues, swaps remain atomic, and failure retires once. Restore native event-time keyboard front admission and bind the same control physics owner to WebMCP; no duplicate authority. Independent host/worker review covers each correction.

CPU016 native channel correction: independent actual-driver evidence shows a same-epoch wheel command with stale main orientation rewinds worker-owned coast before the next RAF jumps forward. CPU016 author owns a narrow driver/helper correction after archiving baseline and recording a command/channel merge table. Preserve unrelated autonomous channels, apply exact explicit affected controls/new-intent epochs/final cancellation, and retain reliable final samples. No protocol change without host coordination. Independent reviewer must verify coast/control concurrency, fresh intent, release/cancel/hidden/dispose and existing motion parity; the author cannot approve this correction.

CPU009/025 cold and resize admission correction: current Chrome reproduced a print-five capacity failure after a usable native mobile packet was resized to desktop. Independent proofs now expose two missing cases: warmup and packet preparation overlap before compile ACK, and old/current packet resources survive while different-size candidate resources prepare. The author may correct scheduling within the existing owners and eliminate duplicate damage preparation only where the actual consumed inputs are exactly equivalent. Preserve all visible geometry, textures, shader math, query precision, live input priority, current-scene continuity and fixed resource caps; do not merely raise limits, downsample, or add an independent producer. Capture compact actual packet dimensions and public artwork/appearance/wear IDs, reproduce the exact current-to-candidate workload, then independently verify cold overlap, resize, cancellation/supersession, disposal and final zero ownership. Hash/key shortcuts must not alias different consumed inputs. Temporary diagnostics remain bounded and must be removed or reduced to appropriate failure context before final acceptance.

CPU025 invisible warmup query lifetime refinement: for compile-only warmup with no main hit/query consumers, the existing adapter may release each artwork's main query owners immediately after its independent renderer geometry/damage/bitmap capture succeeds. Preserve all renderer-side leases and compiled resources through exact full compile ACK or retirement, and do not publish prepared/readiness early. Visible packet/equipped/carry query ownership stays unchanged. Bookkeeping for transfer and renderer ownership must remain independent of cleared query collections. Independently prove concurrent ten-art warmup plus visible packet peak admission, per-art capture failure/cancellation, stale frames, retained current scene, exact full payloads and final zero. No budget increase, second producer, or new user-facing loading path.

CPU009 bounded production peel attribution: same-origin production at6× retains a141ms Scheduler task immediately after pickup, while steady rotation is responsive. Before selecting another optimization, the host author may temporarily instrument existing ProductionDeviceView, StickerCollection, WorkerDeviceCanvas, device-render-host and sticker-transaction-broker call boundaries. Archive exact original hashes; use bounded standard Performance marks/measures for component render, layout-effect owner fan-out, broker complete/admission/postMessage and input byte counts only. No new route, UI, debug command surface, user data or dense buffer contents. Cap recording by both elapsed time and entry count. Lead owns one matching production-build gesture and observer cleanup; author restores every instrumented file byte-for-byte afterward and verifies the archive hashes. Temporary instrumentation is not shipped or committed. Any resulting product change requires its own concrete scope and independent review; preserve immediate input, exact artwork, single authorities and current-scene ownership throughout.

### Measured carry startup correction

The bounded production attribution retained at `evidence/gpu-worker/production/peel-attribution.json` identifies a 73ms synchronous carry update. Source establishes rear geometry copy followed by a second structured clone and borrowed collision snapshot cloning on first worker dispatch. CPU009 author may replace that boundary in `sticker-carry-preparation.ts`, one adjacent private-context helper, `native-carry-controller.ts`, and `sticker-carry-worker.ts` only if context acknowledgement needs it. Use a single bounded private snapshot with exact typed bytes and transfer once; borrowed query buffers stay attached. Preparation must yield before bulk work even on a cold input request and use bounded chunks, latest-generation cancellation, fixed pending/installed byte accounting, single producer, and final-zero cleanup. Proactive preparation may use existing visibility readiness but cannot be the only protection against cold input stalls. No geometry/quality reduction, no main deformation, no global borrowed-array serialization change, no added public debug API. A broader canonical worker lifetime/private port architecture is outside this narrow correction because existing producers do not offer complete retained rear/collision delivery. Author proof and independent review must establish exact bytes, no synchronous cold copy, bounded ownership, cancel/failure/hide/dispose, final intent and current source retention before source commit; production pickup/drop observation follows.

### Captured rotation hover correction

CPU016 author may change `device-native-input.ts` and its existing scoped proofs to track only an orientation gesture actually accepted by that adapter and skip redundant hover raycasts for its captured pointer. The app's existing orientation sample/velocity/public-state path remains the owner; exact down picking, wheel/select, other pointers and ordinary hover remain unchanged. Release, cancel, lost capture, failed admission and disposal must clear ownership and restore affordances. Do not infer native ownership from unrelated pointer capture, approximate shell hits, defer trusted media activation, or claim the first-down/audio cost fixed. Independent review and a matched confirmed production rotation follow. This file ownership is disjoint from the carry correction; each delta gets its own small commit and validation record.

### GL sticker warmup readiness correction

The current full-tier GL route can show the existing artwork failure fallback while native packet handling succeeds. Source inspection identifies a concrete race: `PrepareStickerAsset` starts program compilation when the texture arrives, before its asynchronously prepared `StickerPrint` children mount meshes; later surface readiness does not retrigger the parent effect. CPU009 author may correct only this GL warmup readiness boundary in the existing pack scene preparation component, using existing child readiness callbacks. Compile the exact complete authored warmup group only after every required surface is mounted, retain one current-generation compile, and preserve cancellation, source/material ownership, retry, and error outcomes. No native behavior, geometry, memory cap or visual parameters change. A focused regression must demonstrate the old cold-texture/delayed-child failure and the corrected ordered compile, including out-of-order readiness, disposal, rejection/retry and no obsolete readiness publication. Independent source review precedes commit; the actual GL route must then regain packet/liner interaction. This source race is established; attribution of the observed browser error remains qualified until verified.

### Prime the bounded carry context on packet opening

After the exact-copy correction, a same-page production pair records no long tasks in either pickup but pointerdown event duration184ms on first pickup and56ms on a repeat. This does not isolate context preparation from other warmed resources. The source nevertheless establishes that context copy, worker construction and init transfer still begin only after the first carry request. CPU009 author may add `primeContext(rear, visibility)` to the existing runtime and trigger it from `native-carry-controller.ts` when the packet first starts opening (progress>0 with actual presence/workspace visibility). It must share the same private snapshot, worker, exact-generation deduplication and byte ledger; it creates no pose, deformation request, synthetic carry or public readiness. Do not prime during initial renderer/entry warmup. Preserve the bounded genuinely cold input path, current source retention, latest request and final sample. Repeated notifications cannot restart priming; hide/unmount retires speculative work and stale acknowledgements, and resume alone cannot repeatedly prime. Source files are limited to carry preparation and native carry controller, with existing context helper only if a necessary interface correction is explained. Independent proof must cover no fictitious jobs, shared in-flight work, exact bytes/accounting, packet-open cadence and cold immediate pickup, revisions, close/reopen, hide/resume, cancellation/failure/disposal. Independent source review precedes commit; live packet-opening and first-pickup responsiveness remain required.

### Temporary LCD binding inspection

Matched healthy original GL and current native production captures confirm a darker/flatter native LCD, independently of the GL artwork readiness failure. Author may temporarily instrument only `packages/device/src/Device.tsx`, `screen-mesh.ts`, `device-render-worker.ts`, and `native-element-image.ts` as proposed in `gpu-lcd-binding-investigation.md`, stage one only. Archive exact originals and hashes before edits; retain an instrumented manifest and patch; never stage diagnostic application changes. Inspect actual rendered LCD/glass objects, current studio screen/body environment identity, effective material/texture/alpha/output properties and direct-card scalar transforms. Record initial Black/dark and the subsequent Silver/light state, bounded to at most four scalar console records per owner for distinct actual settings/material generations. No user/library content, dense buffers, full shader code, per-frame logger, global getter, public debug protocol, route or UI. Use existing owner lifecycle/renderer callbacks and preserve their receiver/ordering. Do not patch the original PR5 archive: its already captured healthy optics remain the canonical reference, while current GL can be inspected after its scoped readiness correction. This is static diagnosis, never performance acceptance. No GPU readback is authorized in this stage. After collecting both backends, remove instrumentation by byte-exact restoration and verify all four hashes; only evidence/report/patch artifacts may be committed. A separate concrete source defect or additional readback scope is required before any permanent optics change.

### Temporary before-glass LCD pixel inspection

Stage-one actual GL/native snapshots establish matching material parameters, intended screen/body PMREM roles, direct lights and output settings without a causal mismatch. CPU009 author may perform the conditional second stage in gpu-lcd-binding-investigation.md, limited to screen-mesh.ts and device-render-worker.ts. Archive exact originals first. Borrow the actual installed LCD material in a temporary isolated canonical quad, sample an 8x8 target with explicit matching output transfer and documented row orientation, and restore renderer target/viewport/scissor/clear state before any await. No live material mutation, copied approximate shader, scene visibility change, full-screen readback, public API, route or global getter. Own all temporary buffers/targets/geometry and dispose in finally on success, failure, deadline and retirement; do not dispose borrowed material/texture. Native readback must own its staging buffer if the installed convenience API cannot guarantee rejection cleanup. Bound snapshots to the two required actual colorway generations; exclude teaser/default LCD material. Capture is static diagnosis only. Byte-exact restoration and manifests precede source commits or clean builds. Clean production artifact bd4b9d5 remains reserved for independent packet-opening/pickup measurement while temporary dev sources are instrumented. Permanent optics changes require a concrete result and independent review.
