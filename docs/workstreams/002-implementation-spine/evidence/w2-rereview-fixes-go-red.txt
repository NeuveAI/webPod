W2 re-review response — every fix goes RED when reverted
=========================================================

Each mutation restores exactly the behaviour the re-review measured, in a copy
of the tree; sources are restored byte-for-byte from a backup outside `src/`
before the next. Baseline: 156 pass, 0 fail.

── R1 — count constructions instead of refusing them (the off-by-one guard)
   The exact prior shape: a counter that the singleton's private build
   path does not increment, so the FIRST production call succeeds.
(fail) D-051 — exactly one device per document > the FIRST call in a fresh production module throws [7.80ms]
(fail) D-051 — exactly one device per document > a second call in a fresh production module throws as well [7.13ms]
 154 pass
 2 fail

── R1b — put the factory back on the package entry point
(fail) D-051 — exactly one device per document > the factory is not reachable from the package entry point [0.27ms]
(fail) D-051 — nothing outside a test builds a device > and the only re-export of it is the test-only entry point [3.05ms]
 154 pass
 2 fail

── R2 — let the caller stamp a bump again
(fail) R2 — one clock stamps every published time > a wheel bump is stamped from the device clock [0.39ms]
(fail) R2 — one clock stamps every published time > a Menu bump at the root is stamped from the device clock [0.10ms]
(fail) R2 — one clock stamps every published time > a transport bump is stamped from the device clock [0.10ms]
(fail) R2 — one clock stamps every published time > all three writers agree, whatever a caller passes [0.14ms]
(fail) actions > Menu at the root bumps, and the bump has a fresh sequence number [0.08ms]
 151 pass
 5 fail

── R3 — reclamp only the top frame, not the whole stack
(fail) R3 — the density route that half-worked is now unconstructible > the highlight stays inside the window when density changes [0.23ms]
(fail) R3 — the density route that half-worked is now unconstructible > and when Dynamic Type forces airy [0.10ms]
(fail) R3 — the density route that half-worked is now unconstructible > every frame on the stack is re-clamped, not just the visible one [0.10ms]
 153 pass
 3 fail

── R4a — decay per call again (the 4x frame-rate spread)
(fail) D-060 — the coast is the same gesture at any refresh rate > one flick lands on the same row at 30, 60 and 120fps — exactly [0.18ms]
(fail) D-060 — the coast is the same gesture at any refresh rate > and at every rate from a stuttering 15fps to a 240Hz display [0.14ms]
(fail) D-060 — the coast is the same gesture at any refresh rate > the frame COUNT still scales with the rate, which is the tell [0.08ms]
(fail) D-060 — the coast is the same gesture at any refresh rate > a variable-rate display travels the same distance as a steady one [0.08ms]
 152 pass
 4 fail

── R4b — drop the floor clamp, so a long frame overshoots the stop
(fail) D-060 — the coast is the same gesture at any refresh rate > and at every rate from a stuttering 15fps to a 240Hz display [0.18ms]
 155 pass
 1 fail

── R4c — score coasted detents at the frame's end speed, not their own
(fail) D-060 — the coast is the same gesture at any refresh rate > and at every rate from a stuttering 15fps to a 240Hz display [0.18ms]
 155 pass
 1 fail

── Restored; the suite is green again
 156 pass
 0 fail
