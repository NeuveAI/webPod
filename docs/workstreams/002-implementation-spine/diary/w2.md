# W2 — diary

Lane L-C · `packages/state` · 2026-08-28

---

## What landed

| Commit | What |
|---|---|
| `5f92872` | `feat(state): store contract` — the published surface, first, before anything was built |
| `1bbe51d` | `feat(state): detent reducer` — five input paths, one silence rule |
| `55b34dd` | `feat(state): screen state machine` — menu tree, push/pop, highlight, window, plus the store |
| `4fce0f1` | `feat(state): announcement debounce` — the settled-position summary, and the one timer |
| `0634b34` | `refactor(state): keep the universal gate greps clean` — comments and one field name only |

Five files of implementation, four of tests, 93 tests.

## The contract went out first, and that was the whole point

`packages/state/src/contract.ts` was committed on its own before a single
reducer existed, because W3 is blocked on the *shape* of device state and not
on the code that moves it. Building first and publishing later would have left
that lane idle for the length of this slice.

What that cost: two later commits touch `contract.ts` again — the announcer's
settling shape, a `pageRows` field, a `BumpDirection` return, an optional
`agentOrigin`. Every one of them is additive or a narrowing that no consumer
had yet written against, and all of them landed within the same session. It is
worth naming as a hazard for anyone reading the history: **the contract commit
is a publication, not a freeze.** If W3 had already shipped against it, three
of those four edits would have needed a different treatment.

## Read before writing

`~/code/agentic-context/jotai/docs/core/store.mdx` is marked `published: false`,
so the `store.get / set / sub` pattern this whole architecture rests on is very
likely absent from training data. I read it, then read `atom.mdx` for the
write-only atom convention, then read `src/vanilla/store.ts` and
`src/vanilla/internals.ts` for the exact `Store` type. `DeviceStore` in the
contract is transcribed from `internals.ts` rather than imported, so the
published surface does not drag a vendor type into every consumer.

The full list of what was read, and what came from each, is in
`decisions/w2.md` §1.

**Two skills are broken on this machine.** `/jotai-state` and
`/global-patterns` both send you to `~/code/agent-context/…`, which does not
exist. The clones are at `~/code/agentic-context/` — one word different, and
the skill fails silently rather than saying so. I went to the clones directly,
as the dispatch instructs. Somebody with write access to `~/.claude/skills/` should fix the two
`SKILL.md` files; every lane will hit this.

## The three things I would want a reviewer to attack first

**1. The keyboard has no acceleration, and I made the test hostile.**
The test drives 40 keydowns 5ms apart — faster than any hand and faster than
any OS auto-repeat, and far over every arc acceleration threshold — and asserts
the highlight moved exactly 40 rows with `multiplier === 1` on every single
event. If someone later "optimises" held-key scrolling, that test goes red. I
planted the optimisation myself to check: it does
(`evidence/w2-mutation-check.txt`, mutation 1).

**2. The silence rule is one line, and everything else reads it.**
`const silenced = input.source !== 'human'` inside `detent()`, and
`DetentOutcome.silenced` / `clickerTicks` / `hapticPulses` are computed from it
there. No call site re-derives it. Planting `silenced = false` fails four tests
across two suites (mutation 2). The seam is built and unused: nothing in this
stage emits `"agent"` or `"system"`.

**3. The announcer is pure and the timer is not in it.**
`noteMovement` and `flushAnnouncer` take a time and return a state; the single
`setTimeout` lives in `startAnnouncer` in the store binding. That is why the
U13 gate — 30 detents, one announcement — is proved with plain integers and no
fake timers. There is also a real-timer test of the driver, so the wiring is
not taken on trust. Making every movement announce immediately fails nine tests
(mutation 3).

## Things I decided rather than asked about

Two of these are the kind that should have a second pair of eyes.

**`Shift+Arrow` pages by density (8/6/4), not by a flat 7.** 001 §4.4 says 7;
§4.6 and §4.3 say one full viewport, 8/6/4. Two against one, and "one page"
matches the viewport reading. I think the 7 is a copy of the acceleration
multiplier sitting one column to its left in the same table. Logged as
`⚠ DEVIATES`; it is a parameter, not a constant, so it reverses in one line.

**`Center` returns `handled: false`.** The machine owns the stack; the layer
with the data owns what a row points at. I am confident about the boundary and
less confident it will read as complete to someone who expected the state
machine to do the descending. The menu tree, `findMenuNode` and `menuFrame` are
all exported so W3 can do it in two lines.

Neither triggered a stop-and-ask rule: no `useState`, no timer-based flag, no
resurrected state name, no field recording agent presence.

## What I did not build, deliberately

No React bridge hook. Keeping `packages/state` genuinely React-free is a
stronger proof of the property this slice exists for — the package has no
`react` dependency and a test asserts React is not even *resolvable* from it —
and the dispatch permits a bridge hook without requiring one.

That leaves a hazard for W3, and it is the one thing in this slice most likely
to bite someone: **pin `jotai@2.20.3` exactly.** Bun's isolated linker shares
one physical copy between workspaces asking for the same exact version, so atom
identity holds; two different versions would be two Jotai instances, and a
`Provider` from one is invisible to a hook from the other. The symptom is not a
type error, it is a component reading a default value forever. Written up in
`decisions/w2.md` §4.1.

Also not built: any tool registration, any haptics or clicker audio (the
reducer reports counts and stops there), any agent FX.

## Working in a live tree

Several lanes are editing this working tree at the same time, and it showed up
twice.

`git add` with an explicit pathspec is not enough on its own: when I went to
commit W2.4, `git diff --cached --name-only` listed two files another lane had
staged in the shared index. I committed with `git commit --only -- <my paths>`,
which builds its tree from HEAD plus the named paths and leaves the rest of the
index untouched — verified afterwards that the other lane's staging survived
intact. No `--amend` anywhere, per D-034.

I also never ran anything in the working tree that could disturb it: the
per-commit standalone typechecks and the mutation tests both run against
`git archive` extractions in a scratch directory, with `node_modules`
symlinked in. Nothing outside `packages/state/**` and this slice's own
bookkeeping files was written.

## Gates

`evidence/w2-gates.txt` has the real output. Summary at `0634b34`:

- `bunx tsc --noEmit -p packages/state/tsconfig.json` — clean
- `bun run typecheck` — 11/11 projects clean
- `bunx --bun eslint packages/state` and `bun run lint` — clean
- `bun test packages/state` — 93 pass, 0 fail
- `bun test` (repo-wide) — 140 pass, 0 fail
- `bun run gates` — **exits 1**, which is W0's placeholder behaving as
  designed until the gate lane implements it. Not mine and not a regression;
  the greps this slice owes are run by hand in `evidence/w2-greps.txt` and all
  return what they should.
- Every commit typechecks standalone, checked by extracting each one into a
  clean directory: `evidence/w2-commits-standalone.txt`.

## Open, for whoever picks this up

- **U14 is untouched and unclaimed.** Nothing in this package renders, so the
  occlusion rule does not apply to it yet — but the reducer decides what
  feedback *exists*, and whether the feedback lands where a thumb is not is
  W3's call on a real phone.
- **The clicker's 30/sec hard limit and the haptic's 12/sec suppression.** The
  12/sec suppression is implemented here because the reducer knows the detent
  rate. The 30/sec clicker limit is not: it is a property of the sound layer
  and belongs with it. `DetentOutcome.clickerTicks` is a count, not a promise
  that they all play.
- **`resetInputState` and `clearAnnouncer` are exported and currently unused.**
  They exist for teardown paths that do not exist yet. If they are still unused
  when the panel lands, delete them rather than finding a use.
