# Mobile merge review

## Verdict

APPROVE the resolved source integration of feature HEAD 3288542 and incoming 8f7e1d8. No Critical or Major merge findings remain. This is source/test approval with an explicitly authentication-limited runtime check, not a claim that every repository test or authenticated renderer interaction passed. Lead owns staging the resolved index and the normal merge commit/push.

## Correctness Check

Authoritative integration scope is `../merge-mobile-scope.md` and current repository law. AGENTS.md is unchanged between both commits. Historical Neuve and profiling directions do not override this merge scope. The incoming final acceptance qualifies genuine GPU-loss and physical haptic feel; merge checks do not remove those limitations.

Inspected all five resolutions and adjacent automatic merges in production-device-view, sticker editor/model, contour query, drop transaction, ListViewport, CompositeDevice, device-page and orientation lifecycle. The combined code retains:

- Default worker rendering with complete GL recovery, unchanged device/composite implementation, material/geometry/raster contracts and stable portal host outside renderer replacement.
- Worker drop fitting with exact final coordinates and signal, generation checks before and after save, and no obsolete completion animation. Human touch owns haptic triggers; tool paths only cancel feedback.
- Computation epochs and idempotent artwork-ready publication together with sticker tool transition cancellation, pinned displayed genre, sequential pack/liner animation, earned-only claims, pending/error state and native recovery messages. The added cross-contract test supersedes a pending drop with liner opening and proves its late fitted result cannot persist.
- Hidden-document orientation cancellation and reentry; prepared contour/query integration rather than restoring synchronous dense work.
- Visible-window row formatting with absolute row indexes and full-count scroll indicators, including Up Next. Partial prefixes render immediately and title-only rows remain.
- Existing artist All, continuous root paging, LFU budgets/pinning/oversized-result completeness, cursor retries, provider normalization and playback recovery. Retaining Relationships instead of the incoming legacy map bridge preserves page publication without restoring unbounded caches. Apple's legacy completion reuses its last published snapshot while preserving the actual AC/DC parent-artist fallback.

Independent verification:

- Music management/providers/panel/state/tools source suites: 909 passed, 0 failed, 4,344 assertions across 45 files.
- Sticker adapter/lifecycle/editor-carry, orientation and native sticker tools: 81 passed, 0 failed, 599 assertions across 6 files.
- All 14 project TypeScript checks passed. Scoped ESLint and `git diff --check` passed.
- Expanded device/composite source suites: 412 passed, 3 failed. Isolated rerun: 9 passed, same 3 failed. The failing tests and production files, entire device/composite trees, and referenced legacy route are unchanged from incoming 8f7e1d8. These are stale source-shape assertions: StudioEnvironment expects old envMap JSX and a direct studioEnvironment prop in the legacy route; ViewerLitDeviceFrame expects three literal rectAreaLight tags. They are upstream baseline failures, not suppressed or fixed by this merge.

Lead reports the merged production build passed. Authenticated current-route smoke was attempted without seeded inventory or credential inspection: the local developer-token endpoint responded successfully, but Apple Music library requests returned 403 and logout returned to the landing page. Consequently this merge does not claim live authenticated All, playback or worker/sticker interaction verification. Earlier incoming acceptance remains historical evidence only. No statistical performance improvement, audible playback, genuine GPU-loss recovery, or physical haptic quality is newly asserted.

## Findings

No blocking merge-specific findings. The three upstream assertion failures and current Apple authorization barrier are recorded above. No root test discovery or historical evidence regeneration was performed by this reviewer.

## Suggestions

After Apple authorization recovers, repeat the authenticated current-route worker/GL, artist All and sticker-tool smoke. This is a runtime verification limitation, not evidence that the merged source caused the 403 responses.

## Neuve Dogfood Feedback

Excluded by current repository law. Review used source comparison, independent bounded tests and explicitly attributed lead build/runtime evidence.
