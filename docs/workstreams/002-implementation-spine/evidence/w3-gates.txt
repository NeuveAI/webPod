W3 correction gate run

bunx tsc -p packages/panel
exit 0

bunx tsc -p apps/web
exit 0

bunx eslint packages/panel apps/web/src/routes/index.tsx
exit 0, zero warnings

bun test packages/panel
16 pass, 0 fail, 43 expect() calls

bun test
716 pass, then 1 non-W3 module error: packages/composite/src/tier-store.ts cannot
resolve `jotai/vanilla`. The Playwright suite uses `.e2e.ts`; root Bun test does
not discover it. A prior run before the concurrent W6 dependency edit was green.

PANEL_EVIDENCE_DIR=... PANEL_EVIDENCE_PREFIX=w3 bun run --cwd packages/panel test:e2e
10 pass, 0 fail
Includes 48 state/colourway crops, adaptive art, Dynamic Type, preference emulation,
axe including target-size, provider keyboard traversal, a 4x CPU runtime sample,
and computed raster-compatibility checks in both colourways plus reduced transparency.

rg -n 'canvas|useFrame|useState' packages/panel
zero matches

git diff --check
exit 0

bun run gates
exit 1 with 13 automated passes and three non-W3 failures:
- TESTS: the concurrent composite `jotai/vanilla` resolution error above
- NAMING: scripts/spikes/mint-apple-dev-token.ts (S2-owned)
- TIER: apps/web/src/routes/_probe.capabilities.tsx (W6-owned)
TYPES and LINT pass, all W3-relevant static checks pass, and no Playwright file is
discovered by root Bun. W3 does not modify or commit those foreign surfaces and does
not claim the aggregate is green. Prescribed state vocabulary and truthful comments
were retained as explicitly directed.
