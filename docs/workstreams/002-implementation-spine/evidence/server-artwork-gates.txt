Repository gate — 2026-08-29 Europe/Stockholm

COMMAND
bun run gates

RESULT
exit 0

11/11 TypeScript projects clean
805 tests passed, 0 failed
repo lint passed

PASS TYPES
PASS LINT
PASS TESTS
PASS U8
PASS U9
PASS U10
PASS AGENT-FLAG
PASS HAPTICS
PASS HALO
PASS PROVIDER
PASS TOOLS
PASS FLIP
PASS TRAILERS
PASS NAMING
PASS TIER
PASS CREDENTIALS

16 automated passed; 0 automated failed; 2 manual outstanding.

The outstanding items are the intentionally manual U14 phone-in-hand thumb
occlusion check and U15 reviewer inspection. Neither is satisfiable by this
server-only lane.

FINAL RE-REVIEW RUN

Scoped server/app TypeScript: pass
Scoped server/app lint: pass
Server tests: 62 pass, 0 fail
App production build: 207 client / 370 server modules, pass
Root tests: 812 pass, 0 fail
Root typecheck: 11/11 pass
Root static gates: all pass
Root lint: failed only in the concurrently modified, out-of-scope
`apps/web/src/routes/[_]spike.device.tsx:350` on
`react-hooks/preserve-manual-memoization`. The server artwork paths are clean.

Therefore the final shared-tree `bun run gates` exits 1 (15 automated passed,
1 automated failed, 2 manual outstanding). This lane does not claim a green
repository while the foreign device spike edit remains live.

DECODER REVIEW RUN

Scoped server/app TypeScript: pass
Scoped server/app lint: pass
Server tests: 65 pass, 0 fail
App production build: 207 client / 410 server modules, pass
Client bundle: no Sharp/libvips/server error identifiers
Root tests: 825 pass, 0 fail
Root typecheck: 11/11 pass
Root static gates: all pass
Root lint: the same out-of-scope device-spike memoization error remains at
`apps/web/src/routes/[_]spike.device.tsx:350`.

The shared-tree `bun run gates` therefore exits 1 (15 automated passed,
1 automated failed, 2 manual outstanding); this server lane is scoped-clean.

FINAL NO-AVIF RUN

Scoped server/app TypeScript: pass
Scoped server/app lint: pass
Server tests: 60 pass, 0 fail
App production build: 207 client / 370 server modules, pass
Sharp/libvips: absent from dependency graph and bundles
Root tests: 833 pass, 0 fail
Root typecheck: 11/11 pass
Root lint: pass
Root static gates: all pass

`bun run gates` exits 0: 16 automated passed, 0 automated failed, 2 manual
outstanding (U14/U15).
