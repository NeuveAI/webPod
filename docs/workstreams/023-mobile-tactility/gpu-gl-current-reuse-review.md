# Independent current-owner damage reuse review

Disposition: APPROVE for the one-file source change in `sticker-prepared-damage.ts`, SHA-256 `761130f081597423d3fde543c830f37e3b19f0f6791ad2244d15dc8bb695de27`, against `b2b27ba4`. No application edits by reviewer and no remaining Critical/Major source finding.

The hook's preparation callback reads its current Jotai-displayed PreparedPrint at dispatch and supplies its existing resource descriptors to preparePrint's existing bounded exact reuse option. It does not reuse by artwork name or geometry dimensions alone. Changed consumed normal/UV/artwork inputs still pass through the exact yielding comparator; matching candidates reuse only damage while current geometry gets independent contour preparation. Existing inherited acquisitions, displayed-frame retention, generation cancellation, stale-result rejection, retirement and unmount cleanup remain unchanged. No producer, resource cap or approximate output was added.

Independent validation: the actual React/Jotai mounted proof completes four restored-surface epochs while retaining the prior display during preparation, checks canonical field identity, supersedes epoch5 with6, and unmounts pending7. It reports no preparation/worker failure and final zero transaction/paper ownership. The separate10-case exact proof passes field/GPU/nonempty contour equality, changed normal/UV/artwork negatives, current retention and cancellation. The corrected19-case contour admission proof also passes. Existing alpha/contour/program suites pass17 tests and17,705 assertions; device/composite/web typechecks pass. Scoped source/proof lint passes after author replaced the proof's non-null assertion with a narrowed identifier; reviewer reran the final mounted proof and lint.

Retained evidence is under `evidence/gpu-worker/gl-runtime-failure/`: current-reuse manifest/patch, gl-current-reuse-mounted TS/JSON, gl-restored-surface before/reuse JSON and workload source, plus gl-source-wrapper-exact. The adapter's before result fails the second restored wrapper while explicit exact-current reuse admits four; it demonstrates a valid failure class. The actual live ledger's entries42/45 do not uniquely distinguish epoch refresh from carry handoff, and this review does not require or claim that uncertain attribution.

Fresh settled GL drop/edit/return remains a separate lead-owned runtime gate. No browser/server/full-build actions or timing claims were made by reviewer.
