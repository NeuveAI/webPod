# Independent packed collision review

CPU-014 reviews CPU-010's frozen source delta against da8a204, under dispatch C. The reviewer did not author this lane. The reviewer's separate CPU-011 changes were paused and excluded; CPU-009 host/protocol changes and package/lock changes are excluded. Read full scope and gpu-collision-implementation.md. No browser, trace, application edits or commits performed.

## Verdict

APPROVE the packed collision source/lifecycle milestone. No correctness blocker found in the frozen implementation. Requested one documentation clarification: snapshot() exposes immutable potentially shared buffers, so its old private-worker-transfer wording must not imply every borrower may detach them. This does not require numerical retesting. Whole architecture/runtime acceptance remains open; no measured mobile speedup or GPU performance is inferred.

## Source findings

Packed Float64 bounds and Uint32 links/leaf IDs preserve the former stable median partition and traversal order. The cooperative builder uses stable merge comparisons, including equal centers, and yields within linear/sort passes. Segment traversal pushes right then left, nearest support retains its original distance comparison/tie order and warm-facet exclusion, and triangle overlap retains the original exact tests. Shared Box3 scratch is consumed immediately before another node overwrites it; returned hits retain independent vectors. There are no external callbacks within collision traversal that would invalidate this scratch contract.

Prepared queries adopt the actual packed snapshot rather than restoring or serializing a nested node tree. Disposal drops local references instead of mutating shared metadata. The carry fallback's removed metadata clone is consequently safe. Worker transfer lists contain private typed arrays; shell faces alias packed leaf IDs and are transferred only once through that backing buffer. Geometry/material owners retain their original buffers. Shell raycasts still delegate triangle, side, normal, UV and material semantics to Three, restoring original face/object identities and triangle order. Unsupported cases retain exact source raycasting.

Collision preparation bounds one producer and three waiting jobs, checks estimated private input/output bytes and begins its execution deadline after queued copying. Failure terminates the producer before same-slot cooperative recovery; cancellation does not initiate fallback. Instance/request guards reject retired callbacks. Shared shell preparation has its own worker/queue/input bound, per-geometry/version deduplication and last-owner cancellation. These are subsystem bounds, not a global page CPU budget or a complete JS heap bound. Native typed-array copies/allocations are still non-preemptible; active snapshots held by callers outlive broker execution appropriately.

Registered assembly optimization is an explicit mutation-authority contract. Production registration selects the owning content ancestor through the local wheel ref. Prepared/material/colourway/screen prop changes mark the root; all wheel/Select transform and rest paths mark before invalidation; screen handle material replacement marks independently of React props. Child topology listeners register/unregister recursively and refcount correctly. Equipped/sticker meshes remain outside shell occlusion as before. World/device rotation is excluded from local geometry revision. Unregistered scenes still perform defensive inspection. Future host/local material or geometry mutation owners must honor this same signal contract; merely changing a property of a registered tree without signaling is unsupported and would produce stale queries.

## Independent verification

Executed the retained pinned-baseline experiment, which archives d252c4425e18fff1424f26aff9e3dcb6e28e46f8's device sources and calls its query implementation on the same full prepared assembly. It passed 529 segment, 529 nearest-support and529 triangle comparisons, including identical support traversal counts. Also passed 840 exact shell rays across material sides and poses plus840 synthetic UV-dependent alpha admissions. The UV filter verifies retained hit information; it is not evidence of native browser artwork appearance.

Actual Bun worker and cooperative output matched coordinates, provenance, packed bounds, links, leaf order and metadata. The retained representation counts are 200,913 triangles,65,535 former dense node restorations versus zero packed restorations,20,267,280 packed typed bytes and3,874,572 private input bytes. These are deterministic work/storage counts, not JS heap or wall-time measurements.

Independently reran all 21 lifecycle checks: 100 registered global poses caused one initial walk; actual control and screen mutations, explicit geometry/visibility mutations, topology, refcounts, external defensive mode, shared producer/owner cancellation, retired messages, failure recovery, capacity rejection and borrowed disposal all passed. The lifecycle transport is a deterministic fake Worker with real structured-clone transfer; the parity experiment separately exercises Bun's actual Worker. Neither is current-Chrome evidence.

Independently ran 60 existing tests across collision/preparation/visibility/contour/free-carry/probe/orientation-grab/control/screen: 60 pass, 0 fail, 865,192 assertions. Device and web typechecks, scoped authored source/proof ESLint and scoped git diff check pass. The typedBytes assertion change accounts for the newly typed tree storage while retaining behavioral assertions. Full-repository lint debt and unrelated moving-source gates are outside this scoped pass.

Commands are retained in gpu-collision-implementation.md and evidence/gpu-worker/collision/{parity,lifecycle}.ts. CPU-009 must preserve these local mutation notifications during shared scene extraction. Final integrated 6x CPU/native interaction/appearance and fallback validation remain the supervisor's pending architecture gates.
