# Rotation and entry performance without quality reduction

Status: diagnostic dispatch ready. Primary: user's request to improve janky rotation/entry without reducing render or effects quality. Repo law applies. Prior responsiveness changes are uncommitted and must be preserved; production has not received them. No commits/deployment during this scope.

User steering: prefer GPU operations for graphics/effects wherever feasible and beneficial, CPU as fallback. Evaluate WebGPU/compute and existing vertex/fragment shader paths explicitly. This is not permission to drop HTML display functionality, change shading quality, or assume that GPU migration cures CPU scene-update costs. Classify each proposed computation by required CPU consumers/readback, compatibility and output parity; implement GPU preference where the boundary is sound.

Investigate rotation and entry separately on the current local build and distinguish older production evidence. No assumed GPU bottleneck from CPU throttling. Do not change DPR, geometry silhouette/detail, shading, lighting, shadows, post-processing, animation trajectory/duration, or visible effects. Lossless compression must preserve decoded attributes exactly. Download size, decoded CPU memory, GPU allocation and frame time are separate quantities.

Three independent diagnostic lanes, read-only application code initially:
- motion: apps/web entry/orientation state and composite update paths. Trace per-frame state/React/DOM/geometry work and redundant subscriptions. Report rotation-motion.md here; source references and deterministic work counts, no invented milliseconds.
- geometry: packages/device procedural/model construction, memory/attributes/indexing/sharing/disposal. Report rotation-geometry.md. Determine actual asset format, exact dedup/index/immutable sharing candidates, and possible draw-call batches with material/order correctness.
- rendering: packages/device render warmup/environment/materials and RAF/compile/upload lifetime. Report rotation-rendering.md. Determine entry first-use compilation versus steady rotation work; inspect installed Three/Fiber APIs and local resources. No broad server review since no server dependency is established.

Lead captures browser evidence and consolidates. Implementation dispatch requires a specific sourced change with unchanged-output correctness checks. Safe changes may be made within user authorization after evidence, with separate peer review. No unit-test additions (standing global rule); existing tests and retained temporary parity/measurement experiments are permitted. Typecheck all changed packages, scoped lint, build and existing affected tests. Known unchanged StudioEnvironment source-string test failure must remain distinguished.

Evidence paths: evidence/rotation/{motion,geometry,rendering,browser}/. Subsequent diaries and peer reviews remain here as rotation-<lane>.md and rotation-review-<lane>.md. No editing cert/ or encrypted design.pen, no unrelated workstreams, no force push. Sources: previous mobile-production-profile.md baseline and responsiveness-result.md describe previous work but do not establish current rotation cost. Installed versions supersede differing resource checkouts. Read the full scope before work. Library/runtime claims must cite verified primary docs; explicit quality constraint wins over any optimization convention.

Completion requires measured or clearly qualified findings, fixes (if correctness-preserving opportunity exists), independent review and final checks. Report remaining device/GPU limitations honestly; do not claim optimality or a speedup without evidence. Suggested future staging boundaries: motion publication, lossless geometry memory, entry warmup; no commits automatically.

## Implementation dispatch

Evidence permits three independent changes. Geometry owner implements exact bitwise all-attribute indexing of immutable procedural geometry, preserving triangle order, groups, seams, crownCap attributes, side/raycast/collider semantics and appearance. No tolerance welding or quantization. Existing same-ray/index tests and exact expansion parity are gates; source memory counts are not measured VRAM or FPS. Files: geometry construction/helpers, not canvas/composite motion or paper shader.

Motion owner eliminates redundant CPU synchronization along orientation/entry publications and production-only diagnostics overhead. Preserve shared Jotai tool state, scene transform updates and DOM hit geometry before interaction; coalesce within the render lifecycle without extra frame of stale projection. Coordinate DeviceCanvas ownership with geometry as necessary. Keep trajectory/timing identical. Provide per-frame call-count proof and existing orientation/entry regression tests.

Rendering owner implements GPU-first visual paper bow/curl via existing WebGL vertex shader where source inspection confirms no required CPU readback. Preserve full geometry detail, materials/texture mapping, source-liner matching, accurate normals/edges and conservative bounds. Keep exact CPU worker/cooperative fallback for unsupported path. If parity cannot be established, retain current path and document blocker rather than weaken quality. Also remove unused equirectangular shader warmup only if fromScene is the exclusive path. Own paper GPU helpers/material/scene paper integration, not carry or motion/immutable shell helpers. Source/provider custom shader APIs must be verified pinned Three.

Cross-review: rendering owner reviews geometry; geometry owner reviews motion; motion owner reviews GPU paper/warmup. Review paths rotation-review-geometry.md, rotation-review-motion.md, rotation-review-rendering.md. Each must run independent scoped types/lint and parity checks. Lead runs combined build/type/lint/tests. No commits or deployment. Browser capture was interrupted by foreground changes; exclude that recording from measurements and report any absence of new trace rather than invent timings.

Implementation refinement: prepare the complete immutable front/rear shell pair and exact indexing in a bounded shared worker before scene publication, avoiding added synchronous cold-start indexing or a second GPU upload. Worker failure/timeout must complete via the original CPU construction fallback. The motion owner supplies an inner scene Suspense boundary and gates entry on actual scene warmup; pending preparation cannot masquerade as ready. Shared cache ownership, disposal, timeout and fallback correctness require independent review. GPU paper bounds must preserve the existing CPU return-clearance result, not merely enlarge bounds conservatively.
