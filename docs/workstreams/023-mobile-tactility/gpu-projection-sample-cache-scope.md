# Exact sticker projection sample reuse

Status: ready for bounded implementation after canonical attribute/mutation research below. Parent gpu-feature-complete-plan.md; source8d95ff9. Latest post-guard7.424sec main profile reports stickerProjectedQuad524ms inclusive versus editor348ms/contour215ms. These are overlapping single development-profile measurements, not a production win claim.

Correctness: reuse exact geometry-local sample selection for unchanged geometry across device/camera pose updates. Preserve existing nine sample choices, tie order, validity, projection/clipping/winding/area and current-pose results. No point simplification, approximate bounds or stale mutated geometry. Work moves out of per-pose scans; remaining nine-point transforms are small and do not require worker latency.

Ownership: mobile_framing implements only packages/device/src/sticker-transform-projection.ts plus a nearby helper if necessary, focused proof under evidence/gpu-worker/projection-cache/, and gpu-projection-sample-cache-implementation.md. haptics owns design doc only; independent reviewer follows frozen source. No browser, unrelated source, commits, Neuve shell, state/storage edits. Lead browser/git. Preserve unrelated .gitignore.

Mandatory primary readings: this scope, gpu-feature-complete-plan.md quality/DoD, current sticker-transform-projection.ts and its callers/mutation producers, installed Three0.185.1 BufferAttribute/InterleavedBufferAttribute and BufferGeometry types. Supporting gpu-visible-contour-design.md if present. Existing repeated computation is reference for exact results, not a required lifecycle.

Canonical research before edits: determine attribute identity/backing buffer/version/count/layout and in-place mutation semantics in all callers, including deformation/carry worker snapshot adoption. Record exact cache invalidation authority; if producers mutate arrays without version/invariant signal, do not introduce a stale cache—return bounded source gap to lead. Reuse existing Three attribute types and weak ownership. No lookalike transport type or new worker protocol.

Acceptance: cached and uncached outputs equal on unchanged geometry across camera/content/mesh transforms; retain UV tie order, valid/invalid/noUV behavior, deformed geometry/carry updates, attribute replacement, in-place changes with actual producer invalidation, shared/interleaved normalized attributes where supported. At most current cache entry per geometry/attribute lifetime using weak ownership; no ever-growing pose-keyed map. Per-pose work must not iterate over dense position/UV arrays after warmup. Do not cache viewport or world projection by geometry only.

Proof: representative grid including real pinned97×97 use pattern and mutated/interleaved fixtures, compare exact reference outputs over several poses and mutations. Demonstrate operation counts: repeated unchanged query performs only the existing fixed sample projection rather than geometry scans. Instrumentation belongs in retained proof, not shipped diagnostics. Benchmark in-process only as supporting data; no unqualified production speed claim.

Gates: bun package typecheck for device plus consuming apps/web; scoped eslint changed app/package/proof files; diff check. Independent reviewer reruns behavior proof and types and checks sibling invalidation. Diary exact path above records decisions, source refs, commands/results, changed file hashes and frozen handoff. One coherent commit after review/runtime. No quality, user state, shader, packet timing, cap or fallback change permitted. Remaining contour raycast work stays separately open.
