# Independent rendering review

## Verdict: APPROVE source and numerical correctness; live visual/performance verification remains explicitly unmeasured

Reviewed the rotation scope, rendering diary, full GPU geometry/support modules, PackPaper integration and surrounding return-clearance consumer, StudioEnvironment change, installed Three0.185.1 compile/program ownership and React subscription lifecycle. This review excludes my authored motion/readiness changes; the geometry engineer reviews those independently.

The original standalone deformation shader gate was insufficient to prove the actual patched stock materials linked. The author corrected this: CPU paper remains visible while the actual three materials compile against the target scene, and GPU publication requires every captured program to finish and link successfully. Missing handles, failed links, timeout and context loss retain CPU recovery. Installed Three's `compile` calls `getProgram`, which assigns `materialProperties.currentProgram`; that private shape is checked structurally, so an incompatible dependency remains on CPU.

The full96-segment front and rear grids, indexed adjacency normals, boundary triangles, rear normal offset, UV coordinates, material parameters and no-raycast semantics are retained. GPU math uses a stable half-angle expression for small curls. Conservative spheres encompass the authored all-curl shape and thickness. Box3 callers use the lazy exact CPU boundary mirror, with invalidation on curl changes; no GPU readback enters return motion. Gesture epoch does not require asynchronous ownership on the synchronous uniform path, while the existing CPU fallback still receives epoch.

I independently ran the retained CPU bounds experiment:1512 front/back/edge boxes across72dimension/liner combinations and7curl values match the full reference exactly. I read the numerical transform-feedback source and output:36 full vertex comparisons, max position difference0.000030517578125 and max normal-component difference0.00003178417682647705, for one300×500,pixel1 dimension and both liner modes. That evidence came from the author's isolated headless AppleM4Pro Chrome session before the supervisor's CUA-only reminder. I did not rerun or extend shell browser automation. It establishes numerical agreement for those cases, not full-material screenshots, phone GPU performance or allowed live-app visual validation.

The removed `compileEquirectangularShader` prepared an unused conversion path; both actual `fromScene` studio environments and their cleanup remain unchanged. No DPR, lighting, physical effects, tessellation or transmission setting was reduced.

Independent `bunx tsc --noEmit -p packages/device/tsconfig.json` and scoped ESLint of all four rendering implementation files pass. No new unit tests were introduced. I independently reran `material-gate.ts`: completed link, delayed completion, failed link, missing handle, context loss, deadline and disposed stale poll all pass, with no retained timer. The final helper and retained experiment also pass scoped lint. The original readiness finding is resolved. Live integrated visual inspection and mobile frame timing remain unavailable in the permitted browser session and must not be implied by the numerical proof.
