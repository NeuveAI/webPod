# Panel measurement runtime result

Clean production dbe65c2, existing Chrome/iPhone16ProMax/6×CPU. The stopped and disabled post-panel CPU profile is independently attributed in gpu-post-panel-attribution.md. No offsetWidth or offsetHeight sampled frames remain; the prior full contour chain also remains absent. This is a targeted hotspot result, not an overall FPS or statistical speedup claim.

The profile starts from an actually opened human editor. Earlier fixed140,320 setup taps missed the sticker at the final rear orientation and failed the controls assertion before profiling began. A tap at the observed projected center114,319 opened the editor; the retained profile then uses the same dismissal and hidden-repeat gestures. The standalone active-editor runner has a nine-second stop guard and finally stops/disables the profiler.

The actual native drawable panel measured320×240. With idle front pose, fractional border-box dimensions320.2×240.2 preserved the integer offsets and exact transform string. A content-box change with fractional width/height, padding and border produced324×244 and reciprocal transform columns; columns3/4 remained exact. Original cssText was restored in finally, and original offsets/transform returned exactly.

The initial1e-8 matrix-column tolerance was too strict for the browser-serialized CSSOM string. The app writes full matrix elements; Chromium formats CSS numeric values with six significant digits. At these coefficients the quantization uncertainty is±0.005. Thus scaled-column pair bounds are3.22(X) and2.42(Y); observed differences were0.04 and1.04. At the sampled front homogeneous w3535.26, projected corner differences are0.0000113px and0.0002942px, within the serialization-derived bounds. This accounts for the initial raw JSON columnsMatch:false without hiding it or changing application precision. Source: Chromium CSSNumericLiteralValue::FormatNumber, third_party/blink/renderer/core/css/css_numeric_literal_value.cc, independently checked by the reviewer.

Raw browser values and the bounded restoration runner are retained under evidence/gpu-worker/panel-measurement/browser-check*. The actual browser test validates integer measurement/invalidation and exact restoration; synthetic exact-matrix proofs remain the mathematical parity evidence. Final integrated resize/hide/media checks and GL wrapped editing recovery remain separate gates.
