# W3 decisions

## Sources read

- `AGENTS.md`
- the full W3 packet and 002 scoping bundle named by it
- the 001 sections named by the W3 packet
- Pencil nodes `A76Ib`, `DLqSo`, `H4QpB`, `HYNXu`, `mObBW` through Pencil MCP only
- `/Users/vinicius/code/agentic-context/jotai/docs/core/store.mdx`
- `/Users/vinicius/code/agentic-context/jotai/src/react/Provider.ts`
- `/Users/vinicius/code/agentic-context/jotai/src/react/useAtomValue.ts`
- `/Users/vinicius/code/agentic-context/tanstack/router/e2e/react-router/scroll-restoration-sandbox-vite/src/routes/index.tsx`
- `/Users/vinicius/code/agentic-context/tanstack/virtual/packages/react-virtual/src/index.tsx`
- `/Users/vinicius/code/agentic-context/tanstack/virtual/packages/react-virtual/e2e/app/react-compiler/main.tsx`

## Decisions

1. `deviceStore` is the sole store. The route mounts two Providers over that same store so the colourways remain synchronized and tool callbacks observe the rendered state.
2. Panel internals remain exactly 272×204. The owner preview uses integer page zoom; Dynamic Type scales only the bounded panel stage and is capped at 1.25.
3. Unsupported capabilities are absent from the model, never disabled controls. Radio is therefore absent from S03 under the launch matrix.
4. S08 uses ordinary semantic rows for the fixture and a deterministic eight-row window after 100 rows. Loading remains exactly eight 26px rows. This avoids an app-level dependency mutation outside W3 ownership while bounding the rendered DOM.
5. After coordination approval, the route consumes canonical `@webpod/panel`; the app manifest and lockfile preserve the device/composite lane dependencies already present.
6. HTML-in-canvas, device materials, and composite transforms are outside this package. The panel is independently focusable semantic DOM.
7. H-6 and U14 require owner observation and remain explicitly open.
8. The universal `bun run gates` runner is owned by W5a. Its current red result is recorded as a dependency, not omitted or implemented in this lane; rerun is required when W5a lands.
