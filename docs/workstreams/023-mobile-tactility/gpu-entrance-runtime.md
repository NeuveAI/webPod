# Captured native entrance interval

The capture does **not** establish entrance responsiveness or identify a main-thread CPU bottleneck. Its target main-thread track stops before the reported interval and has no CPU profile. An unclosed task remains; reporting zero main work, a measured continuous long task, or successful independent entrance would all be unsupported.

## Provenance and attribution

Lead captured/stopped the existing current Chrome21 page, mobile440×956, DPR3,6× CPU, DEV server, source22437fc. Authoritative stop response: /tmp/webpod-entrance-interval-result.json. Raw gzip: /private/var/folders/ft/7tsjkpcn20q5fx1q8dwv26x80000gn/T/webpod-native-entrance-interval.json.gz. SHA256 b77edff2040f173150824f6ed6b3d100916ed8b184821ceac72d80568065ff40;57,643,253 compressed bytes. Interval259138966067–259146301959µs spans7,335.892ms. No navigation occurs within that reported interval.

Reproducible parser: evidence/gpu-worker/entrance/analyze.ts; output summary.json. Run with Bun from repository root. It filters target PID39318 only and retains first-party module coordinates, not unrelated tab URLs/content or screenshots. Target-frame metadata also mentions PID34468, which has no trace events. Main TID4511411 is CrRendererMain; renderer worker TID4525946 is identified by its exact device-render-worker module registration. ProfileChunk helper thread IDs are joined to the actual worker Profile IDs rather than mislabeled as main.

External polling first observed warming at navigation-relative34,572.7ms and entering at43,443.5ms (/tmp/webpod-entrance-phase-observations.json). The8,870.8ms polling gap is not an exact warmup duration or CPU attribution. This capture has no phase/logical-elapsed markers establishing precise reveal start,1900ms settlement or3460ms completion.

## What the trace actually contains

| Track | Retained evidence | Interpretation |
| --- | --- | --- |
| Target main | Last timestamp259138682915µs,283.152ms before reported interval; last event RunTask begin without matching end; zero events/profile samples in interval | Incomplete/absent observation. Cannot compute main long-task count, CPU self-time, reveal RAF cadence, React/style/layout/paint attribution or main responsiveness |
| Shell-picking worker4526114 |278 samples,3,911.745ms clipped sample-delta weight; sampled stacks run shell-picking-worker→buildShellPickingIndex→createStickerCollision→collisionSteps/build | Real first-party index construction is present on a worker. Sampling weight is not exact task duration, wall-clock readiness delay or measured impact on entrance |
| Renderer worker4525946 |100 samples,1,998.939ms sample-delta weight at Three's internal Animation.update;175 timeline events in interval, but no complete render/RAF callback boundaries | Worker activity exists. This does not establish changed pose cadence, displayed FPS, GPU time or independence of entrance from main |

Pinned source22437fc `packages/device/src/shell-picking-index.ts:8` constructs the exact collision index; `sticker-collision-cooperative.ts:48` recursively builds/sorts the packed tree. These sampled names are expected worker preparation, not evidence that dense construction fell back to main. The transformed Vite Three module's update coordinate maps to installed Three0.185.1 internal Animation.start callback: it schedules RAF, updates node-frame bookkeeping and calls an optional animation loop. It is not a named GPU draw duration. All100 renderer samples share that node; the sparse profile and missing callback boundaries prevent stronger attribution.

No matching target-frame process-swap/commit/delete event was found. No explicit target main paused-debugger, hidden or BFCache marker was found. The only debugger-named target event is v8::Debugger::AsyncTaskRun on the renderer worker before the interval; this is not proof that main was paused. Lead reports post-trace Runtime.evaluate timing out15s. That is an operational symptom consistent with several possibilities—paused execution, a hung/busy target, process association or capture loss—and this trace cannot distinguish them. Lack of a marker must not be treated as proof that none occurred.

## Concrete next capture

First restore and verify the same target's runtime responsiveness and debugger state through the lead's existing browser connection. Confirm current target/frame/process and no paused execution; do not immediately record another entrance against an unresponsive target. A short steady-state trace should visibly contain that target's main events/profile alongside its renderer worker. If it does not, diagnose the capture/session association before performance work.

Then perform a clean foreground existing-route reload with the same6×/viewport/DPR/backend and no HMR/capture-diagnostic override. Begin before readiness/entry and stop after the full hint; preserve screenshot/frame tracks and main+worker CPU profiling. If permitted by the lead, bounded temporary phase marks for ready/first logical tick/1900/3460 and pose sequences provide precise alignment, removed afterward. These marks observe the existing clock and must not drive it. Record the target mapping with the capture.

The first profiling targets once coverage is valid are reveal tick→authority/store subscribers→native query/Panel/style/paint on main, then independent worker frame submission versus incoming reveal sample cadence. Separately measure shell-index preparation admission/completion relative to graphics readiness; the current worker samples justify checking overlap but not claiming it blocks the entrance. Compare the exact same DEV or production conditions to the baseline; do not divide sampled numbers by6 or infer a speedup.

This analysis preserves the source conclusion in gpu-entrance-verification.md: current entrance progress is deliberately main-clock coordinated. It supplies no new runtime acceptance and no self-review approval. No application edits, browser interactions or unrelated-tab inspection were performed. Parser scoped ESLint passes; raw trace remains external to the repository.

## Later native sample: investigate throttling machinery first

Lead collected a separate one-second macOS sample of the same PID39318 at19:25:41.536+0200, after the trace and Runtime.evaluate timeout. Raw /tmp/webpod-main-native-sample.txt; retained target-main extraction/count metadata in evidence/gpu-worker/entrance/native-sample.ts/json. This is not simultaneous evidence for the earlier7.336s interval.

Of758 main-thread stack samples,757 have the event-loop mach_msg parent. Within that nested branch,657 pass through signal trampoline and627 reach mach_absolute_time under unsymbolized ChromeMain offsets. Only one sampled branch proceeds into unknown JIT addresses. A separate named CPUThrottlingThread is sleeping or calling pthread_kill, with nearby framework offsets. These nested counts are not additive milliseconds or app CPU percentages. They do not show a sampled main application infinite loop or a dense application-native operation. The renderer footprint742.1MiB/peak1.2GiB is whole-process accounting, not attribution to one resource owner.

Inference: the signal/time-polling stack and named throttling thread make the active emulation machinery a concrete diagnostic target. Chromium's [ThreadCPUThrottler declaration](https://chromium.googlesource.com/chromium/src/+/99baeeafe29b54168bc18411791025d4a9419eea/third_party/blink/renderer/platform/scheduler/public/thread_cpu_throttler.h) describes interrupting the main thread from a helper; that historical primary source supports the mechanism, but is not symbolication of this exact Chrome152 build. Live Debugger.enable reportedly emitted no paused notification. Neither fact proves the cause of the runtime timeout.

Recommended bounded diagnostic, lead-owned: temporarily restore1× on the exact emulation session, check a trivial runtime response and current target state, then restore6× once healthy and verify the new capture includes main events/profile. This is a session-health comparison, not a product performance result or a recommendation to lower the final test load. A repeat native sample at1× would distinguish persistent application/native blocking from the observed signal/time-polling pattern. Preserve any failures and do not silently replace the requested6× acceptance with1× results.

## Diagnostic recovery and subsequent clean reload

Lead reports the exact current CDP session accepted raw CPU rate1 immediately; the following Runtime.evaluate also returned immediately. That recovered page was already phase complete on WebGL fallback, with `Canonical paper producer unavailable`. Restoring raw rate6 still allowed immediate Runtime evaluation. This establishes that toggling the throttle restored inspection in this episode; it does not quantify application speedup, prove the original hang's cause, or establish a new producer defect. The producer failure occurred after the prolonged unhealthy inspection interval and requires a healthy reproduction before attribution. Multiple clients do not imply independently multiplied throttling factors; the recorded Blink inspector path uses the ThreadCPUThrottler singleton's rate setter.

A subsequent clean reload with6× restored produced these coarse navigation-relative observations in /tmp/webpod-healthy-reload-phases.jsonl: warming/pending at7,432.2ms; warming/worker-ready at8,301.5ms; entering/worker-ready at8,869.5ms; complete/worker-ready at11,058.7ms, no reported failure. These are polling observation times, not exact phase transitions or a callback/frame trace. In particular the DOM `complete` attribute changes at1900 logical ms while the orientation hint continues to3460ms, so11.06s is not proof of full hint completion. The earlier43s entering observation and missing-main-profile trace are not representative acceptance measurements. Keep the healthy6× observation separate from the1× session-health diagnostic; no before/after product speed claim follows.
