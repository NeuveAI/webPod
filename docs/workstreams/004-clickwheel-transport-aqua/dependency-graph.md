# Dependency graph

```text
provider transport semantics + queue truth
              |
              v
shared physical-input routing -----> global transport behavior
              |
              v
Now Playing mode/wheel intent ------> volume + scrub + queue views
              |
              v
presentation correction ------------> browser evidence
              |
              v
independent review -----------------> completion audit
```

## Execution order

1. Establish provider-authoritative skip and queue behavior.
2. Route physical input without breaking no-context list paging.
3. Implement real center modes and shared wheel intent.
4. Correct scrollbar and marquee presentation.
5. Run focused/full gates and authenticated browser verification.
6. Independent adversarial review; return confirmed findings to implementation.
