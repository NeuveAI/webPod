# Independent captured-hover source review

APPROVE the bounded single-file correction `packages/device/src/device-native-input.ts`, SHA256918654bbe6e96b120684c8d285506827e1dd67e71c0502963d80cff629aa88c8, against archived baseline769d9b2a0cc817afc48f7424b91767f01b3deeb07374f44187e9d7e6cbe0d67e. No Critical/Major found in this delta. Frozen manifest and baseline are retained under `evidence/gpu-worker/rotation-input/` and `/tmp/webpod-rotation-input-freeze`.

The adapter records only its own successful shell admission after the callback returns and canvas capture is verified. The actual application begin function rejects an already active gesture, acquires capture, and installs its separate move/final-sample path; that code is unchanged. Suppression skips only redundant hover picks for that accepted pointer. Wheel/Select active handling stays ahead of the skip, other pointers still follow existing admission, and ordinary hover remains exact. Up/cancel/lost capture/blur/disposal clear ownership; missing capture restores hover before a delayed event. Admission generation prevents reentrant terminal/disposal from installing a stale owner. Neither movement propagation nor final sample/velocity, trusted activation, exact initial hit testing, geometry or query matrix semantics change.

Independent reruns: actual adapter/query-geometry ownership proof22 checks, existing native-input gates and immediate-intent proof,25 existing orientation-grab/clickwheel/control-physics tests with928 assertions, device/composite/web types, scoped source/proof lint. The ownership experiment asserts initial down really raycasts and24 accepted move events perform zero redundant shell picks, plus rejection, unrelated pointers/capture, rear, controls, cancellation and final listener/cursor cleanup. Controlled Happy DOM events are not trusted-browser timing evidence.

First-down/audio cost and public-store subscriber work remain unchanged and unresolved by this slice. Approval permits the bounded source commit; matched confirmed production rotation, overall front/LCD visual parity and full native activation remain separate lead-owned gates. Reviewer authored no application changes and performed no browser/build actions.
