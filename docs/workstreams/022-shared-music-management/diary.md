# Implementation diary

- Read Ready scope, decisions, architecture and provider contract packet before implementation. Applied global-patterns/Jotai/React guidance. Canonical local resource directory absent; read installed Jotai vanilla Store declarations. Modern guidance search executed through bunx; returned only low-relevance guides, no framework migration undertaken.
- Added @webpod/music-management with Jotai vanilla state, stable managed facade, accepted selection and queue context, common command and queue-read generation fences, lifecycle and account boundaries. Plain ordered writes preserve rejection recovery; play/pause supersede pending confirmation immediately. Normal progress remains independent from metadata events and requires no mounted React subscriber.
- Moved progressive library/source contract from app into neutral library. App retains auth, HMR/sticker setup and provider composition; publishes managed facades. Panel retains DOM, granular counter projections, navigation and scrub preview, delegates intent/queue truth/seek-resume. Navigation and production clickwheel paths resolve to same manager. Removed duplicate write queues.
- Spotify retains SDK relinking/UID occurrence and Connect mechanics. Removed its submitted-context total/append policy; native queueTotal is explicitly unknown. Added native play-request generation fencing across activation/connect and old HTTP failures. Common layer supplies selected-list totals and invalidates completeness after mutation/shuffle.
- Existing fixture expectation that confirmed duplicate selection hides its counter changed to require retained 3 of 3. Existing delayed double-toggle expectation changed to immediate pause admission plus mandatory pause after old play settles; proves cancellation without delaying pause behind a native start.
- Extracted existing Apple SDK fixture into test-fixtures.ts for reuse by native suite and real-adapter manager test. Spotify broad native contract fixture runs both directly and through manager; unknown native total versus known managed submitted total asserted explicitly.
- Review fixes: bounded pending SDK errors settle failure before item matching; complete native queue evidence recovers after mutation; selection confirmation refreshes queue even when optimistic identity already matches; logout preserves session notifications while playback is fenced. Apple station seed lookup captures native cancellation generation and uses existing transactional play for queue changes; native paused acknowledgements cannot overwrite newer resume. Accepted PlayTarget is copied before async dispatch so progressive source mutation cannot change the submitted list/count.
- Additional queue-read fix: read validity follows account epoch and queue-read/selection sequence, not unrelated pause generation. A pause during a same-context read no longer strands loading. Added the explicit delayed-read regression.
- Second review fixes: every supported queue mutation uses the same context invalidation helper; complete native evidence recovers after remove/reorder as well as append/insert. Accepted new play immediately clears old queue items to an honest loading state, preventing stale queue selection during handoff. Added actual Apple window+append test (absolute175/native101 remains unknown), and exact Spotify repeat0/1/2 SDK enum assertions in native/managed fixture runs.
