# Responsive computation and scheduling

Status: Ready. Single implementation/review cycle, no commits or deployment during this cycle.

## Correctness and authority

Primary: user's request to offload heavy computation, remove duplicate work, batch updates, and keep playback/UI responsive. Repo AGENTS.md applies. Supporting evidence: docs/research/performance/mobile-production-profile.md and its client/graphics/workers reviews. Existing interaction contracts, geometry, and playback ordering are the behavioral reference. Static suspects are not measured speedups.

Keep touch/control/sticker ownership, final gesture positions, playback commands, geometry results, hidden-tab cleanup, and disposal correct. Worker tasks must have bounded outstanding work, stale-result rejection and explicit failure/disposal behavior. Never move DOM/browser SDK access into workers; do not put heavy work into RAF merely to defer it. No visual quality reduction or new dependencies without demonstrated need. Safe implementation choices are authorized; record choices and rejected alternatives in slice diaries. No open user decisions block these bounded changes.

## Independent dispatch lanes

1. worker: Own packages/device sticker collision/preparation/worker/visibility files and new computational worker helpers; do not edit StickerPackScene.tsx until coordinating with picking lane. Move synchronous collision construction off interaction paths, cache invariant inspection/preparation, examine deformation offload feasibility. Existing worker transfer types and Three installed types are canonical. Verify warm/cold/change/dispose/error behavior and existing collision tests. Diary/evidence: responsiveness-worker.md.
2. picking: Own packages/device/src/StickerPackScene.tsx, Device.tsx and dedicated picking helpers. Eliminate unnecessary sticker arbitration and detailed render-mesh picking for enclosure where exact/cheap input geometry is available. Coordinate interfaces with worker lane. Preserve front/back/transparent/wrapped sticker/perimeter semantics. Verify existing device/sticker suites. Diary/evidence: responsiveness-picking.md.
3. scheduling: Own packages/panel, packages/composite/src/html-in-canvas.ts and apps/web playback publication files only if necessary. Bound visible-list preparation, coalesce duplicate measurement/paint requests, audit RAF and media scheduling without delaying direct transport activation. Keep visual behavior unchanged. Existing React/Jotai/Three and provider contracts canonical. Verify existing panel/composite suites. Diary/evidence: responsiveness-scheduling.md.

All diaries are under docs/workstreams/023-mobile-tactility/. Read the full scope before implementation. Read relevant local resources and installed sources before library/API edits. Record exact source references for protocol choices. No editing cert/, design.pen, unrelated workstreams, or earlier changes outside owned scope. No unit test additions (standing global instruction); execute existing tests and bounded deterministic experiments instead. No commits, force pushes, or deployment. Suggested future commits correspond to the three lanes, staged without earlier mobile/update work.

## Verification and completion

Implementation decision: move the complete rear sticker deformation/contact calculation as one atomic worker job, rather than independently offloading intermediate stages. Geometry, projection/carry inputs and accepted generation must remain aligned. Picking owner hands the PeelingPrint effect to the worker owner after finishing admission edits; that file's shared edits are sequential. The worker reviewer must specifically verify final-pose publication, stale response rejection, immutable input caching and collision parity. This is within the requested heavy-computation offload; it does not authorize a visual approximation.

Picking evidence shows conservative touch rejection still leaves most representative rays on the detailed shell. The picking lane therefore also owns a worker-built immutable shell triangle candidate index and its lightweight main-thread exact query. Preserve original triangle IDs, material side/groups/draw ranges, hit attributes and ordering; do not substitute the sticker collider's looser intersection semantics. Pending/error queries retain exact original behavior. Verify identical-ray parity and worker/version/disposal lifecycle before accepting this addition. Independent review remains with the scheduling author.

Recovery ownership update: scheduling author implements cooperative collision preparation fallback in sticker-collision-preparation.ts and a dedicated helper; worker author retains paper/carry generator recovery. Shared same-math iterative computations must serve worker and fallback paths where practical. Fallback runs only when worker preparation fails/unavailable, yields bounded chunks, supports cancellation and publishes readiness; it must not run a synchronous heavy build inside a hit handler or permanently disable the feature. Picking author independently reviews collision and carry; scheduling author independently reviews paper/visibility and picking, never their own collision changes. Existing REQUEST_CHANGES reviews remain open until recovery is verified.

Run bun run typecheck, scoped bunx --bun eslint, existing affected bun test suites, and production build. Lead independently runs type/lint/build. Existing tests provide objective geometry/order checks; browser existing route provides integration and visual checks, not sole correctness proof. Compare frame/input work where reliable local profiling is available; do not claim measured gains from source inspection. Physical-device quality remains a reported limitation.

After implementations, separate peer reviewers inspect lanes they did not author, concurrency races, transferable ownership, command order, latest-result application, cache invalidation, and cleanup. Reviews: responsiveness-review-worker.md, responsiveness-review-picking.md, responsiveness-review-scheduling.md in this directory. Major/critical findings must be fixed and rechecked. No merges or deployments from this packet. Final consolidated evidence records changed boundaries, remaining main-thread constraints and measured/unmeasured results.
