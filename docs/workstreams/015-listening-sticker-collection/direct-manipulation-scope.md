# Direct sticker manipulation

## Authority and readiness

Primary: the owner's six latest notes and three screenshots (premature collection tease, click-only slide instruction, oversized sync banner). This is new work following the verified packet-laminate correction, not an audit of unchanged delivery. Supporting: tactile-design-brief.md, tactile-collection-handover.md, backend-contract.md and implementation-decisions.md. Existing UI and old passing interaction tests are behavior references and anti-sources where they contradict the latest request. Repo AGENTS.md remains standing law; no Neuve/Kanban exists.

The inspected screenshot sources are /Users/vinicius/.codex/attachments/99082042-553e-4ad0-a831-f593c1725147/image-1.png, image-2.png and image-3.png. The later pasted clipboard images duplicate those views. They are rejection references, not designs to preserve.

Design and read-only profiling are Ready and dispatched. Implementation awaits the designer's direct-manipulation-design-brief.md and profile diagnosis, then explicit file ownership. No blocking owner decision is known. Logged defaults: “voiced” means humane product copy, not audio speech; an already-owned collection remains available through a background import refresh; returning a sticker restores its existing collection seat without revoking ownership. Do not gate existing stickers on completion of an unrelated import or all sixty texture downloads. No new earning, authentication or backend framework is authorized. Use current layout/material family; this is direct manipulation, not a new visual identity.

## Required outcome and verification map

| Requirement | Observable acceptance | Evidence |
| --- | --- | --- |
| Tease only when ready | No collection lip, empty packet, actionable ghost target or false reward before usable owned inventory and required artwork exist. Existing owned content remains usable through refresh/error; sign-out clears it. At least one opened or earned sealed pack is enough; an empty/locked-only catalogue is not. | Readiness state tests for signed-out/loading/empty/error/cached/sealed/ready/art failure; actual delayed-ready browser sequence shows no premature tease and a usable first reveal. |
| Peel on removal and sticking | Lifting a placed sticker deforms at its actual rear origin with adhesive contact then separates; placement presses/uncurls at destination. No disappearing original and unrelated replacement jump. Reduced motion retains direct carry and clear state. | Geometry/state happy/unhappy tests, actual frame sequence from rear lift through relocation/press, with keyboard alternative. |
| Responsive reveal | Pointer-driven pack and liner movement tracks input directly, with a short interruptible release settle. Profile actual behavior with Chrome DevTools MCP before and after; separate sluggish spring timing from scripting/layout/render/GPU costs. | Matched DevTools traces and analysis with source, viewport, browser/tier/gesture metadata, before/after input latency/frame timing and attribution. Do not claim device-wide smoothness from a single metric. |
| Pull the real pack | Grab visible lip/pack or liner edge and physically pull through tease/raised/open/retract states; no giant click-only “slide” CTA masquerading as a gesture. Pointer and touch work with threshold, capture loss, reversal and interruption; keyboard buttons use truthful action labels. | Actual native pointer/touch browser pulls, short/long/reversed/cancelled pulls and hit/render alignment; screenshot/video of intermediate position. |
| Move and return placed art | Grab existing placed sticker, move to arbitrary valid rear position, or drop onto its own collection/seat to return. Preserve width/rotation and pickup offset; no duplicate visible ghost. Invalid drop/cancel/failure restores original pose. Persistence survives reload and stale requests cannot overwrite new intent or session. | State/API integration tests plus native place→grab→move→reload→grab→return→reload; invalid drop, lost capture, failure/conflict, rapid supersession, touch and keyboard equivalents. |
| Better sync status | A compact, readable notice belongs near collection context, does not cover the iPod/pack or dominate the page. Copy states useful outcome and partial-import limitation honestly without technical “sample” jargon as the headline. Failure remains actionable; no busy repeated announcements. | Status tests and actual desktop/375px partial/error/loading/complete captures, focus and announcement inspection. |

## Canonical boundaries inspected

Reuse packages/stickers/src/index.ts StickerInventory, StickerPack, StickerPlacement and isStickerPlacement; positions retain shared safe bounds and server placement revision. apps/web/src/sticker-runtime.ts already has serialized placeSticker/removeSticker writes and inventory publication. packages/state/src/stickers.ts owns shared inventory/status/interaction action atoms; any interaction extension belongs in this shared contract, never component-only useState. packages/device/src/sticker-contract.ts defines StickerPackVisual, DeviceStickerScene and StickerRearProjection; renderer/controller must share projection and gesture semantics. Current projection only maps pointer to rear coordinates; any reverse projection/hit contract must be researched and specified before duplication. app-level presentation may extend these contracts narrowly while server ownership remains authoritative.

## Literature and skill packet

All agents read this full scope and the designer brief before coding. Read app-sticker-handover.md for immutable print/alpha and material invariants, current tactile material diary for geometry/coat ownership, latest packet evidence for normal-scale material reference, and current placement/runtime/state files for canonical persistence. Canonical library research uses /Users/vinicius/code/.better-coding-agents/resources/ and pinned installed sources for differences. UI work uses Modern Web Guidance first via bunx, Interface Craft/storyboard/critique, Interface Design Guardrails and all four resources, Neuve Motion/reduced-motion, Jotai/global patterns. Three/R3F physical deformation and existing lighting override foreign CSS-only/platform branding rules. Reviewer additionally uses strict-critique/runtime-review and acts as a senior product designer and engineer.

## Dispatch and ownership

Designer collection_engineer owns direct-manipulation-design-brief.md and, only through Pencil MCP, optional appended storyboard in the existing stickers.pen. No source changes before brief. Profile engineer flick_engineer owns evidence/direct-manipulation/performance/ and diaries/direct-manipulation-performance.md during baseline capture; owns the profiling browser slot, no implementation edits yet. Independent tactile_runtime_reviewer owns reviews/direct-manipulation-review.md and prepares acceptance/review against all six requirements without editing source. Lead owns this scope, coordination, handover and commits. Implementation file partition and rendering/app boundary changes will be logged after designer/profile research; shared files require sequential handoff, no concurrent edits/builds.

## Gates, artifacts and completion

Expected source changes are within apps/web sticker collection/interaction/motion/runtime/projection integration, packages/state interaction contract and packages/device sticker surface/scene helpers, plus meaningful tests in their existing suites. No cert/env reads, production data mutation, new proof-only UI/API, regenerated approved art, extra renderer/light rig, permanent animation loops or useState. Only owned browser/test processes can be closed. Test synthetic upstream Apple/signing and temporary SQLite through existing native Start route, not live credentials.

Commands: bun run typecheck; bunx --bun eslint with every changed source/test path; targeted bun test suites named in implementation handoff; bun run --cwd apps/web build; WEBPOD_STICKER_EVIDENCE_DIR=<owned evidence path> bun test apps/web/scripts/sticker-browser.integration.test.ts; canonical bun test apps/web/scripts/dev-startup.integration.test.ts after integrated source freeze; git diff --check. Extend actual native browser suite to exercise requested gestures rather than proving only old click behavior. DevTools MCP trace is a required additional profiling deliverable, not substituted by the browser test or static source reasoning. All new non-obvious public/lifecycle functions need concise invariant comments and no unjustified type escapes.

Designer brief→app/render implementation after ownership assignment; baseline profile→targeted performance correction; integrated freeze→independent product/runtime review→fixes/review→lead sanity→coherent commits→clean working tree. Any Critical/Major or missing explicit requirement blocks completion. Evidence: evidence/direct-manipulation/{browser,performance}/; diaries/direct-manipulation-{collection,performance}.md; reviews/direct-manipulation-review.md; direct-manipulation-handover.md. Commit intent: interaction/readiness/status changes with tests, profile-supported rendering correction with tests, then design/docs/evidence. Keep honest limitations and exact source fingerprints; previous goal approval does not imply this one is satisfied.

## Implementation dispatch after briefing

The designer briefing is delivered and independently checked before source changes. Reviewer refinements are binding: partial import plus historical ownership must not falsely announce a first/usable pack before actual artwork readiness; transparent corners of placed artwork must not steal blank-rear flick gestures. Reuse catalogue alpha/visible bounds and renderer hit semantics, not a bounding rectangle alone.

Collection engineer is the single owner of app collection/controller/readiness/status/native tests, packages/state interaction extensions, packages/device sticker-contract, StickerPackScene, StickerSurface and projection/placed-hit integration. Read full designer brief and review contract first. Implement the six functional requirements with coherent carry origin and persistence state, rather than parallel disconnected patches. Profiling continues against the isolated baseline build: initial recorded animation-frame stall is 146ms, including about 130ms in the render callback, while INP is 56ms; cold/warm attribution is pending. No geometry or shader optimization is authorized solely by that initial attribution. Profiler remains source-read-only until proposing non-overlapping helper/cache work or handing scene changes to the collection owner. Single browser/build validation slot must be handed off explicitly; no concurrent changes to shared files. Owned immutable baseline build may continue profiling while the app owner edits working source, provided its source identity stays fixed.

Baseline profile handoff: three warm cycles have roughly 60Hz cadence without an interaction-window long task; the initial render stall is first-use work. Four sheet image requests start about 36ms after open input; a separate fresh 375px context observes texture uploads and a newly linked shader program. The 130ms desktop callback cannot be exclusively apportioned to compilation/upload/driver with the available trace export limits. Prepare the active five visual assets/materials before admission with pinned-source supported renderer APIs, retain bounded subscriptions and use perceptual phase completion for close rather than a long invisible settle. These product changes stay with the single collection owner; no separate profiler source edits are needed. Profiler owns matched post-change DevTools MCP capture and honest before/after attribution. MCP recording works, but its configured workspace policy rejects requested trace file exports; preserve tool summaries, frame/input measurements and the exact limitation rather than bypassing it or claiming a saved raw trace.

Bounded helper handoff during implementation: the collection owner identified disposal during compileAsync as a lifecycle risk and requested parallel assistance. The profile/material engineer may now create only packages/device/src/sticker-program-preparation.ts and its test, negotiating the API with the collection owner before edits. That helper must keep compilation resource ownership safe across unmount/cancel, report readiness only for the current completed preparation and release every owned resource without an orphaned rejection or permanent polling loop. The collection owner retains StickerPackScene integration and all other source. Independent reviewer verifies this helper; baseline profiling evidence remains immutable, and final profiling must use the integrated frozen source. This narrow handoff supersedes the no-profiler-source-edits statement only for these two new files.

First integrated checkpoint (not final acceptance): native183 assertions, types/lint and27 focused tests passed on source2c117a567e1fe08ef6426c5af726f17543f1a6a951255ff01965360a645d357c/380. Independent source review then rejected final-contact snapping that made the sticking peel spring visually ineffective, and coupling of existing-rear controls/animation completion to unavailable pack artwork. Both are mandatory corrections. The profiler retained an immutable candidate snapshot but recorded no after trace and closed its fixture server; source and browser/build slots returned to the collection owner. Final after measurement must use the corrected freeze. Intermediate counts and captures remain attributed evidence, not proof that all six requirements are complete.

## Final delivery disposition

Independent review approves all six requirements for source `58fa8839f150a8fe61175ca416d58ee442fd421b0801b9bd05a4bbca7387c261` /380 files. Native flows, canonical dev startup, scoped tests/lint/types and actual DevTools MCP measurements passed within the limitations in the final review. Root typecheck passed 12/12 projects. The idle endpoint evidence gap is closed by explicitly labeled transcription of the exact earlier MCP responses, independently inspected.

Source commits: `4a01b56` (bounded program preparation) and `5cc196c` (direct sticker manipulation). Final handover, review, diaries and evidence accompany the documentation commit. Owned test resources were cleaned; no live Apple login was required and the owner’s authenticated tab was preserved.
