# Review: sticker surface, shared state and asset distribution

## Current verdict: APPROVE — surface and mocked-API integration checkpoint

See the final re-review below. Initial findings are retained as review history. This is not approval of the deferred live session/authentication/Start route integration or the complete feature.

## Initial verdict: REQUEST_CHANGES

Checkpoint review, 2026-09-06, changes after 8507a63 including untracked source. UI integration/evidence is still in progress; this is not final integrated approval.

### Findings

- **[MAJOR] A transient artwork load failure leaves an equipped sticker permanently invisible for its mounted lifetime, without recovery feedback.** `packages/device/src/sticker-textures.ts:25–28` publishes failure but leaves `started=true`; subsequent subscribers at `:18` cannot start another request and inventory refresh does not unmount an equipped `stickerId` key. `StickerSurface.tsx` renders nothing when texture is null. The intended error callback is omitted entirely at `apps/web/src/production-device-view.tsx:174`. Trigger: fail one sticker PNG request during initial saved-inventory load, then restore network and retry/refresh collection. Result: user still owns and has equipped the item, but it is absent and there is neither an artwork error nor a retry path. D6 error recovery and D7 lifecycle requirements are not met. Provide an explicit bounded retry mechanism that works with live shared subscribers, wire error/retry into the production collection, and test first failure → retry success, duplicate subscribers, late completion after final unmount, and disposal of superseded resources. Do not introduce an endless retry loop.

- **[MAJOR, acceptance evidence pending] Required production material and lifecycle proof is not yet supplied.** `docs/workstreams/015-listening-sticker-collection/evidence/surface/checks.md:8` explicitly defers real WebGL lifecycle/visual checks. Geometry tests cover cap placement and pure curl but do not cover the texture subscriber cache. The initial actual `/` screenshot `apps/web/tests/test-results/sticker-collection.e2e.ts--33140-ad-through-server-inventory/02-pack-open.png` shows a flat, front-facing Night Shift in a wrapper; it cannot prove alpha-gated sheen under oblique lighting, peel/landing continuity, context recovery, or demand rest. D6/D7 and scoped DoD require real pointer peel/cancel, mobile and reduced-motion equivalents, dark/bright/ivory flat-versus-finished comparison, context loss/recovery and rest evidence. Complete the already-scoped evidence and re-review it; no synthetic proof route is needed. UI engineer is actively producing this, so it is an outstanding gate rather than a claim that they failed completed work.

### Cross-lane check

Independent `bunx --bun tsc --noEmit -p apps/web/tsconfig.json` failed at `apps/web/src/sticker-runtime.test.ts:41` and `:56`: mock function is not assignable to `typeof fetch` because Bun's `preconnect` property is absent (TS2345). Relayed to lead/UI engineer; app files are outside this reviewer's implementation ownership. Recheck before integrated approval.

### Correctness check and verified evidence

- Sources: review-system-prompt, implementation-scope, implementation-decisions, app-sticker-handover, surface-research, backend-research, surface diary/checks. Enforced D1–D9 as applicable; no separate repository `docs/decisions.md` or `docs/platform_decisions.md` exists. Did not inspect unrelated workstreams. No Neuve/Kanban applies per repo law.
- Loaded strict-critique, team-orchestration/review-protocol, modern-web-guidance FIRST, Interface Craft storyboard and critique, Interface Design Guardrails and four resources, Neuve Motion tokens/principles/reduced-motion, Jotai, Web Interface Guidelines, React performance, global-patterns and its global/Jotai source documents. Ran modern-web-guidance search and retrieved accessibility/background-processing. Fetched current primary Web Interface Guidelines.
- Canonical versions verified: Three 0.185.1, R3F 9.7.0. Installed `packages/device/node_modules/three/src/renderers/shaders/ShaderLib/meshphysical.glsl.js:171–174,210–212` executes map alpha/test before clearcoat composition. `ShaderChunk/alphamap_fragment.glsl.js:4` samples green; implementation correctly uses RGBA map and does not misuse it as alphaMap. Print/backing shader shares print UV and alpha. `packages/device/node_modules/@react-three/fiber/dist/events-156d8d12.esm.js:15185–15237` confirms reconciler disposal and `dispose={null}` semantics. Borrowed geometry/maps and explicit materials have identifiable owners.
- User reference root `/Users/vinicius/code/.better-coding-agents/resources`: inspected `jotai/src/vanilla/atom.ts:83–126` (canonical atom read/write construction). Three/R3F are grounded in installed source because the research located no reference checkout for them. Shared placement validation lives in `packages/stickers/src/index.ts`; state admits through that same validator, retains stable IDs and uses external Jotai actions.
- Model attachment is beneath `ViewerLitDeviceFrame`; screen-anchored pack remains a sibling under the same Canvas/lights. Rear projection reverses model X and uses actual rear mesh on shoulders. Crop comes from threshold16 exclusive visible bounds. Peel changes geometry and preserves UVs, rather than replacing deformation with a static decal.
- Independently ran `bun test packages/device/src/sticker-surface.test.ts packages/state/src/stickers.test.ts scripts/sticker-assets.test.ts`: **7 passed, 0 failed, 117328 assertions**. Tests cover 60 assets decoding and byte identity at public destinations, cap positions/UVs, rear handedness, off-body rejection, actual peel deformation/fixed contact, deterministic roughness, external store subscription/equip replacement/reset and rejected unearned placements.
- Independent device/state/composite TypeScript checks **passed**. Scoped ESLint covering new surface/state/assets and modified Device/Canvas/Composite/Vite files **passed**. No certification of final production URL HTTP delivery/build is made from the file-copy test.
- New source changes are coherent surface/state/asset slices and avoid credential/encrypted artwork access. Artwork is copied only after manifest path/hash validation.

### Initial visual critique (not final material approval)

Context: a playful listening reward on a physical music player. The rear iPod is the dominant object; the small bottom pack introduces a clear next action. Initial capture shows the `Stick Night Shift` label overlapping artwork and Reset view/Settings occupying the pack bottom. UI engineer reports these collisions are being corrected; final screenshot must verify that. The print is legible but its flat view supplies little evidence of laminate or a tactile edge. Refine and judge during oblique peel frames, not by arbitrarily strengthening coat values. Palette and existing light rig remain coherent with webPod; Neuve-specific colors/marketing rules are inapplicable under the scoped decision.

Provisional facets from the reviewed capture/source: tactile 2/5 (no motion proof yet), legible 3/5 (label collision), cohesive 4/5 (same object/light rig), responsive 3/5 (pure deformation exists; browser timing unverified), accessible 3/5 (semantic keyboard actions present; final browser parity still required). These are critique notes, not owner approval.

### Re-review: explicit texture retry patch

Verdict remains **REQUEST_CHANGES** for the scoped browser/material evidence gate; the original resource implementation defect is resolved in source, pending integrated browser confirmation.

Independently inspected `sticker-texture-cache.ts`, its four tests, `sticker-textures.ts`, `StickerSurface.tsx`, `StickerPackScene.tsx`, exported contract and current app wiring. The cache now deduplicates live URL requests, retries only failed entries explicitly, publishes loading/ready/error to shared subscribers, checks entry identity and generation, and disposes stale late completion resources. No automatic retry timers are introduced. ProductionDeviceView now forwards ready/error callbacks; StickerCollection exposes Retry artwork. The failed-PNG → live retry → visible artwork path still needs the actual `/` browser check.

Independently reran the four scoped suites: **11 passed, 0 failed, 117353 assertions**. Independently reran device TypeScript and ESLint over the new cache/test and renderer changes: **passed**. The tests directly cover first failure/retry success with two subscribers, one request while pending, no autonomous retries after repeated failure, last-subscriber disposal, late completion after unmount and superseded request completion. This replaces the former cache-test evidence gap.

Lead reports successful full production build and matching SHA256 for all sixty `apps/web/dist/client/stickers/playworn` assets. This is attributed lead evidence; this reviewer independently verified source/public-copy tests, not that build invocation. Final browser/material captures are not yet present under the durable interaction evidence path at this checkpoint. No whole-lane approval is implied.

### Final surface re-review

**APPROVE for the implemented surface, asset/state seam and production route exercised with mocked sticker APIs. No remaining Critical/Major finding in this bounded lane.** Live identity, server sessions and Start API integration are outside this approval and still deferred.

The original texture failure finding is resolved: source cache retry/disposal tests pass and the production browser flow fails the first PNG with 503, invokes Retry artwork, successfully loads on the second request, and removes its error. Multi-asset failures now remain separate in app state. Both print and peeled backing use the same alpha UV; source shader ordering plus rear/oblique captures show restrained coat with no visible rectangular reflection around the die-cut artwork.

Independently inspected the durable dark Night Shift, bright On Repeat and ivory Off Beat rear/oblique base/finished image pairs, partial-peel/open/stuck frames and 375px reduced-motion result. Artwork identity/crop/palette is retained between calibration modes. The partial peel visibly bends/compresses the lifted top while retaining the contact region. Final controls no longer overlap the printed art. These support a tactile implementation; a still image is not a claim of human approval of feel.

Independent browser run of both interaction tests **passed** on snapshot `7f90f8801d9c172987e2f2d24ae1546617d2a2c9988dd855c7887b2fd8a71e1c`. Its material test timed out waiting five seconds for artwork before provider bootstrap had finished: the trace contains developer-token 200 but no sticker session request. Preserved mocked-only trace: `evidence/surface/material-combined-readiness-failure.trace.zip`. The test now explicitly awaits successful session bootstrap before evaluating asset loading; it does not treat GPU tier readiness as authentication readiness.

Independently reran corrected `bunx --bun playwright test --config apps/web/tests/playwright.config.ts apps/web/tests/sticker-material.e2e.ts`: **1 passed (26.7s)** on final source fingerprint `e714a9f35b3ec45a82e7f3520356de66d95b0a85acdfaac8425195232e8ba190` (279 browser files). This verifies three actual PNG responses, both material calibration poses, real WEBGL_lose_context, restoration and unchanged actual GPU draw counts over 700ms at rest before/after recovery. Final implementer combined run on that same fingerprint also passed all **3 browser tests (58.7s)**. Durable `material-runtime.json` from that combined run records 991→991 and 1123→1123 draw counts and three loaded artworks.

The original white/broken-image context-loss presentation came from unchanged pre-sticker Composite behavior, verified against baseline. The subsequently scoped bounded status fix now shows “Restoring device view…” while retaining the Canvas, removes unavailable sticker controls and cancels their gesture ownership; recovery removes the status and restores the controls/art. Inspected updated `05-context-lost-fallback.png` and `06-context-restored.png`. This establishes the scoped recovery behavior, not a general T4 renderer or perfect pre/post-loss device-environment parity (the existing right shell wall appears darker after recovery).

Fresh independent app/composite TypeScript and scoped lint for context/interaction/test edits **passed**. Final source review and F1–F3 closure are recorded separately in final-implementation-review.md. Source/package tests, pinned-source inspection, production build attribution and sixty-asset checks above remain applicable.

Final critique facets for this bounded checkpoint: tactile **3/5** (real peel and restrained coat, owner feel judgment remains), legible **4/5** (three palettes and mobile text retain identity), cohesive **4/5** (same renderer/lights/body), responsive **4/5** (live pointer and actual GPU rest evidence), accessible **4/5** (keyboard placement, cancellation, reduced motion and restoring status exercised). No subjective owner approval is implied.
