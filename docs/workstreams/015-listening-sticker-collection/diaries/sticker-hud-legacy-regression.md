# Legacy collection regression diagnosis

2026-09-06. Read the actual saved combined log `/tmp/webpod-editor-final-native.log` after the UI owner supplied its path. Preserved it at `evidence/sticker-hud/legacy-diagnosis/rejected-editor-combined.log`. The editor and restoration tests passed; the collection test reached its whole 120-second budget. The log contains no per-operation call stack. Its final saved screenshot is mobile-keyboard-stuck.png, so the earliest unproven operation is the following placed-seat click. The later Return to sheet click and successful placement-response wait are also unproven. Current product source still contains that control; a removed-label explanation is unsupported. The screenshot and global timeout do not prove process contention or a product regression.

The UI owner explicitly handed over only the legacy test file. Added durable elapsed-time checkpoints around this cleanup sequence and bounded locator/response waits, retaining the real click, successful PUT and all later recovery assertions. Promise.all registers the response before clicking and observes failures of either promise. No timeout increased; the overall120-second budget remains. No semantic assertion removed and no product source edited. Scoped test-file ESLint passes. No browser or build was run because the HUD owner retains that resource slot. A subsequent authorized run must establish the exact failing await before any fixture or product correction is claimed.

The authorized current-build diagnostic passed 188 assertions in35.97seconds, including its end-of-run source fingerprint check and native teardown. Captured cleanup timestamps: keyboard placement painted31785ms, seat click31797ms, return control visible31798ms, successful return PUT31844ms, pack closed31882ms, same-device sign-in32290ms. The actual return sequence completed97ms after the screenshot; it did not stall. Full output and native metadata are retained under evidence/sticker-hud/legacy-diagnosis/current-native.log and run/browser-verification.json, with per-step data in run/mobile-keyboard-cleanup.json.

The old combined-run timeout did not reproduce on the current HUD build. This establishes the current regression pass, not the historical cause. No label mismatch, transient contention or product correction is claimed. No timeout increased or product source changed. All owned native resources closed through the successful test finally; the browser/build slot was returned to the UI owner immediately after completion.
