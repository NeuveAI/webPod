W4 — D-067 shared enclosure geometry
====================================
Captured 2026-08-29 · bun 1.4.0

Authoritative saved Pencil component (read through Pencil MCP):
  VWaJS body              330 × 552
  VWaJS enclosure corner  circular, radius 26

One typed definition:
  @webpod/tokens BODY_W                 330
  @webpod/tokens BODY_H                 552
  @webpod/tokens BODY_CORNER_R          26
  @webpod/tokens BODY_CORNER_EXPONENT   2

`packages/device/src/layout.ts` imports those four values into
`DEVICE_LAYOUT.body`. `Device.tsx` uses that object for the steel shell, back
plate, polycarbonate front, and CSS shadow. `luminance-probe.ts` uses the same
object to derive every body/back target. There is no renderer-local enclosure
constant.

Probe admission:
  body/back perimeter inset     3 body px
  wheel/select recess inset     6 body px
  model transform               live DEVICE_MODEL_NAME world matrix
  back target frame             viewer 168deg -> inverse x -> model local
  light frame                   viewer/world siblings; only model rotates
  identity gate                 first visible object + material must match
  transparent exception         wheel label texel only, alpha proven zero
  failure mode                  throw; no framebuffer sample is admitted

Occlusion correction:
  The former steel shell had a coplanar cap over the complete polycarbonate
  face. The old identity search could skip it and approve the hidden body. The
  shell is now an analytic perimeter frame with a real central hole, and the
  first-hit test plants a nearer unnamed occluder to prove calibration rejects
  it.

Bounded smooth front geometry:
  body crown                    -2.125 body px
  equation                      crown * (1 - (y / halfHeight)^2)
  stop-local controls           none (all optical profiles remain flat)

Tests:
  bun test packages/device/src/layout.test.ts \
    packages/device/src/luminance-probe.test.ts \
    packages/device/src/probe-raycast.test.ts \
    packages/device/src/ViewerLitDeviceFrame.test.tsx \
    packages/device/src/shapes.test.ts \
    packages/device/src/curved-shell.test.ts \
    packages/tokens/src/geometry.test.ts

Result:
  focused device tests          56 passed, 0 failed
  package/web typechecks        passed
  repo typecheck                11/11 projects clean
  repo lint/build               passed
  repo tests                    875 passed, 0 failed
  automated gates               16 passed, 0 failed
  manual gates                  U14 and U15 outstanding

The D-067 tests assert the literal 330×552 / 26 / 2 contract, the analytic
circular corner, full straight-side width, shared layout values, expected mesh
and material identity on every target, rejection of off-surface hits, and the
safe body margin against the same silhouette equation used by the probe.

Superseded evidence:
  The former 33px / n=4.2 geometry record and the 8/43 and 32/43 luminance
  reports are invalid under D-067. They did not grade the geometry the renderer
  displayed and had no mesh/material identity pass.
