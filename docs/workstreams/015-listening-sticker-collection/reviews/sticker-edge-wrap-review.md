# Independent sticker edge-wrap review

## Verdict: PENDING — actual shell contract and implementation required

Read `sticker-edge-wrap-scope.md` fully. The new owner request supersedes inset-safe placement; prior contour acceptance does not authorize clipping, flat overflow, floating vinyl or merely expanded clamps. Reviewer owns source/design/runtime assessment without implementation edits. Existing unrelated concurrent work remains outside this review.

Reusing the previously loaded Interface Craft critique/storyboard, all Guardrails resources, Neuve Motion, iOS HIG including glass/accessibility, runtime/global/Jotai and domain references. Actual shell dimensions and pinned library sources must ground the new contract. No browser/build competition during investigation.

## Existing seam risks sent to both engineers before integration

1. `packages/stickers/src/index.ts` currently checks the rotated occupied rectangle against inset bounds. The replacement must validate the center against the actual rounded rear domain, not silently accept empty bounding-box corners. Legacy normalized body-space center/size interpretation must remain stable or have explicit compatibility mapping. Ownership, revision, finite values and catalogue constraints remain enforced.
2. `packages/device/src/sticker-surface.ts` raycasts only in +Z and rejects non-rear normals or missed rays. True wrap must continue onto actual shoulder/side/front geometry. No ray miss fallback to a flat invisible plane is acceptable. The contract must account for maximum rotated catalogue artwork at corners and its remaining arc length after traversing the narrow side.
3. Existing rear peel adds flat XY/-Z deformation to conformed contact vertices. Applied unchanged to side/front wrapping, it can move attached vinyl through the shell. Detachment needs a continuous wrap-aware source/contact path, preserved UV/clean backing and a physically clear transfer to the free carried print.
4. Current contour interpolation assumes the production uniform 24-grid topology. Adaptive wrap tessellation must provide matching UV-triangle sampling for contour and hit testing; retaining old indexing would detach controls from artwork. Front/back alpha damage must stay identical.
5. The captured editing plane explicitly is not an inverse of the curved shoulder. Wrapped-source pickup must retain the actual grab point and avoid a first-frame jump. Near-sideways projection must reject degenerate transformations coherently. Hidden side/front artwork and controls must not be selectable through the shell; projected outline cannot reveal an occluded hidden contour as an x-ray.

## Independent proof plan

Before source approval, inspect the shared shell mapping contract, real geometry boundaries, seams/corners, distortion and normal-offset definition. Establish how center coordinates map to rear perimeter and how overflow reaches adjacent front surfaces without stretching/clipping. Review geometry/domain ownership handoff and legacy placement preservation.

After a coherent freeze: deterministic all-edge/corner/angle/aspect/size cases, finite values, winding/normal continuity and penetration checks against the actual shell; shared validation/persistence and malformed/ownership/revision cases. Inspect native desktop and 375px images/video of irregular and near-rectangular artwork across edges/corners from rear and oblique/side views. Exercise edge drag/rotation/reset, HUD and alpha hit alignment, worn peel/return/re-stick/reload plus cancellation/conflict/failure. Confirm unchanged UV appearance and no hidden overflow or flat illusion. Final validation must be tied to exact scoped source provenance under ongoing external changes.

No final fidelity or runtime approval is implied by this preparatory contract review.

## Shared contract checkpoint

Read `sticker-edge-wrap-contract.md` fully. **Approved for bounded geometry/domain implementation**, not final numeric or visual acceptance. The actual 330×552 shell, r26 rounded center predicate and retained nominal body-space saved coordinates address the legacy-position/domain concern. The latent arc-length inverse must remain an internal mapping: persisting it as normalized x/y would silently reinterpret existing placement semantics.

Device-centered radial meridians avoid the proposed nearest-corner-normal convergence problem, but their cross-meridian metric remains unproven. Require complete-print positive Jacobians and measured principal stretch/area ratios across maximum catalogue sizes, off-axis corners and front continuation; arc-length preservation along a single meridian is insufficient. Front sampling must use the actual outer envelope, including covering surfaces, rather than the deepest ray hit or a fictional planar cap. Lead-authorized small film bridges need actual support endpoints and bounded spans.

Indexed UV sampling and occluded HUD segmentation correctly replace the old 24-grid assumptions. Wrapped pickup keeps actual UV/physical grab point. Partial peel transitions along surface normals/tangents are a reasonable candidate, but outward interpolation alone does not establish shell clearance around front edges: swept intermediate geometry must be checked. These prerequisites were sent to both lead and geometry engineer before implementation. No concrete fatal contract contradiction prevents this measured candidate from proceeding.

## Independent domain/app checkpoint

Reviewed the pure rounded-center predicate, placement validator, editor constraint/reset delta, HTTP/repository boundaries and meaningful new tests. The shared predicate uses canonical body width/height/radius, excludes empty bounding-box corners and out-of-range/nonfinite centers, with only 1e-10 squared model-unit seam tolerance. Width, angle, wear, known IDs, count, ownership and revision guards remain. All client preview/runtime, HTTP and repository paths use the same validator. Legacy normalized centers are neither translated nor re-encoded. Editor reset now fully straightens at the same valid edge center and clears added wear, rather than preserving an obsolete inset-limited angle.

Independently ran placement/appearance/editor/repository/live tests; result and exact counts are retained in `evidence/sticker-edge-wrap/reviewer/domain-tests.log`. The edge persistence test closes/reopens a real SQLite file, preserves worn rotated maximum-width placement, rejects stale revisions/foreign ownership/duplicates/off-shell corners without revision drift, then accepts unchanged legacy body-space coordinates. Catalogue/perimeter/size/angle validation tests establish admissibility only, not geometric support. No blocking issue in this bounded domain delta. Actual wrap sampler and native gesture integration remain pending; the widened validator cannot ship independently of that renderer.

## Early isolated atlas source review — corrections pending

Read candidate `sticker-wrap.ts` and its nominal-front test. Cache is bounded to 512 angular profiles per surface; triangle buckets retain CPU coordinates and borrow source resources, with release delegated to the eventual shell owner. Integration must establish that lazy profile construction is prepared before interactive wrapping and that cache lifetime follows shell identity. Nominal beveled front alone does not establish actual glass/wheel support.

Concrete source issues sent directly to geometry owner and lead:

- Lines 67–72 measure a missing interval from its first missing sample rather than the previous real endpoint. The stated ≤12 bridge can therefore span approximately 12.5 radial units, and axial endpoint distance is not checked. Use actual support endpoints and define/test the bound's metric.
- Lines 87–90 switch from exact cap radius to interpolation between neighboring meridian radii. These are not generally identical at an intermediate angle, giving a small cap seam discontinuity; quantify/correct before claiming continuous derivatives or normals.
- Interpolation between front endpoints at .5 radial spacing and 512 angular samples may cross a raised overlay rather than follow its outer envelope. Actual shell clearance checks must cover segments/triangles, not merely outermost sampled endpoints.
- Current test name says “without reversed local Jacobian” but checks only finite coordinates and unit normals. A normalized inward or inverted result also passes; signed orientation and singular-value measurements remain necessary and were already planned by the owner.

These are early candidate findings, not claims that the unfinished integration was presented as complete. No visual acceptance or heavy/browser run occurred at this checkpoint.

## Corrected atlas and independent distortion distribution

Re-read corrections: missing support now uses actual 3D endpoints; exact triangle-plane meridian vertices replace radial .5-step chords; exact-angle cap correction removes the position jump. Angular interpolation, overlay clearance and derivative continuity still require actual-resource validation.

Independently instrumented the nominal fixture without editing product/tests, retained at `evidence/sticker-edge-wrap/reviewer/atlas-area-distribution.test.ts` and `.log`. For maximum PW-C03 across three anchors/angles, **1,204 of 9,216 sampled triangles (13.1%) have area ratio below .5**. Minimum .15948 is on the far front at approximately (51,-6.65,28.03), side center (165,0), rotation45°, near the artwork's upper-right edge. Median .824; fifth percentile .364; maximum1.682 occurs on the corner roll. This is broad chart compression, not merely a seam outlier. Fixture samples one triangle per cell; both halves and principal singular values remain necessary.

**Current distortion is not accepted as physically credible vinyl from positive Jacobians alone.** A bounded corrective candidate was sent to the geometry owner: retain meridian arc depth and actual envelope, but express per-sticker tangential material distance along physical cross-meridian arc length at that depth, solving angular coordinate instead of retaining fixed global angular spacing that converges toward front center. Preserve the center tangent metric and prove injectivity/clearance after adjustment. A normal-size glyph render must complement these metrics; no front-design expansion or clipping workaround is requested.

## Cross-meridian correction diagnosis

Independently reproduced the first corrected-chart failure in `reviewer/cross-meridian-failure.log`: side center (165,0), rotation90°, col1/row0, flat-front triangle signed test -2.47884. One finite-difference normal points inward while adjacent normals point outward. This stencil is not near the ±π unwrap seam. The eight-step multiplicative arc solve lacked a residual guarantee, and moving quadrature samples can amplify derivative error; recommended bracketed inversion before changing the surface chart again.

The geometry owner replaced that iteration with bisection before the reviewer's isolated probe snapshot. `reviewer/solver-probe.ts`, `.test.ts` and `solver-residual.log` explicitly represent this newer candidate: residuals at the previously failing stencil are approximately 10^-7–10^-6 model units. The owner now tests both triangle halves and reports principal stretches. Area improves (fifth percentile .728, median .999), but the minimum-area far-front corner case has principal stretches .174×2.367, with overall .169–3.057. That is substantial anisotropic shear despite improved area; final material credibility remains pending.

Recommended a bounded real glyph diagnostic at the exact worst case before more mapping complexity. If necessary, adjust longitudinal depth jointly with angle under center/pose constraints and a positive-Jacobian barrier; further scalar angular gain alone cannot remove cross-depth shear. These are corrective options, not untested claims of a solved mapping. Actual front resources, wrap-aware peel and visible fidelity remain unapproved.

## First actual shell/glyph diagnostic — REQUEST CHANGES

Independently viewed `early/static-1/max-PW-C03-0-{rear,side,front}.png`. Side view turns the print into a long, tapering diagonal banner. Front view shows strongly sheared lettering in a wedge over the top-right screen. The distortion is visible at ordinary viewing size and supports the principal-strain concern; it is not merely benign foreshortening. A small rear fragment at a perimeter-centered placement is expected and does not excuse the adjoining-surface result.

Reject this candidate's material fidelity. Preserve full wrapping and actual outer-shell contact; do not hide the defect by clipping, reducing allowed size, changing UV art or refusing the edge. Proposed bounded experiment: jointly relax longitudinal depth and tangential angle from the positive atlas, minimizing material edge/diagonal-length and angle strain with fixed center/orientation, exact surface sampling and a signed-Jacobian barrier. Evaluate the exact failing glyph first before integrating a broader solver or claiming convergence.

Implementer reports the static diagnostic emitted its captures but failed final aggregate provenance under concurrent writes. It is useful rejected diagnostic evidence, not a passing native gate. No interaction/peel acceptance is claimed from these static images.

## Coupled solver and concurrent provenance proposals

Approved the lead-authorized isolated 17×17 coupled theta/depth experiment for rejected maximum C03 corner and ordinary Pulse edge. Normalize edge/diagonal strain by material rest length, fix center/orientation in the actual local tangent frame, evaluate both cell triangles, and retain signed barriers/backtracking. Original alpha defines occupied material; added wear cannot excuse excessive strain. Full mesh must remain noninverted/nonintersecting. Proposed occupied p95 .85–1.15 and absolute .65–1.5 principal stretch ranges are experimental targets, not assumed feasibility. Coarse-grid vertex contact does not prove triangle clearance over the tight shoulder: refined surface-conforming rendering and matching UV interpolation remain necessary. Report stalled iterations/residual/worst locations and actual glyph result before acceptance.

The proposed test-only concurrent-source provenance gate is acceptable in principle with strict limits: full source manifests must match across build; all built artifact hashes remain immutable across test; test/build helper and dependency/config/lock changes cannot be exempted. After build, only exact lead-approved external paths may differ, with additions/deletions/renames individually approved, every delta retained and rationale recorded. This tests the immutable built snapshot, not behavior of later allowlisted source changes—even an external orientation/controller file can change relevant integration. No blanket directory exceptions or universal current-checkout certification. Final gate implementation and exact allowlist remain pending review.

## Provenance implementation review

Read `sticker-edge-provenance.ts`, its tests and the native harness integration. **Approved scoped helper**: exact 30-path lead-approved list permits post-build modifications only; additions/deletions still fail. Full source scans app tests, scripts, packages, configs/manifests/lock and sticker assets; full client/server dist byte hashes remain immutable. Build source must be stable even on otherwise allowed external paths. Pretest and final reports retain all deltas and explicitly state external modifications are absent from the tested artifact. Native cleanup awaits reverse-order disposers sequentially before its final provenance check. Independently ran **4 tests / 11 assertions PASS** in `reviewer/provenance-tests.log`. No blocker found; this is immutable-artifact assurance, not approval of current external source integration.

## Coarse-contact solver caveat before slack

The coupled solver's capped convergence is not proof that physical contact is impossible. Requested confirmation that its objective distinguishes material surface-path length from endpoint chord length: 7–14-unit coarse cells span tight shoulder/bevel curvature, where shorter 3D chords are expected from bending. Treating that as strain can distort the solution. Refined local derivatives/path lengths and coarse-to-fine convergence should ground the diagnosis. A separate ≤2-model-unit outward-slack experiment is acceptable only with nonnegative localized heights, bending/contact penalties, actual gap distribution and full collision checks—not a global hovering offset. Neither seconds-per-pose solver time nor these exploratory metrics satisfy the eventual interactive runtime gate.

## Architecture reset after failed refinement

Geometry owner reports that interpolating the 17-grid solution and evaluating 33/65/129 grids reveals near-zero local stretch; a fresh fine-grid solve also failed its bounded budget. This falsifies coarse-barrier acceptance, not the physical possibility of the owner's request. A refined metric scattered to coarse chart controls would improve numerical honesty but still hard-constrains the sheet to the problematic exact-contact chart and adds expensive derivative work. Recommended stopping that branch rather than another long solve.

Approved a bounded **actual 3D sheet** experiment: solver vertices are the rendered vertices, original UV strain/shear constraints are distinct from dihedral bending, collision is unilateral against the exposed outer shell union, and localized compliant adhesion may permit bounded slack. No theta/depth remapping after solving. This follows the separation of compliant constraints in the primary [XPBD paper](https://matthias-research.github.io/pages/publications/XPBD.pdf), which supports the method choice but does not prove feasibility for this device. No new physics framework is required; pinned Three supplies geometry primitives, not an existing cloth solver.

First falsifiable gate: analytic developable cylinder at 17/33/65, showing arc-length target agreement and expected chord discretization error converging rather than demanding exact coarse chord isometry. Only then retry the rejected corner with actual exposed-surface collision, triangle-interior/edge-sweep clearance and self-intersection checks, original-alpha strain/gap distribution and convergence. A global rear-normal sign cannot judge front-wrapped orientation. Collision must not use hidden inner faces or closest-point teleportation through the body; soft adhesion should progress to contact. Pin center and local tangent orientation, not distant vertices that overconstrain bending. Bound iterations/time and stop failed probes. Eventual measured warm updates must fit the 60Hz frame budget; a suggested solver sub-budget is approximately 4ms on the actual fixture, subject to measured full-frame behavior. Seconds-per-drag is not a shipping option.

## Metric oracle and cylinder experiment checkpoint

Read `sticker-sheet-metric.ts` and its tests. The material-coordinate inverse and 3×2 deformation Gram matrix correctly yield principal stretches; the cross-product determinant avoids cancellation of the smaller eigenvalue. It explicitly does not claim orientation/collision from positive singular values. Independently ran **3 tests / 693 assertions PASS**, covering rigid/order invariance, analytic cylinder sinc contraction, nonzero sagitta penetration with contacted endpoints, and invalid triangles (`reviewer/sheet-metric-tests.log`). Approved as acceptance instrumentation.

Read and independently executed the retained actual cylinder probe in `reviewer/cylinder-sheet-probe.ts/.log`. It iterates 500 times from a deliberately perturbed analytic seed, with original edge/diagonal rest lengths, soft radial attraction and hard vertex/edge contact; solved vertices are the measured/rendered vertices. Refinement reproduces both-half principal ranges approximately .9931–1.0162, .99735–1.00535, .99855–1.00173, with shrinking angle error/gap/edge penetration. This is meaningful progress over chart interpolation and **justifies bounded actual outer-shell collider implementation next**.

Limits remain explicit: only one near-isometric seed, center pin without an explicit tangent-frame constraint (orientation comes from symmetric initialization), edge-only penetration measurement, no full triangle/self-intersection, bending or compliance-invariance proof. Add nonsymmetric initialization/frame validation and actual-shell topology/interior checks before solver approval. Cold CPU and a cylinder fixture are not interactive runtime or actual corner glyph evidence. No final geometry acceptance is implied.

## Collision primitive proposal checkpoint

Approved bounded implementation of copied actual triangles with provenance, BVH acceleration, earliest segment hit and full candidate-triangle intersection including coplanar/opposite-edge cases. This is collision instrumentation, not yet an exposed support union or adhesion strategy. Actual source transforms must be applied into common content coordinates; explicit bridge faces retain separate provenance and bounded endpoint checks. Owned BVH/arrays must be released without disposing borrowed geometry.

Concrete qualifications sent before implementation: a segment beginning outside can enter an unbridged port/seam and first hit hidden hardware, so “first hit” does not itself prove exposed adhesive support. Preserve provenance and establish outer coverage/bridge policy before classifying support. A static intersection test can miss a sheet triangle wholly inside a solid; maintain an exterior previous-state invariant with swept/containment checks, not just final intersection. Define grazing/coplanar/touch tolerance relative to the .18 film offset and test tunneling. Hidden-plane tests are necessary but not sufficient for open-assembly geometry. Final exterior collider and sheet solver acceptance remain pending.

## Collision helper and aperture-source review

Read `sticker-collision.ts/.test.ts`; independently **4 tests / 17 assertions PASS** (`reviewer/collision-tests.log`). Copied transformed coordinates, per-triangle provenance, bounded-leaf BVH, finite-segment first hit, two-way triangle edge crossings/coplanar separating axes and owned-data disposal are coherent within the explicitly documented primitive limits. Typed-byte statistics do not purport to include all BVH object overhead. The tests correctly show that earliest hit can be hidden through an opening and that wholly-inside does not mean surface intersection. No blocker in this primitive checkpoint.

Verified actual aperture sources: `hardware-apertures.ts` uses hold.depth−1 and dock.innerDepth+1, while `top-controls.ts` defines those as 17 and14.5. Thus the short X/Z opening spans are16 and15.5. Lead-approved port-specific bridging is appropriate; these must be actual polygon endpoints on `hardwareSurfaceY`, with recorded 3D span, rather than blindly equating planar depth to spatial distance or widening the anonymous12-unit seam rule. Front `crownCap` provenance supports removing known hidden negative caps; zero-tagged wall triangles still require explicit exposure/overlay classification.

App integration review also retains a necessary distinction: a visible wrapped-art hit can authorize carrying independently of rear-only packet admission. Frozen source UV/physical grab metadata and open contour spans/anchor-visibility flags are required. Hidden sectors cannot leave clickable handles through the shell; semantic accessibility must remain explicit rather than fabricating visible material anchors. Final geometry handoff is pending.

## First actual 3D nominal-shell probes — corner rejected

Read retained Pulse/C03 prototype and mesh. Pulse's reported .947–1.074 range and zero static intersections are a concrete improvement over the rejected chart, but nominal unobstructed front, sampled edge contact and cold seconds-scale solving remain diagnostic only. No swept/exterior or full-assembly approval follows.

Independently analyzed the saved C03 mesh without re-solving (`reviewer/c03-mesh-diagnosis.ts/.json`). Worst triangle125/126/142 lies beside the anchor in material coordinates x−14.44..−7.22,y−14.27..0, around rounded side/seam x158–167,y258–270,z15–18: minimum stretch .09794, area .11956 and centroid rear gap~2.103. Four triangle intersections cluster at the front-bevel/side junction; several have near-unit principal stretches despite penetration. This separates local strain collapse from missed triangle collision. The owner additionally reports worst gap15.87, exceeding the experimental2-unit slack. Corner candidate is rejected.

Prototype applies distance/frame corrections followed by soft attraction and sampled-edge pushes, without area/bend/full triangle/sweep enforcement. Final contact pushes can undo earlier strain constraints. Requested phase-by-phase residuals at the exact failed neighborhood and completion of area/bend/maxgap plus barycentric triangle/swept collision constraints; simply adding iterations or area alone is insufficient. Normal-dot sign alone is not an inversion verdict because material ordering differs from outward winding.

Discretization is also material: 16-cell C03 rest edges are7.22×14.27 units, comparable to or larger than the8.5 radial shoulder and narrow bevel features. The cylinder sagitta oracle already demonstrates why vertex/edge agreement does not prove bent triangle contact. Approved a bounded same-case comparison with local high-curvature material-triangle refinement alongside completed constraints, preserving original UV rest coordinates and rendering the actual solver vertices. Prefer local refinement over another global65-grid brute-force run. No claim that strict contact is physically impossible is supported by these incomplete/coarse probes.

## Failed constraint ordering — stop full corner cycles

Read retained `early/c03-constraints-probe.txt` after owner-reported coarse/refined failures. Phase traces improve area then lose strain during point/triangle/sweep-gap corrections. The purported gap clamp uses signed normal distance while output measures Euclidean nearest-support distance; it therefore does not enforce its own2-unit spatial bound. Its area pass restores unsigned cross-product magnitude and skips near-zero triangles, so it is not an inversion barrier. Triangle correction samples a6-grid after detecting intersection, rather than obtaining an exact contact witness; final vertex sweep is followed by another point correction that can invalidate prior clearance. These are prototype limitations, not a completed physical solver or evidence of infeasibility.

Requested stopping expensive full-corner cycles. Next causal test should use a narrow developable strip with fresh isometric supporting-plane seed, verified exterior to the whole fixture, then incremental compliant adhesion over20–50 bounded load steps. Final≤2 gap is a staged target, not an immediate hard clamp on the initially flat exterior sheet. Keep collision unilateral, log strain/contact/Euclidean-gap phase residuals and compare load-step refinement. Check duplicate hard edge/both-diagonal/area constraints: all pairwise distances constrain a quad toward its planar rest configuration and may fight bend/contact at coarse resolution. Only proceed to another corner when these phases settle coherently under a measured small update budget. A tangent plane is guaranteed exterior only for a genuinely supporting convex surface; actual assembly protrusions require clearance checks.

## Causal strip independently reproduced

Read and independently executed `early/strip-causal.txt`, preserving reviewer copy/results as `reviewer/strip-causal.ts/.json/.log`. It starts with exact flat rest metric rotated30° on an exterior cylinder tangent plane, uses only actual rendered triangle edges and a center/local-frame constraint, then progressively admits adhesion. Final50-load principal stretches reproduce .97869–1.02844, Euclidean gap1.2332 and analytic edge penetration2.75e-5, below its explicit1e-4 tolerance. Center remains exactly (0,0,26.18). All final vertices have z≥19.86, so their projected triangles cannot enclose the cylinder axis; the edge-minimum interior-distance argument is valid for this specific fixture.

**Approved bounded actual straight-edge experiment next**, before another corner. Strain/contact/gap now coexist in this small causal test. Qualification: 20/40/50 loads also increase total attraction applications and solver iterations at fixed coefficients; the result shows relaxation with more work, not equal-total-load time-step convergence or XPBD compliance invariance. Independent50-step mean was .178ms for65 vertices on an analytic collider; this is not an actual-assembly or shipped-input timing claim. General triangle collision, self-intersection, actual exposed support and corner behavior remain pending.

## Actual-shell cost and loading checkpoint — not accepted

Lead reports the first actual production-shell straight-edge probe maintains approximately .951–1.069 stretch and zero static intersections but leaves gap36.75. The tiny-strip .4-unit load cap does not cover the larger fixture's initial separation. Reported support queries consume98.6% of an8-second solve; this is a concrete measured bottleneck, not a rendering or GC attribution. No fidelity/runtime acceptance follows.

Recommended exact warm-query acceleration before approximate contact caching: cached facet/neighborhood supplies an initial best-distance upper bound, then global BVH traversal still visits every node whose lower bound could improve the result. This preserves global closest support and provenance. A local-only answer requires a geometric exclusion certificate, not a small-movement heuristic; otherwise exact fallback. Cache count must be bounded by active sheet contacts and shell identity, invalidated for resource/transform/partition replacement and disposal. Dimension-scaled adhesion should maintain bounded local steps/backtracking against strain and collision; merely raising a load cap to close the gap in50 steps is not proof. Concrete source, exact-vs-cached crossing tests and actual resource lifetime remain pending review.

## Exact warm support query review

Read revised collider/query and benchmark source; independently **6 tests / 622 assertions PASS** (`reviewer/warm-collision-tests.log`). One facet hint seeds an upper bound but does not bypass global lower-bound traversal. Stable facet-index tie-breaking makes equal-distance selection deterministic; query disposal and parent disposal reject reuse. No blocking correctness issue in this scoped helper. The benchmark clearly labels21 linear trajectory points between initial/final actual-edge vertices, not the full solver trajectory; reported zero position/distance/normal difference and approximately1.7× improvement do not establish sufficient runtime.

Proposed bounded follow-up: skip contact-only refreshes with a conservative clearance certificate. If previous exact distance to **all collision geometry** is d and displacement is bounded by delta, current distance is at least d−delta; when that exceeds film clearance, no new contact query is needed. Otherwise refresh exactly. Distance to approved adhesive-support facets alone cannot certify clearance from excluded collision geometry. Adhesion-direction queries and triangle/swept checks remain separate. Compare skips/fallbacks against the same actual recorded solver-query stream; narrow patch pruning needs equally explicit reach/distance bounds, not omitted geometry. No unproven local facet/normal-cone reuse is approved.

## Product-specific architecture after whole-sheet solver rejection

Lead stopped another whole-sheet attempt that preserved strain/static clearance but left29.53 gap after50 rejected steps and seconds of query work. Agree: a general cloth solver is not the user's requirement, and continued optimization of an ineffective loading method is not justified.

Approved investigating an exact developable strip for genuinely translationally invariant straight shell regions: physical cross-section arc length supplies the wrapping coordinate while the longitudinal edge coordinate stays unchanged. This preserves the flat material metric analytically and can update deterministically without iterative cloth. Ground the invariant interval and cross-section in actual shell resources; front overlays/bridges still require explicit exposed-surface treatment. Preserve original rear rest coordinates/rotation and keep shoulder-center inverse internal.

Do not extend the extrusion claim into rounded corners or interpolate world-space positions between strips: that can penetrate the body or distort material. A separate local corner patch or controlled physical slack must share rest coordinates, seam position/tangent and topology with neighboring strips. Only that double-curvature neighborhood should need more complex handling. Next bounded gate is ordinary Pulse actual straight-edge metric/render, then the same rejected maximum C03 corner with unchanged no-clipping/UV/fidelity/clearance requirements. No method is mandated; all corners and realistic wrapping remain mandatory before product acceptance.

## Cartesian strip source checkpoint

Read `sticker-strip.ts/.test.ts` and optional `rightStrip` sampler. Piecewise section arc length and unchanged longitudinal coordinate remove the device-centered angular mapping. Independently ran the actual rear-section round-trip/per-segment arc-length test, retained in `reviewer/strip-profile-tests.log`. Negative material distance preserves the original rear plane. This is a bounded profile helper, not proof for every shell region.

Geometry owner reports ordinary .20-width Pulse away from inserts at48 cells has principal .99121–1.00032, consistent with small chord discretization. **Approved its bounded actual-route diagnostic capture.** Maximum .35 still crosses a glass transition: refined stretch3.04 near y42 arises from different accumulated per-row step lengths. This is a real incompatibility in applying independent constant-Y arc lengths across an insert boundary, not a reason to reduce allowed size or silently stop at the edge. A local overlay transition must preserve common material coordinates before maximum-width acceptance.

Source qualifications sent: the comment claiming |y|≤90 excludes apertures is inaccurate; actual profiles reach inserts. Final sampler needs explicit finite/degenerate input handling and finite-difference boundary margins. Its256-entry exact-floating-Y cache is bounded but changed row coordinates can rebuild profiles on every pose; actual warm timing/lifetime remain unapproved. Full corner/front-overlay/peel/UI integration remains pending.

## Ordinary strip actual render and insert-transition proposal

Independently viewed `early/strip-1/max-PW-F01-0-{rear,side,front}.png`. **Accept this bounded .20-width static fidelity**: side keyboard and front title/control print retain rectangular proportions and upright lettering, with none of the prior tapered banner. Rear ink is hidden at the exact perimeter placement, consistent with its side/front position. Implementer reports stable build/artifact manifests and no deltas, source b7547fdfb7bdb644cb8aab34d15076419e7479a77310c0d99f1a5bbaba74487d/422, native374 assertions. This is implementer-owned static evidence independently inspected, not a reviewer-run general-wrap gate.

Approved the next .35 experiment: keep a common rear/side datum and front material phase across rows, then a localized ramp between actual insert support endpoints. Full ramp interior must remain outside actual crowned shell/glass; endpoint clearance alone is insufficient. Preserve original UV/rest coordinates and continuous position/tangent joins, measure refined principal strains and Euclidean support gap, and retain source endpoint/≤12 span provenance. That bound does not authorize a blanket front cover. If the actual step height cannot fit the prior experimental≤2 slack, report it rather than silently increasing tolerance. Maximum-size/corner/hit/peel and runtime approval remain pending.

## Shared-phase maximum insert checkpoint — diagnostic capture only

Read the current `sticker-insert-bridge.ts` and `rightStrip` implementation. Front XY material phase is shared across rows; the rounded-glass signed-distance band selects actual envelope endpoints and bounds their 3D span. The ramp uses the actual support outside the narrow band. This directly addresses the previously observed phase shift rather than hiding it with clipping or smaller art.

The retained trials reject spans8 and6.5 because sampled gaps exceed2. Span6 reports lifted principal stretches .956–1.153 and zero full-triangle intersections at96 cells;24 cells has207 intersections and is invalid for this maximum-size diagnostic. **Approve a bounded .35 actual capture only after the post-cap-join96-cell collision rerun is clean.** This does not accept the geometry, corner behavior or gesture cost.

The reported1.926 vertex/centroid gap is not a continuous2-unit clearance certificate: its .074 margin is much smaller than a triangle's sampling radius. Requested an adaptive nearest-surface distance bound (distance is1-Lipschitz), or equivalent converged coverage, before geometry acceptance. Zero static intersections additionally needs a connected exterior-placement argument; it cannot alone exclude a wholly internal component. Requested recorded endpoint pairs/maximum3D span, and tangent checks at the `max(actualSupport, bridge)` switching curve as well as the newly smoothed analytic-crown/cap join. The96-grid allocation and reported37ms sampling are diagnostic costs, not accepted interactive timing.

## Maximum insert certificate and actual render independently inspected

Read the adaptive certificate and reran it against durable `early/shared-strip-final-96.json`, with actual-shell construction verified byte-identical to retained `early/actual-shell.txt`. Reviewer outputs are `reviewer/shared-strip-certificate.ts/.json` and its retained shell helper. Reproduced zero18,432 triangle intersections,22,172 certificate queries, maximum depth6, zero unresolved/violations and sampled maximum1.926250547628. The proof uses the enclosing radius around each triangle centroid plus exact nearest-support distance; the1-Lipschitz distance bound covers triangle interiors. This closes the earlier sampling-margin concern **for this exact mesh and explicit support subset**. It does not establish CCD, self-intersection, or that every support facet represents exposed adhesive contact. The subset is actual rear, restricted front patch and glass; no claim for the rest of the assembly follows.

Independently viewed all three `early/strip-max-1/max-PW-F01-0-{rear,side,front}.png` captures. **Accept this bounded maximum-.35 static fidelity:** side keyboard and front lettering retain rectangular proportions across the insert transition, with no prior diagonal wedge. Rear view shows the narrow perimeter portion; side/front views show the continued same print. Implementer-owned native reports374 assertions/3.98s and zero source/artifact deltas, digest a3a82ee8e603a9514042d408dfc3539987f7a0814d72d0ffa47033a56d27b670/424. No independent browser run is claimed. Full-corner, exterior-contact provenance, interaction/peel and runtime acceptance remain pending.

## Center-local corner experiment contract review

Read the appended Fermi-coordinate proposal. **Approve the bounded rejected-C03 angle0 experiment**, with45/90 following coherent first results, not product integration. The stated geodesic equations match a meridian-arclength rotational sector metric. Each transverse trace must launch perpendicular to the transported unit centerline tangent; fixed angular directions would not implement this construction. Restrict the rotational metric to the actual corner sector and explicitly transition to real strip/front geometry. Preserve the original saved-center anchor and orientation. The proposal correctly requires both principal stretches, cut-locus/orientation detection and independent integration/material-grid refinement rather than asserting isometry. Actual corner glyph fidelity, clearance and direct-input runtime remain mandatory and unproven.

## App grab lifecycle early findings — corrections requested

Read the optional authoritative grab helper and collection integration. Capability-null correctly forbids fallback, and absent capability remains rear-only; this avoids treating the old alpha picker as shell-occlusion proof. Production geometry adapter remains absent and no side-hit acceptance is claimed.

Found candidate lifetime gaps in `sticker-collection.tsx`: threshold transition clears the candidate before collection/slot validation, so the missing-slot branch cannot release its captured canvas through `clearCandidate`; Escape has no subthreshold-candidate cancellation, allowing later movement/up to resume a supposedly cancelled interaction. Also `end()` invokes `move()`, which can invalidate and clear grab ownership, but continues settlement using its previously captured pointer. Requested direct owner corrections and regression coverage before bounded app-base approval. These are early source findings, not a final frozen-source verdict.

## App cancellation corrections independently verified

Re-read all three corrected branches: collection/slot validation precedes candidate ownership transfer; Escape clears the captured candidate before fallback handling; final release sampling checks both pointer identity and interaction generation before settlement. The release helper clears ownership before native lost-capture notification. Independently **11 tests / 67 assertions PASS**, retained in `reviewer/grab-lifecycle-tests.log`. Approve the bounded app base; prior three findings are resolved in this checkpoint.

Coverage limit: the two differently named Escape/missing-data tests exercise the same release helper rather than dispatching those DOM event paths. Source inspection confirms the current wiring, but final native side pickup/cancel/pose invalidation remains required once the actual geometry adapter exists. No full-wrap or browser approval follows from this source/unit checkpoint.

## Bounded crease advisory review

Read `diaries/sticker-corner-advisory.md` after the supported-rear Fermi failures. Approve one same-C03 angle0 boundary-apex hinge-fan experiment with the proposed compliant two-ring seam collar. This is a restricted candidate family; exact panel rigidity is not a user requirement. Before contact optimization, record its complete rest-space polygon/outlet, center anchoring, shared seam vertices, collar width and required displacement. A displacement-to-width estimate is only a preliminary compatibility screen; both UV principal strains on actual collar triangles remain the gate. Do not optimize collision for an already incompatible attachment.

Preserve connected full material, explicitly named physical creases and ordinary tangent joins. One bounded solve must then account for full shell and self-contact, gap/lift footprint, center/UV fidelity and time cost. The earlier2-unit allowance was an experimental bound, not an owner-specified material law; any localized crease lift change needs explicit measured footprint and actual visual judgment, never a blanket hover allowance. No owner decision or general feasibility claim is justified by the failed numerical candidates.

## Bounded Ammo substrate specification review

Read `diaries/sticker-wasm-advisory.md`. Approve exact-pin asset/IDL/init/ownership preflight and one actual-C0333-grid probe, with pre-run corrections. The fixed .5-second press at400 acceleration cap is not dimensionally sufficient for the known tangent seed. Compute the maximum validated seed-to-legal-exterior-target distance D, retain omega20/cap400/.1-second ramp, and set press duration to max(.5,.1+4sqrt(D/400)+6/20), followed by .5-second tack. Verify a free-node integration under that exact schedule reaches the .25 capture band before freezing it. This screens load reachability only; it does not guarantee adhesion under sheet constraints. Use identical physical duration at120/240Hz and keep the two-second CPU cutoff with partial-state reporting.

Validate the three pinned nodes and incident triangles as exterior, record their unavoidable support gap/footprint, and preserve their common tangent/rest frame rather than independently projecting them. Native cluster selfcontact and distance-link bending are approximate substrate capabilities, not certified thin-film behavior. Require full solved-triangle shell/self-contact/UV metrics and native ownership cleanup as specified. Exact asset capabilities have not been independently executed by this reviewer at this checkpoint. No dependency installation, UI/worker architecture or general product approval is implied.

## Ammo preflight artifact/source audit

Read `early/ammo/preflight-probe.txt` and `preflight.json`; independently recomputed cached loader/WASM/license/IDL hashes, all matching recorded SHA256 values. Owning bodies are removed from their world before reverse destruction;18 owned allocations are recorded destroyed, borrowed node/material/world-info wrappers are not incorrectly destroyed. Reserved64MiB heap capacity is explicitly not live-allocation proof. This audit did not rerun the engine.

Pinned `btSoftBody.cpp` explains requested2 clusters returning3: when more than one cluster exists, generation creates an all-node master with `m_collide=false`, moves it to index0, and includes it in the actual-count-squared connectivity allocation. `updateClusters` excludes that master from the collision BVH. Report actual total and collidable counts separately for the full probe; requested128 may yield129 total, subject to empty-cluster removal. The minimal4-node/2-face one-step test verifies callable substrate/init/cleanup, not selfcontact or actual-C03 performance.

Approve computing seed/support reachability with the explicitly provisional exposed partition, reporting missing/blocked targets. This does not approve hidden-face exclusion merely from a cap attribute; each press target needs matching exterior first contact, and unsupported nodes/material may not be omitted to obtain a pass. Full simulation still requires truthful support coverage.

## Seed/load preflight correction before native solve

The implementer correctly stopped with75/1089 unsupported first-contact targets rather than omitting material. Reported flat-seed exterior halfspace margin .1 includes the fixed root; neighbor pin gaps .279/.535 show the declared root frame is not fully tacked. Complete source-backed bridge/first-contact coverage before a full native solve.

The earlier dimension-based duration formula proved insufficient under the actual capped damped force law: reported free-node residual3.483 with substantial velocity at2.407 seconds. Withdraw that formula as a sufficient schedule. Authorize a bounded pre-run duration derivation with unchanged force constants: free-node integrations at120/240Hz up to10 physical seconds, requiring a sustained .1-second interval with gap≤.1 and speed≤1 modelunit/second. Freeze the later successful time rounded up to120Hz, then use the same .5-second tack phase. A fast passage through the capture band is not settling. Failure to meet this preflight cap stops the experiment. Actual solver/support CPU cutoff remains two seconds; partial-time/per-step evidence cannot be called a completed geometry result.

## Native C03 prerequisites independently approved

Read the corrected seed/force artifacts and narrow `castSegment` fallback. Shared-edge misses now require a transverse in-segment plane intersection within128 machine epsilons times coordinate/segment scale of a real triangle edge. Earliest-hit/provenance logic remains; coplanar behavior and full-triangle collision are unchanged. Independently **7 tests / 627 assertions PASS**, including actual long rear facet and1e-7 real-gap rejection, in `reviewer/collision-roundoff-tests.log`.

Corrected seed reports all1089 first contacts legal with the independent exterior halfspace margin and no invented bridges. Force probe implements unchanged coefficients, sustained gap/speed screening and the shared2.908333-second press plus .5-second tack. **Authorize the first bounded actual C03 run** with existing two-second CPU cutoff and fixed120/240Hz comparison. Initial exterior-seed proof does not authorize using query-to-support directions for later inside queries; ongoing contact/exposure checks remain necessary. No completed simulation or product acceptance is claimed yet.

## Explicit limited fixed-target variant

Before execution the geometry owner proposed freezing the1089 certified initial press targets/directions instead of refreshing current support. Approve this **limited substrate/press diagnostic**, with the same CPU cap and full mesh/contact/strain outputs. It does not fulfill the original current-support adhesion test: distance to an original target can miss support after sliding, and any later phase is only fixed-target capture. Neither an adhesion pass nor physical adhesion failure may be inferred from that variant. Preserve this deviation in source/artifacts; actual current-support tack remains a separate unresolved gate. This allows useful native capability/cost evidence without an unreviewed inside-query classifier.

## Native memory diagnosis and bounded repeat

Construction-phase evidence completed successfully at64MiB. Subsequent instrumentation locates OOM at native step184 after183 completed120Hz steps, not construction; no240Hz world had run. Source uses203743 actual triangles, three reused native vectors and disabled vertex welding. Reported native time640ms plus force34ms is partial-run evidence only.

Read pinned sparse-SDF defaults: .25 voxel size and262144-cell clamp. Its possible cache footprint makes dynamic growth a plausible source of64MiB exhaustion, but no actual cell-count attribution is available through the binding. Approve exactly one fixed128MiB repeat per120/240Hz in separate processes, unchanged physical configuration and two-second solver/force CPU cutoff. Preserve OOM and periodic partial meshes. Explicit19-object destruction does not prove inaccessible SDF cells were freed; isolated process exit reclaims the module heap. No production memory acceptance or automatic further heap escalation follows.

## Correction: pinned binary rejects128MiB; no exposed SDF maintenance

The128MiB probe failed at WebAssembly linking before construction because the imported memory maximum exceeds the binary-declared limit. The earlier inference from the loader's `INITIAL_MEMORY` option was incomplete and is withdrawn: JavaScript configurability does not establish this binary's memory compatibility. No128MiB physics run or OOM occurred, and repeating240Hz initialization is unnecessary.

Independently read exact pinned IDL `btSoftBodyWorldInfo` and generated loader: no sparse-SDF getter, Initialize, Reset or clamp binding is exposed. C++ Initialize accepts a clamp and calls Reset, whose loop deletes cell chains, but that source method is not callable through this artifact's public API. No pointer manipulation, binary modification or automatic rebuild is approved. The native experiment remains incomplete with an unresolved memory bound; this is not proof of physical wrapping infeasibility.

## Independent150-step self-contact audit

Read the recovered original-rest/indexed final mesh and audit. Independently tested all six reported nonshared-vertex self-contact pairs with strict Three ray/triangle intersections, without triangle shrinking or coplanar tolerance. **All six have two strict interior intersection witnesses.** For845/848, edge parameters .42444/.39638 hit opposite triangle barycentrics (.09527,.27764,.62709) and (.11247,.26701,.62052), near world(145,271,-25). Evidence: `reviewer/ammo-self-witness.ts/.json`. These are actual material crossings, not expected shared edges.

Near-unit full-triangle strains (.931–1.062 reported) suggest the native constraints preserve material substantially better than rejected charts. They do not establish physical contact: cluster collision presently permits sheet crossing, and134 shell intersections await node/chord stratification. This1.25-second midpress state cannot prove final adhesion or convergence. A further substrate change must address concrete collision limitations as well as memory; longer settling alone cannot be presumed to repair crossed topology.

## Native quality diagnosis before further engine investment

Geometry owner stratification identifies41 nodes behind strictly interior, independently oriented exposed facets;119 of134 shell-intersecting triangles contain such a node. Worst reported depth4.412 cannot be dismissed as triangle chord discretization. One all-nodes-outside chord witness and14 unknown cases remain separately classified. Combined with six independently proven selfcrossings, this is a real contact limitation in the partial candidate.

Read the pinned memory/rebuild advisory. A single reproducible toolchain acquisition/rebuild is justified only as a bounded quality-and-memory diagnostic: reviewed checked SDF cache controls plus bounded read-only cluster membership/connectivity/contact observations, unchanged initial voxel/force configuration, exact toolchain/source/patch provenance and complete solved-mesh oracle. Identify whether the witnessed selfpairs are same/connected clusters that the native path deliberately skips before tuning. The120/240Hz comparison may expose timestep-sensitive shell penetration; it cannot repair structurally excluded contact by assertion. More memory or longer settling alone is not an approved correction. No product adoption, unrestricted build effort or new general collision solver follows from this assessment.

## Narrower native contact configuration before toolchain acquisition

Read pinned `generateClusters(0)` and collision dispatch: the zero argument creates one three-node cluster per face, without the extra master in this branch; CL_RS dispatch is separate from SDF_RS. Approve one unchanged-artifact64MiB probe using per-face clusters and CL_RS|CL_SS|CL_SELF (0x62), preserving all other mesh/force/margin/iteration settings and the two-second CPU cutoff. This directly tests the node-only shell-contact and coarse-cluster exclusion mechanisms rather than merely extending memory or time.

Report actual cluster count and squared connectivity allocation, periodic/final meshes and complete contact oracle. Root-incident triangles require separate scrutiny because anchor-containing clusters can be excluded against static geometry. Shared-node selfpairs remain excluded, and per-face contact is not a proof of exact triangle collision. No toolchain acquisition or product adoption is needed to run this bounded alternative first.

## Per-face native probe audit and collision-proxy boundary

Read face-cluster artifacts and independently recomputed the13 anchor-incident faces from original indices:120Hz final has155 nonroot shell contacts and zero root contacts;240Hz has56 nonroot plus1054/1055 root contacts. Thus root exclusions do not explain the120Hz failures. Both runs stop before press completion. Matched .5-second results report83/55 contacts and9.164 maximum/4.415 RMS position difference; different final times cannot be used as convergence evidence.240Hz wall timing was confounded by overlapping audit work. Zero selfpairs in these earlier states does not prove repair of prior1.25-second crossings.

A source-derived collision proxy/LOD is permissible as a routine bounded implementation technique, with certified conservative exterior/error bounds, preserved actual apertures/support provenance and the full original shell as independent oracle. It is not permission for a box/convex-hull cover or arbitrary decimation. Establish the native concave callback/hull-generation bottleneck with measurements or a bounded same-state cost replay before optimizing based on triangle count alone. Exact coplanar merging is a possible zero-geometry-error first step; curved patches require explicit bounds. Faster contact geometry does not by itself explain or fix the measured deep node penetration.

## Bounded cap-only LOD certificate/replay proposal

Approve investigating the existing crown tessellation knob4→12 for cap interiors only, retaining original boundaries, aperture edges, bevel/noncap/rear/overlay geometry. For the original and proposed piecewise-linear caps on common XY, signed height difference reaches its extrema at vertices of their triangle overlay, including crossed-edge intersections. Require0≤proxyZ−originalZ≤.05 at all such extrema and exact boundary continuity. An analytical smooth-crown estimate alone does not certify the actual rendered triangulation; a uniform outward offset that opens the seam is not acceptable.

Only after certification, compare full/proxy native collision costs from equivalent restored states, separating cold/warm convex-child cache costs and disclosing state-reset limitations. Record resulting triangle count rather than assuming the nominal ninefold grid reduction survives conservative boundary treatment. No full solve or collision-quality acceptance follows from this profiling proposal.

### Replay provenance clarification

Approve paired fresh-world one-step reconstructions from the retained snapshots: identical original rest/index, saved x/v, declared q=x and force0, pin masses and regenerated per-face clusters. Snapshots lack historical q/contact/cluster state, so these are not historical continuations. Construction is separate; native timings run serially without audits. Subsequent warm trajectories may diverge and require separate labeling and position/contact comparison, not a same-state claim. This clarification changes no geometry certificate gate.

## Product requirement audit — experimental constraints are not the product

Re-read the owner-derived edge-wrap scope. The investigation has accumulated stronger numerical assumptions than the user requested. The following are **experimental comparison settings**, not independent completion requirements: two-unit maximum local lift, the measured straight-strip .956–1.153 strain interval, mathematically inextensible panels, a particular cloth engine, simulated adhesion forces, equilibrium convergence, and an exactly conservative .05-unit collision proxy. A procedural adhered mesh is sufficient if it produces the requested experience. Localized print deformation and a modest visible crease are permissible when they still read as the same tactile vinyl sticker.

Necessary observable requirements remain strict: every allowed rear center/size/rotation must work; overflow continues onto the actual adjoining surface; artwork cannot be discarded or visibly turn into a tapered banner; no conspicuous floating tent, visible clipping, z-fighting or impossible interpenetration; moving/editing/peeling preserves the same object and responds promptly. UV/topology orientation and hit/render correspondence remain useful hard engineering invariants. Numerical clearance tolerances should be tied to rendered scale, geometry precision and contact offset rather than rejecting harmless microscopic error while accepting an unattractive shape. Existing substantial node penetration, strict self-crossings and rejected banner renders do not become acceptable through this distinction.

### Bounded product geometry recommendation

Pause further cloth/toolchain expansion and commission one **procedural bend-cage** prototype for the same maximum C03 corner, using the approved exact straight-edge strips as its starting regions. Preserve the original UV rectangle and saved anchor. Apply a primary edge cross-section bend and a localized secondary-corner bend through a small authored control cage; keep front material phase shared, as the successful maximum Pulse insert correction demonstrated. Design the corner transition in the actual3D scene, including one explicit boundary-reaching pleat if required, rather than demanding that a physical solver discover its shape. Ordinary strip joins must remain continuous; a crease must be an actual fold of the shared mesh, not a texture line, clipped wedge, or normal offset of a previously collapsed mapping.

This is a new bounded design candidate, not a claim that composing arbitrary bends automatically conforms to the shell. Sample the final cage-deformed mesh against the actual support, inspect original art from rear/side/front immediately, and reject if support correction collapses or overlaps it. Use just enough local tessellation to keep the silhouette smooth. Allow measured local strain where it visibly looks like bent vinyl; do not require the entire corner to match the straight strip's empirical strain envelope. Record footprint/lift/UV extrema to explain what is visible. Keep the initial experiment to one fixed pose and a small number of explicitly authored controls, with no global optimizer or broad parameter search.

If that static object is credible, the next gate is a short continuous parameter sweep from rear through the corner at0/45/90 degrees, with stable crease/cage topology, original anchor and no switching jump. Only then generalize symmetries and test the full catalog/viewport/gesture lifecycle. A statically authored C03 alone does not fulfill arbitrary placement, so this proposal cannot justify shipping a pose-specific fallback or narrowing access. If no coherent procedural corner can be demonstrated in this bounded attempt, report that specific result; there is currently no independently proven general corner mapping. The recommendation is to return visual design and direct manipulation to the acceptance loop, not to declare a failed numerical method successful.

## Local perimeter cage pre-render review

Read the exact new contract appendix. Approve the first near-bisector maximum C03 angle0 actual rear/side/front capture after finite/zero-area/gross-fold checks. Shared inward front direction addresses the prior polar-convergence failure and warrants immediate visual assessment rather than another elaborate numerical gate.

Later range risk: as the outward direction approaches one straight-edge normal, a transverse row on the neighboring edge can have its tangent parallel to the common front direction, degenerating that map. This does not block the current near-bisector pose, but must be exercised in continuous edge/corner sweeps before generalization. Keep the saved anchor/orientation exact. Wrapped partial peel must also retain its attached contact region; globally lifting every source vertex toward camera is not accepted as a physical partial peel merely because a static wrap succeeds.

## First actual cage render versus original artwork

Independently viewed original `assets/stickers/playworn/rock/pw-c03-last-encore.png` and all three `early/cage-1` rear/side/front captures. The long downward lightning tip is authored artwork; it must not be labeled geometric distortion merely because its visible silhouette tapers. Front rollover looks coherent at default scale, and the source keeps connected original UV topology. The current rear/side views expose narrow portions while front shows the rim portion, so residual ticket/text compression and close contact are not yet confidently assessed.

Requested only an actual camera closeup of the same pose from three-quarter rear/side and front, targeting roughly300 projected pixels of print, with no geometry/material change. This is a bounded visual clarification, not rejection or a new numerical gate. Full-range continuity, side pickup and attached-contact peel remain subsequent requirements.

## Close cage visual disposition and next sweep

Independently inspected original-resolution `early/cage-1-close` rear/side/front images. **Accept the current-pose visual prototype.** The side ticket's SAT/AUG21/1995 text, barcode and original lightning tail remain recognizable; the front ENCORE region rolls around the rim without the rejected diagonal-banner deformation. Perspective compression is present but not a blocker for this view. No conspicuous detached tent or visible clipping is apparent in these captures. This is a visual judgment at one pose, not an exact contact or general-range certificate.

Approve the next continuous nearby-center plus0/45/90-degree sweep, explicitly crossing flat/straight/corner selector boundaries in both directions, including absolute modelX139/modelY250 and near-straight outward normals. Retain fixed-camera video and side observations to detect UV/object jumps, front-row collapse and switching discontinuities. Public placement updates can establish rendered parameter continuity but must not be described as direct-grab proof. Full manipulation, occlusion and partial-contact peel remain pending.

## Near-axis construction correction

The predicted degeneracy is now reproduced: model(156.09,250.01) throws, and250.1/45° nearly collapses. Do not capture a known throw or fall back to the slow rejected atlas. Recommended one continuous perimeter chart whose common front direction is the midpoint of the outward-normal angular interval reachable by the complete footprint across relevant bend/front-rim rows. Require positive dot product with every reached normal. This moves toward the bisector when both neighboring edges are reached, rather than using an anchor normal parallel to another row.

Invert the exact saved center into that same depth/perimeter-offset chart and register original orientation through its root Jacobian. Use continuously varying interval endpoints; no new hard corner selector or world-position blend. A whole-fit flat fast path may remain only with boundary agreement, otherwise defer it. First verify the exact249.99→250.01 boundary path and accepted bisector pose, then capture the short continuous sweep. This is a bounded construction correction, not approval of all-range geometry.

## Prepared wrapped native test source review — changes requested

Read the new interaction driver and admission test without running native fixtures. Requiring exactly four side/front desktop/mobile witnesses and rejecting a missing manifest is sound. Paired screenshots explicitly retain partial-contact visual review as pending, correctly avoiding a false proof from a peel scalar.

Three blockers were sent to the test owner: evidence admission checks only file existence, without binding actual UV/depth/seed/viewport/public pose/build identity; off-rear cancellation intentionally unmounts the overlay, so requiring its peel0 attribute tests the wrong mounted-state contract; relocation lacks an exact one-PUT assertion and its expected center may be so close to the original seed that unchanged placement passes .025 tolerance. Require a meaningful distinct target, settled/reloaded request count, and durable validated witness input hashes/provenance. An absent-or-neutral overlay can establish no active carry without forcing the rear-only packet UI into an off-rear scene. No final native or full interaction approval yet.

## Identity formulation and native-driver corrections

For the global cage issue, advised per-vertex inversion of the unbent planar perimeter chart U(d,t)=rearBoundary(t)+dN before applying the actual bend. Root-only linearization cannot preserve a whole flat print. Flat roots must return exact original XY for every still-flat point, with shoulder compensation tending to identity at the cap join. Use one full rounded-rectangle perimeter parameter with unwrap instead of center-sign quadrant reflection. Fix this known incompatibility before further video; no whole-fit pose selector or world-position blend is accepted as the correction.

Re-read corrected native driver and independently **3 tests / 217 assertions PASS** with the heavy test filtered, retained in `reviewer/wrapped-admission-tests.log`. Evidence now binds immutable build/source sampler, exact seed/viewport/public pose and UV/depth observations; raw inputs are copied/hashed. Neutral absent overlay is accepted without accepting active carry. Meaningful relocation and exactly one PUT are asserted after settling/reload. Prior three driver findings are resolved for this checkpoint. Actual generated witness/camera agreement and visible attached-contact behavior remain final native evidence gates.

## Unified perimeter source checkpoint

Read the new full closed-perimeter implementation during the owned capture run. Per-point flat inverse and root-anchored phase at each radius coherently remove the prior quadrant reflection and curved-flat-art defects; no immediate source blocker to the current sweep. Reported axis/flat probes are not independently rerun here because the build/browser slot is occupied.

Remaining range risks: direction selection samples three radii and three offsets, and the root Gram compensation can enlarge the actual mapped footprint beyond the half-diagonal used to choose that direction. Full-range evidence must cover actual reached rows, positive common-direction dot products and inverse-flat success, especially extreme sizes/angles. This is not a new pre-render gate.

Runtime remains unapproved: each sample evaluates five positions for finite-difference normals, so a96×96 mesh entails roughly47000 inversions and many temporary vectors. The front-rim search is also repeated despite being form-dependent. Measure actual interactive cost before deciding whether to reuse neighboring mesh samples/hoist form invariants; do not assume static finite output implies responsive manipulation. Authoritative grab, occluded contour and attached-contact peel remain unimplemented integration gates.

## Unified cage sampled visual review — bounded pass, integration pending

Independently viewed all 25 original PNGs listed for `early/cage-sweep-1`: rear/right-edge/corner-entry/corner/top-edge at 0°, 45° and 90°, the six associated side views, and both signs of the top/right axis pairs. Evidence source is `09e4355ca319faae0fe97d30be8876986b6d05372b94810fd2b5ecd270d150ed` / 427 files, with reported build/test/artifact deltas zero and 4 tests / 663 assertions. This review does not certify subsequent mutable integration source.

**Bounded visual PASS: proceed with authoritative grab, occlusion and peel integration.** The ticket lettering, guitarist and original lightning shape remain recognizable in the sampled visible regions. Side frames show the print following the shoulder rather than becoming the previous stretched banner or a detached sheet. The decreasing rear fragment is consistent with the artwork turning out of view; the original C03 artwork itself has a narrow lightning tail. Neither top-axis nor right-axis sign pair shows a visible jump. No new visible defect in these images warrants replacing the current construction before integration.

These are separate persisted poses, not an uninterrupted direct drag. The set supplies rear and side views, not fresh front views for each new pose. It does not prove unseen front contact, continuous motion, the complete allowed placement range, or responsive frame cost. Those remain integration evidence requirements, alongside the previously recorded root-compensation/range risks.

The discovered peel topology mismatch is a real remaining integration defect: the shown 96-cell source cannot be indexed using 24-cell peel/display arrays. Require identical indexed UV topology throughout source, partial peel, carried print and target; verify every vertex stays finite, source buffers remain unchanged, and attached rows remain source-exact at several progress values. Actual held side/front views must then establish contact and shell clearance. Numeric attached-row equality alone does not establish that the detached curl clears the shell. Camera transport must not move attached rows, and its transition to fully detached transport must remain continuous. Keep the existing cancellation, revision, wear and source-grab lifecycle tests; final acceptance remains pending.

## App grab/contour adapter review — dynamic ownership correction requested

Read the production forwarding, `adaptStickerGrab`, contour presentation components, their tests and editor call sites. Independently ran the four focused app files: **14 tests / 86 assertions PASS**, retained in `reviewer/app-grab-contour-tests.log`. Authoritative null cannot fall back to alpha-only hidden hits; domain validation and live geometry closure delegation are coherent. Open spans omit synthetic SVG closure, and hidden idle grips produce no pointer or focus targets while visible indices remain stable.

A dynamic lifecycle defect remains in the new visibility filtering: a draft rotation can turn its own anchor invisible, causing `StickerContourGrips` to unmount the button currently holding pointer capture or keyboard focus before pointerup/keyup. This can cancel the admitted rotation through lostcapture or lose its completion event/focus. Static SSR output tests do not cover this transition. Requested a stable admitted gesture/focus owner through completion/cancellation, without admitting new idle interactions on hidden anchors, and a regression that changes visibility during pointer/keyboard editing. This is an app integration correction request, not a rejection of the geometry or static visibility contract. Actual device witnesses, wrapped peel clearance and uninterrupted native movement remain pending.

## Device adapter source checkpoint — three corrections requested

Read `sticker-surface-grab`, `sticker-visibility`, the scene adapter, painted hit testing, contour projection and partial-peel implementation. The adapter uses actual painted UV and nearest visible print rather than saved-center ray substitution; the capture checks source pose, camera/content matrices and canvas rectangle. Matching the displayed source topology removes the earlier 24/96 indexed-buffer defect, and the helper keeps attached rows source-exact. These positive source properties do not yet approve native behavior.

Three concrete findings sent to the device owner:

- Contour occlusion evaluates only vertices remaining after collinear alpha-boundary reduction. Two visible endpoints can span an occluded middle region, so simply splitting at null endpoints can still draw a line through the shell. Subdivide through the shown surface and visibility transitions before assembling open spans; add a visible-endpoints/hidden-midpoint case.
- `PeelingPrint` forces detached transport weight to1 at peel>=1. The final row has weight0 immediately below1, then receives the entire drag translation/depth lift. Nearby rows also jump. Verify continuity with nonzero drag offset at1-epsilon/1, and use a continuous final transport handoff while preserving exact attached rows beforehand.
- Visibility-cache identity strings the result of inverse(contentWorld)*meshWorld. Independently reproduced four distinct keys under four global yaw values with unchanged local geometry; differences were only4.440892098500626e-16. Consequently orientation can rebuild the complete BVH for roundoff. Derive identity from local ancestor transforms or a suitable roundoff comparison; include position/index attribute versions so in-place geometry changes cannot leave stale collision arrays.

No heavy tests or browser resources were used for this review. Actual wrapped pickup, occlusion witnesses, held peel clearance and responsiveness remain pending, and the three source corrections need re-review.

## Grip ownership correction resolved; transport contract bounded approval

Re-read the keyed `ContourGrip` and editor wiring. An admitted pointer owner stays at its captured pointer, and a keyboard owner stays at its last visible coordinate. Both preserve the same DOM button through occlusion; retained hidden controls reject new pointerdown and leave tab order. Blur/selection teardown ends focus ownership. Independently **15 tests / 100 assertions PASS**, saved as `reviewer/app-grip-owner-tests.log`. The new DOM transition test establishes node retention and delivery of pointerup/keyup; real browser capture through an occlusion boundary still belongs to native validation. The source finding is resolved.

Approved an app-owned continuous detach-transport value in principle, with no device clock. The proposed partialDetached+(1-partialDetached)*transport weighting is continuous only if transport stays zero while geometry still has attached rows. Completing geometric peel first and using the remaining existing lift progression for transport can preserve total choreography/travel without adding another scheduler. Free carry's later curl reduction to .4 must not reset detachment; transport cannot simply be derived from that curl scalar. Return/cancel must bring transport back before geometry reattaches; reduced motion may resolve directly. Exact implementation and actual held/release sequence remain pending.

## Device correction regressions re-reviewed

Independently ran visibility, contour-visibility and surface tests: **11 tests PASS**, retained in `reviewer/device-visibility-peel-tests.log`. The 1,145,798 assertion count comes from dense loops and is not broad scenario coverage. Local ancestor matrix composition removes global-pose roundoff invalidation; buffer update versions trigger rebuild. Sampling each reduced alpha edge through the shown grid resolves the visible-endpoints/hidden-middle witness. Separate transport weighting removes the helper's discontinuous full-front tail branch. The app phase publication and actual sequence still need integration review.

One cache lifecycle follow-up remains: replacement position/index attributes with fresh version0 on the same geometry need identity coverage, beyond needsUpdate version increments. Sent the owner this bounded regression request.

The new normal optimization computes adjacency normals from unlifted positions and then applies film lift along them. This also changes lifted vertex positions relative to the former analytic finite-difference normals; it is not exclusively a shading optimization. Refresh visual and witness provenance accordingly. Grab still samples the analytic normal; its initial pointer offset compensates initial differences but does not prove subsequent actual shown-UV tracking. Native tracking/clearance remains the appropriate final check. No new numerical strain or gap requirement was imposed.

## App physical-phase implementation — bounded approval

Read the new lift-phase helper, pointer forwarding, shared-state fields and existing single animation owner. Independently **33 tests / 308 assertions PASS**, saved in `reviewer/app-lift-phase-tests.log`. The .8/1 phase split and inverse current-phase mapping preserve continuous transport before reattachment, while free carry retains full physical detachment independently of .4 visual curl. Pointer travel remains64px; existing publication/supersession guards are retained. This resolves the app timeline source gate, subject to actual interrupted lift/press/return and reduced-motion native behavior.

A test-only witness recorder is acceptable when included in the prebuild manifest and correlated with actual submitted matrices for the identified sticker draw, frame, geometry/UV and canvas rectangle. Merely observing a uniform with a familiar name in a scene containing multiple draws/passes is insufficient. No camera or model matrix may be reconstructed from an assumed public pose and presented as observed. Recorder source/correlation review remains pending its implementation.

## Clean device bundle review — checks pass, sampler lifetime follow-up

Independently **14 helper tests PASS**, device typecheck and scoped lint pass; logs `reviewer/device-final-helper-tests.log`, `reviewer/device-typecheck.log`, `reviewer/device-scoped-lint.log`. Exact indexed-grid sampling now reproduces displayed adjacency-normal film lift for the grabbed UV, resolving the analytic approximation mismatch. Weak attribute/backing identities and counts supplement versions, resolving replacement-buffer stale cache. Dense assertion counts remain loop evidence, not native scenario coverage.

One binding ownership correction requested: `Device.tsx` publishes `bindStickerWrapSurface` from useMemo, while its effect merely returns the already-created disposer. Effect cleanup/setup replay therefore removes the WeakMap binding without establishing it again; an abandoned render also publishes shared state before commit. Require symmetric binding setup/cleanup compatible with child geometry creation, or an explicit sampler dependency rather than render-time shared publication. Production native alone does not establish effect replay safety. All actual UV/occlusion/peel/latency evidence remains pending the coordinated freeze.

## Test-only matrix recorder source review

Actual WebGL2 uniform, texture upload, VAO and raw buffer interception provides suitable observed inputs. C03 URL matching alone cannot identify an equipped print because the packet can use the same artwork. Require exact position/UV/index bytes and model/source identity to select the equipped candidate; the owner is changing capture to at most8 same-pass C03 candidates rather than last matching draw. Ambiguity must fail.

Requested two additional grouped fixes: snapshot the recent-draw ring synchronously before asynchronous SHA calculation, so returned shell context cannot belong to a later pass; clean up deleted programs/VAOs as buffers already are. The64MiB limit is the buffer mirror cap and must not be presented as total memory including copied snapshots/serialized arrays. Missing actual optimized-away uniforms must fail or use an explicitly observed alternative, never guessed matrices. These are capture-input corrections; final offline matching and actual camera/UV evidence remain pending. The full intended source scan added no other product blocker beyond the previously reported sampler binding lifetime and outstanding native/visual/performance gates.

## Sampler lifetime correction accepted — build may proceed

Re-read explicit sampler forwarding from Device through equipped geometry and symmetric layout-effect binding. Render no longer publishes the WeakMap; child geometry accepts the sampler directly before effect setup. A unique binding token prevents stale cleanup from removing a newer binding of the same sampler. Independently **3 tests / 103 assertions PASS**, in `reviewer/device-binding-lifecycle-tests.log`. The source ownership finding is resolved; no blocker to the coordinated fresh build. Recorder fixes now retain bounded same-pass candidates/context and deletion cleanup; actual draw/geometry matching still requires runtime evidence.

## Refreshed final-matrix front visual — BLOCKED

Independently viewed all four `early/final-matrix-1/matrix-{1280,375}-{side,front}.png` originals, then reopened `early/cage-1/max-PW-C03-0-front.png` for direct comparison. The refreshed side views remain coherent. Both refreshed front views show a concrete material regression: ENCORE and the orange ticket become a long, strongly sheared/compressed diagonal ribbon across the screen instead of the earlier compact corner rollover. This is the printed upper ticket/title region, not the original artwork's authored lightning tail. The driver saves and asserts the same WRAPPED_SEED before capture.

**Current front visual is not accepted.** Parent and geometry owner were notified before full native interaction approval. Preserve source `890a4fcfb4f2461a34c1bf7c46da34f04ef787e4df35e992bdf8def12dcd7000` /437 and captured matrices/buffers for bounded diagnosis of the exact affected UV region and geometry change. Normal lift alone is not a plausible explanation for the large change in projected extent; source/root mapping attribution must be investigated, not assumed. The earlier25-frame rear/side pass explicitly lacked refreshed front views and therefore did not approve this deformation. Correct the observable artwork distortion before claiming full wrap fidelity; no new cloth or numerical-gap requirement is introduced.

## Bounded front-correction recommendation

Current source sends the complete nonlinear per-vertex flat inverse (depth and unwrapped perimeter arc) through the front continuation after root Gram compensation. Recommended measuring that residual specifically at the upper ticket/title UVs against its root Taylor chart before attributing the regression. A bounded correction candidate is to retain exact inverse Q on flat contact, form root-linear L with the same value/first derivative, and consume only nonlinear Q−L over the actual bend so it reaches zero by the front rim. Evaluate the resulting chart coordinates through the existing actual supported surface. This does not blend world positions or reintroduce placement islands; center and local frame can remain unchanged because the residual vanishes to first order at the root. Transition derivatives can still fold, so the same affected front render and boundary checks must falsify or validate it. No new simulation or numerical isometry criterion is requested.

## Implicit residual correction — bounded render acknowledgment

The measured upper-ticket sample confirms nonlinear depth excess (Q120.899 versus L63.640) dominates; root Gram was nearly identity. Explicit residual fade introduced opposed triangles and remains rejected. Read the replacement scalar equation D=origin+L+w(D)(Q−L), bracketed between the linear/exact depths with safeguarded Newton. For the nonnegative convex depth residual, decreasing w makes its derivative positive; this addresses the explicit fade's depth-order reversal. No fatal source blocker to the six actual views. The twelve-iteration cap currently lacks a final residual postcondition, so final dense diagnostics should report convergence residual and any negative residual induced by numerical Taylor estimation. This is not a hold on the bounded render. Actual front appearance and subsequent native interaction remain pending.

## Corrected six-view visual disposition — bounded pass

Independently viewed all six original `early/front-residual-1-native/matrix-{1280,375}-{rear,side,front}.png` images, source `6c86ffc6f2f5006d25de9c4c47ad32660c8756e74f02da9dc89ce6a6c6fadebe` /437. **PASS for this saved pose:** the long diagonal front ribbon is absent at both widths and the upper ticket/title returns to a compact corner rollover. Rear contact remains coherent; side artwork is more compressed than the earlier variant but reads as a foreshortened ticket/lightning on the shoulder rather than a new gross banner. No visible detached tent or fresh clipping defect blocks witness generation.

Permit exact equipped raw-buffer matching and actual UV/depth witness generation on this build. This resolves the specific current-pose front regression; it does not approve the full placement/rotation range, uninterrupted manipulation, partial-peel clearance or performance. The previous19-pose rear/side sample cannot substitute for continuous native verification of the corrected mapper.

## Actual matrix witness audit checkpoint

Independently rehashed both actual1280-side draw candidates127/128: position `3a8c7b7aaf6841b8ca383e9796facc786dbd78996dca5cf358d5d71d03fcbb2c`, UV `e92f24ea33c24a3c5580ee3839631624b166a25cf36ea9e8e0f7a953feac6a52`, index `47e6f2c078c0802d332369106d92461e3194bbc06b48d6f199a6c6c03e93f879`; all agree with recorded raw bytes. Source-geometry match and final proof scripts are still being assembled.

Clarified that all four release endpoints solve, not all intermediate centers: candidates retain null previews. Nulls may represent leaving the projected valid domain and do not alone prove a product defect, but endpoint success is not continuous inverse tracking. Requested exact app admission in the offline script: projectCenter is called only after normal lift distance exceeds64px, whereas evaluating every12px partial/early carry sample changes its stateful Newton warm start. Final proof must match actual call order, preserve nulls and classify target reentry honestly. The earlier matrixAutoUpdate reconstruction failure remains invalid offline-fixture evidence, not a product failure. Actual full-assembly native occlusion remains required where the offline shell fixture omits hardware.

## Cold inverse correction reviewed — bounded approval

Correct app admission revealed a real cold Newton failure: an intermediate trial left the legal rear-center domain and returned null despite an attainable target. Read the correction: at most two seeds, six Newton steps each and four legal residual-decreasing backtracking trials; a frozen content-XY plane supplies only an alternate seed. Both routes require the unchanged .5px exact shown-UV residual before publishing a warm start. Rejected targets do not poison the next solve.

Independently **3 tests / 146 assertions PASS**, `reviewer/device-cold-grab-tests.log`. Tests skip all<=64px calls and cover invalid targets, valid reentry and pose invalidation. Their camera/UV fixtures are observed native data but support is a synthetic planar front with regenerated target coordinates; this distinction is explicit and does not substitute for actual assembly native paths. No source blocker to a fresh matrix build. Actual source geometry is unchanged by this inverse-only correction; final proof must bind the new source/build identity and preserve exact admitted-call trajectories.

## Current-build wrapped witnesses — bounded native admission

Reviewed `early/wrapped-witness-2/{generate.ts,finalize.ts,source-shell.ts,png-alpha.ts}` and all four finalized proofs. **Accepted for same-build native interaction**, with the recorder disabled. Independently rehashed the actual captured position, UV and index bytes for each selected draw; each has the complete three-role set and matches the regenerated source hashes. The selected ink draw has actual CCW front-face/BACK culling; the companion backing draw is CW. Selected model matrices equal the generator content matrices. Current sampler/grab hashes match the immutable capture build. Forced content `updateMatrixWorld(true)` fixes the previously invalid offline reconstruction.

The four exact 8+24 pointer trajectories contain respectively15/17/8/14 admitted calls, zero invalid admitted centers and no calls warming the solver at or below64px. Pickup shell separation is .181–.316 model units. Required actual rear/front/glass buffers are correlated within the same render pass; model-matrix errors are zero except glass translation roundoff4.77e−7. Source alpha uses the original decoded RGBA PNG and the production wear survival field, rather than screenshot color assumptions.

Limits remain explicit: the offline shell omits other hardware; production full-scene hit admission must validate the chosen rays. Every current occluded witness is also backfacing, so successful rejection establishes hidden-side behavior but cannot isolate visibility-BVH rejection from FrontSide culling. Partial-attached UV and state do not establish physical peel clearance. Actual native pickup, cancellation, relocation, revision/reload, uninterrupted manipulation and visual lamination remain pending. No complete edge-wrap approval follows from this correlation pass.

## First native interaction failure — invalid orientation expectation

`early/cold-inverse-interaction-1` passed the first1280-side pickup, partial stage, cancel/no-write and reload checks before failing its orientation assertion. Independently traced the chosen hidden ray to actual rear point(146.643381,248.959466,−28.033981). Its rounded-silhouette signed distance is−18.356619, outside `DEVICE_ORIENTATION_GRAB_BAND=18`. `Device.isOrientationGrabHit` requires both first-visible-shell and this band. Therefore an absent orientation grab is expected here; hidden-print rejection does not imply orientation eligibility. This is a witness/expectation defect, not evidence for changing product admission. Retain the negative pickup/no-write assertion; independently establish real shell eligibility for any claimed rotation-lane witness.

Viewed attached and12px partial originals and native-resolution detail crops (`reviewer/first-side-{attached,partial-peel}-native-crop.png`). The seated lower artwork does not visibly jump, but the two views are nearly identical and do not reveal enough detached upper sheet/backing to approve physical peel clearance. Request a larger still-partial displacement and an angle revealing detached rows, while keeping a reference UV beyond the actual source-front position. State-stage success remains distinct from this pending visual gate.

## Replacement witnesses4 — accepted for native rerun

Reviewed generator/finalizer changes and independently checked all32 samples in each new trajectory. Pickup, release, expected centers, source/build identity, camera/content matrices and raw geometry hashes remain exactly those of witness2. Side partial32px yields front.625 with visible attached row.697917; front partial20px yields front.390625 with visible attached row.427083. The view-specific choice retains physical observability instead of requiring an attached reference already occluded by the shell. Admitted calls18/18/10/15 all solve; unadmitted calls remain null and never prewarm the inverse.

Replacement hidden rays intersect actual rear/front shell within the existing orientation band (independently computed signed distances−7.51154,−9.94706,−9.65942,−9.34210), and the source ray test rejects foreground FrontSide ink. **Accepted `early/wrapped-witness-4/manifest.json` for recorder-off same-build native rerun.** These are evidence-only corrections; full-scene routing and visible partial-peel contact still require the run. The backfacing limitation of hidden witnesses remains.

## Second native interaction — routing diagnosis pending

The corrected eligible hidden witness still fails the active-orientation assertion in `early/cold-inverse-interaction-2`; no further point substitution is justified. Source review shows document capture arbitration stops propagation only after a nonnull authoritative painted pickup. A null hit returns after editor dismissal. Equipped print/backing R3F raycasts are no-ops; shell admission additionally requires the first event intersection object and body-local18-unit band. Requested actual DOM capture/bubble target, propagation, first R3F hit/object/localXY and current canvas/pose evidence before classifying a product defect. No blind retry or source correction is approved from the present evidence.

Viewed stronger32px partial original: the upper detached sheet is now visible and lower ticket/lightning tail remains seated, unlike a whole-sheet translation. However the upper sheet extends beyond the viewport top, so its complete silhouette and backing/contact clearance cannot be reviewed from this frame. This is useful partial-contact evidence, not final peel acceptance.

## Hidden route diagnosis — confirmed canvas-coordinate defect

Independently read `early/hidden-route-1/pointer-route.json` and installed Fiber9.7 default event compute. Actual pointerdown targets the composited canvas descendant STRONG in the titlebar: client(567.0681,147.8312), target-relative offset(151.0838,8.6893), while canvas rect is(0,0,1280,900). No sticker stop/capture occurred. Default R3F compute divides target-relative offsets by canvas size, producing a different ray. This is a concrete product coordinate bug; the qualified physical shell point need not change.

Approved bounded correction retaining the default event manager and overriding only ray compute with canvas-relative client coordinates divided by the current CSS canvas rect. Regressions must cover differing descendant offsets at identical client points, translated/scrolled canvas, axis-aligned CSS scaling, offcanvas unclamped coordinates, and invalid/zero rect without stale ray reuse. Preserve event ownership and shell/controls admission. Pinned R3F also uses offsets for initialClick/delta internally; ray correction alone does not claim to change all click-distance semantics. Fresh actual native must validate the same intended witness path after the fix.

## Canvas ray correction — independent source/test pass

Read `canvas-events.ts`, its tests and `DeviceCanvas` hookup. The factory spreads pinned Fiber default events and changes only compute. Normalization uses actual canvas CSS rect and client coordinates; invalid coordinates/layout clear the ray and set camera=null. Confirmed installed Fiber handleRaycast skips that sentinel and resets cameras per event; valid setFromCamera restores the active camera. Independently ran `bun test packages/device/src/canvas-events.test.ts`: **3 tests /28 assertions pass**. Descendant-offset regression, translated/scrolled/scaled rect, offcanvas coordinates and invalid-to-valid recovery are meaningful. Approved fresh build, capture provenance rebinding and the identical failed native path; no geometry/witness-location change required. Actual native resolution remains pending.

## Canvas-fix witness5 rebind — native admission approved

Independently compared all four witness5 manifest entries against witness4: every gesture field is unchanged except evidence paths. Proof seed/pose/camera/content matrices, pickup/hidden/attached points and32-point trajectories are exact matches. Rehashed new capture files and selected actual draw position/UV/index bytes; shell same-pass model errors remain0 or4.77e−7. Rebind source restricts changes to DeviceCanvas and canvas-events implementation/tests. Manifest SHA `cc7d2d119d016ee7560d767b0370d8217a6bf4ff3a80d093130b62a09ae00cf9`. **Approved exact recorder-off native rerun** on fresh canvas-ray build. Prior offline trajectory timings remain historical; no new performance claim or point search.

## Canvas-fix native: desktop side passes; front relocation pending diagnosis

Read the outcome and pointer traces from `early/canvas-ray-interaction-1`. Desktop side completes pickup/cancel/hidden-shell rotation/relocation/reload, with persisted center(.3499636947,.1499867297). Both desktop hidden-flick cases pass, providing actual resolution of the canvas-ray defect. Front relocation subsequently has no overlay; touchscreen cases were not reached. An inverse returning null alone should retain carry, so failure classification must distinguish never-admitted pickup from cancellation, including frozen-grab validity and the orientation-prop cancellation effect. Keyboard orientation publishes immediately rather than using a spring, but rendered pose/readiness can still lag the fixture’s immediate post-reload pickup. No arbitrary wait or source correction approved without trace.

Viewed side32/front20 partial originals and sampled side video6.5/7.2/8s (`reviewer/canvas-ray-side-motion-*.png`). Front partial shows pale curved backing and a seated rim segment; side samples show curled carried print followed by a coherent placement. No gross sampled mesh rupture appears. The side held upper section remains outside viewport, and sparse video samples do not prove all-frame continuity, full clearance or render latency. Pointer-trace timestamps are driver completion timings, not pointer-to-paint measurements.

Subsequent actual route instrumentation supersedes the admission/pose hypotheses above: advisor reports successful capture, peeling and placing through pointer(613.47,156.77), followed by the route error boundary and removal of canvas/stage at(607.01,157.58). Lost capture occurs afterward. Initial admission/readiness is therefore not the explanation. Await concrete error/stack; no fixture sleep or alternate trajectory should hide this crash.

## Front preview crash — footprint direction correction required

Actual error is `Bend has no actual front support` during complete preview geometry creation. Engineer reproduced ten vertices at center(.314332653716106,.0205851851589971), C03width.35/angle0 crossing the opposite left shell. Source inspection confirms bend normal uses center±halfDiagonal perimeter spans independent of material rotation. At this top-edge pose the actual tangential halfwidth57.75 fits the straight top, yet the circular bound includes the adjacent corner and forces an inappropriate diagonal inward direction. This is not missing aperture support; fabricating support, catching and hiding the preview, or changing the pointer path would conceal the mapper defect.

Approved a bounded first correction carrying identical width/height/radian rotation into full-surface and grabbed-UV cage construction. Replace circular halfDiagonal span with the continuous rotated-rectangle tangential support extent at each sampled section radius. This is a support-span approximation, not an exact polygon/boundary intersection claim. Exact boundary intersection remains a possible bounded follow-up only if current full meshes falsify sufficiency. Tests must include all actual admitted trajectory centers at the displayed96-grid topology, the exact crashing pose, nearby center/rotation changes and accepted corner/axis cases. Single grabbed-UV inverse success cannot certify all preview vertices. No new global optimizer or old experimental cloth constraints are required.

## Tangential footprint checkpoint — qualified source/evidence review

Current implementation uses rotated tangential support spans, not exact rectangle/boundary intersection. Surface and captured-UV sampler receive identical width/height/radian angle. The residual solver now handles constant-weight roots analytically (exact flat root when origin+Q<=0, exact front root when origin+L>=frontEnd), solving only the interior bend bracket. This directly addresses prior endpoint rejection and is consistent with the monotone scalar equation.

Read bounded probes and independently matched their cage/wrap hashes to current source.1620 cases use an8-cell coarse grid, not1620 full displayed meshes; they report zero throws/orientation flags and maximum equation residual9.99962e−10. The61 historical admitted centers separately build9409-vertex meshes and are finite. Those results do not yet validate recomputed new inverse trajectories or all fine-grid topology. Independent current helper run:5pass/1fail; the synthetic-front cold-grab test’s regenerated travel is43.6938 rather than>64. Engineer notified during mutable test preparation; no final readiness or native-path failure inferred. Actual unchanged pointer paths must be recomputed and their entire resulting preview meshes checked next.

## Recomputed front inverse — branch classification pending

Read the new `reachability.json`: global-seed search finds distant roots, but algebraic screen coincidence does not establish a continuous visible manipulation branch. Requested source-connected tiny-step continuation with step-halving, screen Jacobian conditioning and actual shell visibility at the first null. A smooth nondegenerate local visible root indicates solver failure; a singularity, forward discontinuity or exit from support/visibility requires mapper or reachability classification. Never teleport to a remote/hidden root to satisfy the recorded pointer path. A genuinely invalid landing preview may remain a free carried sheet and recover on valid reentry, without saving a stale target. The original user requirement does not imply every screen trajectory has a valid adhesive rear-center inverse. Current native trajectory remains the diagnostic until this distinction is evidenced.

## Frame observer source gate — independent pass

Reviewed test-only observer, fixture install and native driver aggregation. Independently5tests/35assertions pass. Passive input starts bounded windows without a before-down barrier; first callback and release-tail gaps are counted; long tasks use entry overlap and drain before disconnect. Limits16windows/2048samples/256tasks/10seconds disclose overflow and insufficient evidence. Driver requires observed admission and rejects insufficient/gross-stall windows using predeclared>250ms once or>100ms twice. Cleanup covers cancel, replacement, pagehide and explicit disposal. This measures down-to-input-release rAF scheduling, not photons or release-handler/settlement work; a separately labeled post-release window is under preparation.

## Softened direction — boundary interpretation not yet final

Read softened footprint-scale N source and128-step continuation. Last successful source-connected centers are visibly near legal boundary: independently computed rounded-body signed distances−.0030865 desktop and−.0730605 mobile model units, with positive reported screen determinants. However existing64-step artifact still describes the rejected support-span direction, so it is not a same-source refinement comparison. Current continuation also breaks when forward Jacobian samples are invalid without recording detailed termination. Requested matched-source64/128 results and explicit failed-step/domain evidence or valid-sided derivatives before calling the remaining null a true domain exit. Do not regenerate positive witnesses solely on the present near-boundary inference.

## Product-aligned manipulation separation — recommended and authorized

Source confirms valid held previews currently spring landing toward.97, blending the carried sheet almost entirely into the seated target. That creates the additional demand to invert the moving wrapped grabbed UV beneath the pointer. Owner scope requires tactile freecarry, real supported placement and full access; it does not require exact seated-UV coincidence during detached motion. Recommended retain camera-plane carried-sheet offset with landing0 while held, start existing press/landing spring only on valid release, and communicate actual target with a quiet surface-following contour/ghost.

Use one captured actual hit-triangle UV-oriented material tangent frame for intended-center delta, preserving initial offset and original rotated rear coordinate meaning. This avoids both nonlinear new-pose inversion and a global rear-plane singularity at side views. Bounds/ownership/revision checks remain; invalid preview clears destination, carries freely and never saves stale target. Source support for every valid complete placement is still mandatory. Root authorized this scoped replacement; previous exact seated-UV inverse acceptance is superseded, not the physical wrap goal.

## Release-to-settled observer extension — independent pass

Reviewed separate window kind and lifecycle; independently6tests/45assertions pass. Window begins in capture before product release/cancel/Escape handler, ignores ensuing lostcapture, and closes after existing driver semantic-neutral assertion. Timeout/disposal/supersession before awaited completion is insufficient. It measures scheduling through that semantic settlement, not photon latency. Existing gross-stall thresholds remain unchanged.

## Freecarry app checkpoint — bounded source approval

Read held-target helper and pointer move/release changes. Candidate validation, target clearing, stage and landing0 publish together; no hover docking clock remains. Final release still samples the latest input through the existing pointer/generation guard, then commits the current source target once; invalid preview returns without placement. Existing release/return spring ownership is preserved. Independently7lifecycle tests/133assertions pass, including valid→invalid→valid freecarry and release-only clock. This approves the state boundary, not final visual interaction. With landing0 and no target cue, the carried sheet can differ from the intended seated destination; actual render must assess whether a quiet actual-surface outline is needed before release.

Device affine preflight: fixed UV-oriented tangent-frame mapping is appropriate, but rejecting pickup whenever projected gain falls below5% introduces a visible grazing-angle restriction rather than only numerical-degeneracy protection. Recommended bounded-gain or camera-oriented fallback for an otherwise valid painted hit, rejecting only truly degenerate/nonfinite geometry. Await exact device implementation and tests.

## Frozen affine device frame — bounded source approval

Read actual hit-face position/UV extraction, mesh-to-content transform, UV edge inversion and orthonormal material basis. The fixed projected2×2 frame uses a singular-gain cap20×strong-direction gain, not a grazing pickup cutoff. Zero pointer delta preserves saved center; material delta rotates once into canonical rearXY. Queries allocate no candidate mesh, perform no root search and retain no prior-solution state. Current source/pose/rect validity guards and legal-center checks remain.

Independently `bun test packages/device/src/sticker-surface-grab.test.ts packages/device/src/sticker-wrap.test.ts`: **8tests/569assertions pass**. Geometry regression covers complete96 meshes for historical admitted centers and the exact opposite-edge crash with neighbors/rotations on an explicitly synthetic full-width front. A separate37°rotated, translated triangle known-material-delta check agrees to floating-point precision. An initial root-directory scratch probe used an incompatible Three module resolution and was invalid; the package-local probe uses the same installed package as production.

Approved bounded fresh visual/native build, subject to target-cue review. New evidence must use the actual current hit triangle/frame and check complete meshes at the new affine targets. Old nonlinear inverse expected centers are historical diagnostics, not the new interaction oracle. Full-range actual support, refreshed visual fidelity, destination clarity and actual frame-stall evidence remain pending.

## Current target outline — bounded source approval

Reviewed device target slot, scene publication/query, app forwarding and landing outline. The slot compares all placement fields against both prepared and current preview, borrows geometry and original damage field, and uses resource-token cleanup without disposing borrowed GPU resources. Target projection refreshes content transform and uses the existing exact indexed-alpha contour and shell-visibility split. The detached mesh world matrix is updated by the existing quad projection entry point. Child readiness/cleanup notifications and the parent committed-preview notification cover preparation and child-before-parent ordering; the app subscribes to projection readiness and actual pointer ownership.

The outline queries the current target without retaining paths, appears only for held placing with landing zero and a valid preview, and disappears on invalidation/release/cancel. It is pointer-inert and aria-hidden, with existing one-pixel open contour paths and no grips. Independently ran `bun test packages/device/src/sticker-target-contour.test.ts apps/web/src/sticker-contour-presentation.test.tsx`: **5 tests / 66 assertions pass**. No source blocker to the bounded fresh build. Actual stationary readiness, visible target correspondence, release hiding, and frame cost remain native checks; helper/SSR tests do not establish those runtime outcomes.

## Invalid release and valid reentry — native driver review

Reviewed the new front-only chronology: a separate valid carry leaves the legal target, clears target outline, remains free with landing zero, releases to neutral and must leave both PUT count and full inventory unchanged. The positive relocation subsequently crosses the invalid point and returns to its valid endpoint before the existing exactly-one-PUT, revision and reload assertions. Evidence admission requires the matching invalid point and captured-triangle-affine null result. Actual proof generation must independently classify that endpoint and validate full target meshes for every new intermediate sample; the schema assertion alone does not prove affine invalidity.

Independently ran `bun test apps/web/scripts/sticker-hud.integration.test.ts --test-name-pattern '^wrapped '`: **3 tests / 359 assertions pass**, heavy native test explicitly filtered out. Combined source review permits the fresh build and six-view capture. Actual gesture, target-cue correspondence, partial contact, invalid-return behavior and input/release frame observations remain pending.

## Affine target build — six fresh static views

Independently viewed all six original `early/affine-target-matrix-1/matrix-{1280,375}-{rear,side,front}.png` images. Bounded seed visual pass: the front rollover stays compact without the rejected tall ribbon; the side retains the authored lightning taper and ticket band, without a detached tent; rear Pulse proportions remain intact. Small mobile artwork limits fine text judgment. These idle captures do not test the held target outline, freecarry, new relocation targets or release motion.

Read capture identity: source `52c6892dd17181e17226c6d074d50415f3fb3e0fdde7be2764fa8685e9d959a4`, artifacts `07d71b1b4f97c07dd43e866e63e6a0c6a5d757c3b35dcc1aa6721c6ec4500b53`; manifest reports zero build/test/artifact deltas. Permit current-build witness correlation and subsequent native run after proof review; no final feature approval.

## Witness 6 — current affine evidence approval

Independently rehashed the complete proof ledger, four referenced captures and selected actual ink position/UV/index bytes. Manifest SHA `c63792c4bd3dfe93b5909d5f74047353d52c201e89e6bd2ffde9da471771a112` matches. All proofs bind to the fresh affine-target build above. Source generation recasts the original pickup pixel against displayed geometry and captures that actual triangle; front positive endpoints derive from declared affine centers .20/.15 and .35/.15. Historical front endpoints explicitly remain null negative cases. Monotone maximum pointer distance preserves admission through reentry. Positive admitted counts are 18/15/10/11, with complete 9409-vertex target attribution; negative and reentry sequences retain invalid samples and full meshes for valid ones.

Attached references were independently matched to exact recorded original UV vertices: side row67 beyond frontier60, front row39 beyond frontier37.5. Both source/peel position errors and transport weights are zero. The front reference has 1.5 grid rows of margin and complements the stronger side contact view; it is not an inferred invisible point or rounded off-grid substitute. Same-pass source shell/model correlation errors are zero or 4.77e-7. Bounded approval for same-build recorder-off native execution; actual contact, target outline, freecarry/release behavior and frame outcomes remain pending.

## Native affine failure — hit-probe transform blocker

The first affine native run passes partial cancel and hidden-shell flick, then remains peeling at the expected valid desktop-side endpoint. Source inspection found a concrete integration gap missed by the earlier isolated affine review: `intersectStickerPrint` returns an intersection whose object is a temporary Mesh with only `matrixWorld` copied. `captureStickerSurfaceGrab` calls that object's `updateWorldMatrix`, and its default automatic identity local transform overwrites the copied world matrix. Thus native tangent extraction differs from the parented mesh used by the offline witness.

Independently reproduced the installed Three behavior: copying a rotated and translated matrixWorld to a fresh Mesh then calling updateWorldMatrix(true,false) yields identity. This is a product handoff blocker, not evidence of affine-domain infeasibility. Engineer notified to preserve the rendered transform/object and add an exact hit-to-grab transformed regression; no pointer-path workaround or native retry is approved from this diagnosis alone. Prior standalone affine/source approval did not cover this temporary-object handoff.

## Hit-probe handoff correction — bounded approval

Reviewed the narrow fix returning a copied intersection record with `object: rendered`. Intersection world point, UV, face and distance still come from the identical borrowed geometry/material; the actual mesh keeps its parent/world transform through capture refresh. Its disabled R3F raycast remains unchanged. No scene mutation, geometry disposal or additional retained resource is introduced.

Read the regression invoking actual `intersectStickerPrint` with prepared alpha, disabled real raycast, transformed parent and translated child, then comparing the production handoff against the original rendered triangle. Independently ran hit-grab, surface-grab and wrap tests: **10 tests / 578 assertions pass**. Permit fresh build/captures and exact witness6 rebind; preserve paths and targets. The native failure remains recorded and final runtime acceptance is pending.

## Witness 7 — exact production hit handoff rebind

All six `affine-hit-matrix-1` PNGs independently SHA256-match the previously viewed affine-target images byte-for-byte. Witness7 generator now invokes actual `intersectStickerPrint` using decoded original alpha and asserts the returned original transformed mesh before affine capture. Finalizer binds both hit and grab source hashes to the fresh build.

Independently verified the proof hash ledger, four capture hashes and raw selected ink buffers. All four gesture fields and positive/negative/reentry center and full-mesh hashes equal witness6, excluding query timing and evidence paths. Build source `b0cd4b600c7148ac0a5cf5ed8d7313f3022ef2c5a6e1051831c06e4d45cb060d`, artifacts `5050154db139ec4effda646e5008559d54a290d5ca3a7b494ad3a903e6de62eb`. Bounded approval for immediate same-build recorder-off native; runtime outcome remains pending.

## First fixed-hit native — visual blockers remain

Independently viewed desktop-side partial, held-target and relocated originals in `early/affine-hit-interaction-1`. Functional side relocation passed, but the held free sheet shows a gross horizontally stretched banner across its upper artwork. The relocated print is readable; this does not validate the intervening carry. Root and engineer notified: complete detached material must preserve readable artwork with plausible curl while partial attached rows and existing timing remain intact.

A separate observed concern requires attribution: the held target outline appears around screen x735–840 while the final relocated print is around x525–625 in the same side view. The prior count-one assertion is insufficient to certify destination correspondence. Asked engineer to check projection/DOM/model frame before inferring a fix. Front target outline absence may be legitimate shell occlusion; it must be classified from actual target visibility rather than forcing a hidden outline or assuming every legal target is visible.

Observer also exposed a measurement-domain issue: a supplied rAF frame timestamp can precede a same-frame pointerdown performance.now timestamp. Callback execution time should be sampled with performance.now for execution-scheduling gaps, retaining the raw rAF timestamp separately; this does not change gross-stall thresholds or establish photon latency.

## Target transform attribution — correction to prior review

Engineer isolated the observed outline offset: the parentless borrowed target mesh has matrixAutoUpdate=false; copying its local matrix without setting matrixWorldNeedsUpdate leaves matrixWorld at identity. Independently read installed Three185 Object3D.updateWorldMatrix: recomputation occurs only when dirty or forced. My earlier source approval incorrectly assumed the projection entry point refreshed this manual matrix unconditionally.

The initial target-visibility probe repeats that wrong transform, so its concrete front-zero/side-visible classification is withdrawn. The general proof-bound visibility assertion remains sound, but all expected counts must be regenerated after the fix. Requested exact transformed slot-to-projection regression including a second changed transform. No old visibility proof or outline correspondence approval carries forward.

## Grab-origin peel and transient raw pull — current bounded posture

The old fixed top frontier leaves the actual grabbed side UV seated at32px while lifting unrelated upper material; this supersedes the old universal row-based attachment witness. Approved a bounded captured-UV/nearest-material-edge crease experiment with zero initial displacement, exact unchanged outside region, current raw pull response and the existing full-detachment transport. No new clock, persistence or threshold restriction is introduced.

Reviewed app transient sourcePull: finite current CSS delta tied to copied source anchor, retained through release and scaled from a frozen return origin. Found and requested repair for ownership sampled after preceding synchronous notifications. The corrected move snapshots generation and anchor owner at entry, guards publication before/after and passes the exact owner. Independently grab/lifecycle tests **17 tests / 206 assertions pass**, including same-source synchronous replacement. Earlier anchor-only review passed15/178. Target manual-matrix correction independently passed2/32; actual visibility classifications must be regenerated.

Canonical original-UV helper and proper-frame interpolation have exact endpoints and captured-depth offset; independent helper/target tests passed4/11687. Those endpoints do not validate the new partial kernel. Current captured-UV experiment reports accurate anchor motion and unchanged outside half-plane, but small intermediate triangle areas and local normal reversals remain unclassified. Root authorizes only a finite/type-clean actual phase diagnostic at16/32/51.2/57.6/64px side/front, to distinguish localized plausible folds from broad compression. No full native acceptance or final visual approval yet.

## Grabbed peel phase 1 — actual visual disposition

Independently viewed all ten raw desktop side/front phase originals at16/32/51.2/57.6/64px and the two57.6 marked originals. Side16/32 improves the previous unrelated upper-sheet flight: ticket print stays in view and responds locally. However, side16–51.2 has a sharp shell-colored interruption through the central flap; front16 has a bezel-colored band across pulled print and later partial frames leave substantial material behind the body. These require actual intrusion versus legitimate hidden-attachment attribution.

The transition remains unaccepted: front57.6 is a thin diagonal sliver before64 shows mostly backing and extends beyond the top viewport. Side57.6→64 also swings to a compact backing silhouette. Backside visibility or an edge-on orientation is not inherently wrong, and sparse frames alone cannot establish a discontinuity; nevertheless these frames do not demonstrate coherent readable carry. Requested spatial diagnosis of the small-area regions and orientation/clearance, retaining the partial response improvement. No full native acceptance run approved from functional diagnostic assertions.

## Extra free-carry phases — endpoint clarified

Independently viewed side/front80/100/164px originals in `early/grabbed-peel-free-phase-2`. Both164px curl.4 endpoints show legible original artwork. The rotated front orientation follows the captured material direction; no permanent mirror/basis error is established. Thus64px backing is a transient legacy curl1 state, not by itself a blocker. The corrected side target outline now occupies the actual left-edge destination region.

Confirmed blockers remain the source-backed inward shell crossing and opaque-triangle compression around57.6, not backing visibility alone. Approve a bounded borrowed-BVH swept-source contact/slide experiment for detached material with explicit unresolved cases, exact unchanged attached region and sampled picked-UV screen check. Source-to-candidate sweeps are a conservative spatial construction, not proof of temporal CCD; full triangle contact and actual visual review remain necessary.

## User-reported editing latency — independent source triage

User reports long rAF/pointerdown work during rotation/wear and explicitly rejects the drag outline. The drag outline decision is superseded; editing HUD remains. Geometry prototype work is paused. Read Runtime Review, Interface Craft, motion and interface guardrail references; restricted investigation to this workstream and preserved unrelated WebMCP changes. No attribution to HMR or external work is supported.

Current equipped geometry memo excludes wear; wear updates two material uniforms. Rotation changes geometry and has an isolated engineer Bun measurement, but that is not browser latency. The common rotation/wear hot path is the editor contour: every editor render calls contour directly, which traces the current alpha boundary, subdivides spans to shown-grid resolution and performs visibility BVH rays. The visibility callback repeatedly obtains camera world position and invokes content.worldToLocal twice per sample, refreshing/inverting matrices despite one fixed projection query. castSegment additionally allocates vectors/ray/closure per call, and strict-triangle-miss precision fallback performs temporary vector/edge work. These are concrete repeated-work paths, not yet measured browser attribution.

Render amplification candidates: editor state writes lastPresented from a layout effect; derived placements creates a fresh array for any editor-state change; the projection effect depends on that array and republishes projectionVersion; editor presence, tooltip and grip states also trigger unconditional contour projection. Alpha contour cache is a bounded four-entry LRU, not an unbounded leak; new wear-byte thresholds retrace boundary candidates, while repeated same-wear renders still repeat projection/visibility.

Next evidence must separate actual input handler/render/contour counts and timings, alpha trace, projected samples, BVH revision/rebuilds, and geometry construction. Use actual rotation and wear sequences plus release windows, recorder off, production build identity retained. No performance pass from startup or isolated Bun timings alone.

## Editing performance — measured counterfactual and carry-HUD removal

Reviewer evidence `early/editor-performance-1/contour-reviewer.ts/.json` uses actual C03 raster, captured desktop-side matrices and the source-derived rear/front/glass/wheel subset. Ten paired wear/rotation contour queries alternate old-closure and hoisted-query order; alpha is prewarmed equally and complete projected output matches exactly in every pair. This is isolated Bun CPU evidence, not browser input latency or a complete visible-assembly benchmark. The old callback is reconstructed explicitly because the hoist had already landed before the measurement hold.

There are 2,754–3,106 visibility rays per contour. Warm old closure costs 27.75–29.95ms and hoisted closure 27.24–30.34ms: no material warm improvement demonstrated. First 59.69/29.82ms pair is cold/JIT/order confounded and cannot support a 2x claim. Initial BVH construction costs 303.98ms, but revision remains 1 throughout all ten queries. Fresh alpha tracing costs about 1.15–2.86ms; warm cache lookup is negligible. Geometry creation in this fixture does not imply production wear remeshing.

Independent React DOM regression reproduces two contour queries for metadata-only property selection and two for one wear preview, excluding additional device projection notifications. Thus duplicate projection is measurable and merits exact-state deduplication; no debounce, input suppression or silhouette simplification is authorized. The transform-hoist regression independently passes 4 tests / 99 assertions, preserving visibility and subsequent-transform refresh.

The user's no-drag-outline instruction supersedes all prior target-cue decisions. Independently inspected production removals and the carry-stage wrapper; DOM regression passes 1 test / 12 assertions: peeling, placing and settling contain no appearance subtree, while idle selection restores rotation/wear and editing contours. Native driver now asserts zero carry contours/handles/wear. Full browser editing-frame validation remains pending; geometry prototype work remains paused.

### Exact editor-query deduplication review

App changes independently pass the actual DOM plus native-schema helper subset (4 tests / 380 assertions) and editor model suite (7 / 45). Derived shown state preserves exit snapshot while avoiding redundant lastPresented publication renders. Exact placement-element identity suppresses only unchanged render inputs; editor metadata notifications remain observable. The contour memo includes exact draft, stable production projector and projectionVersion. Regression observes metadata queries 2→0, distinct wear preview 2→1, readiness refresh 1 and source-switch refresh 1, with correct displayed values and no carry HUD.

Device scratch-ray traversal source review preserves outside-entry pruning, inside-origin retention, left-before-right tie order and returned owned hit vectors. Existing collision/visibility tests pass 11 / 726; new targeted pruning regressions and cold-build changes remain under review. Found a missing global content transform in the new exact contour-cache key (BVH revision intentionally excludes global pose); requested scalar transform snapshot and a compensated-print regression.

Native editing fixture observer independently passes 8 / 59. Requested that its final gate also require release-to-settled windows for cold selection, rotation and wear, and retain per-case traces on failure. Input-only gates could miss setup/settlement stalls. Cold tiny input may honestly be insufficient; its setup window must remain separately measured. No browser performance disposition yet.

### Combined performance-correction source freeze

Independent final device tests pass 14 / 1,473. Scratch collision queries preserve owned returned vectors, inside-origin traversal, grazing/shared-edge contacts and real-gap rejection. Build caches retain original centroid-sum arithmetic/stable partition order and reset their typed arrays after construction. Recorded baseline/optimized ordered tree hashes match `8c98ea15aeb5038a561dfe3eae57aa7234583e9c3e7804bdb2641cffa882b59c` for 203,743 triangles and 65,535 nodes. Separate-process recorded build time improves 298.21→219.65ms; cold cost remains significant.

The one-result WeakMap contour cache now includes exact global content-matrix scalars, in addition to mesh/camera/viewport/geometry-buffer identities and versions, alpha/wear-byte and assembly identity/revision. Compensated-print/global-shell transform regression passes. Unknown visibility callback semantics without an explicit revision remain uncached. Recorded warm full-query work is about 7–8ms versus 28–32ms old traversal; exact repeats skip rays. This is isolated evidence for traversal/allocation changes, not a hoist-only win or a browser latency verdict.

Native fixture corrections reviewed: cold selection, rotation and wear require exactly one semantically completed release/setup window within the unchanged gross-stall budget; rotation/wear input windows also require coverage and budget. Cold tiny input alone is explicitly exempt. Failure paths retain input traces. Combined source freeze approved for fresh build and actual mouse/touch editing run, recorder/profiler off. No final interaction, responsiveness or physical-peel approval follows from source checks.

### First actual editor-performance run — incomplete, no blanket pass

Independently inspected `early/editor-performance-native-1/editor-frame-observation.json`. Desktop rotation input max18.6ms; desktop wear contains a122.2ms execution gap (3557.8→3680) corroborated by raw rAF116.6ms (3552.2→3668.8). Long-task API is available with no overlapping entries, which does not explain or dismiss the missed frames. A separate browser profile is warranted; gross-budget pass alone is insufficient for the user's responsiveness concern.

Cold-selection release first callbacks are35.1ms desktop/18.8ms mobile. Current Scene layout effect prepares the visibility collider before publishing the projection handle, so these do not measure cold BVH construction on first pointer. Startup/readiness cost must remain separately attributed. Presence completion around827ms includes authored animation.

Wear release semantically completes in13.4ms with one rAF and is currently labeled insufficient. Requiring two frames for such a completed short window is artificial; requested an explicit short-completed classification that still checks every execution boundary gap and preserves timeout/overflow failures, without extending the gesture.

Mobile rotation's pre-up DOM69.9711 versus saved76.7564 is not yet classified. Production finish deliberately calls move(event) before committing. Need actual browser-delivered move/up coordinates and DOM/PUT correlation before changing either product behavior or the test oracle. Driver command completion timestamps alone cannot establish the final event coordinate or React publication timing. Source remains frozen pending evidence.

### Same-build delivered-event diagnosis

Independently read `early/editor-performance-diagnostic-1/delivered-events.json` (no overflow). Final mobile touchmove at173.875/394.0625 is delivered at2520.8ms while DOM still shows69.9710974°. Same-coordinate pointerup capture at2522.8ms, before the product release handler, already shows76.7563549653°, matching the committed value. This classifies the mismatch as a premature fixture DOM read, not a release-coordinate or commit defect. Root authorizes one explicit paint-readiness boundary after the final move while the input observer remains active; no waits between the 24 move samples and no arbitrary timeout.

Diagnostic wear profiling did not reproduce the122ms outlier. Profiling/video overhead and nonrecurrence prevent treating this as proof that the earlier stall was harmless or fixed. Final uninstrumented evidence remains required. No product changes justified by this diagnostic.

## Final bounded verdict: owner editing-performance and no-drag-outline corrections

**Accepted for these two corrections on the tested native desktop/mouse and mobile/touch paths.** Independently reviewed `early/editor-performance-native-2/summary.json`, raw `editor-frame-observation.json`, pointer traces, provenance, all four original settled PNGs and chronological video samples (2fps overview plus full-size moving frames at6.5s/14s, retained under `reviewer/`). The native run passes4tests/741assertions. Source `61ba54082a9a11ad7cbe9858a6d016bed82a13aa870a0240a0b364f93d9e55ee`, artifacts `d9fe4fe1693743f440b5a8b64a54f386aaf7d576951e78e3c8880f0605f14373`; independently compared568source and76artifact start/end entries, zero differences; all build/test delta arrays are empty.

Rotation/wear retain readable C01 artwork, live editing controls and exact saved/reloaded values: mouse43.4886399599°, touch76.7563549653°, wear.885 both, one expected PUT per appearance gesture. Actual moving video frames show neither target silhouette nor appearance grips/rail; native assertions verify the same during movement and cancellation leaves inventory unchanged. Ordinary editing outlines remain as requested. Mobile editing layout relocates controls around the compact sticker after wear, but controls remain visible and this is distinct from the removed moving silhouette.

Measured input execution maxima: rotation19.0/18.9ms, wear24.5/25.7ms (desktop/mobile). Raw rAF interval p95 is16.7ms for rotation and16.8ms for wear. No overlapping long tasks or lost observation buffers; release windows complete, with roughly20ms wear releases correctly labeled completed-short-window and no invented cadence. Move/cancel maxima30.8/43.9ms are separately retained. Final-value two-rAF readiness is inside the input window, not an unmeasured wait or per-move throttle. Video encoding remains enabled; no diagnostic preload or CPU profiler was used in this final run.

Limits are explicit: these are scheduling/functional observations, not photon latency or universal60fps certification. The earlier122.2ms execution /116.6ms raw-rAF wear outlier did not recur in the diagnostic or final run but remains unattributed. Cold collider preparation still occurs in scene readiness and retains significant isolated cost; first-selection callbacks36.4/17.7ms do not erase that startup cost. This bounded acceptance does **not** approve the incomplete physical peel transition, all wrapped placements, or the entire edge-wrap workstream. Those pre-existing blockers remain separate.

### Selective app-only patch against HEAD

Approved candidate `early/app-corrections-index-patch/app-corrections.patch`, SHA256 `426126b49f2b98fec9be65a41a1349c61205eabba2b0c14b1787409eadb639b5`, against HEAD `2f1001a0b72b1829a581123e4cc4691f0c60319e`. Reviewer constructed an isolated HEAD source archive, applied the patch only there, and verified exactly four changed files: editor, editor model and their two tests. Workspace imports resolve to the HEAD-backed scratch packages; installed external dependency artifacts alone are reused.

Independent scratch validation passes8tests/67assertions, app-source TypeScript and scoped lint. Existing HEAD provides interaction stage/actions and projectionVersion; patch does not depend on uncommitted peel/rawpull/anchor/contour-grip/device or foreign WebMCP work. It only suppresses appearance HUD during movement and removes redundant app contour publications. The full native performance improvement still includes separate uncommitted device changes and must not be attributed to this app-only commit. Reviewer did not stage, commit or alter working source. Machine-readable result: `early/app-corrections-index-patch/independent-head-review.json`.
