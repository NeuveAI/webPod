# W1 · gates

Run 2026-08-28 on `main` at 116016b, after the review response.
Four other lanes are live in this working tree; where a command reports on a package
W1 does not own, that is stated rather than tidied away.

## $ bunx tsc --noEmit -p packages/providers/tsconfig.json
```
exit=0  (no output above means clean)
```

## $ bunx --bun eslint packages/providers
```
exit=0  (no output above means clean)
```

## $ bun test packages/providers
```
bun test v1.4.0 (34cbb9a40)

 388 pass
 0 fail
 1689 expect() calls
Ran 388 tests across 11 files. [66.00ms]
```
230 → 388 tests. Per-test roster in `w1-tests.txt`; the reporter condenses off a TTY.

## $ bun run typecheck
```
   ok  packages/ui

── tsc --noEmit -p scripts/tsconfig.json
   ok  scripts

11/11 projects clean
```

## $ bun test
```
bun test v1.4.0 (34cbb9a40)

 629 pass
 0 fail
 2472 expect() calls
Ran 629 tests across 20 files. [1114.00ms]
```

## $ bunx --bun eslint .
```
(no output)
```
Clean repo-wide at this commit. At the time of the review it reported two errors and a warning in
`apps/web/src/routes/[_]spike.device.tsx` and `scripts/w4-tune.ts`, both the device lane's
uncommitted files; that lane has since fixed them. Nothing W1 wrote was involved either way.

## $ bun run gates
```
exit=1
$ bun run scripts/gates.ts
bun run gates: not implemented
error: script "gates" exited with code 1
```

⚑ **Expected, and not a W1 failure.** `scripts/gates.ts` is still W0's deliberate placeholder:
it exits non-zero on purpose so an unimplemented gate set can never report "no findings" for
checks that never ran. The gate harness is slice W5a and is unstarted. The greps this slice is
accountable for are run by hand in `w1-greps.txt`, with their exit codes.

---

## Final re-review correction run

Run 2026-08-28 at `9f7f16d` plus the W1 documentation response in the working
tree. W5a had replaced the placeholder by this run; W1 did not edit scripts.

| Check | Result |
|---|---|
| `bunx tsc --noEmit -p packages/providers/tsconfig.json` | exit 0 |
| `bunx --bun eslint packages/providers` | exit 0 |
| `bun test packages/providers` | **400 pass, 0 fail, 1719 expects** |
| `bun run typecheck` | **11/11 projects clean** |
| `bun run lint` | exit 0, repo-wide |
| `bun test` | **693 pass, 1 fail** — the uncommitted W3 Playwright spec is collected by Bun and calls `test.beforeAll()` outside Playwright; no provider test fails |
| `bun run gates` | **13/18 clear, exit 1** |

The five static-gate failures are outside W1 ownership and are preserved rather
than edited around: the same foreign W3 test collection failure; comment-only
false positives for `useState`, `navigator.vibrate`, and the substring `handed`;
and initiative-name matches in composite/panel/spike sources. Provider branching,
types, lint, provider tests, unsupported-tool registration, trailers, and
credential boundaries all pass. MAJOR-5 therefore remains a W5a/workstream gate
blocker even though the placeholder itself is gone.
