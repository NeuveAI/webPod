# Sticker edge wrapping

Status: Contract independently approved for bounded geometry/domain implementation; actual geometry and native evidence remain required. Latest owner request supersedes inset-safe-bounds behavior. Do not recreate a guard that refuses placement merely because artwork crosses the backplate edge.

## Correctness target and primary source

Users may place sticker centers anywhere on the actual backplate, including its perimeter. Artwork extending past the flat rear must bend continuously around the physical rounded shoulder/edge and adhere to the adjoining device surface. It must not float in empty space, stretch into a flat invisible plane, vanish at an arbitrary inset, or clip off excess. Include all four edges and rounded corners, arbitrary sticker rotation and existing size range. This is a real 3D wrap, inspectable from oblique/side views. Preserve contour editing, textured/worn alpha, backing, peel/return choreography, ownership and revisioned persistence. Placement, rotation and reset must stop claiming ‘That is the edge’ for an otherwise physically supported edge location.

Reference: owner screenshot /var/folders/ft/7tsjkpcn20q5fx1q8dwv26x80000gn/T/codex-clipboard-97b8bdcf-544a-4943-b6e2-ccdb62fb9b85.png shows incorrectly refused edge vicinity. Prior contour scope/review is supporting for appearance/lifecycle only; its inset-safe clamp is explicitly superseded. Owner also explicitly confirms concurrent WebMCP agent activity. Preserve all external changes; coordinate only shared files, never stage/discard unrelated work or claim aggregate checkout tested at a different identity. Do not read unrelated workstream016.

## Investigation questions and allowed defaults

Geometry engineer inspects actual shell source/mesh dimensions, current rear conform mapper, true available side/front depth and corner curvature; propose one continuous mapping for overflow with bounded distortion. Domain engineer inspects canonical placement validation/client clamps/server shared schema and legacy stored values. Reuse canonical shared types and /Users/vinicius/code/.better-coding-agents/resources plus pinned installed Three/Effect/Drizzle sources. Do not invent dimensions or a new web framework. Default scope starts sticker center on rear including full perimeter, wraps overflow physically along existing device surfaces; support max-size worst cases within actual shell topology. If overflow reaches front, map onto real adjoining surface, do not silently clip or invent side depth. Resolve seam/corner behavior from actual geometry and record tradeoffs before implementation. No new migration unless representation genuinely requires it; preserve existing saved positions via explicit compatibility mapping if coordinate meaning changes.

## Ownership and artifacts

Geometry/material engineer owns device wrap mapping/mesh/hit/projection helpers and deterministic tests, design proposal in sticker-edge-wrap-contract.md, diary diaries/sticker-edge-wrap-geometry.md. App/domain engineer owns shared sticker placement validation and server tests, app placement/editing/drop projection integration and actual native fixture, diary diaries/sticker-edge-wrap-integration.md. They agree coordinates/surface contract before source integration; explicit handoff for StickerPackScene/StickerSurface, no overlapping writes. Reviewer independently critiques actual-shell contract and source/native geometry/domain boundaries, writes reviews/sticker-edge-wrap-review.md. Lead owns scope/handover/audit/commits. Evidence evidence/sticker-edge-wrap/{early,final,reviewer}/. One heavy build/browser owner at a time.

## Verification and completion

Deterministic: centers along fullrear perimeter, allfour edges/corners, arbitraryangle, smallest/largest aspect ratios across artworkcatalogue; continuous vertices/normals across seams, conform offset avoids penetration/zfight, no inverted triangles/invalid finite values, preserved UV/damage/backing, surface hit and contourprojection agree with wrappedmesh. Domain tests client/server samecoordinate bounds, validedge placements accepted/saved/reloaded, invalidoffdevice/NaN values rejected with no weakening of ownership/revision/securityguards, oldsavedplacements retain intendedposition. Actual native route/Start/SQLite sessions with trusted upstream fixtures: drag fromcenter toallfour edges+corners, rotate atedge, HUD grip alignment, side/oblique wrap visible, worn peel/return/re-stick/reload, reset/unknownwrite/conflict/cancel; unchanged peel timing, no hardedge refusal. Real screenshots/video of at least one irregular and one nearrectangular sticker spanningedge/corner, close and normalscale and375px. Do not use screenshot-only flat illusions or test-only product APIs. Sharedsurface geometry evidence is mandatory, not just expanding bounds until tests pass.

Required UI/motion skills: Interface Craft+storyboard/critique, Interface Design Guardrails allfour, Neuve Motion, requested iOS HIG references for changed controls/motion; global/Jotai/Effect/database skills for owned boundaries. Repo no useState, bun/bunx only, no cert/env/key/token reads, no .pen filesystem access, no worktrees/forcepush/push. Existing authenticated owner tab/server remain intact; owned nativefixtures only. Scoped typechecks/lint and meaningful domain/geometry/native tests, root fulltypecheck when slots free, per-file source provenance under external concurrency. Independent APPROVE required. Commit coherent shareddomain/device/app changes with tests then docs/evidence; leave no uncommitted task-owned work. Goal/task closure only after actual requested wrapping is implemented and all criteria audited; no narrower ‘allowclipping’ substitute.

## Contract approval

Reviewer accepted sticker-edge-wrap-contract.md for implementation. Full330×552 roundedr26 center predicate remains pure shared math; latent arclength coordinates never enter persistedplacements. Continuous radial surface chart must prove positive orientation and quantified complete-print distortion, including maxPW-C03 overflow onto actualfront envelope. Lead accepts tautfilm bridging small real assembly apertures with bounded real endpoints, not pressingvinyl intoports. Existing peel timing/choreography stays; spatial detachment must clear the actualshell. Geometryowner may integrate Device.tsx surface resources, sharedscene/surface changes require explicitapphandoff. No finalapproval from this contract alone.

## Superseding implementation direction

The radial chart in the historical contract approval failed and is not the required implementation. Subsequent native cloth experiments also remain failed. The owner requires the observable wrapping experience, not a particular mapper, force simulation, exact cloth equilibrium, fixed two-unit lift or the straight-strip numerical strain range. Numerical checks must identify visible or behavioral defects rather than silently introduce a new product specification.

Independent review recommends one procedural bend-cage corner prototype using the accepted straight strips and shared front phase, with localized secondary bending and a boundary-reaching pleat only where needed. The engineer records the concrete construction before probing. Review actual rear/side/front renders immediately, then a continuous rear-to-corner and rotation sweep before generalizing. Artwork readability, coherent material continuity, visible shell clearance, full center/rotation/size access and responsive manipulation remain mandatory. This supersedes further cloth-engine expansion; it does not approve an unproven corner candidate or relax the prohibition on visible clipping, lost artwork, detached sheets, arbitrary restrictions or distorted banners.

## Superseding interaction decision: carry first, adhere on release

The owner's requirement is a tactile, responsive sticker that peels, follows the pointer, and adheres to the actual device surface. Requiring the UV of a newly rewrapped target to remain exactly under the pointer while the sheet is detached was an implementation-added constraint. It is superseded after repeated source-connected inverse failures. This does not relax unrestricted valid rear centers, actual shell wrapping, visible-hit admission, persistence, or motion continuity.

A visible wrapped pickup captures the actual hit triangle and its UV-oriented material tangent frame, initial pointer offset, camera/pose validity and source properties. Detached carry uses the existing camera-plane transport and stays under the pointer. Intended adhesive center comes from one frozen, bounded affine material-plane delta; it must not solve a nonlinear newly wrapped mesh or switch to a distant projection branch. Edge-on views must remain finite and stable. The actual rounded rear-center domain remains authoritative.

While held, landing blend stays zero; the quiet existing target contour may indicate the adhesive destination. Only a valid release starts the existing press/landing spring onto the actual mapped surface. Invalid targets clear the preview; releasing cannot save a stale prior target, and returning to valid space restores placement. Preserve attached peel rows, original size/rotation/wear, reduced-motion behavior, generation guards, pose invalidation and the existing total release choreography. No additional explanatory notice is needed.

Independent reviewer recommended this contract after checking renderer and app behavior: the existing landing-to-.97 during held dragging was the coupling that made an exact seated-UV inverse appear necessary. Device and app engineers have disjoint ownership; both changes need independent tests/review and fresh actual-route visuals, mouse/touch persistence and frame-stall evidence before completion.

## Superseding peel-front decision: start at the actual pickup

Native partial-peel evidence shows that the universal top-to-bottom material-row frontier is an implementation error for arbitrary wrapped pickups. The side pickup is near row 67/96; at the earlier 32px frontier (row60), the grabbed print remains seated while unrelated upper artwork unfolds out of view. The old fixed-row attached reference therefore does not prove the requested tactile interaction and is superseded.

The bounded replacement derives a frozen material peel axis from captured UV toward the nearest material-rectangle edge, with deterministic ties. A perpendicular crease starts at the pickup and advances away from that edge. Detached membership follows signed distance to this crease. Curl is a displacement relative to the original wrapped source, zero at rest and at the crease, so selecting a region does not jump the source. Material outside the active half-plane remains unchanged. The grabbed ink responds to actual pull from the first movement, including before the existing 64px carry admission. Raw pull data is transient; no persistence, new animation clock or admission threshold is introduced.

Calibrate the analytic displacement against actual input rather than multiplying total sticker height by phase alone. After detachment, the existing canonical-sheet transport owns the handoff. First validate exact C03 side/front grabbed UV movement, unchanged attached samples, intermediate shape and reverse continuity before expanding native coverage. Old universal-row witness expectations must be replaced by actual crease/visible contact evidence; they are not owner requirements.

## Owner correction: editing performance and no drag silhouette

The owner reports severe lag during rotation/wear editing and explicitly rejects the silhouette/outline shown while dragging. This supersedes the earlier target-cue design decision. Remove the drag target outline and its otherwise-unused projection seam; do not replace it with another marker or notice. Editing contour/grips remain only for deliberate appearance editing, not peel/carry/movement. Native carry expectations become no outline, rather than visibility-conditioned target cues.

Rotation and wear performance now take priority alongside the still-incomplete wrapping task. Profile actual editing input on the current checkout, including pointer handlers, frame work, mesh generation, alpha/wear processing, contour/visibility and state subscriptions. The screenshot's long tasks are evidence to investigate, not a sufficient attribution to HMR or WebMCP. Fix demonstrated hot paths and verify rotation/wear interaction frames and final persisted values without reducing material fidelity blindly. Preserve concurrent WebMCP registry, cancellation/generation guards, actions and tests; the other task is currently idle but its changes remain in the working tree.

The crease-extrusion prototype is paused for these corrections. Its coherent core is retained as evidence: zero core contacts and stretches .862–1.057; all159 contacts occur in artificial localized end collars. The next geometric candidate is a crease returning to the real material edge at both ends, eliminating those collars. That candidate is not implemented or accepted, and the later transition remains unresolved.
