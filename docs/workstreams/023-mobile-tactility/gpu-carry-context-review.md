# Independent carry context source review

APPROVE the bounded two-file correction: `sticker-carry-context.ts` SHA2560a12a95a7d029669b671c394ff99b05e5f50bd60b5da3629f75d04356f2a62b8; `sticker-carry-preparation.ts` SHA256f6ef5eb1287faf69c61df0b301a81888b49a6b396fbdf21bf7367b4c7a4dbd62. Baselines and frozen manifest are retained under `evidence/gpu-worker/carry-context/`. No Critical/Major found in this delta. Reviewer authored no application changes, browser actions or build.

Cold request now captures immutable references, prepared bounds and scalar sizes; it does not copy dense arrays or create a worker synchronously. Existing yieldSteps starts after a macrotask. Numeric copying checkpoints every2048 elements; all rear arrays and packed collision buffers get independent output ownership and transfer exactly once into the existing producer. The former second structured clone of dense context is removed. Metadata is scalar-only and privately copied. The default serializer, worker protocol, deformation math, native controller, query geometry and final input remain unchanged.

One contextJob blocks replacement copying until its finalizer unwinds, including aborted jobs. Stop aborts pending work and retires the installed producer; stale completion checks mounted/hidden/source/visibility/revision before creating a worker. Latest wanted scalar input remains pending, so context readiness dispatches the newest pose. Worker FIFO places context before its scalar job without an acknowledgment race. Context-post failure terminates the producer; default GL failure fallback and native explicit failure retain their existing paths. Prior displayed/query frames and private renderer leases keep their separate lifetimes.

Independent actual authored fixture matches20,906,104 typed bytes and scalar metadata against the original serializer/snapshot through a real MessagePort. Private buffers detach, borrowed buffers retain exact hashes/lengths; reservation20,915,342bytes. The runtime proof uses the actual carry module worker, verifies initial yield, partial-copy abort, cancelled-generation rejection, latest intent, one producer, hide/resume, pending unmount, transfer failure and final zero. Existing private-frame lifecycle/parity proof also passes, including pinned main query and current/candidate bounds.13 existing carry/return/projected-bounds tests pass with862,660 assertions. Device/composite/web types and scoped source/proof lint pass independently.

Limits: the64MiB pending/installed context bound is separate from existing carry-result/private leases, not a whole-browser or aggregate GPU heap bound. Typed-array allocations and buffer transfer are native indivisible operations;2048-element copy checkpoints and a4ms cooperative target do not guarantee every allocation/task fits4ms on a phone. The scalar size calculation also walks bounded source metadata. This correction removes the measured cold synchronous bulk-copy chain from the input request but does not prove a new production latency percentile. Lead must observe actual cold pickup/drop and unchanged final/cancel behavior at6× before runtime acceptance; the separate LCD/glass visual blocker remains open.
