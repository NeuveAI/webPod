# Sticker editor UI diary

2026-09-06. Designer brief preceded source implementation and received independent acceptance. Read sticker-editor-scope.md fully. Skill/reference record is in sticker-editor-design-brief.md: Modern Web Guidance bunx first, accessibility guide, Interface Craft critique/storyboard, all four Guardrails resources, Neuve Motion, global/Jotai and canonical placement/provider/store contracts. Lead authorized native controls with Jotai rather than installing another library for three ranges. Current source remains intermediate until final native/rendered review.

Implemented contextual one-property editor, captured source/draft identity, explicit Apply/Revert, alpha-aware tap candidate before direct drag threshold, keyboard selection/Move, shared-store preview overriding exactly one equipped placement. Routine ready/partial and placed successes are visually silent; actionable failures remain. Material lane owns durable appearance/server/shader; app forwards canonical wear to scene, editor and new placement intents. Mobile frame reuses existing single model-position effect after explicit scene ownership handoff.

Reviewer caught a queued-save race: checking captured source before enqueue was insufficient because runtime place reads currentrevision only when the queue executes. Added optional expectedSource to placeSticker, checked inside serialized dispatch, preserving generic placement API callers. A changed source refuses overwrite and follows canonical409 reconciliation. Deterministic delayed queue test proves no stale PUT is sent.

Reviewer also caught editor framing disappearing at drag threshold and message expansion during range input. Preserve complete modelY during rear carry (not only packpresentation). ResizeObserver defers measured dock updates while a range pointer is active; input feedback does not move the rear under a finger. The initial mobile frame aimed too high, cutting off the device unnecessarily; reduced its translation to the space actually needed above the measured dock.

First native editor run passed19behavior assertions but was REJECTED visually: every equipped print was absent. Saved initial captures and log under evidence/sticker-editor/rejected-shader-first/. No approval inferred. Material engineer confirmed GLSL reserved identifier `patch` and renamed it to abrasionPatch. Added actual browser shader-error rejection and colored-ink checks in the selected rear crop; an alpha hit or slider value alone cannot prove rendering. Next native run is the first candidate for actual material/layout review.

At this checkpoint app types passed and model/status/runtime tests passed23tests78assertions, including queued-save race. Final build/native/fingerprint and remaining direct touch/return/reload/error/keyboard regressions are pending; no commits made by this engineer.

The corrected painted native sequence passed72assertions. Original/mid/high wear is visible at normal scale; the material engineer accepted it. Added real pointer anchor invariance during rotation, dirty touch drag refusal, 1px clean-selection-to-lift projection continuity, worn carry/press, own-pack removal retaining appearance, keyboard re-stick and reload. The initial re-stick screenshot obscured Soundcheck with another print; rejected that presentation and used actual keyboard positioning to an unoccupied upper rear, then the actual Put pack away control. Fresh re-stick/reload captures show the complete worn print, accepted independently by the material engineer.

Post-press device framing originally snapped approximately45px when sourcePlacement cleared. The sole device model-position effect now owns a bounded spring for released carry, publishes current projection through the existing callback, retains the exact current frame if re-grabbed, cancels its scheduled frame on supersession/unmount and snaps under reduced motion. A native observer records the untouched neighboring sticker's projected y during release: initial sample296.98px, final340.22px,30samples, maximum consecutive delta11.14px in the first accepted run. This proves the old single45px jump is removed; it is not a claim of universal60fps or compositor timing. Named stiffness/damping/tolerance/bound govern only the post-press return; physical input retains immediate ownership.

Routine success text is screen-reader-only. Partial import produces no visible status. Ordinary editing save failure retains draft+Retry/Revert locally;409 adopts canonical state; a dismissed pending failure remains recoverable at the affected sticker. Dirty outside/switch dismissal cancels draft intentionally, whereas dirty dragging is blocked until Apply/Revert. A successful Apply rebases the next Revert source to the server result.
