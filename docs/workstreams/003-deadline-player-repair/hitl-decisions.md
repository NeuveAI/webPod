# 003 — Human-in-the-loop decisions

| Decision | Default used for implementation | Owner action |
|---|---|---|
| Exact protected-audio cache depth | Do not claim or simulate “5–6 seconds.” Use supported MusicKit idle queue preparation after a 700 ms stable selection plus low-priority metadata/artwork prefetch. | Validate perceived startup improvement on a real account/browser. |
| Visual generation target | Compact fifth-generation iPod behavior and period Aqua construction; later iPod classic material is only supporting evidence. | Approve black and white captures or request one bounded correction pass. |
| Embedded-browser playback | Treat metadata-correct failure as acceptable if normal Chrome comparison proves a host/DRM limitation. | Run the same track in normal Chrome. |

These decisions are guarded, not blockers to implementation. They block only final owner sign-off.
